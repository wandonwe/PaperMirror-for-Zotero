# PaperMirror 文镜 for Zotero v0.9.9 更新说明

## 修复:翻页后"卡住很久"(取消整页级自动重试)

这是造成"翻到新页有时卡住、甚至很久没反应"的关键架构问题。

以前一个页面是一个**可重试的整页任务**:一旦出现可重试错误(网络抖动、超时、限流),调度器会把**整个页面**从头重跑——重新提取 PDF、重新分批、重新翻译、重新补救——最多重试 3 次,每次还有 300 秒看门狗,最坏情况接近 20 分钟才真正失败。用户看到的就是长时间"正在翻译"。

现在改为:

- 页面任务**最多执行一次**(`maxRetries: 0`),绝不整页重跑;
- 瞬时错误改在**单个请求层**重试(最多 2 次、短退避),**不再重新提取整页**;
- 单个批次请求彻底失败**不再拖垮整页**——该批次交给(已封顶的)补救,后面的批次继续翻译;
- 页面看门狗从 300 秒收紧到 **120 秒**;已经翻好的段落始终立即保留。

配合 0.9.8 的"补救封顶",现在最坏情况也不会再出现几分钟到十几分钟的卡死。

## 尚未做(需真实 PDF 验证,按"我改一块、你实测"推进)

- **整表完整保护**:检测到 `Table N` 后扩到整表边框+表下注释,整块保持原文,禁止表格文字进入正文翻译/合并(先杜绝图1那种半翻译错行;之后再做稳定的单元格级翻译)。
- **active 页面升级为前台**(`promoteActive`):翻到新页时,已在后台跑的该页立即转前台优先。
- **提取队列与 API 队列分离**:PDF 提取不再占用服务商并发槽。
- **整页任务拆成 chunk 级任务** + **真实阶段文案**(正在读取/正在翻译 8/24 段/正在补全/正在适配排版)。

这些会改动表格几何和调度粒度,必须在真实论文上测过再发。

> 提示:自动更新链路修好前,请手动装这个 xpi:工具 → 插件 → 齿轮 → Install Plugin From File。
