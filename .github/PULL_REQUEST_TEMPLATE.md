**What this changes**

**Why**

**Checklist**
- [ ] `npm test` passes (no test was deleted or weakened to get there)
- [ ] `npx tsc --noEmit` is clean
- [ ] No API key, `Authorization` header or full request header is logged
- [ ] Model output is inserted as text, never as HTML
- [ ] `CHANGELOG.md` updated if the behaviour changed
