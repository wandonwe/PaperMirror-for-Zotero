# PaperMirror 文镜 for Zotero v0.9.7 更新说明

## 模型选择器改版:分组 + 精选清单

每个翻译服务商现在提供一组**当前主流、适合论文翻译的文本模型**,在下拉框里按分组排列:

```
推荐
高质量
快速 / 低成本
预览版
旧版兼容
──────────
自定义模型…
```

- 已排除图像生成、语音/实时、Embedding、代码专用、安全审核和即将退役的模型——只保留翻译真正会用到的文本模型。
- 清单只是**选择便利**,不是调用白名单:你手填的任何模型照常可用;预览版模型明确标注「预览」。
- 每个服务商各记各的模型,切换服务商不会带上一家的模型;自定义值会保存,重开设置仍选中。
- OpenAI-compatible / 自定义端点只显示「自定义模型…」(后端未知);微软 / 谷歌 / DeepL 不显示模型项(服务端固定)。

## 默认模型刷新(优先低延迟、结构化输出稳定,而非最强最贵)

OpenAI `gpt-5.6-luna`、Gemini `gemini-3.6-flash`、Claude `claude-sonnet-5`、通义千问 `qwen3.7-plus`、智谱 `glm-5`、Kimi `kimi-k3`、DeepSeek `deepseek-v4-flash`、Groq `llama-3.3-70b-versatile`、OpenRouter `openrouter/auto`、SiliconFlow `deepseek-ai/DeepSeek-V4-Flash`、Ollama `qwen3.5`。

退役别名归入「旧版兼容」或移除:通义 `qwen-plus/max/turbo`、智谱裸 `glm-4-*`(带日期的旧 ID 保留在旧版组)、OpenAI `gpt-4.1*`/`gpt-5-mini`。

## 说明

不做账号隔离、不做在线拉取模型清单——内置清单 + 自定义入口即可覆盖。模型 ID 依据各服务商官方文档(2026-08-10)整理;聚合平台(OpenRouter / SiliconFlow)的 ID 以其控制台为准,所以始终保留自定义入口。

> 提示:如果你的 Zotero 还停在 0.9.2,那是自动更新没升上来(GitHub 最新 Release 还是 0.9.2)。请手动安装这个 xpi 先用上新引擎:工具 → 插件 → 齿轮 → Install Plugin From File。
