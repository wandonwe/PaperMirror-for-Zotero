# PaperMirror 文镜 for Zotero v0.9.8 更新说明

## 翻译效率改造 · 第一阶段(软边界 + 高填充 + 补救封顶)

核心思路:**冻结页面结构,只优化翻译请求计划**。识别、段落、block ID、原文矩形、栏位、表格边界、阅读顺序全部保持不变;效率优化只发生在"请求计划 + 调度"层。

- **语义模块从硬边界改为软边界(上下文标签)。** 以前"模块边界 = 请求边界",小标题一多就产生 4–8 个半空请求。现在**字符预算才是真正的请求边界**,普通页尽量打包成 1–2 个高填充请求。请求预算从 6000 提升到 **8000 字符 / 24 块**,目标填充率 85%。
- **模块被拆分时,标题作为 `moduleContext` 传给后续批次**——只供模型理解,不再要求返回标题译文;标题只翻译一次。上下文直接来自原文,因此多个批次可并行翻译而不丢语义背景。
- **补救流程封顶,杜绝"请求风暴"。** 单页总请求数硬性限制为 **初始批次数 × 2 + 2**;达到上限即停止自动补救,已成功的译文全部保留,剩余留给「刷新本页」(熔断机制仍会把该页改投备用服务商)。英文残留补译也纳入同一预算,不再无限追加。

效果:普通双栏页应回到 1–2 个主请求,而不是因小标题多而爆出一堆请求;个别服务商乱丢 ID 时也不会再拖成请求风暴。

## 后续阶段(需真实 PDF 验证,暂未发布)

当前页双槽并行 + 后台预取让位、PDF 提取队列与 API 队列彻底分离、不可变 `PageLayoutSnapshot` 与三层缓存、刷新/缩放/切换模式严格区分操作范围。这些会改变取数与调度时序,必须在真实论文上验证后再发。

> 提示:如果你的 Zotero 还停在 0.9.2,是自动更新没升上来。请手动装这个 xpi:工具 → 插件 → 齿轮 → Install Plugin From File。
