# PaperMirror 文镜 for Zotero v0.9.6 更新说明

## 本次修复

**首次打开变快:当前页不再被重复提取。**
以前打开阅读器时,插件会对当前页调用两次 PDF 文本提取——一次用于测量正文字号,一次用于真正翻译,在慢速 PDF 上等于两趟最多各 8 秒的往返。现在第一次测量得到的页面数据会被复用,第一次翻译当前页时直接拿来用(仅复用一次;重新翻译仍会重新读取),省掉这趟重复往返。

## 关于表格 / 识别的更大改造(尚未发布,需真实 PDF 验证)

这次的排版审核结论是正确的:真正的瓶颈是**执行顺序**——表格建模发生在翻译之后、段落合并过早破坏结构,而不是并发数不够。完整修复包括:

- 在任何合并之前先建立统一的页面版面(栏 / 标题 / 段落 / 图 / 表 / 行 / 单元格);
- 表格识别提前到提取阶段,生成稳定的单元格 ID,让翻译器直接按单元格翻译;
- 让区域合并「懂表格结构」(而不是简单关掉它——它对某些把整段拆成逐行的 PDF 仍是必要的);
- 页面提取用独立本地队列,不再占用翻译服务商的并发名额。

这些改动会改变表格几何和整页版面,**必须在真实论文上验证**;而当前云端环境没有 Zotero/PDF.js 运行时可跑真实 PDF,盲改直接发布风险太高。因此本版只先落地零风险的第一步(去重提取),其余作为下一阶段,建议在你本机用几篇真实 PDF 边改边验证。
