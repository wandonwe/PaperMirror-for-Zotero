# PaperMirror 文镜 for Zotero v0.9.11 更新说明

## 每个服务商的高级参数(参考 Bob,全部选填)

在「模型 / 地址」下方新增「高级参数(选填)」区,按各家官方 API 正确适配。**所有参数都是选填——只要你不动它,发给 API 的请求和以前一模一样(零回归)。**

- **推理强度 / 思考**:默认 / 最低 / 低 / 中 / 高。
  翻译建议设「**最低**」——gpt-5.x、Gemini、Claude 这些推理/思考模型默认会"深度思考",对翻译是浪费(更慢更贵,有时还会吃掉输出);设为最低会明显更快更省。
  适配:OpenAI / OpenRouter → `reasoning_effort`;Gemini →「最低」映射为 `none`(关闭思考);Anthropic → 仅「高」启用 extended thinking;其它服务商暂不下发此参数(避免报错)。
- **自定义 API Path**:单独的请求路径字段(留空用默认,如 `/v1/chat/completions`),应对特殊代理/网关;「实际请求地址」会实时反映。
- **最大输出 token 数**:OpenAI 官方端点用 `max_completion_tokens`,其它兼容端点用 `max_tokens`。批量翻译建议**留空**,以免输出被截断导致丢段。
- **温度 temperature**:留空用服务商默认(部分新模型只接受默认温度,填了反而可能报错)。

每个参数按服务商各自保存;点「测试连接」会用你当前(未保存的)高级参数实测;免费引擎(微软/谷歌/DeepL)不显示这些。

## 请你验证(我这边没法联真机 API)

这些参数会直接改发给各家 API 的请求体,各家写法不同,我无法在开发环境实连验证。请用你的 Key 重点测两类:

1. **OpenAI gpt-5.x**:推理强度设「最低」,翻译是否明显变快、结果正常。
2. **Gemini / 通义千问 / 智谱 等思考模型**:推理强度「最低」是否生效、有没有报错。

若某家填了某参数报错,把服务商 + 参数 + 报错发我,我调整该家的适配(比如换参数名或改由 extra_body 下发)。

> 提示:自动更新已修好(v0.9.9 起 CI 正常发版)。装新版:插件 → 齿轮 → Install Plugin From File,或等自动更新。
