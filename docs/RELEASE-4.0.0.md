# 4.0.0 发布验证

日期：2026-09-16。功能基线：Beta `49b1f77`（包含 `7fe54bf` 图注提前归属）。本次发布整理版本与文档，不额外改动翻译或排版算法。

## 已完成的本地验证

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过 |
| `npm test` | 1401/1401 通过，含测试类型检查 |
| 真实跨度语料 | 41 页的已审阅快照、源文字符清单、IR 和请求计划检查通过 |
| `node scripts/check-wrap-layout.mjs` | 通过；真实译文三个缩放比例达到既有放置率门槛、最终几何审计无重叠 |
| `npm run bench` | 离线基线通过；不代表网络引擎或 Zotero 真机延迟 |
| `npm run package` | 4.0.0 XPI 生成；23 项资源、版本及允许列表验证通过 |
| `node scripts/gen-updates.mjs 4.0.0` | 更新清单指向 v4.0.0 的 XPI |

## 发布内容

详细链路见 [架构](ARCHITECTURE.md)，用户可见变化见 [Changelog](../CHANGELOG.md)。主要增量是图注提前归属、正文/单元格/页眉页脚的统一区域记录，以及区域内排版约束。

## 验证范围

本次没有重新调用收费翻译接口，没有自动安装或操纵用户正在阅读的 Zotero 页面。已有真机语料提供结构与实际译文回归证据，但不等于新安装的真机验收。未证明所有表格都能恢复，也未证明所有文字都能在原空间放下。

安装后应重点确认：复杂三栏正文与 Figure 3，缩写表与合并单元格，长文滚到底再返回时的译文恢复，取消后的已完成译文保留，覆盖模式及 PDF 导出。

GitHub Release 工作流会在 main 收到版本变更后重新安装依赖、测试、打包，并发布 XPI 与 updates.json；远端结果应以相应工作流及发布资产为准。
