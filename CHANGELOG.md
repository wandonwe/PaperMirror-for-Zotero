# Changelog

All notable changes to PaperMirror for Zotero are recorded here.

The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and the project uses [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [3.1.9] — 2026-09-14

### 并行池只收 LLM 引擎(用户决定)

免费通道(微软 / Google)不吃提示词、不吃字符预算,限流与超时反馈粗糙:页面按整页分给一家引擎,分到免费通道
的那一页所有请求、重试、打捞都绑在它身上,它被限流时 LLM 车道空着也接不过去 —— 混用时"卡住"的根源。
现在:主引擎是免费通道 → 设置里整个「并行翻译服务商」区块不显示,池就是它一家;主引擎是 LLM → 勾选列表里
不再出现免费通道,偏好里残留的勾选也会被剔除。免费通道保留为单引擎模式,行为自洽。

### 修复:Bing 的限流盲区

Bing 抓取通道被限流时不是 HTTP 429,而是 200 里带 `{statusCode: 429}`,或一个静默的空 200。此前两者都被
包成"可重试的坏响应":车道限流收不到信号,按 400ms 快速重试继续锤;`statusCode 429` 还会先刷一次会话再重试。
现在两者都做成 RATE_LIMITED(429,退避 2 秒,有界),调度器据此给这条车道降并发、按退避等待;
空 200 仍先换一次主机(www ↔ cn)再判。

### 单选 Bing 的真机读数(记录)

Nous 2022,Bing 单引擎:41 次请求全部成功,无 429 / 超时上报(注意:限流盲区修好之前这两个数本来就测不到);
实测吞吐 356 字符/秒,单条请求 2–14 秒,车道基本串行;p6 排队 12.8 秒 + 8 次请求共 34 秒,其中约三分之一是
同一块的二次请求(验收未过后的重试)。可放回块 105/107 放回。Bing 的慢是通道本身的慢,不是卡住。

## [3.1.8] — 2026-09-14

### 修复:区域合并丢段落组边界(CAD-RADS 2022 p12 / p16 真机)

那段"按算放得下却 no-room/height"的谜底:碎片吸收与图注归位会把一个**已经合并过、自带段落边界**的区域
当作 b 传给合并函数,而旧实现假定 b 永远是单块,把 b 的全部行塞进 a 的末组 —— 文本里有两段(`\n\n` 还在),
几何里只剩一组。排版于是按整块放,pre-line 又多画一个空行,4 行的盒装 5 行,放不下。现在按组接:
段落边界 → b 的各组整体追加;否则 b 的首组续进 a 的末组,其余组原样追加。用 p16 的真实几何写了锁,
回退到旧实现即变红。

### 3.1.7 真机读数(记录)

同一篇 23 页:414/418 可放回块已放回;未放回(不含 too-small)由 3.1.4 的 41 降到 4(本版再去 1);
`echo` 14 处、几何审计 0 违例。仍待处理:6 条 300–700 字符文献条目 `unrecovered`(文献条目验收);
另观察到页面基准字号取"字号带内的最小值"会把 p1 的 10pt 正文统一到 7pt(四条 7pt 脚注拉低了它)、
p4 的 10pt 正文统一到 8.5pt —— 是否改为按文字量的众数,需要决定,本版未动。

## [3.1.7] — 2026-09-13

### 修复:绕图排的整栏正文被当成"图内文字"丢掉(McCollough 2023 真机)

三段整栏正文(809 / 877 / 1667 字符)以 `image` 为由放弃。图框横跨两栏、伸进正文栏的下部,文字绕图排;
段落的**联合盒**与图框在角上重叠约 10%,而被图挡住的那几行本来就排得短,**没有一行碰到图**。旧规则按联合盒
的 2% 判压图。现在按行矩形判:行面积落进图里的比例 >2% 才是图内文字(遮罩本来就是按行画、按图硬裁的,
准入与遮罩终于同一口径)。没有行矩形的块仍按联合盒兜底。表格格的最小尺寸门槛也改按 PDF 点。

### 3.1.6 真机读数(记录)

8 页全 done,几何审计 0 违例;`echo` 首次出现在 preserveReason 里(4 处)。放弃 8:image 3(本版修)、
no-room/height 3(7pt 作者单位 336 字符、7.5pt 致谢 1359 字符 —— 容量;另一段 9pt 表注 65 字译文在 474×39pt
盒里按算放得下却放弃,与 CAD-RADS p16 同类,待查)、shrink-floor 2("Key Results" 12pt 标题译文比盒宽;
"Age (y)" 表格格)。`unrecovered` 2 条文献条目。

## [3.1.6] — 2026-09-13

### 回声块不再算"未放回"(CAD-RADS 3.1.5 真机)

"P1/P2/P3/P4" 这类标识,验收允许模型原样返回;但拿它去替换原文毫无意义 —— CJK 字体里的拉丁字比原文宽,
9pt 的标签在自己的盒里放不下,于是以 shrink-floor 放弃、计入未放回(p20/p21 共 3 处)。现在排版阶段先判回声
(与验收同一判据:忽略大小写、标点、空白后内容相同):不遮、不放、不计失败,诊断联表记为 `preserved / echo`。

### 3.1.5 真机读数(记录)

3.1.4 → 3.1.5,同一篇 23 页:未放回(不含 too-small)41 → 7;`shrink-floor/height` 16 → 3(剩下的 3 处全是回声,
本版归为 preserved);p22 参考文献页 17 处 `no-room/height` → 0(单行容差与压缩重试叠加后放回)。
`layoutPrefs` 已随导出:字号/行距倍率均为 1,面板 869px。仍未解释:p16 一段 91 字译文(含一个段落分隔)在
474×47pt 的盒里按算放得下却 `no-room/height`,待下一份导出对照。`unrecovered` 7 条:6 条为 300–700 字符的
文献条目、1 条同步发表声明,均为回声被拒后重试仍无译文 —— 长文献条目的验收待单独处理。

## [3.1.5] — 2026-09-13

### 修复:单行小字块全军覆没 —— 高度容差按字号算(CAD-RADS 2022 真机)

3.1.4 导出(23 页)里 `shrink-floor/height` 16 处,几乎全是**单行**块:p1 两条 7pt 脚注、p4 三条 8.5pt、
p12/15/18/20 的四条表题、"No change" 这样的 3 字表格格。它们的盒高就是原文行高(≈1em),而 CJK 字体的
内容区比 1em 高得多(PingFang SC ≈1.32em、Noto Sans CJK ≈1.45em)—— line-height 已压到 1.0,`scrollHeight`
仍比盒高多 0.2–0.45em,固定 1.5px 的容差一概判为放不下。容差改为 max(1.5px, 0.35em):内容区的超出部分
是空白(墨迹在 em 框内,em 框与行框重合),真多出一行至少多 1em,0.35em 永远吸收不了它,溢出的块照样不放行。

### 诊断导出记录版式偏好

`summary.layoutPrefs`:字号倍率、行距倍率、视图模式、面板视图、面板宽度。没有它们,`no-room/height`
的读数解释不了(p13 一段 164 字译文在 130pt 高的盒里按算放得下却放弃 —— 若倍率是 1.2 以上就说得通)。

### 读数(记录)

22 页 done、1 页 released;几何审计 0 违例。放弃:too-small 30(多为图内 2–10 字符标签与短格,合理)、
no-room/height 20(其中 17 处在 p22 参考文献页:8pt 两栏、译文比原文宽 1.3–1.4 倍,正文流不缩字,原位确实
放不下;文章流里完整)、shrink-floor/height 16(本版修)、shrink-floor/none+ink 4、expand-ink/none 1。
`unrecovered` 4:p22 三条 300–700 字符的文献条目(回声被拒,重试后仍无译文)与 p1 一条同步发表声明。

## [3.1.4] — 2026-09-13

### 面板「更多」菜单:诊断与语料的四个入口只在勾选「调试」后显示(用户决定)

「导出诊断文件」「导出翻译语料」「诊断:复制脱敏指标 + 引擎自检」「语料:复制本页布局语料」全部收进
调试闸之后;普通阅读时菜单只剩「保存到笔记」「导出译文 PDF」。推翻 2.8.8 "语料常驻、与调试日志解绑"的决定,
对应的结构锁一并反转。

## [3.1.3] — 2026-09-13

### 3.1.2 真机导出(Goenka 2016,对照模式)证明的两处

- **署名行与文献作者串被回声规则判成"没译"**:p1 的 7 个作者行里 4 个(如 "Nancy A. Obuchowski, PhD"、
  "Wadih Karim, RT")、p8 的作者串("Morsbach F, Bickelhaupt S, Rätzer S,")与期刊尾段
  ("Radiol 2013;48(1):32–40.")模型按提示词原样返回,2.12.12 的回声规则只认 "Powers WJ" / "J. Smith"
  两种人名形态,于是反复重试、最后 `unrecovered`(p1 salvage 2 次、p8 3 次)。新增 `looksLikePersonNames`
  (按逗号/分号/and 分段;每段只许名字词、首字母缩写、学位缩写、姓氏小品词;**必须**含首字母缩写或学位缩写
  这类人名专属证据 —— "Document Title" / "No Evidence" 形态上与人名无法区分,没有证据就仍按没译处理)
  与 `looksLikeCitationTail`(刊名缩写 + 年;卷(期):页)。变异验证:去掉证据要求 → 2.12.12 的两条锁变红。
- **`too-small` 门槛按屏幕像素算**:p8 的参考文献 7pt 行在这个面板宽度下矮于 9px,44–46 字符的条目整条被当
  噪声丢掉(4 块),同一页在宽面板里全部放回。门槛改按 PDF 点(28/50px ÷ 1.333 = 21/37.5pt;高度 6pt),
  "这一块值不值得放"是页面几何的性质,不随缩放变。

### 读数(记录)

8 页全部 done;`preserveReason` 分布 data 29、running-head 25、symbol 14、identifier 1、marks 1;
几何审计 0 违例。放弃原因:expand-ink/none 5、shrink-floor/none+ink 4、too-small 4(本版修)、
shrink-floor/height 3(p3/p5 表格首列格)、no-room/height 2(p8 文献续行)。

## [3.1.2] — 2026-09-13

### 工具条模式菜单:去掉「完整文章流」(用户决定)

3.1.0 加进阅读器工具条模式菜单的「完整文章流」删掉;文章流的入口只保留左右对照面板工具条上的视图按钮。
胶囊上的「查看译文」仍会在覆盖模式下切到对照并打开文章流(那是定位保留段落的动作,不是入口)。

## [3.1.1] — 2026-09-13

### 修复:CI / Release 在 `npm test` 的测试类型检查上失败

首次推送 2.12.x–3.1.0 这批提交后,GitHub Actions 的 CI #49 与 Release #41 都红了:`npm test` 先跑
`tsc -p tsconfig.tests.json`,而 `tests/unit/tableBorders.test.ts` 里 6 处 `SegmentScanStats` 字面量
还是 2.12.8 的形状,缺 2.12.9 加的 `shapeUnknown / newShape / unpainted`。本地一直只跑
`node scripts/test.mjs`(不做测试类型检查),所以没发现。补齐字段;发布前的本地流程改为在干净副本里
完整重放 CI 链(`npm ci → tsc --noEmit → npm test → npm run package → gen-updates → release-notes`)。
无代码改动。

## [3.1.0] — 2026-09-13

### 覆盖翻译模式改走严格原位替换引擎(用户要求「覆盖翻译模式也要按对照翻译的能力优化」)

3.0.x 之前覆盖层有自己的一套排版:按行段分配译文、字号阶梯、放不下就裁掉加「…」。没有表格模型、
没有墨迹检测、没有扩边阶梯、没有压缩重试、没有放弃原因;而左右对照的页面视图早已是 `buildStrictPage`
(遮罩按行、量测后才提交、表格格模型、扩边阶梯、压缩重试、几何审计、每个没显示的块都有 `abandonReason`)。
两套引擎意味着同一页在两种模式下译出两种结果,而覆盖模式的「裁掉 + 省略号」正是排版禁令的头一条。

- 覆盖层现在就是一张严格页:会话把与面板**同一个** `renderDocPage` 装进覆盖层(`setPageRenderer`),
  层充当它的槽。严格页自带的底图副本用 CSS 藏起来(位图仍在内存里供墨迹检测与纸色采样),
  只让遮罩画布与译文层盖在 PDF.js 的实时画布上;层不接指针事件,只有译文块接(悬停看原文、
  双击讲解、右键重译),页面其余部分照常可选可批注。
- 原子替换语义保留:新层先挂进页节点(遮罩起初为空、底图不可见,挂上去什么都看不见),
  渲染承诺兑现、块开始提交之后再摘旧层 —— 缩放时旧译文一直在,直到新译文落地。
- 悬停看原文由严格页新增的 `pmPeek(ids, on)` 实现:清该块遮罩、隐译文;移开恢复。只对已提交块生效,
  不改 committed 状态,统计与审计不受影响。
- 「原文淡化」= 遮罩画布半透明;「悬停显示」= 遮罩与译文不画、指针到才显示。改显示模式直接改层属性,不重排。
- 「严格/扩展」两档适配模式失效(扩边由严格页自己的阶梯决定);接口保留,偏好不再影响任何东西。
- 两个表面共用每页渲染世代:覆盖模式下面板 `display:none`,但 clientHeight=0 的可视范围仍会算出一页来泵,
  隐藏的面板抢到世代后覆盖层那趟的提交回调全部退出 —— 页上留下一层永远不显示的译文。
  新增 `translationPane.setSurfaceActive()`:不可见即不泵;重新可见时所有槽标脏。切覆盖时先停面板的泵再开覆盖层。
- 旧引擎的 `drawPage` / `fitFontSize` / 行段分配全部删除;`overlayLayout` / `textFitter` 模块保留(诊断与测试仍用)。
- 覆盖模式因此**首次**拿到与对照相同的排版统计与放弃原因(诊断导出同一份)。

### 工具条:「完整文章流」入口

阅读器工具条的模式菜单(原文 / 覆盖翻译 / 左右对照)加了「完整文章流」:点了切到左右对照、面板切成文章流、
滚到当前页。覆盖模式下译文只能原位放回,放不下的段落保留原文;文章流是完整、从不裁剪的那份。

### 面板「更多」菜单

不再重复列出「整页对照 / 完整文章流」(用户:只留工具条按钮)。

### 修复

- **导出 PDF 里的「〓〓」**(Goenka 2016 p1 导出实证):区域合并把段落边界记成 `\n\n`,字体没有换行符的字形,
  sanitize 把每个换行换成「〓」—— 摘要每个分节前多出两个方块。空白不查字形,交给 layoutBlock 折叠。
- **短标题标签被 50px 门槛丢掉**(同一页):"Purpose:" / "Results:" 9pt、33pt 宽,有译文、8 字符,`too-small`;
  宽 1.5pt 的 "Methods:" 在同一缩放下刚好过线。标题与表题、图注同用 28px 门槛。

### 3.0.2 真机读数(记录)

p1 标题与 2045 字符的摘要区域均已原位放回(3.0.1 的修复在真机成立);未放回:两个标题标签(本版修)、
两段 7pt 的作者单位脚注(`no-room/height`,174/473 字符,盒高 26/108pt —— 译文在最小字号仍放不下,
属排版容量问题,未动)。`preserveReason` 分布:data 30、running-head 25、symbol 14、identifier 1、marks 1。
另:模型把 2/7 个作者姓名音译成了中文,提示词的"人名原样保留"没有完全生效,待观察。

## [3.0.2] — 2026-09-13

### 修复:「查看译文」切进文章流后一片空白、且回不去

真机截图(Goenka 2016,3.0.1):点胶囊「查看译文」,右侧面板整片空白;此前版本进了这个视图也没法退出。
两个缺陷叠在一起:

- **没有内容**:`pane.setViewKind('article')` 只换壳 —— 清空文章流的每一节、清空页槽,然后等 `renderPage`
  再送状态。整页对照有 `initPageList` 自己把槽建回来;文章流没有,而 3.0.0 的「查看译文」切过去之后什么也不送。
  现在 `readerSession.applyPaneViewKind()` 切完视图把每页已有状态重新喂一遍(与 `applyOverlay` 对覆盖层做的一样),再滚到目标页。
- **没有出口**:`viewKindButton` 从仓库重构(1b62d31)起只声明、从未创建;`onToggleViewKind` 回调只写偏好,不动视图。
  文章流视图在界面上一直没有入口也没有出口。现在工具条上有视图切换按钮(始终显示**另一个**视图的名字,
  窄窗收成纯图标),「更多」菜单里同时列出两种视图带勾选。

「查看译文」不写 `paneView` 偏好 —— 那是一次"去看译文",不是改默认视图;工具条按钮/菜单切换才写偏好。
两条结构锁(paneChrome.test.ts)均经变异验证:去掉重喂循环、去掉按钮创建,各自变红。

## [3.0.1] — 2026-09-13

### 修复:小型大写字的标题被当成"碎片"吸进摘要(Goenka 2016 p1)

3.0.0 真机导出(Goenka 2016,8 页)显示 p1 的摘要格全部空白、`abandonReason = no-room/height`,
而摘要区块长达 2246 字符。原因:标题用的是小型大写字(small caps),文本层里全是小写,
`regionCoalescer.isShard` 只看"首字母小写"就把这条 26pt、204 字符的标题当成上一段的续句碎片,
`canAbsorb` 又不查字号,于是标题整条被 8pt 的摘要区吸收,摘要因此放不下。
用同一份运行时 span 在 2.12.15 上复现,**不是 3.0 的回归**,是一直存在的缺陷。

- `canAbsorb`:超过 12 个字符的碎片,字号比宿主大 30% 以上、或两者相差超过 40%,不吸收。
  ≤12 字符的角标/引用号不受影响;8pt 续句进 10pt 段落照旧(既有测试保持绿)。
- 新夹具 `goenka2016-p1.spans.json`(3.0.0 语料的运行时 span)+ 两条锁;其余 40 个布局快照逐字节不变。
- 变异验证:去掉字号闸 → Goenka 锁与快照变红;把阈值收紧到 16% → 既有的两条"跨字号吸收续句"测试变红。

### 3.0.0 真机导出的读数(记录)

`preserveReason` 只剩 4 种(running-head 22、identifier 1、data 30、symbol 14),
10 个未放回块全部带 `abandonReason`(too-small、no-room/height、expand-ink/none、shrink-floor/*)——
3.0.0 承诺的"每个不译/没显示的块都有原因"在真机上成立。
p1 的 "Purpose:/Methods:/Results:/Conclusion:" 标签仍为 `too-small`(8 字符、盒高不足),待小块准入按角色放宽时处理。

## [3.0.0] — 2026-09-13

**原则定版:有对照的双栏阅读里,故意留一块不译没有收益,只有误判的风险。**

两类错的代价不对称:把自然语言误判成"保留",读者少看一段而不自知;把标识误判成"要翻译",
模型原样回声、验收放行,代价是一次请求。所以 3.0 的规则只有一条:**拿不准就翻译;只有能用
精确规则认定的标识和页面附属物才保留。** 这一版把 2.5–2.12 里积累的"猜形状"保留规则全部清掉。

### 删掉的"过期"保留(全是猜形状)

| 以前保留 | 依据 | 3.0.0 |
|---|---|---|
| 作者名单 / 署名行 / 学位名单 | 逗号密度、首字母大写比例、学位缩写数 | **翻译**;人名由提示词原样保留(2.5.13 起就在) |
| 栏目条(RESEARCH ARTICLE) | 词表 | 翻译 |
| 短版权行(© 2024 The Authors) | 词表 | 翻译 |
| 书目标签(Citation: / Editor: / Published:) | 标签词表 | 翻译;带 DOI 且无句子的引用行按精确规则仍保留 |
| 表格:整列多数是数据 → 整列保留 | 同列邻居 | 有词的格翻译 |
| 表格:表里有邮箱 → 首字母大写的格当人名 | 形态 | 翻译(邮箱本身被片段保护) |
| 表格:跨列格 | 结构 | 翻译;能不能原位放回由排版侧判 |
| 参考文献(选项默认关) | 类别 | **默认开**(选项保留);DOI/网址已被片段保护 |

### 仍然保留的(精确规则)

日期行、DOI/网址/邮箱行、纯资助号/注册号、孤立数字/页码、下载水印、竖排细条、逐页页眉页脚;
表格里的空格、纯标识格、纯数字/符号格、等级标记(I/IIa/IIb/III、A/B/C)、用户不译词、已定义缩写、
引用标签。每一个都带 `preserveReason`。

### 显示侧:每一块"译了没显示"都有原因

以前 `tooSmall` / `imageExcluded` / `tableIntentional` 只是计数,块级摘要里它们仍报 translated ——
"请求花了钱、页面仍是英文"没人看得见。现在这些块与放弃清单合并进 `pmAbandoned`,诊断联表后
`state: unplaced` + `abandonReason: too-small | image | table-structure | <放弃阶段>`。

### 「查看完整译文」

文章流视图(`translationPane` 的 'article')一直是完整的、从不裁剪 —— 缺的只是入口。
胶囊"N 段保留原文"的按钮改为「查看译文」:定位闪烁保留原文的段落之后,把面板切到文章流
并滚到这一页。放不下的译文不再是"看不见的"。

### 证据

40 个布局夹具全文对照(2.12.15 → 3.0.0):**总词数 0 丢失**;译文侧 +540 词、可译块 696 → 733;
保留原因只剩 data / symbol / reference / running-head / marks / identifier / defined-abbreviation,
无原因的 preserve:0。14 个快照按新原则变化(全部是保留 → 翻译),基线相应更新。
十余条旧测试按新原则改写并注明原因。测试 1307 通过。

### 不能说的

- 参考文献默认翻译会显著增加请求量(指南类文献上千条);不想要就在设置里关掉。
  "翻译文献标题、保留作者/期刊/年份"的细粒度做法仍需要文献条目解析器,没有。
- 图片里的文字不在范围内。
- 未做真实 Zotero 页面复现;下一份导出请看 `preserveReason` 分布与 `abandonReason`。

## [2.12.15] — 2026-09-13

**建块阶段认网格。** 真机 2.12.14 的 dual PDF 上,p16 表格本体已各归其位(31 格、`abandoned 0`、
`tableFailed 0`、等级格原样),p22 推荐表逐格正确。剩下的病在建块:

- 第一条灰色标题带的译文后面接着**下一格的第一句** —— 标题带与紧贴其下的格首行(间距 3.7pt、
  同一 x 起点)被段落启发式焊成一块;这个块横跨六列退回段落路径后溢出,那一格只剩下半截。
- 第三条标题带折成两行,第二行 "oral anticoagulation—Section 4" 被单独切成一块,译文页上留着英文
  —— 那一行在行序里排在第 0 位(行序是老问题),行层的相邻判断根本碰不到它。

### 改了什么

1. `buildBlocksFromSpans` 接收 `grid`。**网格行线是硬屏障**:两行之间隔着一条行线,不许合成一个段落
   (与图片屏障同一条路)。
2. `mergeGridCellStacks`:同一格里上下紧邻(间距不到一个字号)、x 重叠、同字号的两个段落合成一个 ——
   在段落层做,与行序无关。隔得远的、隔着行线的、网格外的都不碰(反向锁)。
3. 运行时把 `gridFor()` 的结果传进建块。

ESC p16 离线:标题带块不再含 "Nicorandil…",r2c0 从 "Nicorandil" 开始;第三条标题带两行合成一块。
网格外的块与不传网格时逐字节相同。39 个旧快照与基线不变。测试 1305 通过;4 条变异全红。

### 2.12.14 真机读数(对照 2.12.13)

`unplaced` 71 → 42,`unrecovered` 1 → 2,`preserveReason` 有值了(running-head 144 / symbol 222 / data 108 …)。

### 不能说的

- 这一版的改动没有真机视觉验证;请看下一份 dual PDF 的 p16 三条标题带。
- p22 左表 Grade IV 第三格仍是英文(小格准入,审核第 7 条)。

## [2.12.14] — 2026-09-13

**边框网格默认参与建格。** 证据链在 2.12.13 的真机导出和用户导出的 dual PDF 上闭合。

### 真机 2.12.13 读数(2024 ESC CCS 指南)

- 网格与本地**逐页精确一致**:p22 `[304,321,553,704]` ↔ 离线 `303.8..552.8 × 321.1..704.1`;
  p45 两张 3×8 精确一致;p86 3×27。
- 块状态:preserved 164 → 414(不再丢弃,全部带原因)、unrecovered 22 → **1**(短文本照抄那条修的)。
- `unplaced` 62 → 71,但同一篇文档逐页对照(p13/16/17 的 abandoned 5/10/8)**一模一样** ——
  新增的 414 个遮挡物没有让任何页多丢一块;放不下是本来就有的问题。
- 两处遥测漏洞:`preserveReason` 在诊断导出行里是空的(摘要有、导出没接);
  重抽取时网格走缓存,`edgeSegments/gridCols` 不再回填(p12–18 只剩 `gridInside`)。两处都补了。

### dual PDF p16(Table 4 修订推荐)看到了什么

左栏译文跨格叠字、小节标题带溢出到下一行。原因不是排版:**文字几何路径在这一页一个格都没认出来**,
32 个块被当段落自由摆放。而边框网格是 **6 列 × 12 行**(2019 推荐 / Class / Level / 2024 推荐 /
Class / Level),真机也算出来了(`gridInside 38`)—— 只是"只观测、不建格"。

### 改了什么

1. `structureTableCells` 的 `useGrid` 默认 **true**。显式传 false 仍是纯观测(与不传网格逐字节相同)。
2. **跨列的块不按中心点分格**(`spansColumns`):横跨六列的小节标题带,中心点掉进 35pt 宽的 Level 列,
   127 个字符塞不进去。框比所在列宽出 40% 以上的块退回段落路径。均匀网格表达不了合并单元格。
3. 指南推荐表的等级格(Class I/IIa/IIb/III、Level A/B/C)记 `symbol`,不送翻译 —— 回声规则本来会放它们过,
   但白花请求。
4. 网格取证计数随网格一起缓存,重抽取时回填遥测。
5. `preserveReason` 接到诊断导出行。

新增夹具 esc2024-p16(spans + rawops);其余 39 个快照与基线**纹丝不动**(快照流水线不传网格)。
测试 1302 通过;4 条变异全红。

### 不能说的

- **这一版没有真机视觉验证。** p16 的网格路径给出 33 个格、推荐文字落 c0/c3,是离线结论;
  译文在格里放不放得下,要看下一份 dual PDF。
- 小节标题带退回段落路径后仍可能溢出(截图里已经溢出)—— 这是段落放置的"放不下"问题(审核第 7 条),没动。
- 一页多表仍只接格数最多的那一张。

## [2.12.13] — 2026-09-13

**原则改了:原文侧负责忠实保留;译文侧负责完整理解。元素类别决定排版方式,不决定是否翻译。**
(用户 2026-09-13)产品目标:译文侧默认全文翻译,允许标识原样保留,不允许自然语言内容
因为元素分类而无提示缺失。

### 改了什么

1. **内容决策换成 `classifyContent()`**:`translate / preserve(带原因) / skip`。自然语言一律翻译;
   只有标识保留 —— 人名(`names`)、日期(`dates`)、DOI/网址/纯资助号(`identifier`)、
   书目标签(`bibliographic`)、下载水印(`watermark`)、孤立数字(`marks`)、栏目条(`banner`)、
   短版权行(`boilerplate`)、竖排细条(`sliver`)、逐页页眉页脚(`running-head`)。
   **几何位置(页边栏)不再决定翻不翻。** 与以前相比翻转为翻译的:作者单位(机构名称)、
   作者注/脚注、整句版权与许可声明、资助句、"funders had no role" 尾句、通讯说明整句。
   标签行("* Corresponding author. Tel: … Email: …"、期刊卷页 DOI 行)仍保留。
2. **提取阶段不再整块丢弃。** 两条建块路径(span / blockBuilder)里不译的块全部以
   `translationMode: 'preserve'` + `preserveReason` 保留 —— 墨迹几何进遮挡物,摘要里看得见。
   `SourceBlock.preserveReason` 正式进模型;逐块摘要与诊断导出带上它。
3. **片段保护补齐标识**:邮箱、网址、DOI、NCT/ISRCTN/ChiCTR 注册号被掩蔽后原样还原。
   "Contact the corresponding author at name@example.com." 现在翻译整句、邮箱不动。
4. **表格格(审核第 4 条)**:含邮箱的格翻译(邮箱被保护);剩下只有人名的格保留(`name`);
   纯邮箱格保留(`identifier`);跨列格记 `structure-ambiguous`,不再混进 `data`;
   被"整列多数是数据"带成 data 的格记 `column-data`。每个 data 格都有原因。
5. 页面附属的 preserve 块(页眉/水印/日期行/DOI 行)不参与表格区域探测(孤立数字例外 ——
   表里的年份格在提取阶段看就是一串数字,得进表)。

### 证据

39 个布局夹具全文对照(2.12.12 → 2.12.13):**总词数 0 丢失**;译文侧 +1179 词、
可译块 664 → 667(chen2023-p1 的 9→8 是合并);块总数 1318 → 1442,新增的全是带原因的 preserve;
**preserve 块无原因的:0**。原因分布:data 371 / symbol 140 / reference 96 / running-head 77 /
structure-ambiguous 22 / column-data 14 / names 13 / marks 11 / identifier 9 / name 9 /
defined-abbreviation 7 / boilerplate 5 / bibliographic 1。请求计划基线相应更新(可译块只增不减)。

十条旧测试按新原则改写(单位/通讯/版权/资助尾句/页边栏几何/作者注),每条注明原因。
tableRowFidelity 里两条用例把没有 `-cN` 的 preserve 块也数成了一列,改为只数真格。

### 老实说

- 第 5 条(附属块不进表)造不出能变红的用例 —— 表格判据本来就挡住它们。**这条没有锁。**
- 参考文献仍按 `includeReferences` 选项走(默认保留);"翻译文献标题、保留作者/期刊/年份/DOI"
  需要一个文献条目解析器,还没有。
- 图片里的文字不在这一版范围。
- "放不下时查看完整译文"、真正的单块覆盖翻译(审核第 6、7 条)仍未做。
- 旧缓存:块 id 按序号生成,新增的 preserve 块会让同页后续块的 id 变化 → 整页缓存对那页失效,
  段落缓存(按文本哈希)仍命中;被旧规则跳过的内容第一次打开就会补译。
- **未做真实 Zotero 页面复现。**

测试 1301 通过;12 条变异 11 条变红。

## [2.12.12] — 2026-09-13

**内容保留规则审核(用户 2026-09-13):七条发现全部在当前代码复现,这一版修前三条 + 第五条的第一步。**

### 修了

1. **短文本照抄不再算翻译成功。** `looksTranslated` 以前对散文词数 < 6 的块无条件放行,
   "No evidence of benefit" / "Document Title" / "Year Published" 原样返回都被计作成功、写进缓存。
   现在把"允许原样保留"与"译文合格"分开:**内容相同**(忽略大小写/标点/空白)的回声,只有当
   原文没有自然语言词(数字、单位、缩写 CT/MRI/COVID-19/IIa、人名 "Powers WJ" / "J. Smith")时
   才放行;含普通词的短句原样返回判为未译,进有上限的重试链。不要求短块必含中文。
   整页缓存与段落缓存命中都走同一个 `accept()`,旧缓存里的照抄条目自动被重新检查,不用清缓存。
2. **说明类标签不再整段丢弃。** `RE_META_LABEL` 拆成两类:书目类(Citation / Editor / Received /
   Published / Copyright)仍是元数据;说明类(Funding / Ethics / Patient consent / Data availability /
   Abbreviations / Author contributions / Conflicts of interest / Trial registration)后面有自然语言
   (≥3 个普通词)就翻译,只有纯资助号/纯注册号才跳过。此判定先于资助号规则 ——
   "Funding: This work was supported by grant no. 12345" 是一整句要翻的话。
   布局快照 39 个里变了 2 个(chen2023-p10、hakime2007-p2),**全是加块**:各多出一段
   "Author contributions: …",没有任何块丢失;请求计划基线相应 +1 块。
3. **允许翻译的参考文献能显示了。** 排版的 `geometric` 以前无条件排除 `isReference`,
   而开了"翻译参考文献"只是不打 preserve —— 请求花了钱,页面仍是英文。现在 `isReference` 只是
   内容类别:仍保留原文(`translationMode === 'preserve'`)的参考文献才是墨迹遮挡物;允许翻译的
   进排版,放不下由 `preserved` 接住。新增 `selectGeometricBlocks()`,与 `selectInkObstacleBlocks()`
   严格互补。
5. **排版不再独立重判"值不值得翻"(第一步)。** `strictPageReplacement` 里的 `isMetadataBlock`
   只在**没有译文**时才生效;上游决定翻译并拿到译文的块,显示阶段只判"能不能安全放置"。

### 没修(下一轮,需要设计)

- 第 4 条:表格里 `straddles`(结构问题)与 `data`(内容无需翻译)混同;含邮箱整格保留;
  人名判定只靠首字母大写。
- 第 5 条的后半:提取两条路径(span / blockBuilder)的豁免范围统一,下游继承一次决策。
- 第 6 条:真正的"翻译此处保留内容"(单块覆盖内容策略,贯通提取记录/翻译/显示/缓存)。
- 第 7 条:放不下的译文提供"查看完整译文";小块准入按元素角色而非统一 6 字符门槛。
- `preserveReason` 进逐块摘要;提取时丢弃的块也进摘要(位置、原因、规则来源)。

测试 1296 通过。10 条变异逐条验证。**未做真实 Zotero 页面复现**;这些修正只对审核里
点名的纯函数行为负责,不等于每一处英文残留的原因都在这里。

## [2.12.11] — 2026-09-13

**拿到了真机那篇 PDF(2024 ESC 慢性冠脉综合征指南),2.12.9 遗留的两个问号都钉死了。**

真机 18 页的 `edgeOps` 与本地 5.7.284 **18/18 分毫不差** —— 这些夹具就是真机看到的操作符列表。
六页(p22/p25/p44/p45/p80/p87)转成 `.rawops.json` 夹具(只有几何,无正文),真值两路:
pdfplumber 独立切表 + 渲染叠加肉眼核对。

- **"每条路径恰好 3 条线段" = 三条 lineTo + fill。** 2.12.10 的隐式闭合把第四条边补上后,
  p87 从 633 → 844 条,推荐文字那一整列(x42.5..487)出现了 —— 真机 2.12.9 只看到右侧
  Class/Level 两列(x486..558),原因就是每个色块的左边一直缺着。现在 p87 是 3 列,
  列边 42.5 / 487.0 / 522.6 / 558.4,与 pdfplumber 一致。
- **插图页 p25/p44/p80**:裁剪路径 20/16/29 条计入 `unpainted`,曲线不再误判(`skipped` 0),
  网格 0 张 —— pdfplumber 也说这三页没有表。整页假网格没了。
- **新发现:填充之间的白缝被当成了行。** p87 我报 35 行,pdfplumber 与肉眼都是 27。
  多出来的 8 条正好是 4 条小节标题带的 8 条缝:每条标题带都是"上缝 3.3pt + 灰带 9pt +
  下缝 3.7pt"。一条 3pt 的"行"装不下任何文字。新增 `minCell`(默认 5pt):窄于它的格是缝,
  两条边界合成一条。p87 → 27 行、p22 右表 → 8 行、p45 → 6 行 + 8 行,全部与 pdfplumber 一致。
  9pt 的标题带保留(有反向锁:阈值放到 10 会变红)。
- p22 左表的 **Grade 列(x36.9..73)pdfplumber 漏了**,渲染叠加肉眼看是真的一列(I/II/III/IV),
  以肉眼为准,测试锁的是 3 列。

网格仍只观测、不参与建格。测试 1287 通过。

## [2.12.10] — 2026-09-13

**边框取证在真机上活了;第一份读数又揪出三处旧病,全部本地复现、夹具锁死。**

### 2.12.9 的真机读数

18 页有绘图指令的页面 `edgeSegments` **全部非零**,`edgeNewShape == edgeByCode` 逐页相等,
`edgeShapeUnknown = 0`。解码器对了。但:

- 三个插图页的网格区域恰好是**整页** `[0,0,595,794]`(p25 3×6、p44 1×6、p80 3×6);
- 五页出现 `2 列 × 33~36 行`、宽 72pt、贴着右缘的"网格",`gridInside` 0~3;
- 插图页有 29~36 条路径被 `edgeSkipped`(p44 达 45%)。

### 三处旧病(新旧编码都有,不是 2.12.9 引入的)

1. **裁剪路径当了表格线。** `re W n` 也走 constructPath,可收尾是 `endPath`,一个像素都不画。
   整页裁剪矩形当外框,页内真表格就被拼成"整页一张表"。本地 Powers 2019 p43(整页裁剪)、
   Gulati 2021 p4(图片裁剪框)都复现。现在按收尾绘制码处理:`n` 不画;fill 家族与 `s`
   把开放子路径**隐式闭合**(色块单元格三条 lineTo 也是四条边);`S` 不补。
   旧编码的收尾码是紧随其后的操作符,中间夹着 `clip` 也要看穿。新增 `edgeUnpainted` 遥测。
2. **曲线包围盒算错了。** 2.12.9 说"控制点一并计入,实测 pdf.js 就是这么算的"—— **错**。
   pdf.js 源码走 `bezierBoundingBox`,求导取极值,是紧包围盒。之前 11630 条零误差只因为
   那两篇的曲线全是圆角矩形的四分之一弧,控制点恰好落在紧包围盒边上。插图页的一般曲线
   因此被 minMax 交叉验证误判、整条跳过。现在按曲线真实极值算(自己实现的三次极值,
   测试用密采样独立对照,不抄 pdf.js)。
3. **一页只找一张表。** Powers p43 一页叠三张表(pdfplumber 独立切出 y 66..222 / 265..466 /
   479..749,列边相同)。按整页算,每张表的竖线只盖住合并区间的 40%,围合判据全否。
   而且 `cluster()` 在同一 x 上只留最长的一段,另两张表的边直接丢了。现在:
   - 新增 `borderGrids()` 交出**所有**网格:竖线按 y 区间连通分组,每组各自围合;
   - `mergeCollinear` 先按位置分桶再按起点排序 —— 以前 408.0 与 408.5 两个位置排成前后
     两串,后一串的 from 比前一串的 to 小,把隔着 12pt 的两张表的竖线连成了一根;
   - 同一位置的几段覆盖长度**相加**(被合并单元格截断的列线),但没有线的那一段仍然没有线;
   - `borderGrid()` 保持一张(格数最多的),结构识别暂只接一张;新增 `gridCount` 遥测。

   结果:p43 三张表的行线与 pdfplumber **逐条一致**,表 2、表 3 四列齐全。表 1 仍是 2 列 ——
   313/359 两条列线在那张表里只盖住 46%(有一整行是合并单元格),按"不把短线延长成边界"
   的规矩不算列边。**均匀网格表达不了合并单元格**,这是已知局限,不猜。

### 两处纠正

- **网格至少 2 列且 2 行。** 模块注释一直这么写,代码却只要求 2 条线(= 1 列)。真机 p44 的
  1×6 就是这么来的。一个单列的框是盒子,不是需要分格归属的表。五条 2.12.4 的合成测试
  相应改成 2×2 基础网格。
- **曲线之后当前点跟到弧的终点。** 2.12.9 把它置空,连带丢掉了曲线之后的 lineTo 和闭合弦 ——
  圆角矩形的四条直边全在圆弧之后,置空等于把圆角表格的边框整个丢掉。弧本身仍不产出线段。
- **Gulati p21 的验收值改正:** 2.12.9 说"5 列、y52..529、4 行"。pdfplumber 给的是
  y 72.4..528.8、**3 行**;y=52 那两截是表上方分开的页眉横线,不是表的行。现在与 pdfplumber 一致。

### 仍然不能说

- 真机那 5 页贴着右缘的 2 列 × 35 行(x486..558)是什么,没有 PDF 判不了 —— 从截图看像
  推荐表右侧的"Class / Level"色块列:色块有边、正文列没有,属于 brief 里的"混合表"。
  `gridInside` 0~3 说明文字几乎不落在里面,`useGrid` 若打开也会被 `inGrid ≥ 6` 挡住。
- 真机所有表格页每条路径恰好 3 条线段(p22 276/92、p45 324/108 …)。若是三条 lineTo + fill,
  这一版会补成四条;是不是,要等下一份导出。
- 网格仍然**只观测、不参与建格**。

测试 1281 通过。13 条变异逐条验证。

## [2.12.9] — 2026-09-12

**Zotero 那版 PDF.js 换了路径参数的编码,边框取证一直在读旧格式。**

2.12.8 修掉 Xray 之后真机第一次给出完整读数:`edgeRealOps=true`、`edgeOps` 400~1200、
`imageRects` 从 0 变正 —— 可 `edgeSegments` 仍然每页 0,`edgeByCode=0`、`edgeByShape=2`。

把真机 46 页的 `edgeOps` 与本地同一份 PDF 逐页对照:42 页分毫不差、4 页差 2~3。同一份文件
换到 pdfjs 5.3+,op 总数与 `byShape=2` 全部对上。pdf.js 5.3+ 把 constructPath 的参数从
`[子操作码数组, 坐标数组, minMax]` 换成 `[收尾绘制码, [扁平指令流, ...], minMax]`,子操作码
换成路径内部的小枚举(`0=moveTo 1=lineTo 2=curveTo 3=quadraticCurveTo 4=closePath`,取自
pdf.js 源码常量)。

- 两种编码都认。新解码器先自证再交货:指令流必须刚好消耗完;解出来的包围盒必须与
  pdf.js 自带的 `minMax` 吻合。线段先进暂存区,过关才交货。
- 认 closePath 的闭合边(新编码里矩形是 moveTo + 三条 lineTo + closePath)。
- 遥测补 `edgeShapeUnknown` / `edgeNewShape`:2.12.8 缺的正是前者,"码对上、形状不认识"
  在计数前被 continue 掉,`edgeByCode` 恒为 0 被误读成"操作符码错了"。
- 纠正 2.12.7 的注释:"`win.pdfjsLib` 取不到、退回内置码表"是错的。
- 新增 `scripts/dump-rawops.mjs` 与四份 `.rawops.json` 夹具(只有几何,无正文)。

测试 1271 通过。11 条变异逐条验证。

## [2.12.8] — 2026-09-12

**根因找到了,而且是我自己抄来的老病:边框取证从来没穿过 Xray。**

### 遥测怎么指出来的

2.12.7 加的计数**一个都没出现**(`edgeByCode`/`edgeByShape`/`edgeSkipped` 全缺),
只有 `edgeSegments: 0` 和 `gridMs ≈ 55ms` 在:

    压根没走到解析那一步 —— 55ms 正好是**一次 50ms 轮询**,
    即 `getPage()` 之后、`getOperatorList()` 之前就 return null 了。

### 是什么

Firefox/Zotero 里 chrome 代码看 content 侧的 **JS 对象方法**会被 Xray 挡住,
`wrappedJSObject`(本仓库的 `waive()`)是唯一的穿越办法。

这个仓库**在 2.9.9 就学过这一课** —— 模块开头写着"凡是走 JS 对象方法的路都不通,
走 DOM 的路都通",能用的 `extractFromTextContent` 在 window →
PDFViewerApplication → pdfDocument → `getPage()` 结果 → 内容 **每一跳**都套了
`waive()`。

而 2.12.4 我新写 `getPageEdgesPdf` 时**从 `getImageRectsPdf` 抄的代码** ——
后者是 2.9.9 **之前**写的、从未补上 waive。于是 `pdfDocument.getPage` 看得见,
它**返回的 page 代理**上的 `getOperatorList` 被挡住,连着四个版本
(2.12.4~2.12.7)一条线段都没取到。

**顺带一个发现**:`getImageRectsPdf` 是同一段未 waive 的代码,所以图片矩形
**极可能从一开始就静默返回空数组** —— 图表屏障一路靠亮度网格兜底,坏了没人会发现。
两处都补上了,并加了 `imageRects` 遥测:下一份导出这个数应该从 0 变正。

### 我这几轮错在哪

- 2.12.5:把一个看不见的东西接进主路径(跳过了"先量")。
- 2.12.6:把 p21 的 3 个格归给网格 —— 而**网格从未运行过**,肇事者根本不在场。
- 2.12.7:去修操作符码,而真正卡住的是更前面一跳。

三次都是同一个毛病:**手上没有运行时的数就先下结论**。补上遥测之后,
2.12.7 的数据一眼就把位置指对了。

### 加了一条结构闸

单元测试碰不到真机的 Xray,所以锁**源码形态**:凡是走 operator list 的取证函数,
window / PDFViewerApplication / pdfDocument / `getPage()` 结果 /
`getOperatorList()` 结果 / OPS 表 —— 每一跳都必须穿过 `waive()`。
抄代码的人下次会被这条拦住,而不是等四个版本之后才发现。

5 处变异逐条验证全部转红(其中一条正是"page 代理不 waive" —— 栽了四个版本的
那一跳)。

### 网格仍然只观测

`useGrid` 仍默认 false。下一份导出先对答案:`edgeSegments` 该非零、
Powers p4 该是 3 列 18 行、Gulati p21 该是 5 列、区域 x50..535 y52..529。
对上了再谈接进建格。

### 测试

新增 3 条(两个取证函数的逐跳 waive + waive 本身的退回保证),共 1255 条全绿。

## [2.12.7] — 2026-09-12

尺子装上了,答案一眼就出来了 —— 而且**又更正我上一轮的判断**。

### 遥测说什么

    60 页,每一页:edgeSegments = 0,gridCols = -1,gridMs ≈ 55ms

操作符列表**拿到了**(55ms 不是白花的),但一条线段都没认出来。

这推翻了 2.12.6 的判断:**2.12.5 的 p21 不是被网格弄坏的 —— 网格从未生效过。**
那 3 个格就是文字几何在真机文本层上的结果,与边框毫无关系。我上一轮把一个
从未运行的东西当成了肇事者。

### 为什么一条都认不出

`win.pdfjsLib` 在 Zotero 的阅读器 iframe 里取不到,于是取证退回内置码表;
而内置的 `constructPath: 91` 与 Zotero 那版 pdf.js 不一致,那一支分支永远进不去。

**顺带一个发现**:图片矩形那条路用的是同一个取法(`winOps ?? {}`),所以它
**可能也一直静默失效** —— 它有亮度网格兜底,失效了也没人会发现。新的遥测
(`edgeRealOps`)会把这件事一并说清楚。

### 修法:不再猜码,让它自证

1. **按参数形状认 `constructPath`**。它的参数是 `[子操作码数组, 坐标数组]`,
   两个都是数字型 ArrayLike —— 这个形状很独特:`transform` 是 6 个数的平坦数组,
   `setLineDash` 是 `[数组, 数字]`,`paintImageXObject` 是 `[字符串, 数, 数]`。
   码认不出就按形状认,pdf.js 版本再变都不影响。有反向锁防误认。
2. **子操作码映射先自证再用**。按映射把坐标消耗一遍,必须**刚好**用完
   `coords.length`;不平就整条路径跳过并计数。老实现遇到未知子操作会 `k += 2`
   猜着往下读 —— 产出的坐标全是错位的,**错位的线会推出一张假网格,比一条都
   不取更糟**。有一条用例专门防"猜了恰好配平就放行"。

### 补全的遥测

逐页新增:`edgeOps`(操作符总数 —— 0 才是"压根没拿到指令")、`edgeByCode`、
`edgeByShape`(>0 = 内置码表与运行时不一致)、`edgeSkipped`(>0 = 子操作码也
对不上)、`edgeRealOps`(是否拿到了真实 OPS 表)。

这三个数放在一起,下一份导出能直接分清"没有绘图指令"、"码不对但救回来了"、
"子操作也不对"这三种情形 —— 不用再靠我猜。

### 网格仍然只观测

`useGrid` 仍默认 false。先确认线段取得对、网格算得对(对照离线:Powers p4
应是 3 列 18 行、Gulati p21 应是 5 列、区域 x50..535 y52..529),再谈接进建格。

### 测试

新增 6 条(形状认码、账不平跳过、未知子操作跳过、猜了配平也不放行、形状不误认、
默认码可见),共 1252 条全绿。7 处变异逐条验证全部转红。

## [2.12.6] — 2026-09-12

**把 2.12.5 的边框建格降回"只观测"。** 它在真机上把一页弄差了,而我当时没留任何
遥测,拿不出证据说明运行时算的是什么网格。

### 发生了什么

2.12.5 的导出读的是 Gulati 2021 胸痛指南(不是 Powers —— 我一开始拿两篇不同的
文献比 p4,读错了一轮,这里更正)。这篇的 **p21 正是截图 1 的 Table 5**:

    真机 2.12.5: 3 个格,全部标成 c0,全在 x=345
    离线同一份 PDF 推出的网格: 5 列 x 4 行,列边界 49.6/146.8/243.9/341.1/438.2/535.4

x=345 本该落在第 4 列,却被标成 c0 —— 说明**运行时算出来的网格只有一列**,
与离线完全不同。

查过但**不是**原因的:
- 操作符码。save/restore/transform 与已跑通的 imageObstacles 一致(10/11/12),
  path 系列与 pdfjs-dist 4.10.38 一致。
- PDF 本身。把完整文档的第 21 页与 qpdf 抽出的单页分别跑一遍,**60 段、5 列 4 行,
  逐字节相同**。
- 页索引。语料里 p21 的格 id 是 `page-20-...`,对得上。

到这里我就没有证据了 —— 因为运行时的网格**一个字节都没有被导出过**。

### 这一版做什么

1. **网格降为观测**:照常计算、随诊断导出,但**不参与建格**。
   `structureTableCells` 的 `useGrid` 默认 false,有专门用例锁住"传了网格但
   不开开关时,输出与压根不传网格逐字节相同"。阅读行为回到 2.12.4。
2. **补上遥测**(随现有 ExtractPhases 通道导出,逐页):
   `edgeSegments` 取到几条线段、`gridCols`/`gridRows` 推出几列几行、
   `gridRegion` 网格外框、`gridInside` 有多少块落在网格内、`gridMs` 耗时。

下一份导出就能直接对答案:Gulati p21 的 `gridCols` 是不是 5、`gridRegion` 是不是
x50..535 y52..529。是,就说明网格对、问题在建格那一步;不是,就说明运行时取到的
线段与离线不同,`edgeSegments` 会告诉我差在哪。

### 我的判断错在哪

不是网格算法错了 —— 它有 13 条用例、对着两份真机 PDF 和 pdfplumber 的独立真值验过。
错的是**我把一个看不见的东西接进了主路径**。这个项目一直的节奏是"先量后改",
2.12.5 我跳过了"量"这一步,直接让用户去当探针。

### 测试

新增 1 条(默认不参与建格的惰性锁),共 1246 条全绿。原有的边框用例改为显式
打开开关 —— 网格逻辑本身仍然全部锁着,只是不再默认生效。

## [2.12.5] — 2026-09-12

真机诊断到手。**边框接进了抽取路径**;另外更正一处我在 2.12.2 写错的归因。

### 真机确认:截图 2 的 bug 在 2.12.4 里原样存在

这次读的正是 Powers 2019 那篇,**p4 就是那张表**。2.12.4 的语料显示:

    表格模型只有 2 列(c0=年份、c1=缩写),17 行。
    整个 Document Title 列在表外 —— 那些标题以普通块散落在 x=53。
    最后两条记录的格(N/A、2018 Cholesterol Guidelines)也掉在外面。

与我离线夹具预测的完全一致。文字几何这条路在这一页就是给不出第一列。

### 边框接进抽取路径

`structureTableCells` 多了一个可选的 `grid` 参数:这一页画着网格时,行列直接
由线段给出,不再从文字几何去猜;**拿不到网格就走原路**。

真机页面的效果(对照 pdfplumber 按边框独立切出的真值):

| | 结果 |
|---|---|
| 接线前 | **0/17** 条记录正确(整列不在表里) |
| 接线后 | **15/17** 条完全正确(标题+年份+缩写同行) |
| 列数 | 2 → **3** |
| 格盒 | 文字外接框 → **格线围出的那一格** |

剩下 2 条的年份在**建块阶段**就丢了(两个 "2018" span 确实存在于 top=499/534,
但没被产出成块)—— 网格只能决定"已有的块归哪一格",救不回压根没进来的文字。
已写成用例记录在案。

### 为什么这条路安全

- **绝大多数页面没有表格线**,`borderGrid` 返回 null,行为与 2.12.4 逐字节一致。
  有专门一条用例锁住这点:传 null 与不传,三页真机语料的输出 id 与文本必须完全相同。
- 网格里块太少(<6)不算表 —— 一条装饰线框住半句话不能变成表格。
- 网格之外的块**照旧走文字几何**,同一页可以既有带框表也有无框表。
- 排版期会对抽取期的格二次聚类,真机页面实测**0 个合并** —— 格 id 不会错位。
- 取数是 best-effort:超时 3 秒、逐页缓存、绝不抛;取不到就是没有边框证据。

### 更正:2.12.2 对 bing-free 的归因是错的

当时我写"差别不在端点快慢,在请求条数",却同时引用了 2798 ms vs 281 ms 的
单请求延迟 —— 那恰恰**就是**端点慢。2.12.4 的逐请求计时把这件事说死了:

| | 同样 ~712 字符的载荷 | 5 字符 | 5390 字符 |
|---|---|---|---|
| google-free | **166 ms** | 97 ms | 301 ms |
| bing-free | **3310 ms** | — | — |

google 的耗时几乎与内容量无关(一次批量请求),bing 每次往返本身就要约 3 秒。
**端点就是慢 20 倍。**

分组本身**按设计在工作**:真机 p26 的 34 个片段被打成 9 组(与离线预测的 73%
吻合),但 9 次往返 × 3 秒仍是 21.8 秒。吞吐 238 → 286 字符/秒,跨会话看基本
在噪声里。所以打包是对的、留着,但它治不好这一项;真正起作用的是加权分页 ——
bing 的权重已被实测压到 0.5。

已把这段更正写进 `bingFree.ts` 的模块注释,旧推断保留存档但标明以更正为准。

### 这次运行的健康度

零错误码、零参数自愈、193k 入 / 98.8k out token。engineRotations 4。

### 测试

新增 4 条(接线后的真机页、惰性保证、四条约束、内容守恒+剩余表),共 1245 条全绿。
6 处接线变异逐条验证全部转红:网格存在也不用、块太少也建表、剩余块不走文字几何、
剩余块丢掉、格盒改回文字外接框、data 格标成 translate。

### 仍**没有**做的

- 建块阶段还不认网格(跨格的块应当被拆开),所以那一页仍差 2 条年份。
- 排版端没有改动(实测不需要,但也没有把网格传过去)。
- **没有真机视觉回归** —— 这一版需要你再导一份诊断,看 p4 的表格模型是不是
  真的变成 3 列。

## [2.12.4] — 2026-09-12

用户给了原始 PDF。这一版的结论是**证伪**出来的:纯文字几何**做不到**,边框可以。

### 先说最重要的:2.12.3 的修法在真机页面上只对了一半

拿到 Powers 2019 p4(截图 2 的原表)后,把它做成夹具跑真实管线:

    修复前:整个 Document Title 列**不在表格模型里** —— 17 条记录 0 条正确。
    检测出的区域只有 x 413..533,只剩年份与缩写两列。

原因很具体:行对齐扫掠有一道 `trimmed.length <= 60` 的闸,而文献标题每条
79~242 字符,**永远进不来**。那些标题随后被当普通段落各自摆放,与自己那一行
的年份、缩写再无关系 —— 屏幕上就是"左边标题各自流动、右边还排在行里"。

### 放开那道闸的尝试,以及它为什么必须撤回

试过三种放宽(长文标签列、尾行折行去长度闸、区域向下吃新行)。Powers 页确实
修好了 **17/17**,但同一次放宽让 wu2026-p6 的**正文栏**被吞进表格区域,区域
从一张表长成 x 51..544 / y 71..711 的整页,断词不再重连:

    "substantial" → "stantial"        (词被切断)
    "intermediate-sized" → 不再重连

**丢字是红线,三处放宽全部撤回。** 撤回后 37 页语料丢词 0 页。

### 为什么不是"再调一调判据"

两页在每一个文字指标上都要求**相反**的答案。连最直觉的那条也是反的:

| | Powers 标题列(该进表) | wu2026 正文栏(该挡住) |
|---|---|---|
| 块间距中位数 | **14.9**(紧) | **38.6**(松) |
| 文本长度 | 79~242 字符 | 同量级 |
| 左缘成列 | 是 | 是 |
| 逐行对齐表格行 | 是 | 也能是 |

**纯文字几何分不开这两种情形。** 这不是判据没调好,是证据不够。

### 边框能,而且数据一直就在那里

用**阅读器同一个 pdfjs** 的 `getOperatorList` 读两页的绘图指令:

| 页 | 线段 | 推出的网格 |
|---|---|---|
| Powers 2019 p4 | 141 段(129 lineTo + 3 矩形) | **3 列 × 18 行**,列边界 46.2 / 399.4 / 450.4 / 537.8 |
| Gulati 2021 p21 | 60 段(43 + 5 矩形) | **5 列 × 4 带**,正文是**一整条** 428.9pt 高的带 |

Powers 的 18 行 = 1 表头 + 17 条记录,与 pdfplumber 按边框独立切表的真值**逐格
一致**;Gulati 的"正文一整带"正是"五列独立清单"的结构证据 —— 2.12.3 的文字
几何在这一页推出了 **49 行**,凭空捏造了不存在的行关系。

而 wu2026-p6 那片正文栏周围,**一条这样的线都没有**。

按边框建格后,Powers 页 17 条记录里 **15 条完全正确**(标题+年份+缩写);
剩下 2 条的年份在**建块阶段**就丢了(两个 "2018" span 确实存在于 top=499/534,
但没被产出成块)—— 网格只能决定"已有的块归哪一格",救不回压根没进来的文字。
这条也写成用例记录在案。

### 这一版交付什么

**纯增量,不改变任何现有行为**(37 页语料逐字节对比:31/37 完全相同,丢词 0,
其余 6 页是 2.12.3 已审阅过的分组改进):

- `src/reader/tableBorders.ts` —— 绘图指令 → 线段 → 硬网格。纯几何,可单测。
  brief 明令禁止的六条逐条对应并各有反向锁:不延长短线、不做笛卡尔积、
  不补缺失分隔线、不把闭合矩形都当格、不把页眉线/坐标轴/折线图当表、
  共线但远隔的两截不算一条线。
- `getPageEdgesPdf()` —— 运行时取数,与已跑通的 `getImageRectsPdf` 同一套
  "轮询标志位、绝不 await 内容域 promise"的纪律;拿不到就返回 null。
- `buildGridTableModel()` —— 按网格建格,抽取期与排版期共用同一个函数
  (格 id 必须逐字节一致)。格盒用**格线**围出的那一格,不是文字外接框。
- `scripts/dump-edges.mjs` + 两份真机 `.edges.json` 夹具。

### 还**没有**做的(下一步)

- **没有接进实时路径。** 边框模块已验证,但把它接到 textExtractor /
  readerSession / strictPageReplacement 三处会改变每一页的行为,需要真机诊断
  导出来验证 —— 这正是这个项目一贯的节奏。
- 建块阶段还不认网格(跨格的块应当被拆开),所以 Powers 页仍差 2 条年份。
- 没有原始截图、没有在真实 Zotero 做视觉回归。**不声称那两张表已经好了。**

### 测试

新增 21 条(边框网格 13 + 操作符取段 4 + 网格建格 4),共 1241 条全绿。
14 处变异逐条验证全部转红,其中 4 处是第一轮**存活后补测**的 —— 包括一个
真实设计缺陷:聚类取并集会把远隔的两截共线短线拼成一条贯穿全页的线,
把"隔太远不算一条线"直接架空,已改为取最长段。

## [2.12.3] — 2026-09-12

表格译文错位:两张截图是**两个不同的根因**,不是同一个问题。

### 直接结论

**截图 2(三列指南清单表)= 真 bug,已修。** 行归属在**抽取期**就错了,与"中文比较长"
无关。极端情况下**整张表塌成一行**:三条各自带年份的记录会被焊成
`r1-c0="标题一 标题二 标题三"`、`r1-c1="2019 2020 2021"` —— 屏幕上就是
"第一列多个标题连续堆到上方,下面全是空白格"。

**截图 1(五列独立清单表)= 另一回事,本次未修,如实记录。** 那张表**压根没被
识别成表**:正文区没有横线、没有数值列,而文本表的验收判据要求"≥3 处跨列顶边
对齐" —— 五列各自独立的清单按定义就没有共同行。它于是走散文路径,每个条目
各自翻译、各自就地摆放。

### 根因证据

先查了 brief 的第一个问题:**可见边框有没有进入结构识别?没有。**
全项目唯一读 PDF 绘图指令(`getOperatorList`)的地方只用来找**光栅图片矩形**
(`imageObstacles.ts` 只认 paintImage* 系列操作符);`tableStructure.ts` 开头写着
"Pure geometry over plain boxes — no DOM, no PDF"。表格线是矢量描边,从不进入
结构识别,只存在于背景位图里。遮罩对光栅图有硬保护(clearRect),对矢量边框线
**没有**。

在这个前提下,行的唯一证据是文字几何,而它有两处失效:

| # | 失效点 | 证据 |
|---|---|---|
| 1 | 列内分组阈值 `em*0.8` 是**绝对**值 | 紧排期刊表格的记录间隙常只有 0.6~0.7 个字号,一个断点都产生不出来 |
| 2 | 行首兜底要求"**≥3 个成员**"顶对齐 | 2~3 列的表永远凑不够 —— 这是个代理判据,不是行首的硬几何 |

两条同时失效 → 整表塌成一行。再往下还有三处:

3. `mergeContinuationRows` 要求续行以**小写字母**开头 —— 而文献标题是 Title Case,
   "Cardiovascular Disease"、"Acute Ischemic Stroke" 条条大写,于是每条多行标题的
   折行都被判成新记录。
4. 数值种子路径(`buildTableModel`)**从不并折行**,而"一列年份"就足以让指南清单表
   走这条路。
5. 区域生长够不到远处的列:实测第三列(缩写)距数值区右沿 3.3em,而吸附阈值是 2em
   —— **整列 6 个块掉出表外**;末行的折行落在区域底沿下方 2px,同样掉出去。

### 改了什么(全部用表格自己的度量,没有新增绝对常数)

- **行首的硬几何换成"≥2 个不同列在同一顶边开始"**,法定人数 `min(3, 列数)`。
  对 5 列宽表反而更严(仍要 3 个成员且必须跨列),只对 2~3 列的表放开它本来
  够不到的那道门。
- **分组与续行判据都改用"本表最紧的那种竖向间隙"做尺**,不是绝对的 `em*0.8`。
  nejm-defuse3-p7 实测:折行间隙 **1.0**,每一条真行(含表内小节子标题
  "Imaging outcomes")**5.8**,而固定阈值是 8.0 —— 两者都不断,固定阈值永远
  分不开。取 `min(em*0.8, 最紧间隙 + em*0.25)`,保证永不比原来迟钝。
- **续行补一条不依赖大小写的列覆盖证据**:真正的新记录会填上一行填过的那些列
  (那正是它成为一条记录的原因),折行只延长自己那一列。列数相同的行永远不并。
- **数值路径也接上折行合并**,与文本路径同一套判据。em 由成员字号自己给出,
  不加参数 —— 这个仓库为"两端参数漂移"付过代价(格 id 必须逐字一致)。
- **成列的行对齐短块可以入表**,不论栏沟多宽:本块对齐某个表格行、且另有 ≥2 个
  x 范围相同的块也各自对齐表格行。**没有**去调大那个 2em 阈值 —— 调大会顺手
  吞掉邻栏正文;"一整列都落在表格行上"才是证据。
- **末行折行**(紧贴区域底沿、横向在区域内、窄于区域 70%、同高没有别的块)收进表。

### 前后对比(37 页真实语料)

| | 结果 |
|---|---|
| 全文逐字节相同 | **31/37** |
| **丢词** | **0 页、0 词** |
| 多词 | 1 页 29 词 —— wu2026-p1 的作者邮箱,**以前是整段丢失的** |
| 块数 / 表格格 | −41 / −41(碎片并回单元格) |
| 待翻译单元 | **−21**(API 项更少) |
| 请求数 `chunks` | **0**(不多发请求) |
| payloadChars | 9 页合计 +121 字符 |

nejm-defuse3-p7 的实际效果:长标签的折行并回同一格
("Primary efficacy outcome: median score on modified **Rankin scale at 90 days (IQR)**"),
而小节子标题 "Safety outcomes"、"Imaging outcomes" 各自独占一行,数据列与正确的
行标签对齐。

### 验证边界(**没有**验证到的部分)

- **没有拿到两张原始截图**(它们在 Codex 的系统临时目录里,不在本会话可访问的
  文件夹内),也**没有原 PDF**。所有结论来自代码取证与合成夹具复现,
  **不能声称真实表格已恢复**。
- **没有在真实 Zotero 里做视觉回归**。测试数量、类型检查、基线全绿都不等于
  那两张表看起来对了。
- **截图 1 那类表仍未被识别为表**(已写成用例记录在案)。要真正支持它,需要
  边框证据(解析 PDF 绘图指令)或一条"标题锚定 + 多列短条目纵向清单"的新识别
  路径 —— 两者都不在本次范围内。
- 边框仍未参与结构识别;遮罩仍可能涂到紧贴文字的表格线(padding 1~3px)。

### 测试

新增 8 条表格行归属用例(夹具 A 五列独立清单 / 夹具 B 三列逐行记录 + 反向锁 +
子标题 + 碎片行首),共 1220 条全绿。11 处变异逐条验证全部转红:行首法定人数
退回、行首不看列、去掉列覆盖证据、列覆盖不要求真子集、去掉两处行距尺、行距尺
放大到 em、分组阈值退回绝对值、数值路径不并折行、成列行对齐关掉、尾行折行关掉。
布局快照与流水线基线按各自说明重生成,diff 已逐页审阅(见上表)。

## [2.12.2] — 2026-09-12

修 bing-free 的请求条数;把 deepseek 那次「默认在思考」的排查方法**逐家过一遍**。

### bing-free:收回 2.12.1 的结论

2.12.1 写的是「bing-free 是架构差异,不改」。那句话错了,现在改掉。

当时的判断是:主路端点 `/ttranslatev3` 是 form-urlencoded 的**单条** `&text=`,
不像 google-free 的 `batchPieces()` 能收数组,所以没法打包。端点确实收不了数组
—— 但它**保留输入里的换行**,响应是一个字符串。把多个片段用 `\n` 连起来发一次、
再按 `\n` 拆回,同样能把 N 次往返压成 1 次。是我把「收不了数组」当成了
「打不了包」。

两次独立会话都量到同一个数量级差距:

| 会话 | google-free | bing-free | 倍数 |
|---|---|---|---|
| 2.12.0 | 6435 字符/秒 | 238 字符/秒 | 27 |
| 2.12.1 | 8795 字符/秒 | 351 字符/秒 | 25 |

单请求中位延迟 281 ms vs 2798 ms —— 差的不是端点快慢,是条数:google-free 一页
约 2 个请求,bing-free 一页约 20 个,三并发下要跑 7 轮。

拿 2.12.1 的 62 页语料离线重放分组算法:

| | 总请求 | 每页中位 |
|---|---|---|
| 分组前 | 1738 | 19.5 |
| 分组后 | **431** | **7.0** |

减少 **75.2%**,平均每组 4.03 个片段。**这是往返次数的账,不是 27 倍的账** ——
单次请求的服务端延迟一点没动,别把这条当成追平 google-free。

拆分是**自验证**的:拆出来的条数与送进去的对不上,整组退回逐条翻译。引擎吞换行、
合并换行、多吐换行,任何一种都被这条接住,代价是这一组多一次往返,而不是把第 k 段
的译文落到第 k+1 段上。只打包**不含换行**的片段,所以 `\n` 一定是我们自己加的。
上限沿用已在跑的 900 字符,不往上调 —— 没测出端点真实上限之前,不把一个没验证过的
预算当万能参数。

### 每家大模型都查了一遍:四家默认在思考,我们从没让它们别想

2.12.1 修的是 deepseek 一家:它默认开思考,输出 token 烧在思维链上,出 tok/原文字符
6.706,吞吐 65 字符/秒。修完 0.300、837 字符/秒。

这次按同样的方法核对了**每一个预置大模型服务商**的官方文档。结果是 deepseek 不是
孤例,而且四家的关法**互不通用**:

| 服务商 | 默认 | 关法 | 我们以前发的 |
|---|---|---|---|
| deepseek | 开 | `thinking: {type:'disabled'}` | 什么都没发 |
| zhipu | 开(GLM-5 自适应) | `thinking: {type:'disabled'}` | 什么都没发 |
| qwen | 开(qwen3.7-plus 系列) | `enable_thinking: false` | 什么都没发 |
| siliconflow | 开(DeepSeek-V4-Flash) | `enable_thinking: false` | 什么都没发 |
| moonshot | 开,**关不掉** | 只能 `reasoning_effort` 调档,默认 max | 什么都没发 |

前两家发对象、中间两家发布尔、moonshot 一个都不认 —— 把 deepseek 那套原样套到
qwen 上只会 400。moonshot 关不掉,就取最低档 `low`,不假装能关。

其余几家(openai / gemini / groq 等)核对下来本来就不默认思考,**一个思考字段都不加**
—— 没中招的不动,是这次的一条纪律。

### 认不出这些参数的端点不会被打死

自建端点、老版本网关、第三方中转未必认 `thinking` / `enable_thinking`。
`reasoning_effort` 早就有自愈路径(1.1.11),这两个字段现在接上同一条:
400 且消息里提到这两个字段 → 剥掉重试一次 → 按 (服务商, 端点, 模型) 记住,
后续请求首发就不带。剥掉的只是被拒的那一个,温度和用户显式选的档位都还在。

### 测试

新增 8 条 bing 分组用例、7 条思考参数用例、4 条自愈链路用例(共 1212 条,全绿)。
17 处变异逐条验证:去掉换行守卫、去掉字符预算、条数不符也照收、每家退出/错配自己的
机制、moonshot 默认回到 max、`enable_thinking` 默认反转、自愈判据不看状态码、
bodyNow 不删字段、drop 表不记忆 —— 全部让对应用例转红,无一幸存。

### 顺手捞回一条被悄悄删掉的测试

发布前比对工作树与 Beta,发现 `translationManager.test.ts` 少了一条
`long prose pages publish a completed batch while another request is pending`
—— 它自 2.8.13 起一直在 Beta 里,这次差点被一次无声的删除带走。

捞回来一跑,**挂死**。查下来是它自己过期了,不是产品坏了:用例写死
`<= 3200`,那是当年 DEFAULT_CHUNK_BUDGET 的值;预算后来调到 8000,这页 6244
字符一个请求就装下,"另一个请求还在飞"根本触发不了。更糟的是那句断言写在
`translateRequest` 回调里,抛出后被当成请求失败吞进重试,于是不是转红而是挂起。

没有删它,而是修好:输入放大到必然跨块;预算断言改对当前的 DEFAULT_CHUNK_BUDGET
(守的是"分块尊重配置的预算",不是某个字面量,免得再过期一次);断言移出回调,
不再被重试路径吞掉。它守的是"翻译完成 ≠ 译文已显示"——整页翻完才显示的话,
用户盯着的是一片空白。两处变异验证:预算放大到 40000、缩到 1,都让它转红。

### 仓库卫生

`preview/shots/`(2.5 MB 可重生成的取证截图)与桌面端落在仓库目录里的
`Claude outputs/` 加入 .gitignore —— 这次发布前才发现它们正要被提交进去。

## [2.12.1] — 2026-09-12

查 bing-free 与 deepseek 为什么慢。**deepseek 是真 bug,已修;bing-free 是架构差异,不改。**

### DeepSeek:它一直在思考,而我们从没让它别想

按服务商拆开算输出 token(同一篇论文、同一个翻译任务):

| 服务商 | 出 tok / 原文字符 | 出 tok / 入 tok |
|---|---|---|
| **deepseek** | **6.71** | **5.92** |
| openai | 0.56 | 0.40 |
| gemini | 0.44 | 0.60 |

**24661 字原文吐了 165385 个输出 token** —— 中文译文本身大约只值 10000,
也就是它生成了约 **16 倍于译文**的内容,而那些内容全被丢弃(我们只取最终答案)。

这完全解释了 65 字符/秒:不是网络慢,是在思考。实测单请求中位 22 秒、
P90 52 秒、最长 **73 秒**,期间**无限流、无报错** —— 它不是在失败重试。

根因:DeepSeek 官方文档写着「Thinking mode is enabled by default, with the
default effort being `high`」,而 `REASONING_EFFORT_PROVIDERS` 只含 openai /
openrouter —— **我们从来没给 deepseek 发过任何思考相关的参数**,一直吃着
high 强度的默认思考。

同一份文档还说明:thinking 模式下 `temperature` **无效**。我们给 deepseek 发的
`temperature: 0`(翻译要确定性)因此一直被忽略 —— 关掉思考之后它才真的生效。

**Fixed**:默认发 `thinking: { type: 'disabled' }`。翻译不需要思维链,
我们连读都不读它。用户在高级设置里显式要了强度,就按他要的开
(我们的 minimal/low → `low`,medium/high → `high`,xhigh → `max`);
`supportsReasoningControl` 把 deepseek 纳入,否则设置界面上根本改不了。

用的是 DeepSeek 自己的 `thinking` 对象,**不是** OpenAI 的顶层 `reasoning_effort`
—— 两套词汇不能混,混了就是一个 400。

### DeepSeek:型号名已经换了

核对官方变更日志(2026-09-10):当前名是 `deepseek-flash`(V4.1 Flash);
`deepseek-v4-flash` 只是**暂时**路由过去的过渡名。更要紧的是 `deepseek-v4-pro`
在 **2026-09-14 之后**也会被路由到 V4.1 Flash —— 选它已经拿不到 Pro。

目录里推荐项改为 `deepseek-flash`,两个旧名降为 legacy(不再标 quality 误导);
注册表默认值同步改到当前名。**已显式选过型号的用户不受影响** —— 这只是没选时的默认。

### bing-free:架构差异,不是 bug,所以不改

它和 google-free 同为免费机器翻译,吞吐却差 **27 倍**(238 vs 6435 字符/秒),
中位 2798 ms vs **281 ms**。原因在请求形状:

- **google-free** 有 `batchPieces()`:把多个片段打包进一个请求,上限 1600 字符。
  一页约 2 个请求。
- **bing-free** 没有打包:先按 `\n\n` 切段,每段再按 900 字符切片,
  **每一片一个 HTTP 往返**。一页 20 个短段就是 20 个往返,
  `runPool(tasks, 3)` 三个并发 → 约 7 轮 × 400 ms ≈ **2.8 秒**,与实测中位吻合。

看上去该给它加打包,但**主路做不到**:`translateOne` 先走 `translateViaScrape`
(注释写着「The Bing web channel is the one that currently works」),
而那个端点一次只收一个 `text`。能收数组的是 `translateViaEdge`,
但它是 5 分钟熔断后的兜底,不是常态路径。

在没法对活端点实测的前提下,提高并发、放大 900 的切片上限、或者把短段拼起来
再按分隔符切回来 —— 每一条都是拿正确性赌速度。**所以这一版不动它。**
结论是给用户的:要快的免费引擎,google-free 在这份证据上严格更优。

### Tests

- 更新 6 项既有断言(它们钉的正是"deepseek 不带 thinking"和旧型号名),
  每一条都写清了为什么该改。
- 新增 1 项:`''` / `disabled` / `auto` 都必须**显式**发 `{type:'disabled'}`
  —— 不发等于吃 high 强度的默认思考;五档强度各自映射正确;
  不能同时发顶层 `reasoning_effort`。
- 变异验证 8 项全 kill:不发 thinking、默认改 enabled、错用顶层字段、
  阶梯全映到 max、disabled 也当成要思考、设置界面不给开关、
  目录退回过渡名、注册表与目录不一致。
- 1192 项全绿;37 个布局快照与请求计划基线逐字节不变。

### 下一轮看什么

装 2.12.1,照常读一篇长文,导出诊断。看 `providerThroughput.deepseek`:

- 吞吐从 65 跳到几百字符/秒 → 思考确实是主因,权重会自动跟着重算;
- 仍然是几十字符/秒 → 参数没被接受(或者慢另有原因),
  那时 `出tok/原字符` 会告诉我们思考是不是真的关掉了。

## [2.12.0] — 2026-09-12

**按实测速度加权分页。** 权重来自**本会话自己的测量**,不是我硬编码某一次导出的数字 ——
网络、时段、账号档位都会变,写死的数字第二天就过期。

### 为什么要加权

2.11.2 把分布修匀了(每家约 1/5),但"匀"不等于"好"。真机实测同一篇文档:

| 服务商 | 吞吐 |
|---|---|
| `google-free` | 约 6000 字符/秒 |
| `openai` | 约 500 字符/秒 |
| `gemini` | 约 200 字符/秒 |

**差 30 倍**。等额分页意味着 1/5 的页要等 12–16 秒,而它们本可以落在 0.4 秒的那家上。

### 怎么算权重

- **只统计成功的请求。** 快速失败的引擎看上去会很"快" —— 一个一直 4xx 的服务
  `sendMs` 极小,不设这道门就会把页越分越多给一个一直在失败的服务。
- **样本不足(< 5 次成功)一律按 1。** 冷启动、刚加进池的新引擎、只跑过一两页的,
  都不该凭一两个样本被定性。
- **以中位数为基准,不是平均数。** 平均数会被 google-free 那种量级的异常值拽着走,
  导致所有 LLM 都落到下限、彼此之间反而分不出快慢。
- **权重夹在 [0.25, 4]。** 实测吞吐差 30 倍,不夹住等于"悄悄把 gemini 踢出池" ——
  而它是你自己配进 `parallelProviders` 的。**加权是让慢的少分,不是替你做去留决定。**
- **权重量化到 0.25 的整数倍。** 吞吐每次请求都在抖,不量化的话权重每请求都变,
  页面归属跟着变,页缓存会被反复打穿。

分页用标准的**加权 rendezvous hashing**(`score = weight / -ln(u)`):份额正比于权重,
而且保住 HRW 的最小扰动性质 —— 加一家、删一家、改权重,只有该动的页会动。

**权重全相等时,排序与 2.11.2 的 HRW 完全一致**(`-ln(u)` 是 `u` 的单调函数)——
没有实测数据时行为一模一样,不是另起一套。

### 不加权的两处

- **熔断/手动轮换退让时**(`offset !== 0`):那时要的是"下一名"这个确定的序,
  不是重新按权重挑。仍走原来的降序榜。
- **页缓存身份**(`canonicalProviderForPage`):保持不加权。这和 2.0.5 处理
  熔断轮换是同一个道理 —— live 路由可以变,缓存键必须稳,否则读写键对不上。

### 可审计

`providerThroughput`(每家的成功样本数与字符/秒)和 `providerWeights`(算出来的权重)
都进导出,三条导出路径都带。**自调而不可审计就是黑箱。**

### 一个我要说清楚的副作用

按速度加权会让 **google-free 分到更多页** —— 而它是免费机器翻译,质量低于 LLM。
你选的这套配置里,"快"和"好"是反着的。

这一版只解决**速度**,没有解决**质量混合**:同一篇文章里不同页的译文仍然来自不同引擎。
权重上限 4 倍是有意压着的,免得速度把质量彻底挤掉。要根治得另做一件事
(比如只在同类引擎之间分页),那是下一个决定。

### Tests

- 新增 9 项:样本门槛(0/1/2/3 个样本都不许加权,到门槛才开始分快慢);
  只有一家有数据时不加权;快的多分慢的少分但都不为零;权重有上下限;
  以中位数为基准(两家 LLM 之间仍分得出快慢);权重量化(1% 抖动不改权重);
  **等权重时与 HRW 逐页同解**;份额正比于权重(2:1 → 66.7%,4:1 → 80%);
  加一家时只有该迁的页会动。
- 变异验证 12 项全 kill。首轮 3 项存活,全是**测试自身**的毛病:
  样本门槛的断言用了被测常量本身(把门槛调成 0 照样绿);份额的容差 ±10%
  分不开 `weight * u` 这类写法(改成 2:1/4:1/1:1 三档 ±5%);
  `succeeded` 把门那段根本没测到(它在 manager 里,而那组测试只覆盖 providerPool)。
- 另有一项存活暴露了**真的缺口**:会话级汇总有**三条**导出路径,
  只断言"字段存在"的话,漏掉其中两条断言照样为真。改成与 `engineRotations`
  的出现次数挂钩 —— 哪天新增第四条导出路径而忘了带新字段,当场发现。
- 1191 项全绿;37 个布局快照与请求计划基线逐字节不变。

### 下一轮看什么

装 2.12.0,照常读一篇长文(至少 15 页,让每家都攒够 5 个成功样本),导出诊断。看:

- `providerThroughput` —— 五家是不是都有数据了(2.11.2 之前 deepseek 和 bing-free
  一页都没分到,我至今没有它们的实测数字);
- `providerWeights` —— 权重是不是按预期拉开了;
- `requestTimings[].provider` 的分布 —— 快的那几家是不是真的多分了页。

## [2.11.2] — 2026-09-12

**「长文档越读越慢」查到底了:慢的不是代码,是那一段连续页刚好全落在最慢的引擎上。**

### 答案 — 差的是引擎,而且分配成片聚集

`requestTimings[].provider` 一上线就把问题答死了。两份导出一致:

| 服务商 | sendMs | 吞吐 |
|---|---|---|
| `google-free` | 95–450 ms | 286–11086 字符/秒 |
| `openai` | 1434–9453 ms | 10–935 字符/秒 |
| `gemini` | 11984–16329 ms | 162–217 字符/秒 |

同一份文档里页与页差十几倍,**差的是哪个引擎接了这一页**。
`poolSize: 5`,`gateMs` 仍然全是 0 或 1 —— 调度依旧没问题。

归属由 HRW 哈希决定。我把 `pickProviderForPage` 对真机 12 页跑了一遍:
**12/12 完全吻合**,分配确定且可复现。问题出在**分布**上。

### 我要撤回 2.11.1 里写错的一句

2.11.1 我写「一家拥有所有页」。**错**。那是拿一个 4 家的池推的,而真机是 5 家。
真实后果不是通吃,是**成片聚集** —— 真机 5 家池的前 24 页:

```
google×4  openai×4  google×2  gemini×10  google×4
```

**连续 10 页全归 gemini**,而 gemini 是最慢的那家(12–16 秒/页,google-free 0.4 秒)。
用户端的体验就是"读着读着,连着十几页每页等十几秒" —— 正是最初报上来的那个现象。

机制我上一版说对了,推出的后果说错了,而且我是从 8 页样本外推的。

### Fixed — 哈希末尾补一次雪崩

FNV-1a 的最后一步是 `h = (h ^ c) * PRIME`。页号在字符串**末尾**,之后再无混淆,
于是相邻页的分数只差一个 `PRIME`(16777619),而服务商之间差 **10⁹ 量级** ——
排名几乎不动。末尾补一段 fmix32 式的移位-乘法雪崩,让最后一个字节扩散到全部 32 位。

| | 30 页分布(降序) | χ² | 连续同一家的段数 | 最长连续 |
|---|---|---|---|---|
| 补之前 | 16, 10, 4, **0**, **0** | **32.0** | 5 | **10 页** |
| 补之后 | 8, 7, 6, 5, 4 | **1.7** | 24 | 4 页 |

(均匀期望每家 6 页;χ² 自由度 4,5% 临界值 9.49。补之前**两家一页都分不到** ——
而它们是用户自己配进 `parallelProviders` 的。)

121 页同样从 χ²=13.1 降到 1.8,段数 17 → 102。

**代价:一次性重新分流。** 页缓存身份键走 `canonicalProviderForPage`,
归属一变,换了服务商的那些页读不回缓存,要重译一次。修这个缺陷必然要付,付一次。

函数仍是纯的、确定的;"改池内顺序零变化""加一家只把该迁的页迁过去"
这两条既有性质原样保住(既有测试照旧全绿)。

### 没有动的 — 你的服务商配置

池 = 工具栏选的主引擎 + 你在 `parallelProviders` 里配的并行引擎。**这是你开的设置**,
按设计工作,我一个字没改。

但有件事值得你知道:按当前配置,30 页里 **16 页走 google-free**(免费机器翻译)、
10 页 gemini、4 页 openai。你在工具栏选的是 OpenAI,而它实际只翻了约 13% 的页 ——
**同一篇文章里,不同页的译文来自不同引擎,质量与速度都不一样**。
修完分布之后会更平均(每家约 1/5),但"不同页不同引擎"这件事本身不变。

要不要继续这么用、或者把并行引擎收窄到质量相近的几家,是你的决定,不是我的。

### Tests

- 新增 5 项:每一家都分得到页;χ² 不显著偏离均匀(30 页与 121 页);
  相邻页尽量换家且最长连续段有界;哈希仍纯且与池内顺序无关;
  末位字符必须扩散到全部 32 位。
- 变异验证:回到无雪崩、只做一次移位、只异或不乘、把雪崩搬到循环之前 —— 全部 kill。
  另试了一个**替代修法**(把页号放到字符串前面而不补雪崩):它被**既有**的
  「加一家只把该迁的页迁过去」闸接住了 —— 页号在前,服务商 id 就到了末尾,
  换成服务商变更不再扩散。两个方向都有闸。
- 1179 项全绿;37 个布局快照与请求计划基线逐字节不变。

### 顺带

你这次的截图(Radiology 那篇)上**没有**再看到 2.10.1 修的那种小点。
但这是另一篇文档,只能说"没有反证",不算确认 —— 要确认还是得看当初出问题的
那篇卒中指南的作者名单页。

## [2.11.1] — 2026-09-12

2.11.0 那把尺子答了它该答的问题,也问出了下一个。这一版只加一个字段,
外加记一个**尚未修**的真缺陷。

### 已答 — 慢在服务端,不在调度

`gateMs` 在**每一个请求**上都是 0 或 1,全部时间在 `sendMs`:

| 页 | 块 | 字符 | gateMs | sendMs |
|---|---|---|---|---|
| 1 | 6 | 2384 | **0** | 382 |
| 5 | 5 | 1918 | **0** | 3336 |
| 7 | 22 | 4352 | **0** | 7452 |
| 8 | 24 | 3896 | **0** | 7567 |

等名额、并发挤占、预取抢前台槽位 —— **全部排除**。上一轮我提过
「前台请求排在自己的预取后面」,这组数据把它否了。

慢页之间还很整齐:p5/p6/p7/p8 是 515–585 字符/秒,一个正常的大模型生成速度。

### 未答 — 快页快得不像在生成

p1–p4 跑出 5300–9200 字符/秒。同时这 4 页的 **5 个请求一个都没报 token 用量**
(`usageMissing: 5`,正好等于它们),而 `engineRotations: 0`。

是不是同一个引擎在服务这两批页,现有字段**看不出来**。

### Added — 请求级服务商

`RequestTiming` 多两个字段:

- `provider` 服务商 id 枚举。这是这一组里唯一的字符串,过形状闸
  (`/^[a-z0-9-]{1,32}$/`,不合形状记 `other`)。`lane` 是运行期拼出来的串,
  哪天带上 `@端点` 或 `#网关主机名`,就会把端点带进一份"可以放心贴进 issue"
  的诊断 —— 2.8.6 已经为 `endpointHost` 付过一次这个账。
- `poolSize` 本次请求时刻的池大小。**1 表示根本没走按页分配。**

### 记一个尚未修的缺陷 — 按页分流的哈希没有雪崩

`providerPool.hrwScore()` 用 FNV-1a 算 `${id}#${page}`,但**变化的那个字符
是最后处理的**,之后再没有混淆步骤。于是同一服务商相邻页的分数**恰好相差
一个 FNV 质数 16777619**:

```
        openai       bing-free    deepseek     gemini
page 0  2550532272   1692966947   174711688    617047845
page 1  2567309891   1676189328   191489307    600270226   ← 每格 ±16777619
page 2  2584087510   1726522185   208266926    583492607
```

服务商之间的差距是 **10⁹ 量级**,而每页的抖动只有 **10⁷** —— 永远排不动序。
实测 8 页的降序榜**完全相同**,榜首恒为同一家。

这让模块自己写的设计目标落空:

> 「每页对每个服务商算一个确定性分数,分最高者拥有该页」
> 「删服务商 Y:只有原属 Y 的页各自独立迁到自己的次高分服务商,其余不动」

实际是:**一家拥有所有页**;删掉它就是全量迁移,删掉别家零影响。

**这一版不修**,理由是次序:修哈希会让每一页换服务商,而下一轮要判的正是
「快慢两批页是不是同一个引擎」。先改分流再收数据,就分不清延迟变化来自
重新分流还是别的。等 `provider` 字段把引擎问题结掉,再单独修、单独验。

修法已经清楚(FNV 之后补一个 fmix32 雪崩),代价也清楚:一次性重新分流。

### 我在这一轮里推翻的两个自己的猜测

1. 「快页走了免费引擎、慢页走了大模型」—— 提出后立刻被 `engineRotations: 0` 挡住,
   没有据此改任何东西。
2. 「HRW 会把这 8 页分给 4 家」—— 我直接跑了 `pickProviderForPage`,
   结果 8 页全归一家。**预测被自己的代码证伪**,反而挖出了上面那个哈希缺陷。

### Tests

- privacy 闸从「只许数字与布尔」改为「白名单 + 形状校验」:`provider` 是
  **唯一**例外且必须过闸,其余字段规则不变。新增一项专门盯形状闸本身。
- 变异验证 5 项全 kill(直接塞 lane、闸放行任意串、再混一个自由字符串字段、
  删 poolSize、闸函数被改名)。
- 1174 项全绿;37 个布局快照与请求计划基线逐字节不变。

### 下一轮看什么

装 2.11.1,照常读到第 8 页以后,导出诊断。看 `requestTimings[].provider`:

- 快慢两批页的 `provider` **不同** → 是引擎差异,与代码无关,
  接下来的问题变成"要不要让按页分流可配";
- **相同** → 同一个引擎在同一份文档上差十几倍,那要查的是请求内容本身
  (块数、提示词形状、是否触发了长推理);
- `poolSize` 若为 1 → 按页分流压根没启用,上面那个哈希缺陷对你这台机器无影响。

## [2.11.0] — 2026-09-12

**2.9.9 的 Xray 假设:证实。** 抽取从主要瓶颈变成了噪音。
与此同时,真机第十一轮数据把一个**新的、现有字段解释不了的**差异摆了出来,
这一版只做一件事:把量它的尺子加上。

### 证实 — 21/21 页走通不依赖渲染的路径

| 字段 | 2.9.8 | 2.10.2 |
|---|---|---|
| `extractPath` | `{ text-layer: 21 }` | **`{ text-content: 21 }`** |
| `textContentPath` | (不存在) | **`{ ok: 21 }`** |
| `textContentMs` | 5 ms / 21 页 | 548 ms / 21 页 |
| `textLayerMs` | (主路) | **0 ms** |

DOM 文本层路径**一次都没被用到**。`wrappedJSObject` 就是原因,假设成立。

抽取全程 **1507 ms / 21 页**(障碍 844 + 取文 548 + 建块 108),
而单页翻译阶段最长 **28136 ms** —— 抽取现在占端到端不到 1%。

连带:2.9.0–2.9.6 那批渲染补丁**基本闲置** —— `renderRetries: 0`、
`released: 2`(两次都是 `cancelled`,即快速翻页取消,不是渲染没跟上)、
`releasedPending: 0`。拆除它们的证据基础有了,但一轮 21 页不足以覆盖
长文档快速翻页与冷启动,所以**这一版不拆**。

### 新缺口 — 113 倍的差异,现有字段一个也归因不了

翻译阶段耗时与输入大小**完全不相关**:

| 页 | 原文字符 | 请求 | 翻译阶段 | 吞吐 |
|---|---|---|---|---|
| 3 | 6356 | 1 | **502 ms** | 12661 字符/秒 |
| 9 | 4939 | 2 | **298 ms** | 16574 字符/秒 |
| 17 | 1923 | 1 | **10711 ms** | 180 字符/秒 |
| 15 | 3918 | 1 | **28136 ms** | 139 字符/秒 |

吞吐跨越 **139 → 16574 字符/秒**,而输入在同一档。同时:
`queuedMs` 在**每一个慢页上都是 0**,`rateLimited`、`timeouts`、`salvage` 全是 0。

问题出在尺子上:`queuedMs` 量的是「从 `ensurePage` 到调度器把这页跑起来」,
**不含请求闸的等待** —— `requestGate.run` 是在 `try` **之前** await 取名额的,
那一段时间此前谁也没记。于是「等名额」和「等服务端」这两件事,
现有字段一个也分不开。

### Added — 请求级时序

每个请求一条,全是数字与布尔,不含任何文本:

- `gateMs` 在请求闸里等名额的时间
- `sendMs` 拿到名额之后到响应回来(真正的服务端往返)
- `inFlightAtStart` 出手**之前**本 lane 上已有几个在飞
- `foreground` 是不是当前页(前台有保留槽位,背景预取没有)
- `attempt` 第几次尝试;`blocks` / `chars` 这一批喂了多少

**纯观测**:名额怎么给、给几个、等多久,一行没动;没有提高并发、
没有加长超时、没有加重试。

### 我在这一轮里读错的两处

1. 先抽到文件里第一条 `list` 块(一条表格脚注),就断言「脚注被误判成参考文献,
   导致表格大面积不翻译」。**错**。那 99 条 `isReference` 的 list
   99/99 以编号开头、87 条含期刊年卷页、全在第 19–21 页 —— 是**真参考文献**,
   保留原文是对的。真正非文献的保留只有 **17 个表格单元格**(第 7、8 页),
   字数 1–37。
2. 把 `durationMs` 与 `firstTextMs` 当同一把尺子读。它们**原点不同**:
   前者从翻译阶段开始算,后者从 `ensurePage` 入口算。这也是为什么有的页
   `firstTextMs < durationMs`、有的反过来。

### 本轮数据回答不了的

- **2.10.1 的下伸部修复有没有生效**:`geometryAudit` 全零,但它管的是别的检查。
  要看一张标点密集页的截图才知道。
- 快页为什么快:第 2 页 4600 字符 / 420 ms、`fromCache: false`、1 个请求 ——
  这个速度不像在生成。新加的 `sendMs` 会直接说清楚。

### 下一轮看什么

装 2.11.0,正常读一篇长文(至少翻到第 15 页以后),导出诊断。看 `requestTimings`:

- `gateMs` 大 → 慢在**等名额**,问题在调度与预取的相互挤占;
- `sendMs` 大而 `gateMs` 小 → 慢在**服务端**,与我们的并发无关;
- `inFlightAtStart` 在慢页上偏高 → 前台保留槽位没起到应有的作用。

三种结果指向三个完全不同的方向,而现在的字段一个也分不出来。

### Tests

- 新增 3 项:时序字段只许数字与布尔(一旦放进字符串,模型名、lane 名、
  端点乃至原文片段迟早顺着它流进导出);门内等待与实际发送必须分开量
  (在 acquire **之后**才开始计时,量到的永远是 0 —— 正是 `queuedMs` 现在的毛病);
  进导出、有上限、无请求时不写空数组。
- 变异验证 8 项全 kill。
- 1173 项全绿;37 个布局快照与请求计划基线逐字节不变。

## [2.10.2] — 2026-09-11

工具栏两处调整(按用户要求),以及为容纳它们而做的宽度让位。

### Changed — 同步滚动归入左组

工具条现在按**语义**分两组,弹性空隙是分界:

```
[品牌] 语言 · 服务 · 全文重译 · 同步滚动   ←→   解析 · 术语 · 更多  |  左右 · 设置 · 关闭
        ——— 这一页是什么 ———                  ——— 对它做什么 ———
```

同步滚动是**状态**(两边是不是跟着走),不是动作 —— 它此前被弹性空隙隔到右边,
和「对选中内容做什么」那排混在一起。挪到全文重译之后,与语言、服务同组。

### Changed — 术语回到常驻,并有了图标

2.10.0 把它下沉进「更多」是按频率判断的,但读到一半随手要用的东西进菜单
等于多两次点击。现在与「解析」并列常驻,配一枚同为 1.8 描边的书本图标。
**不在菜单里重复出现** —— 同一个动作两个入口,菜单会越长越像杂物抽屉。

两个文字动作按钮现在走**同一个构造**(`actionButton`),图标样式也从
「解析」专属推广到 `.pm-bar-action svg`:各写一份迟早会在 aria-label、
图标尺寸或标签包裹上悄悄分叉。

### Fixed — 最窄档靠收紧间距腾地方,不靠再藏控件

「术语」回到常驻多占约 28px,实测 390px(窗格声明的 `min-width`)下工具栏
**又溢出 24px、裁掉一个控件**。

省的是空白,不是信息:≤430px 时 `gap` 4px → 2px、工具条内边距 18px → 8px、
分隔线外边距 8px → 4px,合计约 32px。

不选的两个方案:再藏一个控件(服务标记是「当前用哪个引擎」的唯一显示),
或把图标按钮改小(28px 已接近可点下限)。间距紧一点,总好过一个点不到的按钮。

修后实测 **390–1100px 全程:溢出 0、被裁 0**。收纳分档不变:
860px 收服务名、760px 换短语言、640px 两个动作一起收成纯图标。

### Tests

- 新增 3 项:分组顺序(同步在重译之后、在空隙之前;术语紧跟解析;
  更多收尾)、两个按钮走同一构造且窄档一起收、最窄档只收空白不藏控件。
- 变异验证 12 项全 kill。首轮 1 项存活 ——「两个按钮各写一份构造」:
  我的断言只数了构造函数出现几次,旁边再写一个别的名字计数照样是 1。
  改成钉住**两个按钮都从它出来**,再用真正的分叉(就地手写一份)复验,
  两个方向都 kill。
- 另有一条断言首轮切错了块:文件里有**两个** `@media (max-width: 430px)`,
  按第一处文字出现切会切到管 `.pm-scroll` 的那个。改成取包含 `.pm-bar {` 的那一块。
- 1170 项全绿;37 个布局快照与请求计划基线逐字节不变。

## [2.10.1] — 2026-09-11

**译文页上那片小点,是原文每一行的逗号尾巴从遮罩下面漏出来了。**
真机截图定位,根因是我在 2.9.7 写错的一处几何 —— 2.9.9 把这条路径修通之后,
错误的几何才第一次真的上了页面。

### 证据

真机 2.10.0,作者名单那一段(标点极密):译文排在上方,原文被遮罩盖住之后,
下方留下**八行规则排布的小点**。5 倍放大后这些"点"不是字,是单像素的墨 ——
每一行都落在同一条基线上。与左侧原文逐行对照,位置正好对上原文的逗号与分号。

### 根因

`textContentItemRect()`(2.9.7)把包围盒算成从基线 `(e, f)` **向上一个 em**:

```
rect = [e, f, e + width, f + fontSize]        ← 下沿恰好是基线
```

基线以下**零空间**。而逗号、分号、g/p/y/j 的尾巴全在基线以下。

这个错误在 2.9.9 之前看不见:那时这条路径根本没跑通(Xray 边界),
所有行矩形都来自 DOM 文本层的 `getBoundingClientRect()` —— PDF.js 给 span 的
盒子本来就含下伸部。**2.9.9 修通路径,等于把这个潜伏的错误接上了电。**

遮罩的 padding 补不了:`pad = min(3, max(1, fontSize * 0.08))`,
而下伸部约 0.22 em —— 小字号下差一个数量级。错的是矩形,不是遮罩。

### Fixed

em 盒**跨骑基线**,不是坐在基线上:原点沿字高方向的反方向退 `0.22 em`,
上沿仍然刚好盖住上伸部,下沿这才盖住下伸部。**盒高不变**(仍是一个 em),
只是放对了位置 —— 行与行的相对几何不受影响。

下移沿的是**文本自己的**字高方向,不是一律 `-y`:页边那条竖排水印
("Downloaded from …")的 transform 是旋转的,一律往 -y 挪会让它的盒子横向错位。

### Tests

- `textContentPath.test.ts`:三条几何断言更新为跨骑基线的期望值,
  并新增两条闸 —— 6/8/10/12/18pt 下基线以下留白必须在 **0.18–0.3 em** 之间
  (低于 0.18 盖不住逗号,高于 0.3 会吃到相邻行);旋转 180° 的文字必须朝 +y 让开。
- 变异验证 5 项全 kill:回到 2.9.7 的坐姿、下移量减半、下移量过大、
  一律 -y、以及"把盒子变高而不是移位"。
- 1167 项全绿;37 个布局快照与请求计划基线逐字节不变。

### 仍待确认

这一版据真机截图定位、据代码确证,但**没有真机验证过修复效果**。
下一轮装 2.10.1 后,同一篇文章的同一页应该不再有那片小点。
若仍有,说明该页走的不是 `text-content` 路径(导出里的 `extractPath` 会说),
问题在别处。

## [2.10.0] — 2026-09-11

**界面层级不是没设计,是被一条 CSS 规则整体压掉了。** 这一版的主体不是"美化",
是让**已有的**设计第一次真的生效,再把因此变得过响的地方降下来。

### 取证方式

跑不了真的 Zotero,所以把项目**真实的 `translationPane.css`** 与
`build()` **真实生成的 DOM** 在 headless Chromium 里 1:1 复现并测量
(`preview/`,不进 CI —— 它需要浏览器)。**这是模拟预览,不是真机集成验证**:
它证明样式与布局本身的行为,不证明与阅读器的集成、同步或翻译流程。

### Fixed — 一条 0,1,1 的重置,压掉了 9 条着色声明

```
第 135 行  .pm-bilingual-pane button { background: none; color: inherit }   /* 0,1,1 */
第 1233 行 .pm-bar-action { background: var(--pm-accent); color: #fff }      /* 0,1,0 */
```

类型选择器让重置的特指度更高,**写在后面也压不过它**。把代码里真正建成
`<button>` 的 9 个类逐个渲染实测:

| 类 | 设计的样子 | 实际算出来 |
|---|---|---|
| `.pm-bar-action` | 强调色底 + 白字 | 透明 + 正文色 |
| `.pm-chip` | `var(--pm-muted)` 次级灰 | 正文色 |
| `.pm-icon-button` | `var(--pm-icon)` 柔和灰 | 正文色 |
| `.pm-mini-explain` | 强调色底 + 白字 | 透明 + 正文色 |
| `.pm-card-close` | `#7d8692` | 正文色 |
| `.pm-footer-button` | `var(--pm-panel)` | 透明 |

**6 个类、9 条声明,一条都没落地**,整条工具栏塌成同一个正文色 ——
比阅读内容还响,正好与「阅读内容是视觉中心」相反。重置改用 `:where()`
降到 0,0,1,重置照旧生效,组件类压得过它。

连带的实测后果:`.pm-bar-action:hover { filter: brightness(1.08) }` 作用在
透明背景上 **= 零反馈**(hover 前后 `backgroundColor` 与 `color` 完全不变)。
三个最常用的动作按钮没有任何「可点」提示。换成背景变化,与胶囊、图标同一套语言。

### Changed — 修好特指度之后,把过响的降下来

特指度一修,`.pm-bar-action` 会**第一次**真的变成三个并排的实心蓝药丸 ——
阅读区上方一条品牌色横幅,恰恰是要避免的。这条工具栏里其实没有主操作
(翻译由原文侧发起),所以全部保持安静,靠 hover 给反馈。

- **「全文重译」与普通动作分开**:它会丢弃全文已翻译内容并产生翻译费用,
  静止时却与设置/关闭长得一模一样。现在只在 hover/focus 时转成警示色 ——
  不常驻吸引注意,但"我正要点的是哪一个"在点下去之前就看得出来。
  确认对话框仍在 readerSession 侧。
- 标签也改了:`重新翻译` → `全文重译(丢弃已翻译内容)`。四个字看不出后果。
- **服务标记**不再是 `bold italic 16px Georgia` + 硬编码青色 —— 一排无衬线
  线条图标里混一个青色斜体衬线字母,既不是同一套语言,深色下也不跟主题走。
- **「解析」的 `✦` 前缀**换成与其余图标同为 1.8 描边的 SVG。原来那是字体里的
  一个字形,粗细大小跟着字体走。

### Fixed — 窄窗格:控件被**裁掉**,不是被挤

实测旧行为(真实 CSS + 真实 DOM):

| 宽度 | 语言胶囊 | 服务名 | 弹性空隙 | 溢出 | 被裁控件 |
|---|---|---|---|---|---|
| 900 | 158px 完整 | 完整 | 70px | 0 | 0 |
| 800 | 134px | **截断** | **0** | 0 | 0 |
| 640 | **16px** | 截断 | 0 | 0 | 0 |
| 520 | 16px | 16px | 0 | 16px | **1** |
| **390** | 16px | 16px | 0 | **146px** | **6** |

工具栏挤到底仍要 **536px**,而窗格自己声明 `min-width: 390px`,
`overflow: hidden` 把多出来的**直接切掉** —— 390px 时
「更多 / 左右 / 设置 / 关闭」全部消失且**点不到**。

修法不是缩小间距硬塞,也**不是换行**(换行会让表头高度随宽度变,
而页偏移的基准里含着表头)。按使用频率重排 + 分档收纳:

- 一级(常驻):语言 · 服务 · 全文重译 · 同步 · 解析
- 二级(进「更多」):**保存到笔记** · **术语** · 导出 · 诊断
- 窗口控制(常驻):左右 · 设置 · 关闭
- 胶囊一律 `flex-shrink: 0` —— 宁可整块换短形式,也不挤成一个字符
- 760px 起语言换短形式「EN → 简」(两份都在 DOM 里,切换只是 `display`,
  不触发任何重新测量;可读名与 tooltip 始终是全名)

修后实测 **390–1100px 全程:溢出 0、被裁 0**,语言胶囊最窄 63px。

### Added — 可访问性

- **同步开关此前键盘完全够不到**:`<span role="switch">`,实测 `tabIndex = -1`、
  `aria-checked = null`。补 `tabindex` / `aria-checked` / Enter·Space,
  并拦截空格(它在滚动容器里默认翻页 —— 不拦,按一次开关阅读位置就跳一页)。
  `setSyncEnabled` 外部改状态时 `aria-checked` 一并更新。
- **整张样式表此前 0 条 focus 规则**。补统一 `:focus-visible` 焦点环,
  用 `outline`(不参与布局、不会被祖先 `overflow: hidden` 裁掉),
  只在键盘路径亮起。
- 补 `prefers-reduced-motion: reduce`。

### Fixed — 深色与主题跟随

- **主题此前只在建窗格时读一次**。开着窗格切换系统/Zotero 主题,Zotero 其余
  界面当场变,窗格是唯一不变的那块。新增 `watchTheme()` 订阅,关闭时解绑。
- 分隔条 hover 的硬编码 `#cbdafe` —— 深色下手一碰就闪出一条亮蓝。改走强调色混合。
- 解析卡片副标题与关闭按钮的硬编码灰改走主题变量。

**没有动的**:`.pm-repage` / `.pm-repage-slot` / `.pm-fallback-flow` 的白底是
**产品策略**(纸还是纸),不是漏改的深色适配 —— 深色下反转论文页会毁掉阅读模型。

### Fixed — 阅读区默认不再平滑滚动

`.pm-scroll { scroll-behavior: smooth }` 与连续跟随天然冲突:
`setPdfScrollFraction` 在 `updateviewarea` 上**每滚动帧**被调用一次,
每写一次 `scrollTop` 就起一次动画,新的一帧又打断上一帧。

2.9.8 已经发现这件事,但当时的修法是 `scrollInstantly()` 在写之前临时改
行内 `style.scrollBehavior` —— 只护住了**一条**路径。其他任何写 `scrollTop`
的地方仍然走平滑动画。现在默认值改成 `auto`,`scrollInstantly()` 保留为
第二道防线(防行内覆盖)。**滚动几何不受影响** —— 这个属性只管怎么到,不管到哪里。

### Fixed — 样式与文案债

- **`.pm-header` 此前在文件里出现 5 次**(顶层 3 次 + 两个 430px 媒体查询各 1 次),
  三代表头层层叠着,只有最后一条算数。合并成一处,计算样式与几何
  **逐条相同**(390/430/720/1100 四个宽度实测 `headerH = 41px`、`scrollTop = 41px`)。
  过程中发现:媒体查询里那两条 padding 在合并前是**死**的,照搬会让它们**活过来**
  —— 实测 430px 以下表头涨到 49px、阅读区起点下移 8px。所以是删掉,不是搬家。
- 源码里写死的简体中文(`切换语言` / `切换翻译服务` / `术语` /
  `全文重译(丢失已翻译内容)` / `配置翻译服务 →`)进三个语言包 ——
  en-US 与 zh-TW 用户此前也看到简体。
- 删除死字符串 `papermirror-sync-on` / `sync-off`(`🔗 同步滚动` / `⛓ 同步关闭`):
  三个语言包和兜底表里都有,**无人消费**(窗格用的是 `papermirror-prefs-syncscroll`)。

### 我在审查过程中撤回的两个判断

先用 grep 得出、被实测推翻的,如实记下:

1. 「三个动作按钮是实心蓝底白字、大面积品牌色」—— **错**。规则那么写,但从没
   生效过,实际是纯文字。意外的结果比设计意图更接近想要的克制。
2. 「图标按钮命中区 26px」—— **错**,实测 **28×28**。

另外,brief 点名担心的「工具栏高度变化导致阅读位置跳动」**不存在**:
`flex-wrap: nowrap`,`headerH` 在 390–1100px 全程恒为 41px。
它不换行,代价就是上面那张表 —— 改成直接裁掉。这一版把裁掉修了,
恒高这个好性质用测试钉住。

### 尚未验证 / 本次未做

- **一切都是模拟预览**。真机上 Zotero 的字体、缩放与 XUL 环境可能有差异,
  需要真机截图确认。
- **状态模型没动**。brief 第六节要求的"区分翻译完成与显示完成""局部失败保留
  成功内容"等,需要先看真机的状态流转证据,不凭想象改。
- **阅读区字号没动**(9/10/11/11.5/13 五档)。改 `.pm-page-marker` 的 9px 会
  改变它的行盒高度 → 影响页内几何,而严格原位排版下这需要重新验证译文是否
  仍放得进源区域。没有证据就不动。
- `Inter` 被引用 10 次但**没有 `@font-face`、没打包字体**,全部落到
  `-apple-system`;其中 5 处 `font:` 简写的 fallback 里没有中文字体。
  改字体栈会改变文本测量,同样留到有真机证据再动。

### Tests

- 新增 `tests/unit/paneChrome.test.ts` 共 18 项:特指度、hover 反馈、
  破坏性动作区分、焦点环、开关键盘与 `aria-checked`、`scroll-behavior`、
  胶囊不可压扁、短形式、层级重排、表头恒定与唯一、短标签纯函数、
  三语言包齐全、死字符串、主题订阅与拆除。
- **变异验证 27 项全部 kill**。首轮 3 项存活,全是**测试自身**的弱点:
  两条断言匹配到了**自己上面那段注释**里的字样(注释里写着 "aria-checked"、
  "box-shadow"),一条被 `pm-lang-shortX` 这样的前缀蒙混。
  已加 `code()` 去注释后再断言,并把类名匹配收紧到带引号的全名。
- 1165 项全绿;**37 个布局快照与请求计划基线逐字节不变** —— 这一版没有改动排版。

## [2.9.9] — 2026-09-11

**2.9.7 那条新路一页都没走通。** 这一版只做一件事:让它穿过 Xray 边界,
并且把每一页的结局记下来 —— 这样下一轮真机数据能**判定**下面这个假设,
而不是再猜一次。

### 证据链(2.9.8 真机导出)

| 字段 | 值 | 说明 |
|---|---|---|
| `extractPath` | `{ "text-layer": 21 }` | 2.9.7 新加的 `text-content` **一次都没出现** |
| `textContentMs` | **5 ms / 21 页** | 平均 0.24 ms —— 它是**立刻返回**,不是算得慢 |
| `charsPath` | `{ "threw:EXTRACTION_FAILED/present": 97 }` | `getPageData` 方法**在**,97/97 页调用**全抛** |
| `textContentApi` | `"available"` | `getPage` 探针为真 |

同一轮里 `getTextLayerItems`(DOM:`querySelector('span')` +
`getBoundingClientRect`)**一直能用**。

把三条并排看,边界的形状就出来了:**凡是走 JS 对象方法的路都不通,
走 DOM 的路都通。** `getPage` 的 `typeof` 为真、`getTextContent` 却摸不到,
正好是这条边界在一个对象上分两层的样子。

最后一个事实来自代码库本身:`grep -rn "wrappedJSObject|waiveXrays|Cu\.waive" src/`
**零结果** —— 从头到尾没有一处穿过 Xray。

### 假设(尚未证实)

插件代码跑在 system principal 下,PDF.js 活在阅读器 iframe 的 content
compartment 里。跨这条边界时 Firefox 默认给的是 **Xray 视图**:DOM 对象有完整的
Xray 支持(所以文本层那条路一直通),而普通 JS 对象的自有方法在这个视图下
不可见或调用失败(所以 `getPageData` 与 `getTextContent` 从来没通)。
`wrappedJSObject` 是穿过它的标准做法。

**这仍然只是假设。** 它解释了目前全部四个观测,但没有任何一条现有数据能证实它 ——
证实或推翻它的是下一轮导出里新增的 `textContentPath` 字段。

### Changed

- `waive<T>(value)`:取 `value.wrappedJSObject`,取不到**退回原对象**。
  这一层只可能让更多东西可见,不会让已经能用的变得不能用 —— 假设错了也不会更坏。
- `getTextContentItems` 现在**四层都穿**:window → `PDFViewerApplication`
  → `pdfDocument` → `getPage()` 返回的 `PDFPageProxy` → `getTextContent()`
  返回的 content。少穿一层,下一层的方法就"不存在";2.9.7 正是栽在第二层之后。
- `getPdfApplication`、`probePageDataApi`、`probeTextContentApi` 一并走 `waive`。

### Added — 这条路的每一种结局都要有名字

2.9.7 那一轮的真正教训不是走错了路,而是**走不通时日志里一个字都没有**:
只能从 `textContentMs = 5 ms` 反推它"提前退出了",退在哪一步全靠猜。

新增导出字段 `textContentPath`,按页记结局:`ok` / `no-getpage` /
`no-gettextcontent` / `no-items` / `all-filtered` / `threw:<构造函数名>`。
成功也计数 —— 只记失败的话,分布里只剩各种退出原因,看上去像"全都没通",
而真正要判定的那一格是空的。

### 下一轮看什么

装 2.9.9,正常读一篇长文,导出诊断。决定性的字段是 **`textContentPath`**:

- 多数页是 `ok`、`extractPath` 翻成 `text-content` → 假设成立。
  "文本层只对正在渲染的那几页存在"从此不再是主路的毛病,
  2.9.0–2.9.6 那一批补丁(释放记账、事件补回、距离闸、两套重试预算)
  可以开始按证据拆除。
- 仍然是 `no-gettextcontent` → **假设被推翻**,Xray 不是原因,
  下一步要换方向查(worker 通道本身、fork 对标准 API 的改动)。

两种结果都是有用的结果。这一版的价值在于它**能被证伪**。

### Tests

- `tests/unit/textContentPath.test.ts` 新增 4 项:四层穿透各钉一层;穿不过
  退回原对象后路径照常工作;每一种退出都报一个原因;`textContentPath` 必须进导出。
  另补一条断言:走通的页自报 `ok`。
- 变异验证 9 项全部 kill。第一轮里"走通了不报 ok"**存活** —— 只记失败的话,
  下一轮的分布里只剩退出原因,判定 Xray 假设的那一格恰恰是空的;补闸后 kill。
- 1143 项全绿;37 个布局快照与请求计划基线逐字节不变 —— 这一版没有改动排版。

## [2.9.8] — 2026-09-11

**原文与译文对不齐:两把尺子,零点差一个标题栏。** 这一版不来自日志 ——
现有导出里**没有任何同步遥测**,这是一次代码审计,四个缺陷都是静态可验证的。

### Fixed — 同一个坐标系错误,污染了四处

面板里所有涉及位置的代码都在做同一件事:**拿 `slot.offsetTop` 当成一个
`scrollTop` 值用**。可 `offsetTop` 量的是"到**定位祖先**的距离",而
`.pm-scroll` / `.pm-article-host` / `.pm-repage-host` **都没有 `position`**
(translationPane.css 可查),定位祖先一路向上落到 `.pm-bilingual-pane` ——
它才有 `position: relative`。于是 `offsetTop` 里**含着标题栏那一行**,
而 `scrollTop` 是从滚动容器自己的内容顶边算的。

一个错误,四处受害:

1. `setPdfScrollFraction` —— 同步落点系统性偏低约一个标题栏;
2. `handleScroll` —— 反向判"当前页"用的是同一把错尺子;
3. `ensurePageIndex` → `visibleRange` —— 可见窗口、挂载与渲染决策一起偏;
4. 落点末尾那个 **`- 6`** —— 照着这个偏差手调出来的补偿:说不出 6 从哪来,
   也补不对(标题栏远不止 6px)。

修法不是再调一个常数,而是把换算做成**三个纯函数**(`toScrollTop` /
`anchorScrollTarget` / `anchorFractionOf`),四处共用一份口径。矩形差换算
**按构造正确**:不依赖任何祖先是否定位、有没有 padding、border 或 transform。
`- 6` 直接删掉 —— 零点对齐之后它没有存在的理由。

### Fixed — 连续跟随不该走平滑动画

`.pm-scroll` 带 `scroll-behavior: smooth`,而 `setPdfScrollFraction` 在
`updateviewarea` 上**每滚动帧**都被调用。于是每写一次 `scrollTop` 就起一次平滑
动画,下一帧又给它一个新目标 —— **右侧永远在追、从不到位**,动画自己还在不断发
scroll 事件回灌。这正是"跟随慢"和抖动的来源,也是当初那个 300 ms 时间窗存在的
原因。现在连续跟随走瞬时滚动;离散跳转(点击块、跳页)仍然平滑。

实现上用临时改写 `scrollBehavior`,不用 `scrollTo({ behavior: 'instant' })`
—— 后者在老引擎上会被当成无效值、**静悄悄回落到 CSS 的 smooth**。

### Fixed — 回声窗口有两个,中间留着缝

面板写 300 ms、`SyncGuard` 写 400 ms,各写各的。中间那 100 ms 里面板已经不再把
自己的回声当回声,而 `SyncGuard` 还在压制 —— 两边对"现在谁说了算"的判断不一致,
正是双向同步最容易打架的那种缝。现在两处读同一个 `SYNC_ECHO_MS`。

### Fixed — 滚动热路径逐页扫;几何变化后按整篇百分比回位

- `handleScroll` 每个事件从第 0 页线性扫,每页读 `offsetTop` + `offsetHeight`:
  97 页的文档就是**一次滚动 194 次布局读取**。而索引(建一次、用很多次)就在
  旁边没用上。改走二分。
- `relayoutSlots` 用 `scrollTop / scrollHeight` 这个**整篇文档百分比**做锚点。
  页高不是按同一个比例变的(页宽有上限、混合纸张、页间距是定值),换算完就落到
  别的页上,**文档越长偏得越远**。改为锚在"第 N 页的百分之几"上。

### 未验证 / 未改动

- **这一版没有真机数据支撑,只有代码证据。** 现有导出里没有任何同步遥测 ——
  页级匹配率、页内锚点误差、同步延迟一个都量不到。四个缺陷都是静态可验证的
  (CSS 里查得到、函数里读得出),但"修完偏差是否归零"必须真机验证。
- 纯 mock 不能证明浏览器真实滚动与 scroll anchoring 的行为:本版测试覆盖的是
  **算术与接线**,不是渲染引擎。
- 2.9.7 的新抽取路径同样尚未真机验证 —— 两版改动一起装,若出问题需要分辨来源。

## [2.9.7] — 2026-09-11

**抽取不再依赖页面有没有被渲染。** 连着两版只量不改,换来的是一次有把握的改动。

### 2.9.6 真机验收 —— 两个决定性的数字

```
charsPath      : { "threw:EXTRACTION_FAILED/present": 97 }
textContentApi : "available"
```

- **`present`** —— fork 的私有 `getPageData` **方法在**,不是 API 没了。
  但 97/97 页调用都失败。我上一版押"API 没了",押错了;**押对的是"它从来没通过"**。
- **`available`** —— 标准 PDF.js 的 `getPage()` 可用。出路通了。

于是过去七个版本的处境彻底清楚了:插件一直只剩 DOM 文本层一条路,
而**文本层只对 PDF.js 正在渲染的那几页存在**。2.9.0–2.9.6 做的每一件事 ——
释放记账、事件驱动补回、按原因分流、距离闸、两套重试预算、五道闸的计数器 ——
都是在给这个固有属性打补丁。

### Added — `text-content`:走 worker 解析结果,任何页随时可读

- 新增抽取路径 `text-content`,用标准 `pdfDocument.getPage(n).getTextContent()`。
  它**不碰 DOM、不等渲染**,排在 DOM 文本层**之前**;文本层保留为回退。
- 唯一真正的新逻辑是 `transform` → 包围盒。**不是** `[e, f, e+width, f+height]`:
  真机每页都有一条竖排的页边水印,它的 `transform` 是旋转的,图省事就会得到一个
  横跨半页的盒子,把正文全框进去,阅读序和分栏当场被毁。现在按前进方向与字高
  两个向量取四角 min/max,0°/90°/任意角都对。
- 建块之后的每一步由 `blocksFromSpanPage` **单一实现**,两条基于 span 的路径共用
  —— 各写一遍的话,迟早会在阅读序、表格结构化或合并上悄悄分叉,而分叉出来的差异
  会伪装成"这一页的结构变了",把语料的结构比对引向错误结论。
  共用的另一个好处:**37 个布局快照直接就是新路径下游的回归测试**。
- 新增分段计时 `textContentMs`;`extractPath` 枚举加一个取值(枚举闸同步收紧到六个)。

### 未改动(有意为之)

- **2.9.0–2.9.6 的脚手架一个都没拆。** 释放记账、事件补回、原因闸、距离闸全部
  原样保留 —— 新路径要先在真机上证明它确实让 `text-layer` 退居回退,才谈得上拆。
  下一轮诊断里 `extractPath` 的分布就是那个证据。
- 分批预算、并发、预取窗口、缓存策略照旧没动。

## [2.9.6] — 2026-09-11

**路径 1 不是慢,是每一页都抛错。** 2.9.5 的那个字段一次就把话说清楚了。

### 2.9.5 真机验收(98 页)

```
charsPath: { "threw:EXTRACTION_FAILED": 98 }
```

**98/98 页,每一页都抛错。** 不是"慢"、不是字体编码(`undecoded-cid` 一次没出现)、
不是返回空 —— 整条路**根本走不通**,而且从来如此。`extractPath` 分布也印证:
90/90 页全是 `text-layer`。

这解释了 2.9.0–2.9.5 六个版本的处境:**插件一直只靠文本层活着**,而文本层只对
PDF.js 正在渲染的那几页存在。所谓"文本层什么时候渲染好"的时序难题,不是一个要
绕的边界情况 —— 它是唯一那条路的固有属性。我在它周围造了六版脚手架。

这一轮其他数字(供对照,行为未改):`pageCacheHitRate` **0.713**(历次最好),
`segmentHitRate` 0.89,`render` 159 起 / 156 提交 / `notReady` **2**,
`releasedPending` **8/98**。

### Added — 两个同步探针,一轮定去向

- `EXTRACTION_FAILED` 太笼统:调用抛错、chars 不是数组,都归它。
  **分不出是"这篇 PDF 不行"还是"这个 API 没了"**,而两者修法完全不同。
  新增 `probePageDataApi`:`no-app` / `no-pdfdocument` / **`api-missing`** /
  `present`,接在结局枚举后面(`threw:<种类>/<API 状态>`)。
- 更要紧的是**出路**:标准 PDF.js 的 `pdfDocument.getPage(n).getTextContent()`
  是**公开 API**(不是 fork 的私有 `getPageData`),而且**同样不依赖页面渲染**
  —— 文本层本身就是用它渲出来的。新增 `textContentApi` 探针:
  `available` / `no-pdfdocument` / `no-getpage` / `unreachable`。
- 两个探针都是**同步的、不发 RPC** 的:只看对象和函数在不在,`typeof === 'function'`
  (属性存在不等于能调用 —— fork 换实现时很容易留下占位值)。探针自己不抛。

### 未改动(有意为之)

- **行为仍然一个字没改**,连着两版纯加计量。下一步是改抽取主路,那是这轮
  2.9.x 里最大的一次改动,不该踩着猜测动手。
- 补回策略、原因闸、距离闸、分批预算、并发、预取窗口、缓存策略,全部原样。

## [2.9.5] — 2026-09-11

**我上一版的假设被自己加的计数器证伪了。** 这一版不再在补回策略上打转 ——
退一步去问一个五个版本都没问过的结构性问题。

### 2.9.4 真机验收(97 页)

达成:

| | 2.9.3 | 2.9.4 |
|---|---|---|
| `segmentHitRate` | 0.756 | **0.949**(历次最好) |
| `extractMs` 人均 | 171 ms | **127 ms** |
| `releasedPending` | 13/57(22.8%) | **12/97(12.4%)** |
| scope 连续性 | 无洞 | 无洞(1–97) |

**证伪:**`renderRetryBlocked` = `{has-state: 6, reason: 1, too-far: 1}` ——
**总共只挡了 8 次**,其中距离闸只有 1 次。而 `renderRetries` 成功 65 次。

也就是说 2.9.3 上第 42–45 页 `renderRetries: 0` **根本不是窗口闸挡的**。
我放宽窗口这件事本身没错(它确实不该那么紧),但**它不是那几页卡住的原因**。
这一轮第 91–94 页重演了同样的模式:释放 4–8 次、原因 `text-layer-not-rendered`、
`renderRetries` 全是 0,而闸只挡了 8 次 —— 说明 `onPageRendered` **对这些页压根
没被调用过**,或调用时 `released` 里还没有记录。`textlayerrendered` 没为它们发。

其他退步:

- `render.notReady` **7/95(7.4%)→ 169/324(52%)**。65 次事件补回各自带来若干次
  空渲染 —— 补回本身在制造渲染浪费;
- `text-layer-not-rendered` 释放 37 → **71**。事件说"渲染好了",我们去抽,
  **还是抽不到** —— 补回是异步排队的,等真正跑到抽取那一步,PDF.js 可能已经把
  这页回收了(虚拟化)。

### Added — 问一个五个版本都没问过的问题

2.9.0–2.9.4 一直在绕同一个难题:**文本层什么时候渲染好**。但抽取有三条路,
**路径 1(fork 的 char 流 `getPageData`)根本不依赖渲染** —— 它能用,这个难题
从根上就不存在。

而真机数据里有一个一直没人看的异常:`charsPathMs` 整轮 **80 ms / 85 页**,
约 **1 ms 一页**。PDFWorker 的 RPC 往返不可能这么快 —— 这不是"慢",
是**立刻失败或立刻返回空**。它为什么不出活,现有日志一个字都没有。

新增 `charsPath`:路径 1 每页的结局枚举计数 ——
`ok` / `no-chars-array` / `empty-chars` / `undecoded-cid` /
`chars-but-no-blocks` / `threw:<错误种类>`。
错误只取枚举化的 code 或异常类名(复用 `failureKind` 的形状白名单),
message 一个字不带;一页只记第一个结论,免得反复抽取把分布压歪。

### 未改动(有意为之)

- **行为一个字没改**。这一版纯加计量 —— 上一版的教训就是拿着猜测去改闸。
- 补回策略、原因闸、距离闸、分批预算、并发、预取窗口、缓存策略,全部原样。
- `render.notReady 52%` 与 `CANCELLED 24%` 都没动,等 `charsPath` 的结果再定
  下一步往哪走。

## [2.9.4] — 2026-09-11

**2.9.3 的窗口闸收过头了。** 三个预判两个成立、一个只部分成立,而失败的那部分
指向的是我自己上一版加的闸。

### 2.9.3 真机验收(57 页,第 42–98 页)

2.9.3 的 CHANGELOG 写下了三个可证伪的预判,逐条对账:

| 预判 | 结果 | 判定 |
|---|---|---|
| `segmentHitRate` 回到 0.8 上下 | 0.479 → **0.756** | **成立** —— "暴跌是抖动的后果"这个判断对 |
| `render.notReady` 跟着回落 | 46/250(18.4%)→ **7/95(7.4%)** | **成立** |
| 尾部页 `releaseCount` 掉回个位数低位 | 最高 11 → **9**,分布整体下移 | **只部分成立** |

附带:`CANCELLED` 58/174(33%)→ **22/103(21%)**;
`renderRetries` 70 → 27。

### Fixed — 预取窗口只有 3 页宽,渲染事件根本赶不上

- 第 **42–45 页**(用户这一趟的起始几页)连续被释放 1/3/4/5 次,原因全是
  `text-layer-not-rendered`,而 `renderRetries` **全是 0** —— 事件补回一次都没
  成功发起。原因闸明明放行了。
- 2.9.3 的窗口闸用的是 `wantedPages().includes(...)`,而预取窗口是
  **current ±1**,一共 3 页宽。渲染事件到达时用户常常已经走过两三页,于是
  次次落在窗口外。**我为了防抖动加的闸,把 2.9.2 的本体能力一起压住了。**
- 改成距离闸,半径与翻页轮询取同一个 `RELEASE_RETRY_RADIUS = 3`。理由:
  渲染事件**本身就是**"这一页现在抽得到"的确定信息,不需要预取窗口再背书;
  真正要防的只有"拉起一页用户已经远离的内容",那用距离判断就够。
  防抖动的主力是原因闸(`cancelled` 不补),它一个字没动。

### Added — 别再靠猜哪一道闸挡的

- 上面那段推断本来是猜出来的:原因闸放行、`renderRetries` 为 0,那只能是窗口闸、
  `pages.has` 或 `isScheduled` 挡的,**而日志里分不出是哪一道**。
- 新增 `usage.renderRetryBlocked`:五道闸各自挡掉多少次
  (`has-state` / `budget-spent` / `reason` / `too-far` / `already-queued`)。
  纯枚举计数,不含页码或任何文本。猜出来的结论不配写进代码 —— 下一轮直接读数。

### 未改动(有意为之)

- 原因闸、`MAX_RELEASES`、`MAX_RENDER_RETRIES`、分批预算、并发、预取窗口本身、
  缓存策略,一律没动。
- `CANCELLED 21%` 的根子(快速翻页大量撤单)仍未处理,单独排期。

## [2.9.3] — 2026-09-11

**事件补回和导航取消打起来了。** 2.9.2 的机制生效了,但在文档尾部产生了抖动。

### 2.9.2 真机验收(103 页)

达成:

- **`untouchedGaps` 空,`pages: [1..103]` 连续** —— 中间的洞没有了;
- `releasedPending` **27/77(35%)→ 18/103(17.5%)**;
- `renderRetries: 70` —— 84 次释放里 70 次被渲染事件唤醒,机制确实在跑;
- `extractMs` 人均 **147 ms**(2.9.1 是 139),2.9.1 的性能没有回退。

没达成,而且冒出新形态:

- 尾部第 **88–93 页释放了 5–11 次**(`releaseCount: 11, 10, 11, 9, 7, 5`),
  原因多为 `cancelled`;
- `attemptErrors.CANCELLED` **58 / 174(33%)**,比上轮更高;
- `segmentHitRate` **0.851 → 0.479**,`segmentLookups` 1828 → 4060 ——
  查询数翻倍而命中率腰斩,正是"同一页反复重来"的指纹;
- `render.notReady` 回到 **46 / 250**(2.9.1 曾降到 2/188)。

### Fixed — 两类释放的语义本来就不同

- 抖动的环路:用户在文档末尾来回滚动 → `setCurrentPage` 的 `cancelExcept` 撤掉
  离开窗口的任务 → 释放(`cancelled`)→ **滚动中 `textlayerrendered` 对刚离开的
  页照样发** → 事件补回又把它拉起来 → 又被撤掉。释放 11 次就是这么来的。
- 所以事件补回收窄两条:
  1. **按释放原因分流**。`text-layer-not-rendered` / `extract-timeout` =
     "当时看不见",渲染完就该立刻重来 —— 这正是 2.9.2 要的;
     `cancelled` / `navigation-superseded` = "**用户已经不在这页了**",
     该等他回来(翻页轮询,或当前页那条无条件路径),不该被一个滚动事件拽起来。
  2. **仍在预取窗口内**。渲染事件对已经滚出去的页一样会发,拉起一页用户正在
     远离的内容只是再烧一次配额。
- 2.9.2 的本体能力没被削掉:仍在窗口内的"看不见"型释放,事件照常立刻拉起 ——
  这一条单独有测试盯着,收窄条件不能把它一起收掉。

### 未改动(有意为之)

- 分批预算、并发、预取窗口、缓存策略一律没动。
- `segmentHitRate` 暴跌与 `render.notReady` 回升,判断是抖动的**后果**而非独立
  问题,故本版不单独处理 —— 下一轮数据会说明这个判断对不对。
- `CANCELLED 33%` 的根子(快速翻页大量撤单)仍未处理,单独排期。

## [2.9.2] — 2026-09-11

**2.9.1 的账单来了。** 性能目标达成得很彻底,但同一份日志显示它是拿"预取更容易
放弃"换来的。这一版不回退,改成**由 PDF.js 的事件驱动补回**,两边都要。

### 2.9.1 真机验收(77 页,同一篇文档接着读)

达成:

| | 2.9.0 | 2.9.1 |
|---|---|---|
| `extractMs` 人均 | 864 ms | **139 ms** |
| `textLayerWaitMs` 合计 | 40,729 ms | **5,949 ms** |
| `render.notReady` | 49 / 205 | **2 / 188** |
| `pageCacheHitRate` | 0.092 | 0.24 |

`charsPathMs` 合计 10 ms —— 再次确认它与此事无关。

代价:

| | 2.9.0 | 2.9.1 |
|---|---|---|
| `text-layer-not-rendered` 释放 | 29 | **52** |
| `releasedPending` | 8 | **27** |

### Fixed — 预取本来就发生在页面渲染之前

- 2.9.1 之前,抽取遇到没渲染的页会干等 2.5 秒 —— **那段干等里 PDF.js 常常正好
  把页渲染完了**,于是预取"歪打正着"。不等了,预取就几乎必然落空。
- 靠翻页轮询补回是在**猜**"现在渲染好了吗",猜错就消耗一次配额,3 次用完就
  `exhausted`。而 PDF.js 自己会在渲染完文本层时发 `textlayerrendered` ——
  那是确定的信号。在那一刻重抽,命中率接近 100%,一次配额都不浪费。
- 新增 `TranslationManager.onPageRendered(pageIndex)`,由 `readerSession` **单独
  订阅 `textlayerrendered`** 驱动(不能用 `pagerendered`:画布画完早于文本层,
  拿它当信号会又一次落空)。
- **两套预算故意分开**:`MAX_RELEASES = 3` 管盲目轮询(猜错要付钱,给得紧);
  `MAX_RENDER_RETRIES = 2` 管事件驱动(确定信号,只为防病态留个小上限)。
  滚动/缩放会反复重发这个事件 —— 已经有页状态的页既不再抽,**也不烧配额**。
- `exhausted` 的含义随之写清楚:只表示**盲目轮询的配额用完了**,不等于放弃 ——
  事件仍会唤醒它,用户真翻到那一页也会无条件再抽。

### Added — 中间的洞要自己跳出来

- 2.9.1 那轮处理了第 8–88 页,而**第 9、25、26、27 页完全不在清单里** ——
  既没有页状态,也不在 `released` 里,说明 `ensurePage` 从头到尾没被调用过。
  在文件里它长得跟"用户根本没读到这儿"一模一样。
- 新增 `usage.untouchedGaps`:碰过的最小页到最大页之间、一次都没碰过的页号。
  **它不解释原因(现有数据解释不了),只陈述事实**:中间有洞。
  没有它,下一轮还是只能靠人肉比对页号才发现。
- 新增 `usage.renderRetries` 与释放行的 `renderRetries`:事件驱动的重抽次数,
  与盲目轮询分开计。

### 未改动(有意为之)

- 分批预算、并发、预取窗口、缓存策略一律没动。
- 本轮新出现 `attemptErrors.QUOTA_EXCEEDED: 8` —— 服务商配额,不是代码问题。
- `CANCELLED 46 / 195` 仍然偏高,单独排期。

## [2.9.1] — 2026-09-11

**别再对着没渲染的页干等 2.5 秒。** 2.9.0 装上的分段计时第一轮就把账算清了,
而且把我自己的假设证伪了。

### 证据(真机第四轮,74 页,2.9.0)

- 59 页的 `extractMs` 合计 **50,987 ms**,其中 **`textLayerWaitMs` 占 40,729 ms(80%)**;
- 我原先怀疑的 PDFWorker char 流 `charsPathMs` —— **整轮只有 19 ms**。那条假设错了,
  「文档级 char 流闩」这条分叉作废;
- 我们自己的 CPU 建块 `buildMs` 合计 410 ms,可以忽略;
- 最慢的页 `textLayerWaitMs` 1.4–2.6 秒,p28=2583、p29=2527 **直接跑满 2500 ms 上限**;
- 同一轮 `releaseReasons['text-layer-not-rendered'] = 29`。

### Fixed — 那条早退永远不触发

- `waitForTextLayer` 原本有一条 `count === 0 && !pageViewOf(...)` 的早退,意图是
  "PDF.js 都不知道这页就别等了"。可 **PDF.js 给文档里每一页都建了 PDFPageView**,
  `pageViewOf` 对任何页号都返回非空 —— 这条早退**从来没有触发过**。
  于是预取去抽一个没渲染的页 = 白等满 2.5 秒 = 抽不到 = 释放;2.9.0 的补回再试
  一次 = 再白等 2.5 秒。而这 2.5 秒还占着抽取信号量(最多 2 个并发)——
  **白等的同时挡住用户正在看的那一页**。
- 换成两道闸,任一成立立刻返回:
  1. `renderingState` 是 INITIAL(还没排上)或 PAUSED(排过又被推迟)—— 精确,
     但依赖 fork 保留该字段;
  2. **冷启动上限** `TEXT_LAYER_COLD_MS = 400`:一个 span 都没出现就最多等这么久。
     真在渲的页几十毫秒内就会吐出第一批 span,所以这条不依赖任何 PDF.js 内部 API,
     是闸 1 拿不到状态时的兜底。
- 两道闸都**只管「一个 span 都没见过」的情形**。已经开始长 span 的页照旧按
  完整的 `timeoutMs` 等它长稳 —— 2.8.13 那条稳定判据一个字没削弱。
- `pageRenderState` 拿不到就返回 `null`,交给冷启动上限,**绝不据此断言「这页没文字」**。

### 2.9.0 真机验收(同一份日志)

- **scope `pages: [1..74]` 连续无洞** —— 上一轮缺的第 36/41/45 页不再出现。
  `released: 49`(其中 41 次被补回)、`releasedPending: 8`、
  `releaseReasons: {text-layer-not-rendered: 29, cancelled: 19, navigation-superseded: 1}`。
  释放这件事一直在高频发生,只是以前完全看不见。
- **渲染记账对上了**:`started 205 = committed 149 + cancelled 7 + failed 0 +
  superseded 0 + notReady 49`。

### 未改动(有意为之)

- 分批预算、并发、预取窗口、缓存策略一律没动。
- 新发现但本版不碰:`render.notReady = 49 / 205`(24% 的渲染是对还没译文的页空跑);
  `attemptErrors.CANCELLED = 46 / 161`(29%)。各自单独排期。

## [2.9.0] — 2026-09-11

**2.9.x 第一步:整页凭空消失。** 本版同时包含未单独发布的 2.8.15(抽取分段计时、
文本层就绪短路修复、渲染记账、导出失败种类)—— 见下方「随本版一并发出」。

### Fixed — 三整页从诊断里整行消失,用户看到一片英文

- 真机 51 页那一轮(冷缓存、真翻译、153 次 HTTP、输出 19.9 万 token):
  **第 36、41、45 页不在导出 scope 里** —— 不是标成失败,是整行不见,
  而它们两侧的页都正常处理过。同一份文件里 `attemptErrors.CANCELLED = 36`。
- 根因:代码里有**五处**把页状态从 `this.pages` 删掉,然后指望"用户翻过去时
  自然会重抽"——抽取僵尸、抽取超时、文本层未渲染(2.8.13)、导航取代、请求取消。
  每处只写一行日志。于是两件事同时发生:
  1. 页从 `exportScope()` 里消失,违反本项目第一天就定下的「**不许静默漏页**」;
  2. 那个指望本身有个洞 —— **单向顺读时用户只经过这一页一次**。释放若发生在他
     已经走过之后,就再也没有第二次,这一趟永远不译。
- 现在五处统一走 `releasePage`:
  - **记账**:释放过的页留在导出清单里,`status: 'released'`,带 `releaseReason`
    枚举(五种)与 `releaseCount`;会话摘要新增 `released` / `releasedPending` /
    `releaseReasons`。补不回来也看得见。
  - **补回**:每次翻页把 `RELEASE_RETRY_RADIUS`(3 页)以内释放过的页重新入队,
    优先级 `RELEASED_RETRY` 低于一切预取,**绝不跟当前页抢**。
  - **有界**:同一页最多 `MAX_RELEASES`(3)次,之后停手并标 `exhausted: true`
    —— 无限重试在本项目是明令禁止的「修复」。用户亲手点重译会清掉计数。

### 随本版一并发出(原 2.8.15,未单独发布)

- **修**:2.8.13 的「等文本层两次采样不变」被一道短路绕开 ——
  `if (!hasRenderedTextLayer(...))` 里 `hasRenderedTextLayer` 就是「span 数 > 0」,
  于是稳定判据恰在**它唯一要防的情形**(层已长出第一批 span、还在继续长)下被跳过。
  改为无条件等;已就绪的页多付约 100 ms。
- **加** `metrics.extractPhases`:`obstaclesMs` / `charsPathMs` / `textLayerWaitMs` /
  `textLayerMs` / `plainTextMs` / `buildMs`。证据:真机 51 页里 22 页 `extractMs ≥ 700 ms`,
  其中 16 页 `requests: 0`;全会话抽取合计 42 秒。最快的页只要 14 ms —— 那是**等**,不是算。
- **加**渲染 `superseded` / `notReady` 计数,`started` 与其余五项对得上账。
- **加**导出 `readFailureKind`:只取枚举 code 或异常类名,message 一字不带。

### 未改动(有意为之)

- 分批预算(`DEFAULT_CHUNK_BUDGET = 8000`)、并发、预取窗口、缓存策略一律没动。
- 真机已量到:页面字符普遍 5000–6400,**整页正好塞进一个请求**,于是
  `requests: 1` 的页最惨 —— p44 整页 32.1 秒、**首字也是 32.15 秒**(屏幕上
  32 秒一个中文字都没有);而 `requests: 5` 的 p54 首字落在 33%。这是下一步
  (请求内分段计时 → 分批与切句)的依据,但阈值必须由计时数据定,不由猜测定。

## [2.8.15] — 2026-09-11(未单独发布,已并入 2.9.0)

**先量,再改**。真机第二轮(30 页)的诊断把"翻到后面越来越慢"定位到了**本地抽取**,
但没定位到抽取里的哪一段 —— 这一版补上分段计时,并顺手堵住 2.8.13 漏掉的另一半。
除文本层就绪那一条外,**没有改任何调度、并发、分批或缓存策略**。

### Fixed — 2.8.13 的稳定判据被一道短路绕开了

- 2.8.13 给 `waitForTextLayer` 加了"连续两次采样 span 数不变才算就绪";但调用处有
  `if (!hasRenderedTextLayer(...))`,而 `hasRenderedTextLayer` 就是"span 数 > 0"。
  于是这条判据**恰恰在它唯一要防的情形下被跳过**:文本层已长出第一批 span、还在
  继续长,短路当场放行,读到半成品。真机第 9 页当初只抽出 2 个块、抽取耗时 7 ms,
  正是这条短路放的行 —— 2.8.13 只堵住了"层根本不在"那一半。
- 现在**无条件**等就绪。已就绪的页多付一次采样(约 100 ms);与实测 0.8–2.6 秒的
  抽取等待相比是噪声,而少抽半页是用户直接看得见的错。

### Added — 抽取分段计时(`metrics.extractPhases`)

- 证据: 30 页里 **15 页的 `extractMs` 在 0.8–2.6 秒,而这些页一次接口请求都没发**
  (`requests: 0, fromCache: true`)。同一条路最快的页只要 **14 ms** —— 那 2.6 秒
  是**等**,不是算。但 `extractMs` 是个总数,分不清等的是谁。
- 新增 `obstaclesMs` / `charsPathMs` / `textLayerWaitMs` / `textLayerMs` /
  `plainTextMs` / `buildMs`。特别地 `charsPathMs` 记在 try/catch **之后** ——
  本文档 30/30 页最终都落到 `text-layer`,说明路径 1(PDFWorker 的 char 流)
  每次都白跑,白跑的那趟才是要量的。全是毫秒,不含任何文本。

### Added — 渲染计数不再留 34% 的缺口

- 真机 `started=79, committed=47, cancelled=5, failed=0` —— 27 次(34%)没有去向。
  查下来是两条**合法**路径从不记数:渲染期间被更新取代,以及渲染器返回
  "这页还没内容"。缺口既可能是合法路径也可能是泄漏,数字本身分不出来。
- 新增 `superseded` / `notReady`,`started` 与其余五项从此对得上账。

### Added — 导出读失败记种类

- 真机连着两次导出各有一页读失败,而文件里只有 `pageReadFailures: 1`,
  连是抛错、超时还是类型错都看不出来,无从下手。
- 新增 `readFailureKind`:**只取**枚举化的错误 code 或异常构造函数名,
  两道形状白名单把任何不像枚举/类名的东西降成 `unknown` ——
  异常 message 可能带路径或原文,继续一个字都不带出去。

### 未改动(有意为之)

- 调度、并发、预取窗口、分批预算、缓存策略一律没动。分段数据到手之前,
  改这些只是猜。

## [2.8.14] — 2026-09-10

**真机第 9 页的另一半**。2.8.13 修好了"整页抽不到东西"那一半;这一版修剩下的一半 ——
**东西抽到了、译文也拿到了,排版却把它无声丢弃**。用户审阅意见第 1 条。

### Fixed — 表标题被排版无条件丢弃

- 第 9 页整页只有两块: 一行表标题 + 一行页脚,**两块都已有译文**(`state: translated`),
  排版结果却是 `{ replaceable: 0, committed: 0, abandoned: 0, tooSmall: 1 }` ——
  页脚是那个 `tooSmall`,**表标题连计数都没有**: 它在准入过滤 `b.type !== 'table'`
  那一步就被丢掉了,既不算失败也不算保留原文,凭空消失。用户看到的是"这页一个字没译"。
- 这个过滤器从一开始就搞错了对象。`type === 'table'` 在整个代码库里**只在两处产生**
  (`blockBuilder` / `spanBlockBuilder`),两处判的都是**标题那一行**("Table 3: …"/"表 3 …"),
  `tableGuard` 也正是照这个含义读它(拿它当标题锚)。表格**主体**的格子 type 是
  `paragraph`,靠 `tableRow` / `-table-T-rR-cC` 认。所以 `type !== 'table'`
  **挡掉的只有表题,一个表格主体都没挡到** —— 想保护的东西一个没保护到,只误伤了表题。
- 现在表题走图注那一套: 进替换流水线,最小宽度门槛 28px(不是正文的 50px),
  孤立块的缩字梯,图注的行距。边界扩展、图像遮挡、几何审计一条都没放松。

### Changed — 墨迹遮挡物与替换集合重新对齐

- 表题从墨迹遮挡物名单里移出。两个集合必须**严格互补**: 表题若同时留在遮挡物里,
  排版成功的表题会把自己的原盒当遮挡物,扩边与几何审计当场判它"压盖自己"并回退。
  排版**失败**的表题不会因此失去遮挡 —— 扩边预校验与末端审计里的 `preserved`
  本来就是"进了 geometric 却没成为 item 的块",自动接住它。

### 未改动(有意为之)

- **表格判定一个字没动**。区域探测、单元格成员、页面基准字号的样本全部走新的
  `tabular`(= 改动前的 `geometric`,不含表题),与改动前逐字相同 —— 表题在表框外,
  放进去只会撑大区域范围。这一版只放表题进排版,不把两个变化搅在一起。
- 分类器没动: `tableGuard` 依赖 `type === 'table'` 表示表题,改分类器会连带动到表格判定。

## [2.8.13] — 2026-09-10

**真机修正之五: 整页不翻译**。用户报"长文本翻译卡顿、页面元素不翻译(比如第 9 页)",
2.8.12 导出的诊断 + 语料把机制钉死了 —— 这是**翻译流水线的正确性 bug**,不是导出的问题。

### Fixed — 6/19 页被永久标成"这页没有可译内容"

- 那 6 页 `extractPath: 'empty'`、0 块、状态 `done`;而**同一批语料在导出时把其中几页
  重解析出了 12–14 个块、约 200 个 span** —— 文字一直都在。原因: **PDF.js 只渲染视口
  附近那几页**,预取却在页面渲染之前就去抽取,文本层根本不存在。三条抽取路径都空,
  旧代码一律当成"这页没文字",把页面标成完成 —— **永不重试**,用户翻过去一个字没译。
- 现在抽取先问一句**文本层节点在不在**(`textLayerExists`,只看层、不看字):
  - 层在、但没字 → 这页真的没有可译内容,照旧标完成;
  - **层不在 → 抛可重试的 `EXTRACTION_FAILED`**,那是"看不见",不是"没文字"。
- 管理器据此分流:**当前页**如实报可重试的错(不标完成);**预取页**忘掉这次结果,
  等用户真翻过去再抽 —— 那时文本层就在了。这两条路都不会再把空结果写成定论。

### Fixed — 半成品文本层被当成整页

- 第 9 页只抽出 2 个块(一条表题 + 一行页脚),抽取耗时 **7 ms**。旧的就绪判据是
  "文本层里**有一个** span 就算渲染好了",而 PDF.js 是**边渲染边往里塞 span** ——
  抽取赶在半路读走了。
- `waitForTextLayer` 改为**等它稳定**: 连续两次采样 span 数不变(且非零)才算完成。
  一页只多付一次采样(100 ms 粒度),换掉的是"整页正文没进翻译"。

### 不变量

37 页布局快照与请求计划基线**逐字节不变** —— 夹具直接喂 spans,不经过 adapter。
7 项突变全部被杀(退回"有 span 就算好"、0 也算稳定、textLayerExists 又去看 span、
层不在也判 empty、抛不可重试的错、管理器把它标成完成、预取页不释放)。

### 还没动(有实测数字,等下一轮)

- **段落库整库重写**: 本次 33 次 flush 写了 **4.33 MB**(131 KB/次),`segmentMs 1709`。
  2.8.4 说"等真实数字说话" —— 数字说话了,`JSON.stringify` 整库在主线程,会随会话变长。
- **预取浪费**: `prefetchedUnviewed 19 / 35`(54%),`attemptErrors.CANCELLED 5 / 33 次发送`。
- **单页长尾**: 第 12 页 `durationMs 27400`(1 个请求)、第 5 页 7855 ms。

## [2.8.12] — 2026-09-09

**真机修正之四: 第一份语料是废的**。文件写出来了(2.8.11 起一切正常),但打开一看:
39 页**全部** `unverifiable`、spans 全 `missing:not-rendered`、结构块全空、译文一条没带。
诊断文件同一批导出却很干净 —— 问题出在语料的取数设计上。

### Fixed — 语料的取数顺序错了

方案原文假定"结构只能靠重解析",漏了最短的一条路:

- **没被淘汰的页,内存里就躺着翻译当时用的那份结构**(`state.blocks`)。它不是重建的
  近似,**就是原件**;译文与它同源同 id,对齐是定义上成立的,不需要核对,更不该被
  "结构不匹配"扣下。新增第四档 **`structureMatch: 'as-translated'`** —— 它比 `matched`
  强:matched 说"重建出了一样的东西",as-translated 说"这就是那个东西"。
- **重解析在这个文档上根本走不通**:它的抽取路径是 `text-layer`,而 **PDF.js 的文本层
  只对当前渲染着的那几页存在**,导出时早翻过去了 —— 于是 39 页全都重解析出 0 块。
  取数顺序改为 **内存原件 → 重解析 → 如实标缺失**。

### Fixed — 两处"看着有数据,其实在说谎"

- **空块表被标成 `blocks: 're-extracted'`**,等于说"重解析过了,这页就是没内容"。
  真相是"这页没渲染过,文本层不在,拿不到"。现在如实标 `missing:not-rendered`,
  并给 `unverifiableReasons: ['re-extraction-unavailable']`。
- **本来就没有译文的页被报成 `missing:structure-mismatch`** —— 那是栽赃,会把排查引到
  结构上去。这个原因**只在真有译文却被扣下时**才说得出口;其余照实报
  `missing:evicted` / `missing:cache-miss` / `missing:never-processed`。

### Added

- `result.blocksBySource: { live, re-extracted, missing }` —— 一眼看出这份语料有多少是
  原件、多少是重建、多少压根没拿到。
- manifest 的说明句点出文本层这条限制,读的人才不会以为文档半边是空的。

### 不变量

8 项突变全部被杀(内存原件也走重解析、原件标成 re-extracted、空块表冒充 re-extracted、
as-translated 不许贴译文、来路不进 availability、没译文也报结构不匹配、blocksBySource
不计数、manifest 不提文本层限制)。**诊断文件这一版未改** —— 真机产出已核对无误:
39 页、首行 manifest、末行 result、`blockSource` 19 页走摘要(2.8.5 的淘汰页明细如设计
生效)、端点全是枚举、无原文无译文。

## [2.8.11] — 2026-09-09

**真机修正之三: 选好位置点 Save,磁盘上一个文件都没有**。对话框正常、取消正常,
但真正写文件那一步从第一行就失败了 —— 而且**连一句提示都没有**。

### Fixed

- **`IOUtils.write(..., { mode: 'append' })` 不会建文件**。Gecko 的 `WriteMode` 里
  另有 `'appendOrCreate'`,正说明 `'append'` 要求文件已存在;而 `prepareTarget` 又
  **刻意保证目标文件不存在**(免得追加到旧文件上拼出两份)。两条规矩正面撞车,
  第一行就抛 `NS_ERROR_FILE_NOT_FOUND`。改为**第一行 `create`,其后 `append`**。
- **`void this.runExport(...)` 把漏出去的异常静默吞掉**,于是失败连提示都没有。
  `runExport` 现在有兜底 catch:导出可以失败,但**不能没有回音**。

### 这次为什么没被测试挡住

测试替身太宽容: 它对 `mode: 'append'` 一律照单全收,不管文件在不在。真实平台会抛。
**替身不照平台语义走,"目标文件必须不存在"与"第一行用 append 写"这对矛盾就能一路
绿到真机。** 现在的替身会按 Gecko 的规则拒绝(append 到不存在的文件、create 到已存在
的文件都抛),改回任何一种写法都立刻变红 —— 4 项突变全部被杀。

## [2.8.10] — 2026-09-09

**真机修正之二: 在保存对话框里点「取消」,又被追问一次**。2.8.9 的对话框能正常
打开了(三级尝试生效),但用户点 Cancel 之后,紧接着弹出「无法打开保存对话框 ——
是否改为保存到 …」。取消就该是结束。

### Fixed

- **取消之后读路径抛异常,被当成"没有对话框"**。`pickSavePath` 原先把「拉起对话框」
  与「读取选中路径」放在同一个 try 里,于是取消后读 `picker.file` 抛出的异常
  (Zotero 包装在没选文件时读 `file` 会抛)也走进了"没有对话框"分支。现在:
  **对话框只要开出来过,此后无论出什么岔子都不会再问"无法打开保存对话框"**。
- **判"取消"改成判"成功"**。原先按 `fp.returnCancel` 比较,而 Zotero 的包装**不暴露
  这个常量** —— 拿 `undefined` 去比,任何返回值都成了"不是取消",**取消于是被当成
  选好了**,再往下才在读路径时炸开。现在按 `returnOK` / `returnReplace` 判成功,
  且三个返回值与 `modeSave` **写死数值**(XPCOM 接口的稳定契约),不依赖包装是否暴露。
- **`pickerPath()` 统一读路径且永不抛**:认路径字符串(Zotero 包装)、认 nsIFile
  (裸 XPCOM),null / 空串 / 读一下就抛,一律返回 null。
- 新增第四种结局 **`no-path`**:对话框开出来了但没给路径 —— 既不是"没有对话框"
  (不该再问备用目录),也不能说成"用户取消了"(他没有)。提示"没有拿到保存位置,
  已取消",到此为止。

### 不变量

9 项突变全部被杀(取消后读路径抛错仍算没对话框、没拿到路径回退成 no-picker、
路径为空当成选好了、pickerPath 会抛、pickerPath 不认 nsIFile、常量写错、
改回按 returnCancel 判断、路径读取不走 pickerPath、no-path 也去问备用目录)。

## [2.8.9] — 2026-09-09

**真机修正: 保存对话框拉不起来**。2.8.8 在真机 Zotero 上直接落到了「无法打开保存
对话框 —— 是否改为保存到 …」那条回落分支。回落本身按设计生效了(没有默默把文件
写到别处),但对话框本该能打开。

### Fixed

- **`nsIFilePicker.init()` 的第一个参数**:Firefox 111+ 起要的是 **BrowsingContext**,
  不是 window。2.8.8 传的是 `Zotero.getMainWindow()`,于是 `init` 直接抛错,每次导出
  都退到询问备用目录。改为**三级尝试**:
  1. Zotero 7 自带的 `FilePicker` 包装(`chrome://zotero/content/modules/filePicker.mjs`)
     —— 它本来就是为抹平这件事存在的;注意它的 `file` 是**路径字符串**,不是 nsIFile,
     当成对象取 `.path` 会永远拿到空;
  2. 裸 `nsIFilePicker` + `win.browsingContext`;
  3. 裸 `nsIFilePicker` + `win`(更老的 Zotero)。
  三条都不成才回落到「询问备用目录」—— 那条分支原样保留,平台再变也不会变成
  "默默写到某个地方"。
- **拉不起来的原因现在会记进日志**(`save dialog unavailable: …`)。2.8.8 只说了句
  "无法打开",线索当场丢掉;这次要不是回落对话框把路径显示出来,连是哪一步出错
  都无从谈起。原因**只进日志,不进任何导出文件**。

### 不变量

`pickSavePath` 的决策逻辑(取消即结束、拉不起来交回调用方去问、路径为空也算取消)
与平台细节彻底分开,平台形态换了不影响它。新增结构闸钉住三级顺序:包装拿到手
必须**直接返回**(不能构造完又不用)、裸 picker 必须**先试 BrowsingContext 再试
window**、包装的 `file` 必须按字符串读。6 项突变全部被杀。

## [2.8.8] — 2026-09-08

**导出方案 P3: 文件真的落盘了**。P0–P2 建的是纯函数内核,一个字节都没写到磁盘上;
本版把它接到平台上 —— IOUtils 追加写、保存对话框、「更多」菜单两个入口、进度与
阶段化失败提示、「显示文件」。**导出方案至此四期做完。**
37 页布局快照与请求计划基线**逐字节不变**。

### Added

- **`src/export/fileSink.ts`**: `FileJsonlSink` 用
  `IOUtils.write(path, bytes, { mode: 'append' })` 逐行追加(与 cacheManager 同一套平台 API),
  整份文件从头到尾不必同时在内存里。落盘前建目录并**确认目标文件不存在** ——
  追加到旧文件上会把两份内容拼成一份。写失败**不吞异常**(磁盘满了却提示成功
  比失败坏得多),并 best-effort 删掉半份文件。
- **「更多」菜单两个入口**: **导出诊断文件**(整篇,不含原文/译文/密钥)与
  **导出翻译语料**(整篇,含原文与译文)。语料入口**常驻,与调试日志解绑** ——
  调试日志只决定排版探针是否采样,不该拦住语料导出。
- **`src/export/pluginVersion.ts`**: `startup(params)` 把运行中的版本记下来,导出时取用。
  硬编码或读常量的版本会把排障引到另一份代码上;拿不到版本时内核**拒绝写文件**。

### 四条不肯让步的规矩(都有测试与突变钉住)

1. **保存对话框取消 = 直接结束**:不写文件、不回落别的目录、不提示成功,只一句"已取消"。
   用户点取消表达的是"我不要这个文件",不是"你随便找个地方放吧" —— 语料里是整篇
   未发表稿件的原文。
2. **对话框拉不起来时先问再写**:弹一次"是否改为保存到 <备用目录>",同意才写。
   **绝不自动把含全文的语料写到用户没点头的地方**;语料的隐私确认弹不出来时,直接不导出。
3. **重名绝不覆盖**:逐个探测追加 `_2`/`_3`,包括用户在对话框里选中的已存在文件
   (覆盖掉的可能是上一次导出的唯一一份证据);探测到上限就带时间戳,仍然不覆盖。
4. **失败只报失败并指明阶段**(准备 / 写入汇总 / 写入页面数据 / 收尾),失败后绝不
   提示成功;`Zotero.File.reveal` 不可用时把路径写进提示,不假装"已显示"。

### Changed

- 原先的「诊断:复制到剪贴板」与「语料:复制本页语料」**与文件导出并存这一版**:
  文件路径尚未在真机 Zotero 上跑通之前,先留一条已知可用的退路,确认无碍后再撤。
- `nsIFilePicker` 与 `Zotero.File.reveal` **没有做一次性探针**,而是各自带回落分支
  (拉不起来 → 询问备用目录;reveal 不可用 → 提示路径)。真机跑通后把结果补进 CHANGELOG。

### 不变量

15 项突变全部被杀(取消后回落备用目录、没有对话框就自己挑地方、file 为空仍当选中、
重名直接覆盖、选定路径不避让、已存在也照样追加、覆盖模式写、没写过也去删、
reveal 抛错仍报成功、版本读常量、startup 不喂版本、语料入口退回调试日志闸后、
语料不问就导、失败也提示成功、备用目录不问就写)。

## [2.8.7] — 2026-09-08

**导出方案 P2: 语料文件的写出内核 + 结构一致性核对**。语料**故意**含原文与译文,
所以这一版钉的不是"别泄露",而是**别说谎** —— spans 没被留存过,导出时只能重新解析,
文件里必须如实说清这一点的效力边界。仍不碰抽取 / 结构 / 排版的行为,不发任何请求;
37 页布局快照与请求计划基线**逐字节不变**。

### Added

- **`src/export/structureMatch.ts`** —— 三态判定:
  - `matched` 有可靠对照且逐项全等 · `mismatched` 有对照但不等 · `unverifiable`
    原始结构已被淘汰/从未留存,**或对照本身不可信**。
  - **四项跨页累积 / 可中途改变的输入**(`bodyFontSize`、`referencesAlreadyStarted`、
    `includeReferences`/不译词表、抽取路径)任一已知变化 → **必须 `unverifiable`**,
    哪怕逐项全等 —— 那只是碰巧。这时**不做比对**,也不给分类计数(给了会让人以为比过了)。
  - 比对**不止 id 与原文哈希**:`type`、`boundingBox`(容差 0.5 px,**容差写进文件**)、
    阅读序与栏、`tableRow`/`tableCol`、`translationMode`、原文哈希,各自成一类计数,
    另有 `missing` / `extra`。只比 id + 哈希会放过"文字一样但被切到了另一栏/另一行"
    这类真正会毁掉语料的差异。
- **`src/export/corpusJsonl.ts`** —— 语料文件的写出:
  - 首行 manifest 的**第一个字段**就是 `CONTAINS_SOURCE_TEXT: true`,外加
    `spansPolicy: "re-extracted-at-export"` 与一句写死的说明:matched 只说明用同样的
    流水线重建出了同样的结构,**不等于这就是翻译当时的 spans**。
  - `availability.spans` **恒为 `re-extracted`,匹配也不升格**;没渲染过的页拿不到
    文本层,标 `missing:not-rendered` —— 不拿重建的结构冒充 spans。
  - **结构不匹配时,写出器就地扣下译文**并记 `missing:structure-mismatch`,
    `result.translationsWithheld` 数得出来。块 id 是按位置生成的,照 id 贴译文会产出
    "看起来对齐、其实错位"的语料,**比缺失更糟**。这条闸放在写出器里,不靠数据源自觉。
  - `result` 行带三态计数 `structureMatch: { matched, mismatched, unverifiable }`。
- **`src/export/jsonlWriter.ts`**: 把 P1 的写出骨架(逐行追加、result 行的含义、
  阶段化报错、逐页短时 pin)抽出来两个导出器共用 —— 否则两份文件的这些语义迟早各长各的,
  而它们恰恰是读文件的人最依赖的部分。
- **`TextExtractor.extractInputsFor()` / `currentExtractInputs()`**: 每页抽取当时的可变
  输入快照(不译词表只记哈希)。没有它,重解析对不上时连"是不是输入变了"都答不出来。
- **`readerSession.corpusExportSource(version)`**: 重解析**直接调 `extractor.extractPage`**
  —— 那就是翻译当初走的同一条流水线,共用同一个函数,不会有"检查用的链和真链慢慢分叉";
  译文来路三分 `live` / `restored-from-cache` / 缺失原因(`missing:evicted` 与
  `missing:cache-miss` 分开说,后者不冒充前者)。

### 不变量

19 项突变全部被杀,含:输入变了仍照比、匹配后 spans 升格、不匹配也贴译文、扣下的译文
不计数、只比 id 与哈希、表格行/列各自不比、栏号不比、缺块被忽略、容差大到吃掉真差异、
淘汰页当成比过了、自我声明不在首位、探针没采样却标 live、三态计数不进 result、
宿主另写一份检查用解析、查了却不喂给 checkStructure、evicted 与 cache-miss 混为一谈。

## [2.8.6] — 2026-09-08

**导出方案 P1: 诊断文件的写出内核**。一次导出 = **一个 JSONL 纯文本文件,不打包**
(用户定的方向;此前的 ZIP 方案作废)。本版做的是内核与数据源:纯函数、可单测、
不碰抽取 / 结构 / 排版,也不发任何请求。保存对话框与「显示文件」属于平台交互,留给 P3。
37 页布局快照与请求计划基线**逐字节不变**。

### Added

- **`src/export/diagnosticsJsonl.ts`**: 一行一个 JSON —— 首行 `manifest`(版本、时间、
  冻结的页清单与统计)、次行 `summary`(引擎 / usage / render / cacheWrites)、
  其后每页一行、末行 `result`。
  - **`result` 行是"这份文件写完了"的唯一凭据**。manifest 在第一行,写它的时候后面
    会不会失败还不知道 —— 完成情况只能记在最后。没有 `result` 行的文件就是半份,
    读的人一眼看得出,不会把截断的文件当成完整证据。
  - **某页读失败不终止导出,但必须被数出来**:那页照样出一行标
    `availability.pageRecord = "missing:read-failed"`,并计入 `result.pageReadFailures`。
    异常的原始消息(常带路径)不进文件。**不能静默漏页**。
  - 失败一律抛 `ExportStageError` 并带阶段(prepare / summary / write-page / finalize),
    调用方据此提示"读第 N 页失败"而不是笼统的"导出失败"。
  - **逐页短时保护**:读一页之前 pin、读完(哪怕抛错)立即释放,`readerSession.exportPins`
    接进 `isPageInUse`。**不是全局暂停淘汰** —— 那样长文档导出时内存会一路上涨,
    把 2.8.3 省下来的又吃回去。
  - 逐行 `append`,不把整份内容拼成一个字符串;写出前实查一次"行内无真实换行"。
  - 拿不到运行中的插件版本就**拒绝写文件**:版本写错的诊断比没有诊断更坏,
    它会把排障引到另一份代码上。
- **`src/export/exportNaming.ts`**: `PaperMirror_v2.8.6_diagnostics_20260908_153042_287+0800.jsonl`
  —— 版本从运行中的插件取,时间带毫秒与 `±HHMM` 时区,重名追加 `_2`/`_3` **绝不覆盖**,
  **文件名不含论文标题**(文件名是别人第一眼看到的东西),版本号进文件名前先消毒。
- **`TranslationManager.exportScope()` / `exportPageDiagnostics(pageIndex)`**: 逐页取诊断,
  与整份 `exportDiagnostics()` **走同一个** `pageDiagnosticsRow` —— 两条取法逐字节相同,
  不会"一次性诊断"与"导出的文件"各说各话。
- **`readerSession.diagnosticsExportSource(version)`**: 把"从哪儿取数"与"怎么写文件"
  分开。summary 行剥掉 `pages`(否则整份文档的页数据出现两遍);每页附
  geometryAudit / placement;探针未采样如实标 `not-sampled`,不冒充"这页没问题"。

### Changed

- **诊断不再导出真实端点主机名**(`src/export/endpointKind.ts`)。此前带的是
  `endpointHost` —— 一份"可以放心贴进 issue"的诊断会顺带公开用户自建网关的域名或
  公司内网主机名。改为固定枚举 `endpointKind`:`official` · `custom` · `local` ·
  `invalid` · `unknown`,外加 `customEndpoint` 布尔。**不导出域名、IP、端口、路径,
  也不导出主机名哈希** —— 哈希看着无害,但候选集小到可以枚举,等于没脱敏。
  引擎自检失败也只报 `selfCheckFailed: true`:异常消息里常带着端点 URL。

### 不变量

14 项突变全部被杀(重名覆盖、时间戳丢时区、版本不消毒、custom 报成 official、
回环判定失效、失败仍写 result、读失败页被跳过、pin 不释放、空版本照写、页序不排、
summary 重复整份页数据、pin 不被 isPageInUse 认、逐页与整份口径分家、自检退回主机名)。

## [2.8.5] — 2026-09-08

**导出方案 P0: 省内存不再吃掉诊断结论**。2.8.3 的冷页淘汰把 `blocks` 与
`keepOrigin`/`rejectReasons` 一起卸掉之后,逐块诊断明细就没了依据 —— 导出诊断只
看得到一页空块表,分不清"这页没有可译内容"和"这页的明细被省内存省掉了"。本版在
**卸载现场、清空之前**拍一份定长无文本的逐块摘要,并记下该页实际走通的抽取路径。
不改任何翻译、结构或排版行为;37 页布局快照与请求计划基线**逐字节不变**。

### Added

- **逐块诊断摘要**(`src/translation/pageBlockDigest.ts`): 块 id、类型、字符数、
  最终去向(`translated`/`preserved`/`untranslated`)、keepOrigin 原因枚举、最后
  一次拒绝原因枚举。约 60–80 字节/块,随轻量页状态活着。摘要带 `runId`/
  `revision` 两个审计位,重新翻译后一眼看得出手上这份属于哪一轮。
  - 活块与摘要**走同一个函数**(`digestRows` → `diagnosticRows`),逐块状态的判定
    规则只有一份,不会"内存里算一套、摘要里算另一套"。
  - 诊断行的 `state` 口径**沿用 2.3.7**(translated / preserved / keepOrigin 原因
    / untranslated),`joinPlacementOutcome` 与 baseline-report 不受影响;新增的
    `keepOrigin` 是把原因**另外**显式列一栏,不替换 `state`。
  - 摘要在**卸载现场**拍,不在页面完成时拍 —— 那是最后一个 `blocks` 与两个 Map
    都还看得见的时刻,只有一处调用点,没被卸的页零开销。
- **`blockSource`**(诊断每页): `live` / `digest` / `none` —— 空块表不再需要靠猜。
  取 `digest` 时附 `blockDigestRun` / `blockDigestRevision`。
- **`extractPath`**(诊断每页): 这一页的块究竟是哪条抽取路径产出的 ——
  `chars`(fork 字符流)/ `text-layer`(渲染文本层)/ `plain-text`(PDFWorker)/
  `rendered-recovery`(当前页恢复)/ `empty`。三条路径拿到的数据差别很大,不记下
  来的话,后续"重新解析 spans 再与当时结构比对"的核对说不清是排版变了还是路径
  变了。只记枚举:privacy 闸钉死了取值集合,以及五个记录现场只能写枚举字面量 ——
  字段名里带 "path",最容易在后续改动里被塞进真实文件路径。

### 不变量

- 摘要**只收枚举与数字**,原文、译文、异常原始消息一律不进;
  `diagnosticsPrivacy` 与新增的行为测试都拿哨兵字符串反查过。
- 行为闸:被卸过的页导出的逐块明细与卸之前**逐字相同** —— 省内存不得改变诊断
  结论。8 个突变(不拍摘要、拍晚了、preserve 报成 untranslated、译出的块仍挂旧
  拒绝原因、state 不回落 keepOrigin、blockSource 恒为 live、抽取路径出口漏记、
  宿主不接线)全部被杀。

## [2.8.4] — 2026-09-08

**性能第五批(上半): 先量,再改**。本版**只加计量、不改任何行为** —— 缓存分片与
补译尾部的改动要等这几个数字在真实长文档上说话。全部是计数与毫秒,不含原文、
译文、路径与密钥;37 页布局快照与请求计划基线**逐字节不变**。

### Added

- **页级时序三段**(`PageDiagnostics`): `queuedMs`(抽取完到调度器真正让这页开跑
  —— 它大说明并发/优先级有问题,不是模型慢)、`extractMs`(PDF 抽取)、
  `firstTextMs`(从这页开始跑到**第一块译文到手**)。`durationMs` 回答的是"整页
  多久译完",回答不了"用户多久看到第一个中文字";三段合起来才说得清慢在哪。
  首字时刻**只记第一次**,后来的通知(单块重译、补救回填)不得把它往后推。
- **`usage.hotPages` / `usage.retainedPages`**: 此刻还持有完整内容的页数,以及
  轻量状态的页数。前者与第四批的 `RETAIN_LIMIT` 一起看,才知道上限定得合不合适。
- **缓存写盘计量**(`cacheManager.cacheWriteStats()`): 页缓存与段落库各自的写入
  次数、字节数、耗时,加上失败次数。段落库是**整库重写** —— `segmentBytes /
  segmentFlushes` 就是"每写一条段落要重写多大一坨",分片值不值得做先看这个数。
  快照是拷贝,外部改不到内部计数。
- **渲染计量**(`pane.renderMetrics()`): started / committed / **cancelled** /
  failed / totalMs。`cancelled` 大是第一批的取消在频繁生效(翻页快),不是故障;
  它与 `layoutMs` 一起看才知道排版耗时被谁吃掉。
- baseline-report 新增 9 列:`queuedMs` `extractMs` `firstTextMs` `hotPages`
  `renderCancelled` `renderMs` `cacheWriteMs` `cacheWriteKB` `segFlushKB`。

### 明确**没有**做的事

按第五批的约定,以下三项都要等真实长文档的数字确认瓶颈之后再动,本版一行未改:
缓存按稳定哈希分片、补译尾部只补缺失块、限流时降低对应服务商压力。结构闸钉住了
"计量绝不参与任何分支判断"与"仍然只有页缓存 + 段落库两处整体写盘"。

### Tests

- diagnosticsPrivacy 3 例(时序/热页数只有数字且不夹带文本;写盘计量字段集合固定、
  快照是拷贝;"只量不改"的结构闸),translationManager 2 例(时序三段各自成立 +
  **首字时间不被后来的通知改写**;热页面数量随淘汰下降,与 `retainedPages` 分别
  回答两个问题)。7 个突变各一红。999 全绿。

## [2.8.3] — 2026-09-08

**性能第四批: 限制已完成页面的内存保留**。`TranslationManager.pages` 此前只增不减:
一篇 300 页的文档读到底,300 页的 `blocks`(每块带 `lineRectsPdf` 行矩形数组)与
`translations` 全都留在内存里,而其中绝大多数早已滚出视野、也早已可靠落进页面
缓存。37 页布局快照与请求计划基线**逐字节不变**。

### Added

- **`src/translation/pageRetention.ts`**:`pagesToEvict` 纯函数决定"哪几页可以卸掉
  完整内容"。`RETAIN_LIMIT`=20、`KEEP_RADIUS`=2 —— **都是待用真实长文档验证的初值**。
  四条保护任何一条成立就绝不淘汰:没翻完(`status !== 'done'`)、译文还没可靠落进
  页面缓存(`cached`)、正在渲染或仍挂在面板上(`inUse`)、落在当前页前后 2 页内。
  可淘汰的页不够时**持有量就停在上限之上**,绝不硬卸。
- **轻量状态与完整内容分开**:卸掉的只有 `blocks` / `translations` /
  `keepOrigin` / `rejectReasons`;页状态本身(状态、页级指标、诊断)留着,所以
  导出诊断不会因为省内存少掉几页(卸过的页带 `evicted: true`,块表为空是这个
  原因,不是"这页没有可译内容")。
- **`TranslationDeps.isPageInUse`**:宿主回答"这一页此刻还被用着吗"。会话接的是
  当前阅读页 + `pane.hasMountedPage`(面板上挂着的页与正在重建的页)。问不出来
  时按"在用"处理 —— 宁可多占内存,不冒丢译文的险。

### Fixed

- **`cached` 只在落盘**真的**成功后才标**。四个 `writeCache` 现场各一处;那个
  best-effort 现场用 `.then(成功分支)` 而不是 catch 之后无脑标。有未译块或
  keepOrigin 的页按设计不写页面缓存,于是它们永远不会被淘汰 —— 这正是对的。
- **整页缓存命中的页也标 `cached`**。漏掉这一条,"翻回旧页"复原出来的页会被
  永久钉在内存里,来回翻几次就把上限彻底架空(实现过程中真的踩到了)。
- **被卸过的页必须能重新装载**:`ensurePage` 对 `evicted` 的已完成页不再早退,
  走的是与"从未翻过的页"完全相同的路径 —— 重新抽取 + 读页面缓存,**零 API 请求**。

### Tests

- pageRetention 7 例(上限边界含"远低于上限"这条 —— 少一道闸就会被
  `slice(0, 负数)` 反过来把该留的页全卸了;四条保护逐条反证;LRU 顺序按
  `touchedAt` 而非页码;已卸页不占容量;可淘汰的页不足时不硬卸;三个接线结构闸),
  translationManager 4 例(**连读 100 页后持有量 ≤ 20、回到旧页 API 增量为零**;
  正在用的页与没落盘的页不被卸;LRU 看的是"最近用到"而非页码;从缓存复原的页
  仍可再被卸)。16 个突变各一红。994 全绿。

## [2.8.2] — 2026-09-08

**性能第三批: 安全的分批显示**。在 2.7.10 那一版增量显示之上,把触发判据从
"已到块数"换成**译文修订号**,并补上一处 2.7.10 漏掉的安全检查。37 页布局快照
与请求计划基线**逐字节不变**。

### Fixed

- **半成品页此前跳过了几何审计** —— 这是 2.7.10 的一个真缺口。`auditStrictGeometry`
  住在 `reportPlacement` 里,而 2.7.10 让半成品页整个跳过 settle 回调,于是墨迹闸与
  跨块碰撞检查一起被跳过了:一个在半成品页上违例的块可能带着叠印显示出去。现在
  半成品页照跑审计(违例块由审计回退/保留原文),跳过的只有两件事 —— 压缩重试
  (对着还在长的页面重试会白花请求)与统计入账(会把半成品的数字当成终值)。
  `auditPartialGeometry` 不写 `geometryAudits`,终版渲染写真的那份。

### Changed

- **触发判据改为 `PageTranslationState.translationRevision`**。管理器在 `notify`
  前算一遍译文指纹,内容**真的变了**才 +1。指纹在这**一处**算,九个
  `translations.set` 写入点自动全覆盖 —— 不必在每个现场记一次,漏一处就是一个
  不会报错的静默 bug。用块数当判据有两个漏洞:原地重译(单块 replay、逐块补救、
  缓存回填)块数不变而内容变了,会**漏**;同一批结果被重复通知时又会**白重建**。
- **可见页每 500 ms 最多刷新一次**(原 2000 ms),并且**同一修订版本只渲染一次**;
  `done` 时记下最终修订号,终版无条件提交。去掉了"至少 3 个新块"这条阈值 ——
  修订号去重之后,节流本身就是限速器,再加块数门槛只会让"最后两块迟迟不来"的页
  白等。
- **后台页只更新数据,不立即排版**(可见性闸,沿用)。

### 已经满足、这次补上回归闸的

- **渲染期间保留上一版页面**: `renderDocPage` 的两处 `slot.replaceChildren` 都在
  新页面**已经建好之后**,整个函数里没有"先清空/先画遮罩再慢慢建"的写法。
  这一条本来就成立,现在有结构闸钉住,不会被后来的改动破坏。

### Tests

- slotState 4 例(三道闸各自把关 + `MIN_INTERVAL_MS === 500`;"最后一个块一直不来,
  前两批照样显示且不空转重建"的时序推演;半成品必须跑审计、不写诊断、不压缩重试;
  渲染期间不清空槽的结构闸),translationManager 2 例(内容变了才 +1、重复通知不
  前进;**原地重译块数不变也要前进** —— 正是不用块数当判据的原因)。
  9 个突变各一红。981 全绿。

## [2.8.1] — 2026-09-08

**性能第二批: 可见页定位不再遍历整篇文档**。仍然只动渲染侧;37 页布局快照与
请求计划基线**逐字节不变**。

### Fixed

- **`visibleRange` 改二分,滚动期间零 DOM 几何读取**。它此前每次调用都从第 0 页
  起逐页读 `offsetTop / offsetHeight` —— 而滚动、翻页、译文到达、每渲染完一页
  之后都会调它。500 页的文档,一次普通滚动就是每帧 500 次布局读取,每次读取还
  可能触发强制同步布局。新模块 `src/ui/pageOffsetIndex.ts`:
  - `PageOffsetIndex.build` 一趟**批量**读完所有槽的位置(读与写不交错),
    `rangeFor` 是纯二分。位置取自**实测**而非按页高推算 —— 混合纸张、横向页、
    页间距、页码标签高度全都自然包含,不假设整篇等高。
  - 未排版的槽会报 top=0;顶边取运行最大值夹住,保证非递减,二分的前提不塌。
  - `CachedPageIndex` 把"建一次、用很多次"做成可测的东西:`builds` 计真正读几何
    的次数。滚动只调 `get`,它一次都不涨;只有几何真的变了(页宽 / 缩放 /
    页面尺寸 / 换文档)才重建。
- **`releaseFarSlots` 只看已挂载页**。此前每趟泵送结束都把整篇扫一遍,500 页里
  496 页本来就是空的。新增 `mounted: Set<number>`,渲染落地时登记、回收时删除、
  重排时清空;回收先集中收集再集中改 DOM,读写不交错。

**阈值说明**: 本批没有引入任何新阈值 —— 索引是精确的,不是近似。

### Tests

- pageOffsetIndex 6 例: 与逐页扫描在混合纸张(A4 纵/横、Letter、超长折页、
  半高页)上**逐个滚动位置比对**;页缝、零高视口、整篇之外、空文档等边界;
  未排版槽的非递减保证;50 / 200 / 500 页的"建索引读一次、滚动零读取";
  缓存在 400 帧滚动中 `builds === 1`、作废与换文档才重建;两个接线结构闸
  (visibleRange 不得再出现 `offsetTop`;回收只遍历 `mounted`,且其登记/清除/
  清空三个点都钉住)。10 个突变各一红。977 全绿。

## [2.8.0] — 2026-09-08

**性能第一批: 当前页优先 + 取消过时渲染**。只动渲染调度,不碰翻译调度、结构识别
与排版算法;34→37 页布局快照与请求计划基线**逐字节不变**(本批不触碰提取路径)。

### Fixed

- **当前页不再排队等一个卡住的旧任务**。`pumpRenders` 此前在可视窗口里按**页码
  从小到大**挑第一个待渲染的页(注释写着 "nearest first",代码并不是),而且一页
  重建卡住就占住整个泵 20 秒 —— 用户翻到新页,新页要排在那 20 秒之后。
  新模块 `src/ui/renderPump.ts` 把这套调度独立出来:
  - `orderRenderCandidates` (纯函数): **可视页整体先于预取页**,层内按到当前页的
    距离排(当前页距离 0,自然第一),距离相同先小页码。分层在距离之前 —— 一个
    更近的预取页也不该抢在任何一个用户正看着的页前面。
  - `shouldAbortInFlight` (纯函数): 在飞任务一旦离开「可视范围前后各
    `KEEP_PAGES`=1 页」立刻取消。只翻一页不取消,翻到远处才取消。
  - `RenderPump.request()` 在每次泵送前先取消过时任务,所以当前页立刻开始,
    而不是排在旧任务后面。
- **20 秒超时现在同时触发取消**,不再只让 `Promise.race` 先返回、留一个没人管的
  任务继续跑到底、最后往一个早已不该更新的槽里提交 DOM。正常完成会清掉定时器,
  不再每页留一个 20 秒挂单。
- **取消真的传到渲染器**。`renderDocPage(pageIndex, slot, width, signal)` 把
  `signal?.aborted` 折进 `current()` —— 这条路径上"我还该不该动这个槽"只有这一个
  判据,于是每一处已有的检查点(每次 `await` 之后)自动同时是取消检查点;两处
  提交 DOM 之前各加一道显式闸。被取消的一页只标脏、不写槽状态,也不动降级预算。
- **泵送期间到达的更新不再被吞掉**。此前 `pumping` 为真时 `scheduleEnsure` 直接
  返回,而 `ensureTimer` 已经烧掉了 —— 那次更新就永远没人处理。现在记 `pending`,
  步数用满退出时补排一次。

**边界(本批遵守)**: 只取消页面渲染,不取消任何翻译请求(译文有缓存价值);
同页缩放仍然重新排版但绝不重新翻译 —— 渲染与翻译是两条独立路径,本批未触碰后者。

### Tests

- renderPump 8 例: 排序两层(距离 + 可视先于预取)、`shouldAbortInFlight` 边界、
  "上一页迟迟不返回 + 翻到远处 → 旧任务取消、新页立刻开始、取消真的传到渲染器"、
  "只翻一页不取消"、"超时同时 abort + 正常完成清定时器"、"步数用满时补排"、
  两个接线结构闸(renderDocPage 的 signal 折入与两处提交闸;面板不再自己按页码
  扫描)。9 个突变各一红。971 全绿。

## [2.7.10] — 2026-09-08

**页视图增量显示 + 新语料来源**。计划里的"第二批·3"终于有了证据:2.7.8 的真实诊断
里 Radiology 2026 PCCT 综述 p13 有 33 个可替换块、3 个批次、`durationMs 34653` ——
首批约 12 秒就到手,用户却要等满 34 秒才看到第一个字。

### Changed

- **页视图不再等整页译完才显示**(`translationPane.renderPage` /
  `readerSession` 的页渲染器)。管理器本来就每译完一个 chunk notify 一次
  (2.3.x 的 progressive rendering per chunk),此前页视图只在 `status==='done'`
  时重建,那些通知全被丢掉。现在途中也画,但要过 `shouldRenderPartial` 的三道闸
  (纯函数,单测锁死):**页面可见**(看不见的页重建纯属浪费,`done` 时还会再画)、
  至少 `MIN_NEW_BLOCKS`=3 个**新**译文块、距上次增量重建 ≥ `MIN_INTERVAL_MS`=2000 ms。
  已到的块画译文,没到的保持原文。
- **半成品页不结算**:`settleStrictPage` 的回调在 partial 时直接返回 —— 不做压缩
  重试(对着一个还在长的页面重试是白花请求)、不报排版统计(会误报数字)、
  重建抛异常也不弹"排版失败"提示(译完那一趟还会再来)。新的
  `PageRenderResult` 取值 `'partial'`:画上去了,但不是终态,不排重试、
  **不复位降级预算**(否则半成品重建会把确定性失败页的重试预算反复刷回)。

### Added

- 三个新语料夹具 `radiology2026-p4 / -p10 / -p13`(Radiology 2026 PCCT 综述,
  双栏,语料库里的新来源)。夹具总数 34 → 37,基线同步扩到 37 条,
  **原有 34 页逐字节不变**。
- p13 顺带钉住一个**已知缺陷**:那是一张"Description (or Opportunity) /
  Implications and Recommended Solutions"两列文本表,`detectTableRegions`
  完全没识别出来,于是 34 个块里 22 个是被打散的表格碎片、标签与描述在
  阅读顺序里交错(`tableCells: 0`)。快照如实记录当前行为,留给下一批修。

### Tests

- slotState 4 例(`'partial'` 的槽处置;`shouldRenderPartial` 三道闸各自把关 +
  五种非 translating 状态都不走增量;readerSession 半成品不结算的结构闸;
  renderPage 增量分支的结构闸)。6 个突变各一红。961 全绿。

## [2.7.9] — 2026-09-08

**尝试去向明细**。2.7.8 装机后的首份真实诊断显示 `httpAttempts 30` 对 20 个响应
(18 报告 + 2 缺用量)—— 多出来的 10 次发送**说不出是什么**。计量层补两个枚举
计数器把这个洞堵上;提取/结构/排版零改动,34 个布局快照与请求计划基线逐字节不变。

### Added

- **`usage.paramHeals`**(`RejectedParam` 枚举计数): 适配器因 HTTP 400 剥掉参数
  重发时报出被拒的是哪一个 —— `temperature` / `reasoning_effort`(openaiCompatible)、
  `thinking`(geminiNative)。新钩子 `TranslateOptions.onParamHeal` 与
  `TranslateHooks.onParamHeal`,与 2.7.7 的 `onAttempt` / `onUsage` 同路。
- **`usage.attemptErrors`**(`PaperMirrorErrorCode` 枚举计数): 已经发出去、最终以
  错误告终的"白发的尝试"。排队期间取消的(一次都没发出)不计;`BAD_RESPONSE`
  已作为响应计过(`validationFailures` / `usageMissing`),不重复计。
- 两个字段都只在非空时出现,只含枚举键与数字;错误 `message` 从不进诊断
  (`diagnosticsPrivacy` 新增一例,用原文哨兵塞进 message 做反证)。
  恒等式:`httpAttempts ≈ usageReports + usageMissing + paramHeals + attemptErrors`。
- baseline-report 新增 `paramHeals` / `attemptErrors` / `unexplained` 三列,
  `unexplained` 就是上面那个恒等式的残差 —— 对 2.7.8 的诊断跑出来正是 10。

### Tests

- translationManager 2 例(自愈与失败按枚举分开计 + 恒等式;干净会话省略两个字段、
  未发出的取消与 BAD_RESPONSE 都不进 attemptErrors),openaiReasoningRetry 2 例
  (两种参数各报各的枚举;不自愈就不回调),advancedParams 1 例(gemini thinking),
  diagnosticsPrivacy 2 例(枚举键与数字断言 + 原文哨兵不泄;readerSession 三个钩子
  转发的结构性回归闸)。10 个突变各一红。957 全绿。

## [2.7.8] — 2026-09-08

**外部审核·第三批(表格结构)**。三项都是 `src/reader` 里的纯函数规则,排版层零改动。
7 个布局快照与请求计划基线按预告变化,其余 27 页逐字节不变。

### Fixed

- **表格横向范围不再吞邻栏正文**(`tableGuard.detectTableRegions`)。双栏页上,
  一栏的统计密集正文行(百分比、p 值、置信区间)此前被当成数字种子,聚成与邻栏
  表格同行的"格"并被并入表格区(wu2026‑p6 Table 4 吞掉右栏 13 行正文)。两条几何
  闸门:(a) `columnExtents` —— 宽度 ≥ 所在正文栏宽 85% 的块是整行正文,不是种子;
  (b) `columnGutters` —— 行对齐扩张不得跨栏沟(区域本身已跨沟的通栏表除外)。
  wu2026‑p6:142 → 129 块,表格格 125 → 105,右栏正文回到散文流(payloadChars
  1817 → 2323 —— 这些行此前作为 preserve 数据格根本没有送译,是整段英文残留)。
- **文本表折行续行并回上一行**(`tableStructure.mergeContinuationRows`)。三列长文格
  同一视觉行一起折行时,折行行首顶对齐又贴列带左沿(或因邻列宽格自成一组),
  与真实行起点几何不可分,radiology2023‑p11 Table 4 的 "Gold / nanoparticles"、
  "Extensive preclinical / use, synthetic control …" 被切成两行六格。几何分不开的
  靠文本:一行**每一格**都(a) 上一行同列有格、(b) 小写字母或续行标点开头、
  (c) 与上一行同列格竖向间隙 ≤ 0.8em(与格内折行同一阈值),且上一行不是单个
  跨列子标题 —— 整行并回;任一格大写/数字开头即新行证据,整行不并;并回后再看
  下一行(三行折行)。radiology2023‑p11:57 → 54 块,9 行 → 8 行。
- **短格三类明确不译证据**(`tableStructure.cellPreserveEvidence` /
  `preserveReasonFor`,`TableCell.preserveReason`)。只认硬证据,**不**把任意全大写
  短词判 preserve("YES"/"TOTAL"/"Age (y)" 照常翻译):
  `glossary`(用户不译词表逐字命中)、`defined-abbreviation`(本页正文/表注定义过:
  "… detector (PCD)"、"AS = aortic stenosis"、"CT, computed tomography";连字缩写各
  部分都定义过也算)、`citation-label`("Kim et al,19 2022"、"van der Berg et al")。
  证据取自**整页**文本(定义通常在表注,不在表区),抽取期与严格排版期同一份证据
  (`StrictPageInput.noTranslate`、`TextExtractor` 选项 `noTranslate`,三处共用
  `readNoTranslateList`)。快照变化仅 5 页各 1–2 格,全部是表注里定义过的缩写:
  chen2023‑p10 "HR"/"HR*"、chen2023‑p4 "NSTEMI"、chen2023‑p5/p8 "RCA"、
  radiology2023‑p2 "DECT" → preserve。

### Tests

- tableGuard 3 例(`columnGutters`;邻栏统计密集整行不当种子;通栏表反例),
  tableStructure 9 例(证据收集、三类证据与反例、两种表模型记 `preserveReason`、
  整页证据与词表经 `structureTableCells`;续行并回、新行证据不并、子标题不吸收、
  小写跨列行不并、三行折行)。16 个突变各一红。950 全绿。
- 基线 `UPDATE_BASELINE=1` 重生成:7 页变化如上,其余不变。

## [2.7.7] — 2026-09-05

**外部审核·第一批(调度与计量)**。提取/结构/排版零改动,34 个布局快照与请求
计划基线逐字节不变。

### Fixed

- **请求闸的真实 HTTP 上限跟着 429 走**(`translationManager.laneFeedback` →
  `requestGate.setCap`)。页面级 lane 自适应(429 减半、超时减一、连续 5 次成功加一)
  2.0.5 起就有,但请求闸(真实在途 HTTP)的上限固定为 max(2, 页面 max):调度器被
  压到 1 并行页时闸仍放行 4 个并发请求。现在闸上限 = max(2, laneCap),起点用
  `initial` 而非 `max`;两层含义保留不变——一个管并行页数,一个管真实服务调用数。
- **服务商冷却期**(`RequestGate.cooldown`)。429 带 Retry‑After 时此前只有撞上的
  那一个请求在退避,同 lane 其他请求照常出手。现在整个 lane 进冷却:新名额不放行、
  在途的不打断、到点由定时器唤醒;等待者取消仍即刻 reject;别的 lane 不受影响;
  较短的 Retry‑After 不缩短已有冷却。`dispose()` 释放定时器。
- **计量放到正确位置**。`HttpJSONOptions.onAttempt` 在传输层每次真正发送时触发
  ——适配器内部的参数自愈重试(剥 reasoning_effort / temperature / thinking 再发)
  此前对 `attempts` 不可见;`TranslateOptions.onUsage` 在响应到手、译文校验**之前**
  上报用量——校验失败(BAD_RESPONSE)的响应同样花了 token,此前随异常丢掉。三家
  适配器同步。`meteredTranslate` 经回调计量,已回调的不再从响应字段重复累加;
  发出前就取消的不计尝试;不支持回调的引擎按一次调用一次尝试。
- 诊断 `usage` 新增 `httpAttempts`、`validationFailures`;baseline-report 新增
  `httpAttempts` / `attemptsPerBatch` / `validationFailures` 列;`requests` 仍是
  逻辑批次。

### Tests

- requestGate 3 例(冷却放行/唤醒/隔离、冷却中取消、冷却不被缩短)、
  translationManager 2 例(429 → 闸上限 4→2 + 冷却;回调计量:自愈重试 4 次尝试、
  失败响应 token 计入、缓存命中零新增)、openaiReasoningRetry 2 例(onAttempt 两次、
  用量先于校验)。5 个突变各一红。938 全绿。

## [2.7.6] — 2026-09-05

### Fixed

- **CI 红**:2.7.5 的新测试里一处 `as PaperMirrorError & {…}` 把运行时导入的值当
  类型用,`npm test` 前置的 `typecheck:tests` 报 TS2749。本地只跑了
  `node scripts/test.mjs`,没跑 `npm test`,漏掉了这一步 —— 发版仪式改为必须跑
  `npm test`。GitHub Release 只在 manifest.json 变化时触发,v2.7.5 未发布,故直接
  以 2.7.6 发布,内容与 2.7.5 相同。

## [2.7.5] — 2026-09-05

**外部审核修复**(GPT 对 2.7.4 的独立审核,6 项;5 项修复,1 项判定为提示词措辞
错误而非产品缺陷)。提取/结构/排版规则零改动,34 个布局快照与请求计划基线
逐字节不变。

### Fixed

- **P1 服务商回显不再进 error.message**(`errors.ts`)。HTTP 400 的响应体片段此前
  脱敏后拼进 message,而 message 进 logger 与面板 —— `sanitize` 只认密钥形态,
  服务商回显请求正文时论文原文就进了日志。现在被拒参数归一为
  `PaperMirrorError.rejectedParam` 枚举(temperature / reasoning_effort /
  thinking / model / other),message 只有固定措辞;自愈判定
  (`isTemperatureRejection` 等)改看枚举。新增哨兵测试:400/422/418 回显原文时
  message 与 `recentProblems()` 都不含哨兵,参数仍被正确归类。
- **P1 限流退避可取消**(`translationManager.delayUntilAbort`)。请求级重试的
  `await this.delay(wait)` 不看取消信号,Retry-After 120 s 会把 `resetAllAndWait`
  (刷新 / 换服务商)拖住整段 —— "点了刷新还卡着"的一条真实路径。现在等待与
  signal 绑定:取消即刻 CANCELLED、释放槽位;未取消时仍完整交给 delayFn 计时。
- **P2 诊断分开记批次与尝试**。`PageDiagnostics.requests` 保持"逻辑批次"
  (预算闸口径),新增 `attempts` = 实际 HTTP 尝试(含重试):两次失败一次成功
  = 1 / 3。**单段重译改走 `meteredTranslate`**,此前绕过计量,重译的 token 从
  诊断里消失。
- **P2 扩边裁决三道检查分开记账**(`expansionVerdict`,pure)。此前
  `clean = ladderFits && !violatesGeometry` 合并后,容量不够也被记成 geometry
  否决,`expand-geometry` 虚高。现在容量 → 几何 → 墨迹依次执行,只有实际执行
  并失败的检查才是裁决;两处扩边循环共用。
- **P2 请求计划基线成硬门禁**(`pipelineBaseline.test`)。基线缺失自动生成即
  通过、新语料页只打印提示、删语料页无人察觉 —— 三条绕过路径全部关闭:缺文件、
  缺条目、多条目一律失败;只有 `UPDATE_BASELINE=1 npm test` 才生成/覆盖,与
  `UPDATE_SNAPSHOTS` 同一姿态。

### Not changed

- 审核第 6 项"参考文献应一律 preserve、设置项应失效":这是给外部模型的项目简介
  里把"默认 preserve"写成了"一律 preserve"。"翻译参考文献部分"是有意的用户选项
  (默认关),不是漏洞;已更正简介文档,产品行为不变。
- 审核提醒的"暂存区含 2.4.2 旧内容":发版一直用临时索引提交(`GIT_INDEX_FILE`),
  `.git/index` 自 2.4.2 起没更新过,HEAD 与 Beta 一致;本次已 `git read-tree Beta`
  重建索引,`git status` 不再显示假删除。

### Tests

- 新增 8 例(errors 2、translationManager 4、fitFailure 2),基线门禁改写;
  7 个突变各一红(片段回拼、普通 delay、attempts 移出循环、重译绕过计量、
  裁决合并、基线缺失、基线多余条目)。931 全绿。

## [2.7.4] — 2026-09-05

### Fixed

- **对照翻译窗格里的状态胶囊(右下角进度圆环)整个消失**,覆盖模式正常。胶囊
  样式表默认 `position: fixed`:在覆盖模式的 PDF 文档里那就是视口右下角;但
  窗格挂在 Zotero 主窗口文档上,fixed 的包含块由宿主窗口布局决定 —— 标签页
  容器一旦带 contain/transform,胶囊就相对它定位,再被窗格自身的 `overflow:
  hidden` 裁掉。胶囊代码本身自 2.0.8 起零改动,是宿主环境的定位语义变了。
  现在 `.pm-bilingual-pane > .pm-status-capsule` 按窗格(position: relative)
  绝对定位,右下 22px,不再依赖宿主窗口的包含块。新增 paneCapsuleCss.test
  (突变一红)。

## [2.7.3] — 2026-09-05

**恢复批次**(六维审核路线图·批次 4,D-1)。依据 2.7.2 装机后导出的诊断
(jacc2020 ECV 荟萃分析,11 页,52 个放弃块、abandonReason 全量可读)。提取/
结构零改动,34 个布局快照与请求计划基线逐字节不变;改动全在排版放置层。

### 放弃原因分布(2.7.2 诊断,52 块)

- `shrink-floor/height` 43(全部是 9px 高的单行表格单元格)、`shrink-floor/none`
  7、`expand-ink/none` 2。也就是说 83% 的放弃不是"译文太长",而是**单元格被
  排进了错误的字号**。

### Fixed

- **表格单元格不再参与页面基准字号,也不再被统一成正文字号**
  (`strictPageReplacement`)。单元格块 type 也是 paragraph:一页 100 个 7pt 格
  + 20 段 9.5pt 正文,`bodyAnchorPt` 的中位数落在 7pt —— 整页正文被排成表格
  字号(Table 1/3 所在页正文明显比原文小);反过来单元格自己按 anchor 字号
  排进 7pt 高的格盒,一行都放不下(43 个 shrink-floor/height 的来源)。新增
  纯函数 `isTableCellBlock` / `bodyAnchorSizes`:基准只看正文流块,单元格按
  自身字号。
- **孤立块第三档缩字 0.82**(`shrinkStepsFor`)。标题/图题/表格单元格/微小单行
  各自独立,再缩一档不与正文比出大小差;正文流仍止于 0.88,发花闸不动。
- **底线组合逐个试扩展**(`orderExpansions`)。此前地板字号只配"最大扩展"试
  一次,右扩单独就够、双向却压上分栏线时整块放弃;现在右扩/下扩/双向按新占
  面积从小到大逐个预校验 + 墨迹闸。
- **放弃原因补 veto 位**:`shrink-floor/none+ink`、`…+geometry` —— 地板字号 +
  扩边几何上放得下、却被墨迹闸/几何预校验拦下的情况此前一律报成 shrink-floor,
  扩边失败的真因被遮住。仍是纯枚举串,无文本。

### Tests

- fitFailure.test 新增 5 例(veto 标签、单元格判据、基准字号不被单元格拖低、
  三档梯、扩展排序);五个机制逐一突变各至少一红。922 全绿。

## [2.7.2] — 2026-09-04

**结构批次**(六维审核路线图·批次 3,C-1 / C-2)。数值表表头向上扫掠、字距
拉开的 REFERENCES 标题、以及扫掠暴露出的三处网格根因。34 个布局快照中
**13 个有意更新**(逐一审阅,见下),其余 21 个逐字节不变;请求计划基线有意
重生成(语料请求 48 → 50,输入字符 122,140 → 119,284)。

### Fixed

- **数值表表头向上扫掠**(`tableGuard` 规则 (d))。列头是多行堆叠的短文本
  ("Detector Type"、"No. of Energy Thresholds"),无数字、在种子区域上方、与
  数据行不同高,三条旧规则都不沾,于是逃出网格成孤立单词块被无语境翻译
  (radiology2023-p3 一页 11 个)。现在紧贴区域顶边之上 ≤2.5em、x 中心落在区域
  横向范围内、≤6 词/≤60 字符、不以句末标点收尾、不是表标题的块并入区域,逐轮
  生长把堆叠的上一行也收进来。两道闸:**天花板** —— 种子区域上方横向相交的
  最近一条 "Table N" 标题的底边(无标题时种子顶边上方 6em),之上的块一律不收
  (wu2026-p6 曾顺着上一张表的脚注一路爬进上一张表);**行伴** —— 同基线上另有
  句子续行/长行/标题的短块是标题续行("size"),不是表头。
- **多行表头**(`buildTableModel`)。表头深度 = 首个含纯数字格的行之前的全部行
  (上限 3),此前只把 row 0 当表头,扫掠进来的第二行表头 "(n = 708)"、"P Value"
  会落成 preserve;数值列强制(`coerceNumericColumns`)同样跳过表头行。
- **列带不吞并邻带**(`inferBands`)。跨两列的成员("NAEOTOM Alpha IQon
  Spectral")并入左带时把带拉宽到盖住右列,右列每个数字格都被分到左带 —— 两列
  熔成一列(wu2026-p3 "0.50 0.50")。扩张后若会盖住别的带就保持原宽,成员照旧
  算已落座,由 `assignBand` 判 straddles。副作用是正向的:chen2023-p8 此前
  "Left main artery to LAD 697 (68)" 一格,现在标签/数字分列。
- **文本表跨列顶对齐行起点**(`buildTextTableModel`)。表头与首行只隔 0.55em
  时每列的表头都和首行并成一组,组顶给不出首行起点,整表表头与第一行熔成
  一格(radiology2023-p11 Table 4)。补一类起点:≥3 个成员顶边对齐且各自贴着
  列带左沿(悬挂缩进的折行不算)。
- **字距拉开的 REFERENCES**(`spanBlockBuilder`)。"R E F E R E N C E S" 由单字母
  span 拼成、字号同正文、分类为 paragraph,`REFERENCES_HEADINGS` 只对
  heading/title 生效永远认不出它(jacc2020-p15 的 62 条文献按正文翻译,5,522
  字符)。正则允许字母间空白(三处同步);单行段落块也参与判定。**分带页几何
  门**:该页正文双栏在上、文献三栏在下,标题落在第 0 栏底部,正文第 1 栏在
  merged 序里排在标题之后却在页面上位于其上方 —— "序后即文献"会把整栏正文冻成
  preserve。分带页先预扫标题矩形,标题所在带内只有顶边不高于标题顶边的块算
  文献,后面的带整体算文献;均匀页行为不变。

### 快照审阅(13 个有意更新)

- radiology2023-p3:Table 3 十一个孤立列头("Detector"/"Type"/"Collimation"/
  "Thresholds"…)进 row 0/1;p2:Term/Abbreviation/Definition 表头与首行分开;
  p11:Publications 独立成列、表头行独立、折行格接回。
- chen2023-p4/p5/p8/p10、nejm-defuse3-p7:表头行进网格;p8 标签/数字分列;
  p10 三层表头(Events / Univariate Analyses / HR · P Value)全部 text。
- wu2026-p1/p3/p5/p6:p3 三列不再熔成两列;p6 Table 3/4/5 各自成网格、
  脚注不再进表(旧快照本就把正文段落划进 Table 4 区域,该问题仍在,见 leftovers)。
- jacc2020-p15:三栏文献全部 preserve,上带正文(含 CONCLUSIONS)不受影响。

### Tests

- tableGuard:扫掠正例/三类反例/天花板/行伴 4 例;tableStructure:多行表头、
  列带不吞并、文本表顶对齐行起点 3 例;spanBlockBuilder:字距标题 + jacc2020-p15
  语料几何门 2 例。八个机制逐一突变(关掉扫掠/天花板/行伴/表头深度/吞并闸/
  顶对齐/单行标题/几何门)各至少一红。

## [2.7.1] — 2026-08-29

**请求合并**(六维审核路线图·批次 2,EF-1)。提取/排版零改动,34 个布局快照
逐字节不变;请求计划基线有意重生成(仅 chunks / maxBlocksPerChunk 两列变化,
其余字段 34 页全部相同)。

### Changed

- **正文流与单元格流两趟独立打包**(`planChunks`)。表格硬边界(2.2.0:一个
  请求绝不混装单元格与正文)不变,但此前是单趟顺序打包 —— 阅读序里格与正文
  每交替一次就断一批,断开的同类批次再不合并,chen2023-p5 一页 1570 字正文
  发了 9 次请求(平均 174 字正文配 1389 字系统提示,非正文开销 80%)。现在
  正文流、单元格流各自按预算/模块边界/块数上限打包,先正文后单元格输出;块
  id 驱动译文映射,批次顺序与正确性无关,流内阅读序不变。
- **单元格批块数上限 24→40**(`MAX_CELLS_PER_REQUEST`;显式 `maxBlocks` 仍对两
  流都生效)。字符预算仍是硬边界。
- **位置上下文只在同流相邻批之间传递**。单元格批不再收到与之无关的正文段尾
  (它的语境是表格标题 moduleContext);跨页续接只喂给正文首批。`PlannedChunk`
  新增 `kind: 'prose' | 'cells'`。

### 量化(34 页离线语料,与 2.7.0 同一脚本)

- 请求 78 → **48**(−38%);系统提示字符 108,310 → 69,386(−36%);previousContext
  字符 13,210 → 4,210(−68%);正文占输入字符 41% → **52%**。
- 表格页:chen2023-p5 9→2,wu2026-p6 9→2,chen2023-p10 5→2,nejm-defuse3-p7
  6→3,radiology2023-p3 / p11 4→2。

## [2.7.0] — 2026-08-29

**遥测批次**(六维审核路线图·批次 1)。不改任何翻译/排版行为,34 个快照逐字节
不变;目的是让"结构恢复"与"token 效率"两个维度从此可量化。

### Added

- **API token 用量计数**(审核 F-2)。三家服务商通道(OpenAI 兼容 / Anthropic /
  Gemini)的响应用量归一为 `inputTokens / outputTokens / cachedInputTokens`
  (`usageMeter.ts`,纯数字),按请求累加进诊断:`usage.{inputTokens,
  outputTokens, cachedInputTokens, usageReports, usageMissing}` 与
  `pages[].metrics.{inputTokens, outputTokens, cachedInputTokens}`。压缩重译与
  单块重译走同一计量口。免费引擎不报用量时计入 `usageMissing`。
- **放弃原因**(审核 D-1)。排版放弃的块现在带 `abandonReason`(枚举串
  `stage/overflow`:expand-ink / expand-geometry / no-room / shrink-floor /
  compress / audit × width / height / both),进 `placementProbe`;不含文本。
- **块级口径联表**(审核 B-1)。诊断 `pages[].blocks[].state` 此前按"有译文即
  translated"计,排版放弃的块(译了、从未显示)也报 translated;现在与 strict
  页的放弃清单联表 → `unplaced` + `abandonReason`(`diagnosticsJoin.ts`,纯函
  数;段落拆分块按区域前缀归并)。无论是否开启调试日志都生效。
- **排版量测耗时**(审核 A-2)。`placement[].layoutMs`:该页所有 ladderFits 量测
  (扩边、缩字、几何复核重测)的墙钟之和,不含 API 等待与渲染。
- `scripts/baseline-report.mjs` 新增列:unplaced / inTokens / outTokens /
  cachedPct / tokPerPage / layoutMs,并打印每篇的放弃原因分布。

### Notes

- 审核报告里"Anthropic 加 cache_control"一项**不做**:Anthropic 提示缓存的最小
  可缓存前缀为 1024 token(Haiku 2048),OpenAI 自动前缀缓存同样 ≥1024 token,
  而本插件系统提示约 350–400 token —— 两家都拿不到缓存折扣。token 杠杆只剩
  批次 2 的合并请求。
- 隐私不变量不变:新增字段全为计数/枚举,`diagnosticsPrivacy.test.ts` 扩展覆盖。

## [2.6.4] — 2026-08-29

**审核 2.6.x**。两处防御性修补,34 个快照逐字节不变。

### Fixed

- **弱沟**(审核 2.6.3 的全局沟纵向限定):分带通道要 ≥5 行连续证据,一块
  不足 5 行的短双栏区(图下两栏说明、双栏作者单位)若栏沟又窄于
  columnGapThreshold(1.6em),限定作用域后会左右焊成一行 —— 2.6.3 前不限 y
  的全局沟是切开它的。折中:通道外的行在间隙 ≥1.05em 时仍按全局沟切,词间隙
  (p5 "Figure 3:" 后 0.9em)保持一行、真栏沟(≥1.1em)照切。
- **近邻并入的宽度门**(审核 2.6.3):并入近邻 regime 的块必须至少占一栏宽的
  40% —— 未被探成表格的表尾窄格贴着正文 regime 时不能借近邻并入混进栏序,
  仍作 -1 分隔。

## [2.6.3] — 2026-08-29

**单沟抢救与全局沟纵向作用域**(fletcher2024 Radiology 胸部 PCCT 综述截图 +
Diagnosis and Corpus 3.rtf 实证)。新增 fletcher2024-p5/p7 夹具(34 个);6 个
旧快照经逐一 diff 审阅后有意更新(见下),其余逐字节不变。

### Fixed

- **满幅内容压顶的双栏正文整节逐行拉链**(p7 实证:底部双栏 29 行各自成段、
  左右交错)。分带检测只找到一条合格正文沟时,`bandedColumnStamp` 的早门
  (`gutters.length < 2`)在两个抢救触发之前就判了 null —— 而 p7 正是【单条
  正文沟 + 全局投票全盲 + detectColumns 焊成单带】。早门放宽为"零沟才退",
  单沟页走触发判定;均匀页在触发处照旧 null,逐字节不变。
- **图注被全局栏沟拦腰切开、注文并进邻栏正文**(p5 实证:"Figure 3:" 与注文
  被切离,注文与右栏正文焊成 2700 字巨块,译文注文叠印英文原文)。全局沟
  从前不限 y、整页每行都按它切;现在有 ±20pt 对得上的分带沟就用分带沟的
  【真实纵向通道】限定作用域(同一 x 被打断成多段时逐段生效),分带没看见
  的沟保持不限(兜底不变)。图注行横贯沟位但通道不覆盖 → 不再被切。
- **regime 顶界骑界块判 -1 插乱阅读序**(p7 实证:双栏区左栏先于右栏起笔,
  头两行落在沟通道之外)。距 regime 边界 ≤24pt(约两行)且不跨该 regime
  任何栏沟的块并入之;更远的(表格区、页面家具)仍是 -1 分隔,radiology2023
  Table 4 的 -1 语义不变。

### Changed(旧快照有意更新,逐一审阅)

- wu2026-p1:摘要块序修正 —— "Purpose To compare…" 现在先于 Methods 残句
  "assessed using ICC…"(此前顺序颠倒);RESEARCH/Abstract 栏章 0→-1。
- horst2024-p1:作者行("Marilyn J. Siegel, MD • …")整行被元数据过滤识别,
  不再送翻译 —— 人名保持原文,与 2.5.13 人名保护方向一致。
- chen2023-p8:游离的 "(" 单字符 preserve 格消失(并回所属行)。
- aquino2023-p1(×2):两条全宽前置件行 0→-1,阅读序不变。
- wu2026-p6:表侧统计句的碎行部分并组、部分归入邻表格列,净可读性持平偏好。

## [2.6.2] — 2026-08-29

### Fixed

- **2.6.1 的菜单折行反而行行叠印**(用户截图实证:三项的折行文本互相压盖、
  菜单底部截字)。根因:菜单行是 HTML `<button>`,在 Zotero 阅读器文档里
  button 的 UA 内部盒不随折行内容长高,标签折出的第二行溢出到下一行行头。
  行改为 `div[role=menuitemradio]`(键盘可达性以 tabindex + Enter/Space 补
  回),折行即长高;菜单宽度上限再夹到宿主宽度内 (`min(340px, 100%-12px)`),
  窄窗格里菜单永远整体可见。

## [2.6.1] — 2026-08-29

### Fixed

- **「更多」菜单第二项被省略号截断**(用户截图实证:「诊断:复制脱敏指标 +
  引擎自检(不含原文/密…」—— 恰好把隐私说明截掉)。菜单标签由
  nowrap+ellipsis 改为折行 (`overflow-wrap: anywhere`),任何标签在任何窗格
  宽度下都能显示完整;菜单 max-width 280→340px,常规宽度下最长项一行放下。

## [2.6.0] — 2026-08-29

**版面结构三连**(radiology2023 PCCT 综述 14 页截图 + 诊断/语料实证):散文
regime 抢救、文本表网格化、提取级叠印裁剪。新增 radiology2023-p2/p3/p11 三个
布局夹具(32 个);标题折行合并有意更新了 5 个旧封面页快照(多行大标题合并
为完整单块,详见下)。其余快照逐字节不变。

### Fixed

- **满幅表格页的双栏正文左右行交错**(p3/p11 实证:正文段落被逐行拉成拉链,
  译文整节不可读)。根因:满幅表格把全局 detectGutters 的投票盖死(p11 全局
  沟 = [])或把 detectColumns 的 x 投影焊成假单带(p3 带数 = 1),而分带检测
  明明找到了正文栏沟,却因"只有一个散文 regime"被 `bandedColumnStamp` 拒绝。
  两个新触发:① **globalMissed** —— 全局沟【数量】撑不起 regime 栏数且【位
  置】对不上(±20pt;数量条件防 chen2023-p10 同沟偏 9pt、hakime2007-p2 分带
  假沟两类误伤);② **plainCollapsed** —— 分带栏数超过 detectColumns 带数,
  且 regime 的沟全部被全局确认(±20pt;确认门防 wu2026-p6 表格碎沟假证)。
  行级切分闸门同步喂入带数,p3 "Virtual monoenergetic│Images based on…" 相邻
  列焊行随之解开。31 个既有快照逐字节不变,五个触发组件均经突变验证。
- **纯文本定义/综述表整张碎成散文**(p2 Table 1、p3 Table 2、p11 Table 4 实
  证:格子是整句散文、无一个数字种子,种子聚类永远探不到,逐行跨列交错翻
  译)。新增**标题锚定文本表**:从 "Table N:" 标题往下扫掠收块,以网格几何
  验收(≥2 左缘列簇、存在 ≤35% 区宽的标签列、≥3 个跨列基线对齐行顶 —— 悬
  空标题下的双栏正文全部三条不过,一个块也不收);`buildTextTableModel` 按
  文本表组格:列带只由窄成员推断(全宽漏网行不再焊死列带)、行归属按成员
  逐个判(标签列发现的行界把邻列长文格切对)、连字符折行格内接回;
  "Note.—" 表尾脚注按标记词判墙,保持散文翻译。三张表全部成格
  (mode=translate 逐格在位替换)。同修 `detectTableLineIndices` 与
  `structureTableCells` 的提前返回 —— 只看数值种子区域、白白丢掉文本表区域。
- **图注译文叠印在邻栏正文上,几何审计报 0**(p4/p9 实证)。L 形图注(首行
  横贯图宽、尾行只占一栏)的联合外接盒斜压进邻栏正文的盒里:行矩形互不相
  交,译文 div 却按外接盒铺排;审计只报"新增"侵入,对提取级自带的重叠天生
  失明。新增 `planOverlapClips`(纯函数):行级几乎无重叠而盒级重叠显著的
  块对,把自家行矩形基本不进重叠区的一方沿损失最小的边裁掉重叠区(裁剩盒
  ≥40×12px 才裁),遮罩按行矩形绘制不受影响。
- **多行大标题逐行分开翻译**(radiology2023-p1、booz2019-p1 实证:窄行被判
  第 0 栏、全宽行判 -1,阅读序隔开后各自无语境,中文连不成句)。title 块按
  【几何链】合并:同字号 (±10%)、竖向紧邻 (≤1.3em) 的链并为一块,文本按视
  觉行序拼接。5 个旧封面页快照有意更新(aquino2023×2、booz2019、horst2024、
  wu2026 —— 均为标题半句合并为完整标题)。
- **厂商/产品名被意译**("MARS Bioimaging"→"火星生物成像")。2.5.13 的人名
  保护规则扩为专有名词规则:公司/厂商、机构、产品/型号名一律原样保留。

### Notes

- 文本表的行粒度:长文列(整句描述列)的格子上下顶着,行归属以标签列发现
  的行界切分,个别行仍可能按视觉行独立成格 —— 列归属与阅读序正确,后续再
  精化行锚定。
- globalMissed 的 ±20pt 容差在现有夹具上不可观测(9–52pt 之间无实证页),
  按 chen2023-p10 实测同沟偏移 9pt × 2 余量取值,上界受 hakime2007-p2 邻沟
  距 53pt 约束 —— 突变 M5(20→8)为文档化等价突变。

## [2.5.13] — 2026-08-29

**结构与专名**。审核第三批:标题焊段、表格标签语境、人名保护。

### Fixed

- **字号跳变判据不对称,小节标题向下焊进正文**(wu2026-p4 "Polyp detection
  rate and reader confidence" 实证:标题带首段焊成一块,标题结构在译文页消失,
  又因原文首句与标题同词开头,读起来像"标题重复")。原式除以【当前行】字号:
  10pt 正文接 12pt 标题 = 0.20 能断,12pt 标题接 10pt 正文 = 0.167 断不了 ——
  标题恰好接近满栏宽时 wrapped 守卫又拦掉兜底的间距断段,焊死。除数改为两行
  字号的较小者(对称),spanBlockBuilder 与 blockBuilder 同修。28 个既有快照
  零改动 —— 只救此前漏网的形态。
- **表格标签格无语境逐格翻译**("Pitch"→沥青、"DoseRight"→正确剂量 的根因)。
  caption 块 (type 'table') 被 riskOf 隔进 slow 单块道,单元格批次里连表格标题
  的影子都没有。现在 caption 文本作为 moduleContext 随同表格的每个单元格批次
  下发(caption 自身除外),"Table 1 The CT image acquisition and reconstruction
  protocols" 正是 Pitch→螺距 需要的领域提示。
- **署名行与联系人信息不再被猜成汉字**("Weifeng Han"→韩伟峰、"Yuxi Ge" 同页
  两处两套汉字)。三层: ① `looksLikeAuthorList` 分隔符增加间隔号 '·' ——
  Springer 系署名行 ("Xiaofei Wu1 · Huiqing Gao2 · …") 此前整行漏网; ② 联系
  人表格里含 email 的格、以及同表的纯 TitleCase 人名格按数据格 preserve 原样
  保留(判据要求表内存在 email 格,数据表不受影响); ③ 系统提示新增规则:
  人名(作者、被致谢者、被引研究者)一律原样保留,禁止音译或转写 —— 兜住
  正文句子里的人名 ("Dr. Joo Myung Lee"→李柱明 一类)。
- 新增 wu2026-p1(署名行 + 联系人表)与 wu2026-p4(对称跳变标题分离)布局
  夹具。874 测试全绿,五处变异验证全部命中,既有快照零改动。

## [2.5.12] — 2026-08-29

**验证链不再冤杀译文**。修 wu2026 摘要整段留英与第 4 页夹生译文漏检 —— 三处
互相放大的验证链缺陷。

### Fixed

- **Likert 中位数[IQR] 被当引用掩码**(第 1 页摘要 1925 字符、第 2 页方法段
  2228 字符双双 unrecovered 的根因)。"5 [4, 5] vs. 3 [3, 4]" 是中位数与四分
  位距,却精确命中数字引用正则 —— 一段摘要被掩成十几个占位符,模型必然错排,
  清单校验一遍遍否掉本可翻译的输出,批次→重试→打捞→纯文本兜底全数烧完。
  判别: 引用括号跟在【词】后,从不紧跟裸数字 —— 前邻非空白字符是数字的
  方/圆括号组是数据,不掩。真实摘要占位符 18 → 11,六组区间全部原样透传。
- **占位符降级重试**: 纯文本兜底里译文本身合格 (looksTranslated 通过) 而
  只死在占位符清单上时,最后再【不掩码】重发一次,只验 looksTranslated ——
  引用与统计式交给模型原样保留。这一块失去公式逐字节保证,但另一边的代价
  是整段不翻;只在掩码链彻底失败后才走到。新增原因码 plain-placeholder。
- **英文残留检测的中性 token**(第 4 页 "将圆形感兴趣区域(ROI)放置在息肉,
  with a standard ROI area of 3 mm² for polyps…" 夹生译文年久失修的根因)。
  小写连跑规则被 'a'(单字母不满足 {2,})、'ROI'(全大写)、'3'(纯数字)各
  重置一次,6 词连跑永远凑不满。数字/符号、单字母、全大写缩写改为中性 ——
  既不计数也不重置;含 CJK 的 token 仍然重置,整段中文里零星散布的英文虚词
  不会被误连成跑。

## [2.5.11] — 2026-08-29

**伪表格不再吞正文**。修统计密集的正文段落被表格保护误判成表格的问题 ——
Abdominal Radiology(Wu 2026)第 5–7 页实证:Results 整段被逐行切成"单元格",
数字行按数据格 preserve 永久留英文,其余行逐格无语境翻译,连字符断词处
("quanti-"/"tatively")被拦腰切开成 "次stantial" 式半词。

### Fixed

- **窄双栏统计段落被判成表格**。`looksTabular` 的符号密度规则(≤80 字符、含
  数字、密度 ≥30%)对窄栏期刊失效:行宽只有 50–60 字符时,"overall image
  quality (ICC=0.934; 95% CI: 0.898–0.957)" 这类 Results 行行行过检,≥4 行
  自成聚类,再经 columnMerge(左右栏行共基线、栏沟 < em×8)把两个正文栏焊成
  一张横跨页面的伪表。新增**种子判定** `looksTabularSeed`:一行里有 ≥3 个带
  小写字母的 ≥3 字母普通词,它是句子,不能当聚类种子。只收窄种子 ——
  `structureTableCells` 的格子数据/文本分类仍用 `looksTabular` 本体,真表格里
  「标签+数值」复合行的 preserve 语义逐字节不变(chen2023-p10 快照零改动)。
- **相邻正文栏的续行被"表侧行标签"规则收编**。两端对齐的正文行行尾贴到栏沟边
  (gap ≈ 1–2em)、又与真表格的行同高,区域扫掠把它整行收走随表冻结。判别:
  ≥3 普通词且【小写开头】(接续上一行的句子)或【连字符结尾】(折行进下一行)
  的是句子续行,不是标签;真标签一律大写开头("Left ventricular ejection
  fraction"),1.2.0 的 NEJM 行标签用例全部保持全绿。
- **分带栏归属的跨栏悬垂误判**(2.5.10 审核项):`bandedColumnStamp` 判"横跨
  栏沟即整宽分隔符"的容差是 ±2pt,而 `groupIntoLines` 允许连字符行悬垂到栏沟
  中心后 min(6, 0.6em) —— 分带页上一个左栏段落会被误盖 -1、在阅读序里当横向
  分隔符把两栏拦腰切断。容差放宽到 8pt(> 最大悬垂),真跨栏标题不受影响。
- 新增 wu2026 第 3/5/6 页布局夹具:第 5 页钉「统计段落 + 真 Table 2 并存」,
  第 3 页钉「真 Table 1(采集参数表)完整存活」,第 6 页钉「四张真表占版」。
  全部 24 个既有快照零改动;866 测试全绿,三处变异验证全部命中。

## [2.5.10] — 2026-08-28

**分带栏检测**。修一页之内栏数会变的排版 —— 期刊尾页最典型:**上半页双栏正文、
下半页三栏参考文献**。这类页的双栏正文译文会「消失」。

### Fixed

- **上双栏正文 + 下多栏参考文献的页面,双栏正文被焊成乱码整块**(JACC 2020;76(10)
  第 15 页实证)。栏沟检测 `detectGutters` 原是**全页一次性投票**:正文的栏沟在
  页面中部,但参考文献的**中栏正好盖住这条栏沟**,于是「此处是空隙」这一票被下半页
  的参考文献行全部投否,达不到 60% quorum —— 全页只认出参考文献的三栏沟。结果正文
  每一行的左栏行尾与右栏行首之间没有栏沟,被拼成一条横跨两栏的乱序长行,整段双栏
  正文要么校验不过、要么翻成乱码,在译文页上「消失」。
- 新增**分带栏沟** `detectGuttersBanded`:每条栏沟带自己的纵向区间 —— 它只在**连续
  若干宽行都留白**的那段高度上成立(容忍标题/连字符悬垂造成的个别覆盖行,并要求
  留白行占多数)。切行时,一行只在**它所在高度真实存在**的栏沟处断开。正文段落带得到
  中部那条栏沟、参考文献带得到三栏沟,互不干扰。
- 新增**分带栏归属** `bandedColumnStamp`:把页面按栏结构分成纵向 regime(上:双栏
  正文;下:三栏参考文献),给每块盖 `regime 序号 × 100 + 栏内序号` 的复合栏号,于是
  阅读序**先出上带的所有栏、再出下带的所有栏**,每栏各自从上到下 —— 双栏正文回到
  「先读完左栏、再读完右栏」的正确次序。
- **只在栏数确实沿页变化时**才切到分带路径:单一 regime(普通单/双/三栏页)、以及
  区分不出栏结构变化的页面一律回退到原 `detectColumns`/`columnOf`,逐字节不变;
  数据表(单带 ≥4 栏)由既有表格结构化通道处理,不被误判成正文栏 regime。全部 23 个
  既有布局快照零改动;新增 JACC 第 15 页作为该混排结构的回归载体。

## [2.5.9] — 2026-08-28

**吞吐与成本**。2.5.8 修的是「卡住」,这一版修的是「白等」和「越读越贵」。

### Changed

- **段落写盘不再占着调度槽**。页完成路径上的 `writeSegments` 是 `await` 的,
  而它坐在调度槽**里面** —— 落盘有 500ms 的合并节流窗口,于是每页翻完都要
  白占一个槽至少半秒,再加整库序列化与落盘;全局并发只有个位数时这笔账很可观。
  压缩任务那一笔更糟,它跑在**前台 lane** 上。
- 但不能简单丢掉 await:2.0.7(审核 P2-3)记着,fire-and-forget 会让写盘悬在
  队列里与「刷新全部」的清盘竞态,被丢弃的译文复活。所以改成**登记而不等待**
  —— 任务立刻交还槽位,`resetAllAndWait` 新增一道 `flushPendingWrites` 把这些
  写收口,那条防线原样保留。
- **学得术语的注入设了上限(40 条)**。`docMemory` 边翻边学、上限 200 条,而
  缩写天然反复出现 —— 读到文末命中比例相当高,每条命中都变成提示词里的一行,
  最坏一次请求多出两百行、数千输入 token,越往后越贵。超限时按**本页出现次数**
  保留(出现得多的,前后一致的收益也大);次数相同先留更长的 source。
  用户词汇表**一条不裁** —— 判据是「是不是用户那份里的」,不是 `mode`,因为
  用户自己也可以把某条设成 suggested。40 是天花板而非常态:一页真正出现的
  不同缩写通常在二三十条以内,多数页面碰不到它。

### Fixed

- **平台给不出文件指纹时会留下一条警告**。2.5.3 的解析缓存全靠 `IOUtils.stat`
  的 `size` + `lastModified`;任一缺失,连**刚解析好**的库都会被丢掉,于是每页
  整库重解析 —— 2.5.3 修掉的 O(N²) 原样复活,而此前**一行日志都没有**,只会
  表现为「莫名其妙又变慢了」。每进程只警告一次,不刷屏。
- 这条实现里踩了一个坑,记下来:第一版连 `stat` **抛错**也一起告警,结果
  库文件还不存在时(首次写入前探测合并基底)每次都抛 ENOENT,警告在正常流程里
  就被点着了 —— 而且它先于测试取快照,导致用例假绿。现在只对「stat 回了对象却
  没有那两个字段」告警,那才是真正的平台能力缺失。

### Tests

- 853 → 859。新增:写盘不再阻塞页任务但 `resetAllAndWait` 仍等得到、写盘失败
  不影响翻译结果;术语上限按出现次数保留、用户词汇表不被裁、未超限时原样返回;
  指纹缺失只留一条警告。三条改动逐一变异验证 —— 其中「把 await 还回去」那次
  直接让用例**挂死**,正是它要防的症状。

## [2.5.8] — 2026-08-28

**「后面几页突然卡住」**。三条,都在调度与并发闸上。

### Fixed

- **容量为 1 的前台 lane 上,后台任务一个都启动不了 —— 预取从来没跑起来过**。
  保留槽的判据是 `laneActive >= cap - 1`,`cap === 1` 时就是 `laneActive >= 0`,
  **恒真**,哪怕调度器完全空闲。而 `cap === 1` 不是边角情况,是**默认配置**:
  默认引擎 `bing-free` 属 free 类,band 固定 `{ min: 1, initial: 1, max: 1 }`
  (unknown 类同);LLM lane 也会被两次 429 从 3 折半衰减到 `min = 1`。单服务商
  时所有页共用一条 lane,那条 lane 恒等于前台 lane —— 于是队列里堆的全是永远
  起不来的任务,每页只在你翻到时才开始翻。加上 `cap > 1` 这个前提:容量为 1
  的 lane 本就没有「多留一个」的余地,保留槽在那里没有意义。
- **闸上的等待现在认 abort 了**。`RequestGate.acquire` 既不收 `AbortSignal`
  也没有超时,唯一的唤醒来源是别的持有者释放名额。模块开头写着「abort 交由
  fn 自身处理」—— 但**等待本身**不可中断,而这一点的下游后果此前没有被追下去:
  `run()` 是在 `try` **之前** await 取名额的,页任务停在这里 → `translatePage`
  挂着 → 调度器 `execute` 的 `finally` 不可达 → 该 job 一直留在 `active`,
  既占着全局与 lane 的槽位,又让 `isScheduled` 恒为真。现在 abort 会立刻把
  等待者摘出队列并 reject;入闸前就已取消的请求干脆不占名额。
- **正在解绕的僵尸任务不再挡回重新排期**。`cancel()` 对运行中的任务只发信号,
  它要等自己的 `run()` 结算才离开 `active`(这是刻意的:`active` 同时承担槽位
  与并发记账,提前删会超额放行)。可这期间页状态仍停在 `translating`、
  `isScheduled` 仍为真,而 `ensurePage` 的每一条早退都会把它当成「正在好好跑」,
  `promote` 又只对**排队中**的任务生效 —— 导航取消过的那一页于是既不结束、
  也无法重新排期,用户翻回去只看到原文。新增 `isAbortedActive`,在所有早退判据
  **之前**把僵尸识别出来,等它真正解绕完再照常往下走。

### Tests

- 844 → 853。新增:cap 1 的前台 lane 允许后台启动 / cap ≥ 2 时保留槽照旧生效;
  闸的 abort 四例(等待中 abort 立即出队、入闸前已取消不占名额、拿到名额后
  abort 由 fn 收尾、不传 signal 行为不变);`isAbortedActive` 区分僵尸与真在跑、
  `cancelAndWait` 之后同名任务可立刻重排;以及一条端到端用例 —— 页任务在飞时
  被取消、用户翻回来必须真的重新翻译。三条改动逐一还原成旧判据,对应断言都
  立刻转红。

### 审核中查证但**未**采纳的两条

- 子代理把「段落库整库重写」列为首要停顿源,估计几百毫秒到数秒。**实测不是**:
  到 2 万条上限时库约 3 MB、`JSON.stringify` 约 39 ms。真正的代价是被 `await`
  的 500 ms 节流窗口白占一个调度槽 —— 那是吞吐问题,留给 2.5.9。
- 闸的前台插队没有 aging,理论上后台可被无限饿死。**没能复现**:一批前台跑完
  后台仍轮到了。记录为风险,不当作实锤,不据此改动。

## [2.5.7] — 2026-08-28

**正文字号的病灶**。2.4.7 曾为这同一页收紧 heading 的长度上限来压住症状,这一版
动的是病灶本身。

### Fixed

- **前置信息不再冒充正文,决定全页的字号基准**。`classify` 里
  `ratio = fontSize / bodySize` 的分母原先是按**行数**取众数,而这个分母问的是
  「正文有多大」—— 封面页上两者会分道扬镳:jacc-ccta2020-p1 的 6pt 单位与利益
  声明占 **57.8% 的字符 / 27 行**,7.5pt 的摘要只有 33.1% / 17 行,于是正文被判
  成 6pt,页面上**任何**比 6pt 大的东西 ratio 都 ≥ 1.1。后果是两条:摘要末行
  「American College of Cardiology Foundation.」被撕下来当**标题**(摘要因此断在
  「…on behalf of the」),9pt 的栏目条与 12.5pt 的副题也一并够上了 title 门槛,
  一页冒出三个 title。
- 新的 `pageBodySize` 两条修正:① 按**字符数**计权 —— 一行长正文不该和一行两字
  的标签同票;② 众数之上若还有一档**份量可观**的更大字号(≥25% 字符且大出
  15% 以上),取它 —— 小字是脚注,正文不会比脚注还小。份量门槛挡住的正是标题:
  标题只占百分之几的字符,永远够不上 25%。
- 还有一道闸是被既有单测逼出来的:**只有一行的字号档当不了正文**。否则一个
  25 字的 20pt 标题按字符数就能压过三行 7 字的正文,「a large short line is
  classified as a title」当场转红。正文按定义是多行的。
- **停在虚词或连字符上的碎片会被接回去**。`planMerges` 原先只认逗号类收尾,
  看不见「…on behalf of the」这种停在虚词上的断口,摘要于是在译文页上断成两段。
  现在复用 2.5.1 的行尾判据。前一块若已收了句号,`endsSentence` 早就把合并挡掉
  了,所以这条只作用在真正被拆开的段落上。`horst2024-p5` 顺带接回三处正文
  过切(「The higher 120- and」+「140-kV mode…」、「…performed at 70, 80, or」+
  「100 kV, varying…」、「…120 kV over」+「70 or 90 kV because…」)。
- **图注可以在断词连字符处接回下一段**。跨类型合并本来被 `bodyOnly` 挡着,但
  断词连字符是唯一无歧义的续接信号 —— `horst2024-p5` 的图 6 说明就断在
  `photon-` / `counting` 之间。标题与列表项都不会以连字符收尾,放行不带来别的
  风险;没有连字符时照旧不跨类型合并。

### Tests

- 838 → 844。新增正文字号三例(前置信息压不过摘要 / 单行档当不了正文 / 普通版面
  不受影响)、`planMerges` 虚词与连字符收尾、图注连字符跨类型合并及其反例,
  外加一条**端到端接线闸**:把正文字号换回按行数取众数,9pt 栏目条立刻从
  heading 变回 title —— 已验证转红。
- 23 页快照:`jacc-ccta2020-p1` 8 → 7 块(摘要接回一整段、栏目条归位 heading)、
  `horst2024-p5` 8 → 6 块、`chen2023-p8` 两个表头单元格 heading → paragraph,
  其余 20 页逐字节不变。

### 已知余留

- 该页仍有两个 `title`:真标题(18.9pt)与副题「JACC State-of-the-Art Review」
  (12.5pt)。副题在版面上确实是按标题排的,暂不强行区分。

## [2.5.6] — 2026-08-28

**封面页的前置信息**。审阅 JACC 封面页语料时列的第 1、3 条缺陷,外加查证过程中
撞见的一处**从未被发现的正文丢失**。

### Fixed

- **作者单位的长度上限改成密度判据 —— 顺带找回一整段被丢掉的正文**。
  `looksLikeAffiliation` 原先第一行就是「超过 600 字符即放弃」,而 20 位作者的
  单位块有 **1647 字符**,恰好在它最像作者单位的时候被这条上限否掉,连同利益
  声明共约 2700 字符前置信息被原样翻译。长度分不开两者,密度可以:该页单位块
  **26 个机构词 / 49 个逗号**,同页摘要 **1 个机构词 / 10 个逗号**。现在要求
  机构词数随篇幅一起长(≥ 文本长度/200)。
- 更要紧的是这条上限的**另一头**:它会把**刚好没超过 600 字**、又碰巧提到两所
  机构的正文段当成作者单位丢掉。`aquino2023-p2` 的方法学开篇 ——「The protocol
  of this prospective, single-center… approved by the local institutional review
  board at this academic medical center…」589 字符、两处 “center”、3 个逗号 ——
  **整段一直在被丢弃**,而快照只记每块前 40 个字符,块干脆不存在、无从对比,
  所以从没被发现。这一版把它找回来了。
- **页边窄栏不再被拼进正文、也不再把正文切碎**。JACC 页脚左侧那条音频摘要小栏
  (x 24–99)与利益声明(x 110–)共用基线,原先的结果是两种损坏叠加:
  - 小栏与正文之间只有 **11pt**,而 `columnGapThreshold` 对 6pt 字算出 1.6em =
    9.6pt 后被 `Math.max(…, 12)` 的下限抬到 12 —— 差 1pt,那一行两栏文字焊成
    一句「Listen to this manuscript's **Michael A. Wiener Cardiovascular
    Institute**…」。下限 12 → 8,只影响 7.5pt 以下的小字,而 1.6em 仍远超两端
    对齐能拉出的词距(约 0.6em)。
  - 其余几行虽然切开了,却因为 `columnOf` 把版心之外的内容一律兜底成 0 栏,
    与正文按 y 交错排列,把利益声明切成六块并把小栏碎片夹在中间(「…SINO
    Medical Technology, / **Dr. Valentin Fuster on** / ReCore, Terumo
    Corporation…」)。现在版心左侧的内容自成一栏。
  该页 **14 块 → 8 块**:小栏合成一整句,前置信息整体退场。

### 判据为什么只认左边

- 「不落在任何栏带里」不能用作判据 —— `chen2023-p5` 的 198 行里有 **109 行**
  落在栏带之间(那页栏带只认出 63–158 与 410–485,单元格大多在缝隙里),
  给它们独立栏号会把整张表从行序读成**列序**。
- 「版心之外」也要再收一道:23 页语料里落在版心**左侧**的一共 8 行 —— 3 个页码
  加上 JACC 小栏的 5 行,**没有一行是正文或表格**;而右侧有 42 行,其中 37 行是
  `chen2023-p5` 那些栏带没认全的表格单元格。右页边栏在期刊里确实存在,等语料
  里真出现一例再放开 —— 不为想象中的版式冒打乱真实表格的风险。

### Tests

- 835 → 838。新增长单位块必丢 / 提到两所机构的正文段必留 / 短单位块照旧丢弃、
  6pt 页面 11pt 间距必须切栏、两端对齐词距不得误切、版心左侧自成一栏而右侧不动。
  三处改动都做了变异验证:逐一还原成旧判据,对应断言立刻转红。
- 23 页快照:`aquino2023-p2`(及其重名副本)各 +1 块(找回的正文),
  `jacc-ccta2020-p1` 14 → 8 块,其余 20 页逐字节不变。

## [2.5.5] — 2026-08-27

**表格页的三处塌陷,根子上是同一条**。审阅整篇语料时列的第 4、5、6 条缺陷,
查下来 5 和 6 出自同一个判据。

### Fixed

- **「下一段」不再允许出现在页面上方 —— 负间距也要拦**。`planMerges` 的间距
  判据原先只拦「离得太远」(`gapAfter <= size * 1.2`),却放过了**负间距**。
  而阅读序是把满幅块排在分栏块**之前**的,于是「下一段」常常在页面上方几百点
  处:第 4 页表 1 底下那条以逗号收尾的「Note.—…BMI = body mass index,」,
  它后面跟的是**页顶的页眉**,`gapAfter = −349` —— `-349 <= 10.8` 照样成立,
  两者被焊成一个纵跨整页(y 404→753)的块。后果是连锁的:
  - 页眉被拼进正文中间(「…BMI = body mass index, **Radiomics Model to
    Identify Vulnerable Plaque and Predict Cardiovascular Events** Table 1:…」);
  - 那个纵跨整页的块把**表格区域从页顶一路撑到页底**,`structureTableCells`
    因此认不出网格,表 1 的 77 个单元格连同表题、表头一起塌成**一块**
    `preserve`(即整张表都不翻译),而且排在它本该垫底的 Note 之前。
  现在间距双向卡死,只容忍上下标/悬挂标点造成的轻微重叠(−0.5em)。第 4 页
  **8 块 / 2 个单元格 → 90 块 / 77 个单元格**,表格恢复成格、阅读序恢复正常;
  第 10 页的表 5 也从「Note 打头」回到「表题打头」。
- **与正文同号的加粗小节标题被认出来了**。文本层只给 text/rect/fontSize ——
  **看不见 bold**,于是「Model Prognosis Assessment (Study 2)」「Study Sample
  Characteristics」「Materials and Methods」「Animal Model」这类与正文同号的
  加粗标题全部落成 `paragraph`,随后被区域合并并进邻段,整节结构在译文页上
  消失(第 6 页最终只剩 3 块,`hakime2007-p2` 干脆出现过「Materials and Methods
  Animal Model」被焊进正文的情形)。字号帮不上忙,改从排印形态判:**自成一行、
  ≤60 字、不以句读收尾、不含逗号、每个实词都大写**。
- 三道排除项都是被语料逼出来的:**逗号**挡住作者行(「Yi Ning Wang, MD, PhD」
  同样"实词全大写",而 heading 是元数据过滤的豁免类型,误判会把作者名单放回
  正文);**实词全大写**(而非"多数")挡住「Clinical presentation」「Risk
  factor」这类表格行标签;**行尾虚词**(复用 2.5.1 的判据)挡住被版面截断的
  碎片「Dr. Valentin Fuster on」「Odds Ratio or」。

### Tests

- 830 → 832(另有 23 页快照与流水线基线重新生成并逐行审阅)。新增负间距不得
  合并 / 轻微重叠仍可合并两例,以及同号标题识别与三类误判排除。
- 语料实证:11 个 fixture 新认出小节标题(「Study Participants」「CCTA Analysis」
  「Advances in Knowledge」…),四张表的单元格数一个不差(77/177/71/56/27),
  首页逐字节不变 —— 没有把元数据放回来。

## [2.5.4] — 2026-08-27

**整篇语料揪出的三处文本损坏**。语料库第一次收进一份**完整**文档
(Radiology 2023;307(2):e221693,11 页,5 表 5 图),当场暴露出 13 页单页语料
从未覆盖到的三条路径。

### Fixed

- **图表说明不再被当作者单位丢弃**。第 3 页的图 1 说明整段消失(连同全部缩写
  定义)—— 这篇的队列名就是医院名:「Huaian set = Affiliated Huaian No. 1
  People's Hospital of Nanjing Medical University set; PUMCH set = Peking Union
  Medical College Hospital set」,`looksLikeAffiliation` 数到 ≥2 个机构词 +
  ≥3 个逗号就判成作者单位。过滤点原先只豁免 `title`/`heading`,现在把
  `caption`/`table` 一并豁免 —— 这两个类型只由「Figure N」「Table N」开头产生,
  是内容的自证,作者单位块永远不会这样起头。同篇 Figure 2/3/4/5 的说明本就活着
  (它们不含机构名),这条修的正是"含机构名就被误杀"。
- **10pt 正文接 12pt 小节标题终于能断段**。`fontJump` 的判据是 `> 0.2`,而
  `sizeOf` 把字号按半点分桶 —— 10 和 12 算出来**恰好是 0.2,不大于**,于是
  期刊排版里最常见的一档标题级差从来断不开段:第 3 页的 "Results" 被焊在
  「…(version 4.1.1;www.R-project.org).」尾巴上,整节结构在译文页上消失。
  改成 `>=`。更小的级差(10→11 = 0.1)仍不算跳变,正文里的字号抖动不会把段落
  切碎。
- **上下标不再自成一行、劈开段落并注入游离 token**。第 2 页输出过
  「MACE = major adverse**CT** cardiac events」—— 那个 CT 是上一行 FFR 的下标,
  掉进了下一行;第 3 页整个右栏段落被劈成两块、第二块以「CT on MACE was
  explored…」起头;第 8 页同样被劈开还多出一个假 heading。机制与首字下沉同源
  而方向相反:配对容差取的是**来客自身**的高度,下标高 4.9pt、离上下两行中心
  分别 3.7 与 5.8 —— ±2.9 的容差两边都够不着。现在**字号明显小于本行**的来客
  改按【纵向重叠】归行:上下标与它所属的那一行必然大面积重叠,与相邻行只擦
  一点边。
- 判据特意用**字号**而不是行盒高度:行盒会随成员增长,一旦被某个高 glyph 撑大,
  后面每个正常行都会被误判成"小来客"而走上宽松的重叠匹配 —— 中途试过按行盒
  高度,`aquino2023-p2` 的「Exclusion criteria were (a) refusal to consent…」
  当场被下一行的「…tion to iodine-based…」污染,`horst2024-p5` 的图注也错位。
  这条反例已固化成单测。

### Added

- **chen2023 全 11 页进回归语料**(23 个 fixture)。这是语料库里第一份完整
  文档,一次覆盖跨页续接、四张大表、五幅图注、小节标题四类此前无覆盖的路径。
  原先手抄的 `chen2023-p1` 换成机器导出 —— 两者的快照**逐块完全一致**,只差
  1 个字符的载荷,反过来验证了当初的转写精度。

### Tests

- 823 → 828。新增图注豁免(含机构名的图注留下 / 真作者单位照旧丢弃)、
  字号跳变边界、上下标归行(下标并入正确行 / 同字号碎片不受影响)五例;
  23 页快照与流水线基线重新生成并逐行审阅 —— 三处修复各自只动了它该动的页
  (p3 加回图注与 "Results"、p2 与 p8 各少一次错误劈分),其余 20 页逐字节不变。

## [2.5.3] — 2026-08-27

**越往后翻越慢,修好了**。

### Fixed

- **每翻一页不再整份重读段落库**。这是「长文越到后面越慢」的根因:`readSegments`
  为了解析出本页那二十来个 hash,每页都把**整份**共享段落库 `readJSON` 一遍;
  而 2.3.5 把段落库从「每篇一份」改成**全局共享**之后,这份文件跨论文只增不减
  —— 全仓没有任何清理,只有「清除全部缓存」能删。于是第 N 页读的是已含前 N−1
  页的库:**单篇内 O(N²)**,常数还由你读过的所有论文带进来。落盘侧虽有 500ms
  合并窗口,但每次仍是整库读—合并—重写,且在页面完成路径上被等待。
- 现在进程内保留**已解析**的库,用 `(size, lastModified)` 作指纹校验 —— 命中时
  一次 `stat` 顶掉一次整库解析;自己写盘后直接就地更新指纹,连 `stat` 都省了。
  指纹拿不到(平台 `stat` 缺字段、读失败)就老实重读,**绝不假定缓存有效**;
  文件被别的窗口/进程改过,指纹立刻不符,照常回到磁盘。「合并基底瞬时读失败
  就放弃本次写、绝不以空库截断」这条 2.0.7 的硬约束一字未动。
- **共享段落库有了条目上限**(2 万条)。超出时在落盘时按插入序淘汰最旧的,而
  读命中会把条目重新插到末尾 —— 于是插入序就是近似 LRU:常被命中的样板句
  (资助声明、伦理声明、方法套话)活得久,一次性段落先出局。复写一条老段落
  同样刷新它的位置,不会被当成"最旧"误杀。
- **页底图缓存改为按像素计容**。原来按**条数**取 4,上限只能照最坏的一张定
  (整页 ~3.2M 像素 ≈ 13MB × 4 ≈ 50MB),于是窄栏那种一张只有几十万像素的
  情形也只敢存 4 张 —— 往回翻超过 4 页必然落空,要整页重新 rasterize
  (`getPage` 轮询 5s + 渲染 12s 的预算),回看时先转一会儿圈才出译文。现在
  预算 16M 像素(≈64MB):最坏情形 5 张,常见的窄栏/中等缩放下 7–10 张,
  内存上界反而更可控。淘汰时直接把 canvas 宽高清零立即释放,不等 GC
  ——(页面内容早已 `drawImage` 复制到各自的画布上,清零动不到已经画好的页)。

### Changed

- `LruCache` 支持计权:传 `weigh` 后 `capacity` 是权重总量而非条数;不传时行为
  与旧版逐字一致。单条就超预算时保留它 —— 否则缓存永远为空,每次都得重算。

### Tests

- 810 → 823。新增段落库解析缓存(连读 20 页 0 次解析、外部改动即失效、
  `stat` 无指纹时退化为每次重读、清缓存后不得命中)、条目上限与近似 LRU
  (最旧先走、读命中保命、复写刷新位置)、计权 LRU 六例;既有的两条
  「合并基底读失败」不变量用例改为先让指纹失效,确保测的仍是真正读盘那条路。

## [2.5.2] — 2026-08-27

**往回翻译文消失,修好了**。

### Fixed

- **一次偶发的重建失败,不再把整页永久钉死在英文**。这是「往回翻译文消失」的
  根因,而且**译文从来没丢** —— `translationManager` 里五处删页状态全部有守卫
  (`status !== 'done'`),缓存层也没有任何 LRU 或容量上限。丢的是**重建**这一步:
  离开 3 页后槽位就被释放成占位,回来要整页重建;而重建的起点是 100% 原文
  (每个译文块先 `visibility:hidden`、遮罩故意不画,只有最后一趟字体就绪后才
  揭示)。任何一次没走到揭示,这页就停在英文。
- 致命的是**面板把这个结果当成了终态**:`pumpRenders` 只会重排「占位」或
  「已标脏」的槽,而标脏只在翻译完成通知时置位、已完成的页永不再通知。于是
  那一页整个会话都不会再重建,只能靠翻走 ≥3 页再回来、缩放、或手动刷新本页救。
  现在重建结果分成四种:`translated` / `original`(纯图扫描页,画原文是**正确**
  终态)/ **`degraded`**(有译文却没重建出来)/ `false`。`degraded` 先把原文画上
  (总比空白强),同时按 1.2s → 2.4s → 3.6s 递增退避自动重试,最多 3 次;确定性
  失败的页停在原文不再空转,排版失败提示每页只弹一次。
- **覆盖模式的字号阶梯从来没真正跑过**。`drawPage` 把整层建好后才
  `appendChild`,而字号测量循环排在挂载**之前** —— 量的是一棵游离子树,
  `getBoundingClientRect().height`、`scrollHeight`、`scrollWidth`、`clientWidth`
  全部返回 0,`fits()` 在第 0 级阶梯就恒真:每个框都拿最大字号、永不收缩、
  溢出标记永不置位,`overflow: hidden` 直接把译文裁掉。源码注释写着
  「Measure only after everything is in the document」,而它当时是假的。
  现在新层以 `visibility: hidden` 先入文档再测量 —— 隐藏元素照常参与布局,
  量得到真实尺寸,旧层仍占画面,原子替换语义(新层落地前旧层一直可见,
  缩放不闪回原文)一字未改;页节点若在测量期间被 PDF.js 换掉,隐藏层撤走
  并排重画,不留死层。

### Tests

- 801 → 810。槽状态机抽成纯函数 `nextSlotState` 并逐分支锁死(6 例);覆盖模式
  新增源码级顺序闸(挂载先于测量、隐藏—揭示—摘旧层的次序、换页时撤层),
  已用变异验证:把 `appendChild` 移回测量之后,两条断言立刻转红。

## [2.5.1] — 2026-08-27

**删掉跨页去重,修好栏底丢字与下沉首字焊错行**。

### Removed

- **跨页重复内容去重整块下线**(`src/reader/repeatRegistry.ts` 及其单测一并
  删除)。2.4.5 从 MinerU 借鉴来的这层登记表,在 13 页真实语料上实测:两本
  期刊、14 处页眉页脚类版面家具,**0 处走到登记表** —— 现有三层过滤(出版商
  样板行 + 元数据块 + 8% 边带形状)已经在它之前把它们全部拦下。一层永不触发
  的机制只会平添误删正文的风险,不留。

### Fixed

- **栏底正文不再被当页脚丢弃**。`isRunningHeadOrFoot` 原先只放过「以小写开头」
  的续行,可正文折行完全可能从大写词起头:Chen 2023 左栏栏底那句
  「These features have been reported to be associated with」单行、54 字符、
  首字母大写、y 落在 783pt 页的底部 8% 带内,于是整句消失(2.4.8 诊断里记录
  的内容丢失)。新增**行尾判据**:停在连字符或虚词(with / of / and / the /
  to …)上的行是**没说完的句子**,一律放行 —— 页眉页脚是自足的标题或引文短语,
  绝不会以虚词收尾。带域名或卷期号的期刊脚注仍照旧丢弃。
- **下沉首字不再焊到第二行**。段首那个两三行高的大写字母,盒心恰好落在
  **第二行**的基线带里,按盒心配对就产出了「Acauses of morbidity…」,而真正的
  第一行「cute coronary syndrome…」反被挤成孤行。下沉首字与第一行是**顶边**
  对齐的(实测 A 顶 167.90 / 第一行顶 167.40,差 0.5;第二行顶 155.30,
  差 12.6),故含下沉首字的行改按顶边配对。判据收在「行内最高的 item 是个
  1–2 字符、≥18pt、且比来客高 1.8 倍以上的大字母」上,标题行与上标不受影响。
- 三页语料的段首同时修正:`chen2023-p1`「Acute coronary syndrome…」、
  `hakime2007-p2`「Conventionally, therapeutic response…」、
  `horst2024-p1`「The operational efficiency of CT…」;`booz2019-p1` 的
  「disor-der」「al-though」两处跨行连字符也顺带接回。

### Tests

- 797 → 801。新增栏底虚词收尾(放行 4 例 / 仍丢弃 4 例)与下沉首字配对
  (顶边并入第一行、上标不受影响)两组单测;13 页布局快照与流水线基线按上述
  改动重新生成并逐行审阅。

## [2.5.0] — 2026-08-27

**修好「双栏被当单栏」**。2.4.9 定位的根因,这一版落地。

### Fixed

- **满幅前言压倒双栏正文时,栏归属不再失效**。`detectColumns` 的贪心链条按 x
  投影,而**满幅段落的末行天然短**(封面页的作者串、单位、摘要末行都是
  250–350pt),躲过 `FULL_WIDTH_RATIO = 0.62` 混进投影,横跨白槽把左右栏焊成
  一栏;反焊接的 10% 侵入配额被 10+ 行桥接行击穿。新增 `columnsByLeftEdge`
  兜底,**只在一栏都没分出来时才跑**,已分对的页面逐字节不变。
- 实测效果(三页语料,块数是「逐行碎片 → 成段」的直接度量):
  `chen2023-p1` **26 → 9 块**、`booz2019-p1` **46 → 8 块**、
  `Aquino…p1` 亦然。满幅前言现在正确标为 `column = -1`,左右栏各归 0/1。

### 兜底的判据

- **左边缘聚类**。关键洞察:满幅前言和左栏**共用左边缘**,靠宽度分不开;但
  右栏的左边缘是独立强信号 —— 一堆行整齐地从版心中部起排,只可能是第二栏。
  而**栏的右边界只由「不越过下一栏左缘」的成员决定**:满幅行会伸过去,于是
  自动被排除在右边界之外,再也焊不动两栏。
- **三道闸,全部由 14 页语料实测定标**:每簇 ≥6 成员(零星缩进行不能立栏);
  相邻簇中位左缘相距 ≥15% 版心宽(那是分栏,不是 1em 的首行缩进);
  **均衡性 —— 最窄栏 ≥ 最宽栏的 60%**。第三道闸是实测加上的:`horst2024-p11`
  会被聚出 96pt + 384pt 的「两栏」,那是表格的窄标签列,横向切开会毁掉表格
  阅读序;4:1 的悬殊宽度正是表格与分栏的分界。
- 被数据否掉的两条捷径(记录以免重走):**降低白槽票数**会在参差右边缘上造出
  假白槽;**要求通道两侧都有字**会误杀 `horst2024-p5`(票 19、两侧有字仅 3,
  却是真白槽 —— 两栏基线本就常不对齐)。

### Changed

- 2.4.9 的特征化测试翻转为正向断言(40 行前言 + 12 行正文 → 2 栏,白槽 291–309),
  对照组保留。
- **移除 2.4.8 加的 `radiology-radiomics2023-p1`**:它与既有的 `chen2023-p1`
  是同一篇论文的同一页,产出的快照逐块完全一致 —— 手工转录的独立验证通过,
  但作为语料是重复的。**这个缺陷一直就在语料里**,只是旧快照长得像「26 个平平
  无奇的段落」,没人看出那是逐行碎片。
- 805 测试全过;`pipeline-baseline.json` 随块数变化重新生成。

## [2.4.9] — 2026-08-27

**把「双栏被当单栏」这个缺陷定位到根、并锁进最小复现**。**行为零变化** ——
根因不是一处能补的洞,不带未验证的改动上线。

### Added

- `groupIntoRows` 导出(仅为可测性)。白槽投票发生在**基线行**这一层;此前用
  切分后的 `SpanLine` 做探针必然测不出东西 —— 2.4.8 那次失败的修复尝试正是
  栽在这个错误的观测口径上。
- **特征化测试**:40 行满幅前言 + 12 行双栏正文的合成夹具,断言 `detectColumns`
  当前**只报 1 栏**。附**对照组**:同样的 12 行正文去掉前言 → 正常识别 2 栏、
  白槽落在 291–309。对照组是这条测试的意义所在 —— 它证明缺陷来自「前言压倒
  正文」,而不是夹具本身认不出栏。修好后该测试会失败,那正是更新它的提示。

### Notes — 根因(两处独立失效,叠加成同一个症状)

1. **`detectGutters` 的票被满幅前言稀释**。实测 RSNA 封面页:43 行宽行里只有
   12 行能看见栏间白槽,60% 法定票数是 26 → 真白槽被否决。
2. **`detectColumns` 被前言的「段末短行」焊住**(这才是栏归属失效的直接原因)。
   全幅阈值 `FULL_WIDTH_RATIO = 0.62` 只滤掉 >368pt 的行,但满幅段落的**末行
   天然短**(如 x=84..341 = 257pt),混进投影后横跨白槽,把左右栏焊成一栏
   `[83..529]`。反焊接机制 `splitBandByCoverage` 允许 ≤10% 成员侵入,而这页
   有 10 行以上这样的桥接行,远超配额。

### Notes — 为什么不当场修

- 试过两条捷径,都被数据否掉:**降低白槽票数**会在别的页造出假白槽(参差右
  边缘也能凑出「无字通道」);**要求通道两侧都有字**会误杀 `horst2024-p5`
  —— 那页票数 19、两侧有字只有 3,却是真白槽(两栏基线本就常不对齐)。
- 真正的解法是**先按纵向切出双栏区域、再在区域内做栏检测**(MinerU 的
  region-first 思路),属于结构性改动,不是补丁。已列为下一批的候选。
- **严重性澄清**:覆盖模式下每块仍按自己的原始矩形就位,页面**不会串位**;
  受损的是对照面板的阅读顺序,以及「整段」被拆成逐行翻译带来的译文质量下降。

## [2.4.8] — 2026-08-27

**第二篇真实封面页入库**(Radiology/RSNA),暴露两个既有严重缺陷 —— 本轮**只收
语料、不改代码**:诊断已清楚,但修复涉及核心几何模块且首次尝试未通过验证,
不带未验证的改动上线。

### Added

- 回归语料新增 `radiology-radiomics2023-p1`(RSNA 综述封面页,117 个 span)。与
  JACC 那页版式完全不同:25.2pt 首字下沉、20pt 双行标题、`•` 分隔的作者串、
  带 run-in 粗体小标题的结构化摘要(Background:/Purpose:/…)、7pt 单位块。

### Notes — 本页固化的**已知缺陷**(快照记录的是当前行为,不是正确行为)

- **双栏正文被当成单栏,左右栏逐行交错**(最严重,译文会串栏)。根因已定位在
  `detectGutters` 的投票口径:栏间白槽只有 12 行双栏正文能看见,而标题/作者/
  单位/摘要有 ~35 行**满幅行** —— 它们盖满整宽、投不出白槽,却照样计入 `counted`
  把 60% 法定票数抬到 ~28,于是 12 票的真白槽被否决。`detectColumns` 因此只报
  1 栏 `[83..529]`。同样的症状在 `booz2019-p1`(同为 Radiology 版式)上也存在。
- **栏底正文被当页脚丢弃**(内容丢失)。左栏末行「These features have been
  reported to be associated with」落在页底 8% 带内(y=25.3–35.3)、单行、大写
  开头、54 字 —— `isRunningHeadOrFoot` 判为版式家具直接丢掉。这正是单页判据
  **误伤**的方向,和 2.4.5 补的漏判方向相反。
- 首字下沉「A」被焊到了第二行而非第一行(`groupIntoRows` 的基线中心匹配对
  25pt 字高失准)。

### Notes — 2.4.5 跨页去重的实证复盘(**结论是否定的**)

- 拿两篇不同期刊、共 14 个页眉页脚元素逐一喂给真实判据:**0 个能到达跨页
  登记表**。JACC 的 9 条全部落在 8% 带内被形状判据抓走;RSNA 的 5 条里 4 条
  同样被形状判据抓走,「This copy is for personal use only…」被内容判据
  (`isPublisherBoilerplateLine`)抓走。
- 原因:期刊按设计就把版式家具放在极窄的页边,而既有过滤是**三层**(内容样板
  + 元数据 + 8% 形状),比我当初估计的完整得多。2.4.5 设想的盲区(带外、或
  超过 2 行/140 字的重复块)在这两篇里根本不存在。
- 2.4.5 无害(纯附加、7 个测试、零回归),但目前**接近死代码**。再找一类版式
  (页边距大的预印本/报告/学位论文)验证一次;若仍为空,应予移除。

## [2.4.7] — 2026-08-27

**第一页真实语料入库,当场逮到一个既有分类 bug。**

### Added

- 回归语料新增 `jacc-ccta2020-p1`(JACC 综述封面页,161 个 span)。这是语料里
  **第一张封面页** —— 大标题、作者名单、摘要、单位/披露声明块、左侧边栏、刊头
  一应俱全,覆盖面与既有的正文/表格页完全不同。

### Fixed

- **摘要被切成四块、其中三块误判为标题**(封面页连环误判)。`classify` 的 heading
  判据是「ratio ≥ 1.1 且 ≤2 行且 < 160 字」。这一页 6.0pt 的单位/披露声明有 **26 行**、
  7.5pt 的摘要只有 **17 行**,`dominantFontSize` 按行数取众数,于是把 bodySize 判成
  6.0pt;摘要行 ratio = 7.5/6.0 = **1.249**,轻松越过 1.1 门槛,整段摘要被切成
  「heading + 正文 + heading + heading」。后果不只是标注错 —— 分块错了,摘要就不是
  作为一个整体送去翻译。
  **修法**:heading 长度上限 160 → **70**。真 heading 是短标签,这一点全语料给了
  干净的证据:22 个 heading **中位 16 字**,除这两处误判外**最长 42 字**,而误判的
  是 108 / 112 字的摘要行 —— 43~107 之间是一条空隙,70 落在正中,两侧余量都充足。
  该页从 16 块降到 14 块,摘要合并回一整块;**其余 12 页语料快照零改动**。

### Notes

- 这个 bug 与 2.4.4/2.4.5/2.4.6 无关,是既有口径在「小字声明块比正文行还多」的
  版式上失效 —— 正是补语料要找的那类东西。
- 同页仍存在、**本轮未处理**的两处(已记录,待定):
  ① 摘要末行「American College of Cardiology Foundation.」(42 字)仍判 heading,
  在 70 上限之内;要收到 40 才能盖住,但那样对真 heading 就没有余量了。
  ② 底部单位/披露声明块与左侧边栏在**阅读序里交错**(块 7–13 一个隔一个),
  两者本是不同栏,不该交替。
- 804 测试全过。

## [2.4.6] — 2026-08-27

**MinerU 借鉴第三项:LO-10 未建模墨迹**(把 2.4.0 大标题另置证明可行的「拿渲染
底图当真相」推广到扩边路径)。功能审核 2.1.6 排版项 LO-10,挂了三个批次。

### Fixed

- **扩边不再叠印分节横线/分栏竖线**。扩边的避让清单只认**建模过**的东西:图形盒、
  preserve 墨迹、兄弟块盒。页面上还有一类墨迹从不进任何模型 —— 分节横线、分栏
  竖线、脚注分隔线、装饰边框。扩边对它们失明,译文盒长过去就是叠印;末端几何
  审计同样看不见,不会回退。现在扩展提交前对底图采样,命中未建模墨迹即拒绝该次
  扩展、回退原盒(继续走压缩/缩字/保留原文的既有流程)。三处扩展分支全部接入
  (`pmExpandFit`、`pmShrinkFit` 的阶梯扩展与底线组合扩展)。

### Added

- **`expansionStrips(original, expanded)`**(纯函数,已单测):只返回「扩展后盒 −
  原盒」这个 L 形的两条不重叠矩形。**只采样新增条带** —— 原盒底下是本块自己的
  原文,它会被遮罩盖掉,采样它只会得到假阳性。
- **`bitmapHasInk(...)`**(纯函数,已单测):像素判据从闭包里抽出,两档口径可测。
- placement 统计新增 `inkBlocked`(因未建模墨迹被否决的扩展次数,按次计),
  随诊断 JSON 导出;`baseline-report.mjs` 增列 `annexed` / `inkBlocked`。

### Notes

- **阈值不能沿用另置那一档,这是本项的关键**:另置要判「这块区域是不是空白」,
  2% 够用;扩边要抓的是**一条细线** —— 一条 1px 分栏竖线在 100px 宽的条带里只占
  1%,2% 门槛必然漏掉,LO-10 说的失明正是这个。改为 0.5% 且至少 3 个采样点命中
  (`minPoints` 挡住反锯齿单点噪声)。**两档口径的差别已固化成测试**:同一条
  1px 竖线,2% 口径判 false、0.5% 口径判 true。
- 2.4.0 遗留在另置路径里的 `areaHasInk` 与本项合并为同一个 `regionHasInk(minShare,
  minPoints)`,两处只是口径不同,不再有两份采样代码。
- 采样只对新增条带(通常几十×几十像素)做,且抽样上限 ~900 点;无底图 / 采样
  抛错时**不否决**(与探针同姿态),几何预校验仍在,不会因此丢失可放置的块。
- 804 测试全过(新增 7 组:条带几何 3 组 + 墨迹阈值 4 组)。
- **本项的验证不依赖多页语料**(底图采样是单页的),两条核心主张都已由纯函数
  单测覆盖;真实文档上的启用频率可直接看诊断 JSON 的 `inkBlocked`。

## [2.4.5] — 2026-08-27

**MinerU 借鉴第二项:跨页重复内容去重**(页眉页脚登记表)。

### Added

- **`repeatRegistry.ts`** —— 文档级登记表,累积「页边带内出现过什么文本、在哪些
  页出现过」,同一条在 **≥3 个不同页**出现即确认为版式家具并丢弃。挂在
  `TextExtractor` 上(与 `referencesStartedByPage` 同为文档级状态),经
  `SpanBuildOptions.repeats` 传入 `spanBlockBuilder`。
- 归一化抹掉数字(`J Med Phys 2024;51:88` → `j med phys #;#:#`),逐页变化的
  卷期页码因此收敛成同一条;页码本身自然并成一类。

### Notes

- **补的是单页判据的结构性盲区**:`isRunningHeadOrFoot` 要求「8% 带内 + ≤2 行 +
  ≤140 字」,所以落在带外(页边距大)或超过行数/字数上限的**多行版权与许可声明块**
  一律放行,每页当正文翻一遍。短页眉在形状判据那步就已丢弃,根本到不了登记表 ——
  登记表专治它漏掉的那一类。
- **纯附加、单向**:只丢单页判据漏掉的,绝不把它已丢掉的救回来。救回方向需要在
  第 1 页就知道全文重复情况,逐页管线做不到,强行救会让早期页面把真页眉当正文翻。
- **已知代价**:阈值要到第 3 页才跨过,前 2 页已渲染且不回溯重排(回溯会让已读
  页面闪变)。收益从第 3 页起,早期页面维持现状 —— 严格优于现在,不会更差。
- **两道防误伤的闸**:数字归一会把「Table 1/2/3」并成 `table #`,三页一凑就够阈值,
  故 ① 归一后长度 < 10 不进登记(题注编号正落在这里)② 题注/表格/标题类型不进登记。
- **带宽 0.12 是被测试改出来的**:初版取 0.15,端到端测试当场证伪 —— 页边距小的
  版式里正文首段就落在顶部 15% 带内,一旦逐页重复即被当成家具丢掉。收窄到 0.12
  只覆盖 8% 判据之外那一圈盲区,正文首段安全在带外;该断言已固化进测试。
- 797 测试全过(新增 7 组:归一化/带判定/阈值/按页去重/两道闸/端到端丢弃/无登记表
  时行为不变)。
- **回归语料对照:0 丢弃、0 确认家具** —— 无回归,但同样**未覆盖目标形态**:语料每篇
  仅 1–3 页且为跳页抽样(horst2024 是 p1/p5/p11),凑不够阈值。连续两项改进都卡在
  同一处缺口 —— 语料需要**同一篇文档的连续多页**。

## [2.4.4] — 2026-08-27

**MinerU 借鉴第一项:块级列表/目录判定**(移植自 `para_split.py::__is_list_or_index_block`,
opendatalab/MinerU,Apache-2.0;致谢已在 `THIRD-PARTY-NOTICES.md`)。

### Added

- **`looksLikeListBlock(lines)`**(纯函数):既有的 `looksLikeListStart` 只看块
  **开头那一行**——多个条目被 `groupIntoParagraphs` 黏成一个块时,后续条目的
  标记都在块中间,看不见。新判据改看**行的分布**,三条任一成立即列表块:
  (a) ≥60% 的行以条目标记开头(黏在一起的参考文献/条目列表);
  (b) ≥60% 的行是目录引导点行;
  (c) ≥80% 的行以 `.。;；` 收尾且 ≥3 行(MinerU 主判据——散文段是**折行**的,
  除末行外都停在句子中间;每行各自收尾的块是条目)。
- 接入 `spanBlockBuilder.classify`,排在 title/heading 判据**之前**:三行各自
  以句号收尾的块是条目列表,不是标题,即便字号偏大。

### Notes

- **影响面刻意极小**:块型 `paragraph → list` 后,下游的锚字号、缩字许可、
  区域合并、跨页续接对两者完全同权,唯一改变的是 `planMerges` 的 `bodyOnly`
  门不再把它们黏进相邻正文段 —— 而能触发本判据的块(每行各自以句号收尾)
  本就会被既有的 `endsSentence` 挡住合并,所以**结构上不可能造成回归**。
- (c) 要求 ≥3 行是本移植加的保守边界:两行都恰好以句号结尾在真散文里并不
  罕见(「…et al.」/「…2019.」),3 行连续对齐才足以排除巧合。
- 789 测试全过(新增 5 组:三条判据各一 + 折行散文/CJK 散文负例 + ≥3 行边界)。
- **回归语料对照:12 页零改判** —— 无误伤,但也**未覆盖目标形态**:现有 layout
  语料全是正文/表格页,没有参考文献页与目录页。要真正验证这条规则(以及后续
  版面规则),语料需要补这两类页面。

## [2.4.3] — 2026-08-23

### Fixed

- **Windows 上工具栏按钮单击无反应、下拉点不开(macOS 正常)**。根因不在点击
  逻辑,而是 Firefox/XUL 的**窗口拖拽区**:Zotero 7 在 Windows 上默认隐藏原生
  标题栏,阅读器工具栏整条落在 `-moz-window-dragging: drag` 区域内;区域内元素
  的 mousedown 被窗口移动逻辑接管,`click` 根本不派发到内容层。macOS 的标题栏
  拖拽走另一套系统机制,点击照常穿透 —— 这就是"只有 Windows 坏"的原因。
  Zotero 自家工具栏按钮全部显式标了 `no-drag`,我们注入的节点漏了。现在
  `.pm-compare-wrap / -btn / -caret / -menu` 及菜单项一并声明
  `-moz-window-dragging: no-drag`。
- 注入后增加**拖拽区自检**(仅调试日志):读回 `-moz-window-dragging` 的计算值,
  若不是 `no-drag` 说明样式未生效,一眼可辨。纯字符串,不含文档内容。

## [2.4.2] — 2026-08-23

### Changed

- 工具条「导出」文字按钮换成通用的三点「更多」图标:菜单里常驻**导出译文 PDF**
  与**诊断**;开启调试日志时追加**语料**(点开时现读偏好,无常驻观察者)。
  「诊断/语料」不再作为独立的调试期按钮组出现在工具条上。

## [2.4.1] — 2026-08-23

### Changed

- 刷新按钮悬停注释精简:「清缓存重译全文…(会丢失已翻译内容)」→「全文重译
  (丢失已翻译内容)」。
- 工具条「解析选中内容」按钮改为「解析」(悬停提示仍是全句说明);en-US/zh-TW
  语言包补上此前缺失的 `papermirror-explain-selection` 键。
- 「保存到笔记」与「导出」确认不重叠,两者都保留:前者把**当前选中/当前块/当前页**
  的原文+译文存成条目下的 Zotero 笔记(带页码回链);后者生成**整本**译文 PDF
  (单语+对照)作为附件。

## [2.4.0] — 2026-08-23

**LO-7 · 封面大标题「整体另置」**(功能审核 2.1.6 排版项,P2/工作量 L;范围按议定
只做封面大标题,边缘竖排旋转刊名条继续不译不处理)。

### Added

- **大标题另置策略**:展示型大标题(`type='title'`)走完行距/字距梯子、无损扩边、
  压缩、末位缩字仍放不下时,不再整块保留英文 —— 原文标题**原样保留**(不画遮罩),
  译文以缩小字号(0.72/0.6/0.5 档,下限 9px)**整体另置**到标题正下方(次选正上方)
  的空白处。安全面三层:
  1. 候选区先对**底图逐像素采样**(≤900 点网格,>2% 非纸色即否决)——作者行、刊头、
     装饰线等**未建模墨迹**一票否决;这是 LO-10 指出的扩边盲区,另置路径不允许有。
  2. 仍走 `boxNewlyViolates` 几何预校验:不新压盖已提交块/图形/保护区,不出界。
  3. 全部候选失败则恢复原状,落回既有诚实放弃流程(保留原文 + 计数)。
- 另置块带 `data-pm-annex` 标记;placement 统计新增 `annexed` 单列(已含在
  `committed` 内,仅供诊断观察启用频率);探针行带 `annex: true`。
- 纯函数 `annexCandidateBoxes`(候选位置与页面安全边逻辑)独立导出并测试。

### Notes

- 784 测试全过(新增另置候选 2 测试;placementTally 基线补 `annexed` 字段)。
- 触发条件苛刻(放弃前最后一步 + 双重安全校验),常规文档零行为变化;预期只在
  封面/扉页大标题上生效。

## [2.3.9] — 2026-08-23

**用量统计计数器**(计划 API-P1/WF-4 的观测子集,第三批 item2 议定的最小集):回答
两个调参问题——缓存到底省了多少、预取浪费了多少。纯计数、零文本、零网络。

### Added

- **诊断 JSON 新增 `usage` 段**:`pageCacheLookups/FullHits/PartialHits`(+ 命中率)、
  `segmentLookups/segmentHits`(+ 命中率)、`prefetchedPages`(后台预取翻译过的页数,
  按页去重)、`prefetchedUnviewed`(其中用户到导出诊断时仍未读到的页数 = 预取浪费)。
  用户滚到某页即从 unviewed 移出;导出等非预取路径不计入。
- **`baseline-report.mjs` 增列** `pageHitRate` / `segHitRate` / `prefetchWaste`
  (旧诊断无 usage 段时显示 n/a)。

### Notes

- 782 测试全过(新增预取浪费去重/移出断言 + 段查询/命中累计断言)。

## [2.3.8] — 2026-08-23

**菜单栏回归原布局 + 导出修复**(用户指定):撤掉 2.3.0 引入的「更多 ⋯」聚合菜单,
按钮回到工具条原位;修掉「导出译文 PDF 点击后没反应」。

### Fixed

- **导出译文 PDF「点击后没反应」**。三个洞叠加:①状态胶囊在每页翻完后自动折叠成
  56px 圆环,且胶囊尊重折叠状态——导出的进度与失败信息全部渲染进折叠小环,肉眼
  等于无事发生。现在导出启动/失败时强制展开胶囊,并在任何 await 之前先推 0% 进度。
  ②条目与文件路径解析的两个 `await` 在 `try` 之外,一旦抛错整个 `void` 调用被静默
  吞掉。已移入 `try`,失败必显示原因。③内置中文字体经 `Zotero.HTTP` 加载
  `jar:file://` URL 的行为随版本有差异——加 `fetch` 兜底通道,两条都失败才报错
  (且错误可见)。

### Changed

- **菜单栏恢复原布局**:语言对 · 引擎 · 刷新 · 同步滚动 · ✦解析 · 保存到笔记 ·
  术语 · 导出 ·(诊断 · 语料,仅调试开启时)· 布局 · 设置 · 关闭。
- **刷新按钮回归直达**:点击即「清缓存重译全文」(仍带确认对话框),悬停 tooltip
  说明后果;「修复本页」仍在状态胶囊圆环上。下拉三档菜单移除。
- **「术语」回到工具条**,保留 2.3.1 行为:预览去重后确认保存到词汇表(可撤销),
  「仅复制 TSV」仍是确认框第二按钮。
- **「导出」上工具条**(原在「更多」菜单里)。
- **「诊断/语料」恢复 debug 门控**(1.1.10 原样):仅在设置勾选「开启调试日志」时
  显示,随偏好实时显隐。诊断内容不变(脱敏指标 + 引擎自检)。

### Removed

- **「更多 ⋯」按钮及聚合菜单**(2.3.0 引入)整体移除。
- **「清除本文缓存(不重译)」连同代码删除**(`confirmClearDocCache` 与三份语言包
  文案);「清缓存重译全文」是唯一清缓存入口。
- **手动「换引擎重译本页」入口连同代码删除**(`rotateCurrentEngine`);引擎不稳时
  的自动换家熔断(`onProviderUnstable`)与 `retranslatePage('rotate')` 能力保留。

### Notes

- 781 测试全过;`copyDiagnostics` 隐私门(诊断不含原文/译文/密钥)不变。

## [2.3.7] — 2026-08-23

**基线驱动修复第一轮**:三篇真实文档的诊断基线(第五批采集)暴露的问题,当轮修复。
证据:doc1 表格密集页 tableFailed 48;doc3 表单/单行密集页 abandoned 56(placementProbe
显示全是 13px/9px 高的单行小盒);两篇的 preserve 单元格在诊断里误显示为失败。

### Fixed

- **表格单元格与微小单行块恢复末位缩字(修 2.2.7 的误伤)**。2.2.7 为治正文「发花」
  禁了 paragraph/list 缩字,但把两类**孤立小盒**一起禁了:表格单元格(格间本就独立)
  与微小单行块(标签/表行——单行盒行距梯子无从发力、扩边被下一行挡死,缩字是唯一
  救法)。doc3 实证后果:abandoned 从个位数涨到 56,探针里 13px 单元格行与 9px 表单
  行成批放弃,且同表短格 committed、长格 abandoned(长度依赖,证据闭合)。现在
  `allowsFontShrink` 豁免 `isTableCell`(节点 `data-pm-cell` 标记,整格与逐 member
  兜底块都打)与 `tinyLine`(盒高 ≤1.7×字号且 ≤120 字符);多行正文段维持 2.2.7
  页内统一字号。
- **微小符号格(R²、n=5、±SD)直接归 data/preserve (doc1 实证)**。≤4 字符且不含
  2+ 连续字母/汉字的格翻译无意义——送去只会被验收拒掉再计入 tableFailed,白费请求。
  现在分类阶段直接保护,不进翻译。(`tableStructure` 分类器;既有布局快照零扰动)
- **诊断新增 `state:"preserved"`(口径修正)**。preserve 块(数据单元格等)是**有意**
  不翻译,此前显示 `"untranslated"`——表格密集页在诊断里看起来一片失败,实际全是
  保护性保留(doc1 页 5:126 格 intentional 被误读)。`baseline-report.mjs` 相应单列
  `preserved`,不再算进 keptOriginal。

### Notes

- 三篇基线的达标面:429/超时 = 0、几何违例 = 0(27 页)、段落缓存链实证生效
  (doc2 五个页面 0 请求毫秒级完成、跨文档命中)。本轮修复正对未达标面:表格/表单
  密集页的排版成功率与请求浪费。
- 781 测试全过(新增缩字豁免四断言 + 微小符号格分类测试)。

## [2.3.6] — 2026-08-23

优化计划**第五批·性能回归基线**。基线分两半:离线半锁进 CI(本版落地),真实
世界半提供采集器 + 操作指南(需在真实 Zotero 环境按指南跑 20 篇 PDF)。

### Added

- **离线流水线基线入 CI**(`tests/integration/pipelineBaseline.test.ts`):对仓库内
  12 页真实 PDF 语料(8 篇文档)跑与运行时同构的确定性流水线,把**请求计划形状**
  锁进 `tests/fixtures/baseline/pipeline-baseline.json`——blocks/translatable/
  tableCells/modules/chunks(fast/slow)/maxBlocksPerChunk/payloadChars(token 代理)/
  dupBlocks(API-2 去重量)。任何让 chunk 数或 payload 回涨的改动都会红 CI;有意的
  优化删基线重新生成、diff 进 PR。基线缺失自动生成(与布局快照同一约定);新增
  语料页只提示不红。
- **`npm run bench`**(`scripts/bench.mjs`):同一流水线的每页中位数耗时 + 计划形状
  信息表(当前 12 页语料合计 ~38ms)。耗时环境相关,信息性、不进 CI 断言。
- **真实世界基线采集器**(`scripts/baseline-report.mjs`):汇总若干篇「更多⋯→诊断」
  导出的诊断 JSON,输出每篇 + 合计基线表(页数/请求数/请求页比/补救/429/超时/
  段落命中/页均耗时/已译与保留块数/排版 placed·kept·成功率/几何违例),并写出
  `baseline-report.md`。纯计数聚合,不含任何文本。
- **操作指南**(`docs/reviews/性能基线-操作指南.md`):20 篇 PDF 的覆盖面建议
  (双栏/表格/公式/长文/扫描/复杂封面)、冷/热缓存口径、判定门槛建议(请求/页
  ≤1.5、排版成功率 ≥97%、几何违例 =0、热缓存重读 ≈0)、首屏时间与峰值内存的
  手工口径、以及 API-3 跨文档命中的直接验证点。

### Notes

- 首屏时间/峰值内存暂为手工口径(指南§三);其余指标已可复现采集。
- 780 测试全过(基线对比路径二跑确定性验证)。**至此优化计划五批全部落地**
  (暂缓项:第三批 item2 用量统计——观测性,按约定跳过)。

## [2.3.5] — 2026-08-23

优化计划**第四批 item 7·省调用打磨**(本审计 API-2/3/4/7/8)。五个省 token/省请求
的小改动;至此**第四批全部落地**。

### Performance

- **同页相同内容去重 (API-2)**。同一页里 sourceText 完全相同的块(模板化表头/重复
  短语/密集表格)只把**代表块**送去翻译,其余同文块在代表译文到达时镜像共享
  (渐进渲染同帧可见;salvage/兜底等晚到路径由收尾 sweep 兜住)。代表失败时同文块
  同样保留原文、诚实计入 untranslated。键 = segHash(内容+语言),与段落缓存同一把。
- **跨文档共享段落库 (API-3)**。段落 store 从「每 attachment 一份」改为全局共享
  目录——段落身份本就是 内容+语言 × 引擎/模型/提示词/术语表,与文档无关;同领域
  批量读论文时样板句(资助/伦理声明、方法套话)可占 5–15%,跨文档命中即 0 请求。
  写入只写共享库;读取先共享、后旧 per-attachment 路径(存量平滑迁移)。
  「清除本文缓存」确认框如实说明共享段落库不在此列(「清除全部缓存」照常全清)。
- **按请求内容条件化 system prompt 规则行 (API-4)**。载荷里没有占位符/样式标记/
  上下文的请求不再携带对应规则——大多数请求省 ~50–120 输入 token;对确实携带这些
  标记的请求提示词一字不变(**无需 bump PROMPT_VERSION、不作废任何缓存**)。
- **上下文体量收窄 (API-7)**。chunk 尾随上下文与跨页尾 600→300 字——术语/语气衔接
  靠最后一两句就够,600 字边际收益递减,每 chunk 白付 ~150 输入 token。
- **深度解析结果缓存 (API-8)**。同段落+同引擎/模型/语言的解析在会话内 LRU 缓存
  (容量 20),重复选中/误触零请求。

### Notes

- 新增 4 组测试:同页去重(重复块不进请求、镜像共享、三块全译)、prompt 条件化
  双向(裸请求不带规则/满载请求一字不少)、跨文档段落命中 + 异 context 隔离;
  并发 chunk 测试夹具改用两段不同文本(其考点是并发,不是去重)。779 测试全过。

## [2.3.4] — 2026-08-23

优化计划**第四批 item 6·导出版式打磨 + 表格逐 member 兜底**(本审计 LO-4/5/6/8)。
「导出译文 PDF」链条(LO-1 + WF-1 之后)的质量收尾。

### Fixed

- **导出 PDF 断行遵守 CJK 标点禁则 (LO-4)**。此前贪心断行会让「。,)」」顶行首、
  「(《「」吊行尾——中文排版硬伤。现在 `wrapAt` 断行后过一遍禁则:收尾标点悬挂到
  上一行行尾(可微越界,典型中文做法)、起始标点下移到下一行;每行至多迁 2 字防
  病态,迁空行删除、行数只减不增(已过高度检查的排版应用禁则后仍适配)。
  (`textWrap.applyKinsoku`)
- **导出 PDF 标题/小标题可译 (LO-5)**。此前 heading/title 一律跳过,交付 PDF 节
  标题全英文、与阅读器不一致。现在放行——LO-1 的「放不下保留原文」兜底保证超长
  标题译文绝不截断,封面大字标题放不下就原样保留。
- **导出 PDF 有扩边阶梯,不再一路缩到 5pt (LO-6)**。此前无扩边,略长段直接逐级缩
  到 5pt 近不可读,右/下留白弃之不用。现在与阅读器同序「先无损、后有损」:原盒
  原字号 → 右扩 → 下扩 → 双向(全程不缩字)→ 最大扩展+缩字 → 仍放不下保留原文。
  扩边被邻块 union 盒最近边截断(3pt 边距、参考文献/表格墨迹也算遮挡物),右
  ≤0.6×宽、下 ≤max(2.8×字号, 0.5×高),页界 95%/5%。(`exportExpansionAllowance`)
- **阅读器表格文本格逐 member 兜底 (LO-8)**。此前单元格任一 member 缺译就**整格**
  保留原文。现在缺译时降级为逐 member 放置:有译文的 member 各自用自己的行矩形
  独立放置,只有真正缺译的 member 保留原文并如实计入 tableFailed;整格齐全时仍走
  整格路径(拼合译文进单元格盒,排版最好)。数字列整列保护(对齐保护)不动。
  (`strictPageReplacement` 单元格装配)

### Notes

- 新增 7 条单测:禁则三向(行首悬挂/行尾下移/迁空删除)+ wrapAt 集成、导出扩边
  两向截断与钳制、unionOfRects。LO-1 集成测试(放不下保留原文)在扩边阶梯下仍然
  通过——扩到最大仍放不下的块照旧整块保留。775 测试全过。

## [2.3.3] — 2026-08-23

优化计划**第四批 item 5·首次引导 + 隐藏首选项上界面 + 单段提示**(本审计
WF-3/WF-5/WF-6)。三个小改动,各堵一个「用户不知道」。

### Added

- **隐私卡片加「配置翻译服务 →」按钮 (WF-3)**。新用户首次打开时往往还没配置任何
  服务——现在不必先点「我已了解」再自己找设置,一键直达设置页选服务商/填 Key。
- **三个只能改 prefs.js 的隐藏首选项上设置页 (WF-5)**:
  - **覆盖模式排版**(阅读界面):允许扩边(默认)/ 严格原框(`overlayFitMode`);
  - **携带上下文翻译**(语言与请求):邻近段落作为上文一起发送,译文衔接更连贯;
    关闭省流量/更私密。说明如实提示改动会使已缓存译文失效(useContext 在身份
    观察者内,改动即时生效);
  - **单次请求超时**(性能与并行):秒显示、毫秒存储,10–300s 钳制(数字 pref 手动
    接线,不用会写坏类型的 preference 绑定);长段请求仍按文本量自动放宽。
- **译文块悬停提示补交互指南 (WF-6)**:悬停 tooltip 在原文之后追加
  「双击:解析此段 · 右键:重译此段」——两个此前只有读代码才知道的隐藏交互。

### Notes

- 768 测试全过;`useContext`/`timeoutMs` 的生效路径核实:前者走身份 pref 观察者
  静默重启,后者每次请求现读,均无需重启 Zotero。

## [2.3.2] — 2026-08-23

优化计划**第四批 item 4·普通用户流程固定**(Codex 工作流)。流程七要素逐项核对,
唯一缺口「按方向备下一页」在本版补齐。

### Fixed

- **预取按实际阅读方向,向回看时窗口翻转**。此前 `wantedPages` 永远向前预取
  (current+1…+F、身后只 1 页)——用户向回翻页(复查前文/查引用回跳)时,大窗口
  浪费在他刚离开的方向,行进方向只备 1 页。现在 `setCurrentPage` 记录翻页方向
  (前进/回看,同页不动保持既有方向),预取窗口与「下一页优先级」都以**行进方向**
  为准:回看时 forward 窗口整个翻向更早的页、身后只留小窗口。
  (`translationManager.readDirection` / `wantedPages` / `prefetchNeighbours`)

### Notes

- 流程七要素审计结论(其余六项此前已固定,本版零改动):①默认原文——新开 PDF
  恒为原文模式,不建会话、不发请求;②选模式——工具条一键开关 + 尖角菜单
  (原文/覆盖/对照);③当前页优先——调度器前台保留槽 + 请求闸前台插队 (2.1.8);
  ④失败胶囊「保留原文 N 段」一键定位 (P2-19);⑤圆环默认修复本页 (2.1.10);
  ⑥破坏性/高级动作全部收进菜单——换引擎/清全文在「重译…」,导出/清缓存/术语/
  诊断/语料在「更多⋯」(2.1.10 + 2.3.0)。
- 新增方向感知预取单测(5→4 回看时 3、2 都被预取——旧实现只会备 3)。768 测试全过。

## [2.3.1] — 2026-08-23

优化计划**第四批 item 3·名称与行为统一**(Codex 工作流 + 本审计 WF-8)。按钮说的
就是它做的。

### Changed

- **「解析」→「解析选中内容」**(工具条按钮)。按钮只解析 PDF 中选中的文字,旧名
  让人以为会解析整篇文章。块级 mini 按钮(悬停译文段出现)语境明确,仍叫「解析」。
- **「术语」→ 预览并保存到词汇表,不再只复制 TSV**。点开后:与既有词汇表**去重**
  (source 大小写不敏感,学得列表自身也去重)→ 确认框**预览**新术语(最多 12 条 +
  总数)→ 保存为「参考」(suggested,不强制模型)→ **可一键撤销**(再点「术语」时
  提供恢复保存前快照);**编辑**在设置页词汇表编辑器闭环。「仅复制 TSV」保留为确认
  框第二按钮,旧能力不丢。确认框如实提示:词汇表是译文身份的一部分(glossaryHash
  入缓存键),保存会使本篇已缓存译文在下次打开时按新术语重译。
  (`glossary.dedupeLearnedTerms` 纯函数 + `readerSession.previewSaveLearnedTerms`)

### Notes

- 无法弹确认框的极端环境下不做任何破坏性动作(直接放弃保存)。
- 新增 `dedupeLearnedTerms` 两条单测(词汇表去重大小写不敏感、自身去重+脏条目丢弃)。
  767 测试全过。

## [2.3.0] — 2026-08-23

优化计划**第四批 item 2·界面入口「更多 ⋯」菜单**(本审计 WF-1/WF-2)。导出译文
PDF、清本文缓存从控制台 API 搬上界面;诊断去掉 debugLogging 门槛并附引擎自检。

### Added

- **译文面板新增「更多 ⋯」菜单**,收纳五个动作(复用现成 ftl 串「更多/清除本文
  缓存/本文献缓存已清除」):
  - **导出译文 PDF(单语 + 对照两份)** —— 此前只能在开发者控制台调
    `Zotero.PaperMirror.exportTranslatedPdf()`,普通用户根本不知道有这功能 (WF-1)。
  - **清除本文缓存…(不重译)** —— 清本文档全部页面+段落缓存,屏幕显示不变、下次
    打开重新翻译(隐私/磁盘用途);带确认。与「重译…」菜单里的「清缓存重译全文」
    互补,两者都破坏性、都先确认。
  - **术语**(从工具条移入)、**诊断**、**语料**(仅 debugLogging 时显示)。
- **诊断附带引擎自检 (WF-2)**:诊断 JSON 新增 `engines` 段——每个已启用引擎的
  密钥是否已配(布尔)、端点主机、模型、是否需要密钥,外加会话内熔断/轮换页数
  (`engineRotations`)。**纯本地检查,不发任何网络请求,绝不含密钥本体**。

### Changed

- **「诊断」不再要求 debugLogging** (WF-2):它只含脱敏指标,任何用户报问题都需要
  它。**「语料」仍随 debugLogging 显隐**(P0-2 隐私姿态不变——它含整页原文,不该在
  普通用户一层可见);菜单点开时现读 pref,去掉了常驻的 pref 观察者。
- 工具条减负:原「诊断/语料/术语」三个零散按钮收进「更多 ⋯」。

### Notes

- 诊断隐私回归闸(「诊断不得含语料/原文」)更新为容忍 async 签名,不变量不变、
  继续生效。765 测试全过。

## [2.2.9] — 2026-08-23

优化计划**第四批 item 1·事件驱动替代 350ms 轮询 + 不可见即停**(Codex 性能P2 +
本审计 PF-8)。后台/静止时接近零开销,翻页响应反而更快。

### Performance

- **页变化改为事件驱动,350ms 轮询降级为 1.5s 兜底**。此前每 350ms 一次
  tick(读当前页、比较、布局看门狗)常驻运行——绝大多数 tick 什么都没发生。现在
  `syncCurrentPage`(廉价比较、幂等)挂在既有 PDF.js eventBus 订阅上,`updateviewarea`
  每滚动帧触发——**翻页当帧即被捕捉**(此前最坏延迟 350ms);轮询只兜事件失效的底
  (eventBus 不可得/事件被吞),间隔 350ms → 1500ms。分栏漂移看门狗(refreshLayout)
  与内存回收(lastPartial)随兜底 tick 走;分栏尺寸另有 ResizeObserver 实时兜住。
- **窗口不可见即停**。主窗口 `document.hidden` 时:兜底 tick 直接跳过(不做布局
  看门狗、不做页同步——不触发 `setCurrentPage` 就不会产生新的翻译/预取请求),
  事件驱动的页同步同样跳过;`visibilitychange` 恢复可见时**立即补一拍**,不必等
  下一次兜底 tick。内存回收(lastPartial 断开释放)保留在 hidden 检查之前——后台
  窗口更该回收 26MB 级的位图引用。监听器随会话 destroy 解除。

### Notes

- 语义零变化:同一套页同步逻辑,只是触发源从定时器变为事件 + 慢兜底;
  quiescing/原文模式的既有闸不动。765 测试全过。


## [2.2.8] — 2026-08-23

优化计划**第四批·导出 PDF 正确性 LO-1(放不下保留原文,不涂白截断丢正文)**。
第四批从唯一的正确性 bug 开始。

### Fixed

- **导出译文 PDF 不再丢失正文内容**。此前 `translatedPdfBuilder` 对每个可替换块
  **先涂白**原文行矩形、**再排版**译文——译文在最小字号(5pt)仍放不下时,也只画
  一段带「…」的截断译文:原文已被涂掉,段落尾部在导出文件里**永久丢失**,且无任何
  提示。现在顺序倒转:先排版、确认放得下才涂白绘制;放不下(`overflow`)的块**整块
  跳过**——不涂白、不画截断,原文完整保留——与阅读器严格替换同一条铁律「宁保原文,
  不毁内容」。`clippedBlocks` 相应更名 `keptOriginal`(语义从「截断了多少」变为
  「保留了多少原文」),导出完成时通知用户「N 段译文放不下原区域,已保留原文
  (内容零丢失)」。(`pdfgen/translatedPdfBuilder.ts`、`readerSession` 导出通知)

### Notes

- 新增真 pdf-lib + 真内置字体的**集成测试**(Zotero.HTTP 以本地文件桩替):超长译文
  塞进 40×8pt 小盒 → `keptOriginal=1` 且 mono/dual PDF 均有效可再解析;宽裕盒 → 0。
- 第四批其余项(事件驱动轮询、界面入口、名称统一、CJK 禁则、扩边阶梯、表格兜底、
  省调用打磨)后续逐项推进。

## [2.2.7] — 2026-08-23

优化计划**第三批 item 7(b)·正文缩字按页决策(不单独缩致发花)**(本审计 LO-3)。
补齐 item 7 余项;至此第三批全部落地(item 2 观测统计按约定暂缓)。

### Changed

- **正文段落不再单独末位缩字,页内正文保持统一字号**。此前放置失败的块走「扩边→
  缩字(94%/88%)→组合」的末位保底,对正文也一样:一段正文缩到 88% 而上下相邻段
  仍 100%,读起来就是**发花**(同栏正文忽大忽小)。现在正文(paragraph/list)只靠
  行距/字距梯子、无损扩边、压缩把译文放进**原字号**盒,放不下就**保留原文**(诚实
  计数、可「查看保留原文」),绝不单独缩它一块。末位缩字梯+组合仅对**独立元素**
  (图题 caption、标题 heading/title)保留 —— 它们是孤立的,缩自己不会与正文比出
  大小差。表格单元格由独立表格模型渲染、根本不进这条 items 流水线,不受影响。
  (`strictPageReplacement.pmShrinkFit` 按 `allowsFontShrink(type)` 分流)

### Notes

- 取舍:正文「按页统一字号」优先于「单块缩字保住译文」。个别过长正文段因此可能保留
  原文而非缩排 —— 但正文仍先经无损扩边与(预算引擎的)压缩,末位缩字本就只覆盖最后
  极少数情形。整页统一的「全字号 + 保留原文」远好于忽大忽小的发花。
- 未采用「整页正文按最深需求统一缩小」的方案:为一段过长正文把**全页**正文缩小、并
  重排已提交块(闪一下),是更重的锤子、体验更差;`allowsFontShrink` 为纯函数单测覆盖。

## [2.2.6] — 2026-08-23

优化计划**第三批 item 7(a)·摘要拆段「尽力对齐」**(本审计 LO-2)。段数不等时不再
整块塌顶,尽力把译文分段落到各组盒。(item 7(b) 正文缩字按页决策留待后续。)

### Fixed

- **合并区域(摘要等)译文分段与源段数不等时,不再回退成整块塌顶**。此前
  `splitRegionForPlacement` 只在译文段数与源段落组数**完全相等**时才拆分,一旦引擎
  多切或少切一段就整块回退——整个区域塞进一个高union盒,结构化摘要塌到顶部(封面
  「标题空洞/摘要塌顶」正是此类)。现在改为**尽力对齐**:段数不等时把**多的一侧**
  按顺序均匀并入**少的一侧**,得到 K=min(段数,组数) 个盒子——每盒都有文本、每段
  译文都落地、顺序不变、盒子几何覆盖原区域全部行矩形。译文比组多(引擎多切)→ 组盒
  几何不变、多出的段按序并入相邻桶;译文比组少(引擎并段)→ 相邻组盒并集承载。只有
  引擎把整段合成一坨(全无换行、无从拆分)才仍回退。(`strictPageReplacement.splitRegionForPlacement`)

### Notes

- 纯函数改动,新增 P>G / P<G 两向单测;段数相等时仍一一对应(最忠实),行为不变。
- item 7(b)「正文缩字以页为单位、不单独缩致发花」涉及已硬化的缩字/异步未适配解决流,
  留待单独一版,不在本次。

## [2.2.5] — 2026-08-23

优化计划**第三批 item 6·底图位图 LRU**(本审计 PF-4)。切换页/回看/同宽度重建时
不再重复做最贵的 pdf.js 整页 rasterize。

### Performance

- **底图位图按 (页, 宽度桶) 小容量 LRU 缓存,复用底图只重建遮罩+文本层**。此前
  `renderDocPage` 每次都调用 `renderPageBitmap` 把整页重新 rasterize —— 而这类重建
  频繁发生:翻译完成刷新、对照模式切换、`relayoutSlots`、滚动回看同一页,都会重跑
  一次整页渲染(pdf.js `page.render`,是整条链路里最贵的一步)。现在把渲染出的独立
  底图按 `${page}@${宽度}` 存入容量 4 的 LRU:命中即复用,`buildStrictPage` 只重建
  遮罩+文本层。底图只随 (页, 宽度) 变、与译文/配置无关,无需失效;缩放换宽度自然
  落到不同键、旧宽度条目由 LRU 淘汰。(`readerSession.baseBitmaps`,新增纯工具
  `util/lru.ts`)

### Notes

- 只缓存 `renderPageBitmap` 的**独立** raster;`getPageRender` 回退(左视图 live
  canvas,宽度/缩放都不对)不入缓存,避免错位。单张底图受 oversample 像素预算封顶
  (~3.2M px ≈ 13MB),容量 4 → 内存上界 ~50MB,会话 dispose 时全部释放。
- `LruCache` 为纯数据结构(无 DOM/平台依赖),命中顺序与容量淘汰由单元测试覆盖。

## [2.2.4] — 2026-08-23

优化计划**第三批 item 5·段落缓存分片(内存合并 + 节流落盘)**(本审计 PF-3)。去掉
整篇翻译时对段落 store 的 O(N²) 全量重读重写。

### Performance

- **段落写入改为内存合并 + 节流落盘,不再每页重写整篇 store**。每 attachment+context
  只有一个段落 store 文件,旧的 `writeSegments` 每次调用都 read→merge→**整篇重写**;
  整篇论文按页翻译时 store 随页增长、每页各重写一遍全库 → 全篇累计 O(N²) 的 JSON
  读写。现在写入先并入**内存 pending**(按文件路径合并),节流窗口(默认 500ms)到点
  时把该路径**当前累积的全部** pending 一次性 read→merge→write —— 突发整篇翻译里
  几十次每页写坍缩成个位数次全库重写。`readSegments` 先查 pending(落盘前另一页刚
  写的段落也能命中,跨页去重不因节流丢失)再落盘。(`cacheManager`)

### Notes

- 语义零变化,三条既有硬约束全部保留:① 同路径写经 `enqueueWrite` 串行,并发页
  不互相覆盖(P1, 1.3.0);② 合并基底瞬时读失败 → 放弃本次落盘、绝不以空库截断
  (P2-1, 2.0.7);③ 原子 tmp 写 + 0700/0600 收权。`writeSegments` 返回的 Promise 仍
  在本批真正落盘后才 resolve,调用方可照旧 await 持久化。
- `clearAttachment`/全清前先丢弃相关未落盘 pending 并清定时器,杜绝「落盘复活已删
  文件」。flush 窗口可注入(测试置 0 取确定性即时落盘)。

## [2.2.3] — 2026-08-23

优化计划**第三批 item 4·页面级原子提交(核心:提交前几何预校验)**(Codex 排版P1 +
本审计 LO-3/PF)。根治「译文显示了又被抽走」的闪烁/发花。

### Fixed

- **扩展后的块在揭示之前先做几何校验,不再显示了又被末端审计回退**。此前顺序是
  「扩边/缩字 → 提交(揭示)→ reportPlacement 末端审计发现越界/压盖/越页 → 回退」——
  于是靠扩展才放下的块**显示一下又被抽走**(闪烁、发花),尤其两个相邻块先后扩进
  同一片空白互相压盖时。现在把与末端审计**同一套**「新增侵入>容差」判据前移到
  提交前:`pmExpandFit` 与 `pmShrinkFit` 的每一步扩展,只有既适配、又不新压盖**已
  提交**的翻译块/真实图形/preserve 数据区/页界时才 commit;揭示出去的块都是预先
  校验过的。末端 `pmGeometryAudit` 退化为几乎无事可做的安全网,不再产生可见回退。
  (新增纯函数 `layoutSafety.boxNewlyViolates`;`strictPageReplacement` 提交前闸)

### Notes

- 非扩展块(盒==原盒)对每一项的「新增侵入」恒为 0 → 恒通过,**首屏原字号揭示不受
  影响**,只有扩展盒才会被这道闸拦下;同一片空白由**先处理者**占用,后者顺次退让到
  缩字/保留原文(确定性,不再靠事后归责回退)。
- 本版聚焦「提交前预校验」这一核心。缩放不重触发翻译(`setDisplayScale`)、字体晚到
  的 provisional/final 双测门(1.2.2)此前已到位,本版不改。

## [2.2.2] — 2026-08-23

优化计划**第三批 item 3·排版失败处理顺序 + charBudget 含空白**(Codex 排版P1 +
本审计 LO-6/API)。把「缩写译文」这步从**第一手段**降为**扩边之后**才动用,既保真
又省 API。

### Changed

- **排版失败处理顺序重排为:原字号实际矩形算容量 → 本地行距/字距(梯子)→ 模块内
  安全空白扩边 → 仍溢出才压缩译文一次 → 最后小幅缩字 → 仍不适配保留原文并标记
  可修复**。此前预算型引擎的溢出块**直接送去压缩**(让模型缩写、牺牲信息、还费一次
  API),无损的空白扩边被埋在缩字流程里、要到两轮压缩用尽才轮到。现在新增
  `pmExpandFit`:压缩/缩字之前先只靠邻近安全空白(算法3,原字号、原文一字不动)
  把能救回的块无损救回——"图1→Figure 1"式越译越长的标签/标题正是这样放下,不牺牲
  译文、不费 API。仍不适配的块才进入压缩→缩字→保留原文。
  (`strictPageReplacement.pmExpandFit` / `expandStrictBlocks`;`readerSession.resolveStrictUnfit`)

### Fixed

- **压缩预算计入可用空白,模型不再一上来过度缩写**。`budgetFor` 此前按**仅原盒**算
  容量交给模型,框偏小 → 模型狠缩、信息流失,而这页本可先无损扩边。现在预算按
  「原盒 + 可安全扩进的邻近空白」(`estimateCjkCapacity(box + expansionAllowance)`)
  算容量:模型只需缩到「盒+空白」放得下,不必更狠。(`strictPageReplacement.budgetFor`)

### Notes

- 扩边幂等(已提交块跳过),压缩后递归回来会对更短文本再扩一次;扩边不适配即回退
  原盒,后续压缩/缩字预算仍从真实盒起算。几何安全复核与既有 `pmShrinkFit`(扩边+缩字
  +底线组合)全部不变——本次只是把无损扩边**提前**成独立的一手,不改其数学与避让。

## [2.2.1] — 2026-08-23

优化计划**第三批 item 1·服务商稳定映射 + 发送前查全服务商缓存**(Codex API-P1)。
两处根治「换/增/删服务商就白重译一整篇」的 API 浪费。纯映射 + 只增读路径,零破坏。

### Fixed

- **页→服务商改用一致性(rendezvous / HRW)哈希,不再 `pageIndex % 池大小`**。取模
  的致命处是**池一变全盘重排**:加一个服务商、删一个、甚至只改顺序,几乎每一页的
  规范引擎都换人 → 页缓存键全变 → 整篇 miss 白重译。HRW 只让**受影响的约 1/N 页**
  换人——加服务商:只有恰好对它评分最高的页迁过去;删服务商:只有原属它的页各自
  迁到次高分服务商;**改顺序:零变化**(评分只认服务商 id 与页号,与池内位置无关)。
  新增 `rankProvidersForPage` 给出该页服务商降序榜,熔断/手动轮换按榜名次确定性退让
  (offset0=规范拥有者、offset1=次高分…),取代旧的 `pageIndex+offset` 取模 hack。
  (`providerPool.ts`、`readerSession.providerForPage`)
- **发送翻译前先查「全服务商」页缓存,任一命中即用、绝不重付费**。此前只查**规范
  引擎**一个键,miss 就重译。现在规范键 miss 后,遍历池内其余已启用服务商各自的
  完整页缓存——只要上个会话的主译引擎、池变动前的旧拥有者、或熔断换过的引擎**任一**
  曾译过这页,就直接复用。跨会话、增删服务商后最常见的「白重译整篇」由此根治。
  (`readerSession` readCache;`getApiKey` 已 memoize,逐服务商构键几乎零开销,
  成本仅几次文件存在性检查,且只在真正 miss 时发生)

### Notes

- 与旧缓存兼容:HRW 改变了既有页的规范拥有者,但「查全服务商」这层会把旧键(仍启用
  的服务商写的)照样找回来 —— 迁移**不丢缓存、不重付费**。命中的键与规范键不同不回写
  (读写键仍恒等,下次继续靠这层探测)。

## [2.2.0] — 2026-08-23

优化计划**第二批 item 3·表格单元格语义**。补上「第二批」架构核心的最后一块:
文本型表格单元格从此走**表格分组**、绝不与正文段落同请求。快照兼容(不改
`block.type`、不动渲染路径),故次版本号 minor bump。

### Fixed

- **文本单元格不再与正文段落打进同一个请求**。结构化后的单元格是合成块——`type`
  仍是 `'paragraph'`(渲染需要),但成对携带 `tableRow`/`tableCol`。此前 `layoutModules`
  与 `planChunks` 只认 `type === 'table'`,于是文本单元格被当普通正文,和上下文段落
  塞进同一次翻译请求:表头/数据行译文互相污染、块 id 漂移、错位补救。现在通过
  `isTableCellBlock`(凭 `tableRow`+`tableCol` 标记,不改 `type`)把连续单元格锚定为
  **table 模块**,`planChunks` 在单元格与正文之间设**硬边界**——一个请求要么全是
  单元格、要么全是正文,绝不混装。单元格之间仍按字符预算**批量**打包(不逐格请求)。
  数字/统计单元格另由结构化时设的 `translationMode: 'preserve'` 保护,根本不进翻译。

### Notes

- 零渲染风险:`buildLayoutModules` 仅在 `translationManager` 翻译分组处调用;不改
  `block.type`,其余所有 `paragraph` 消费点(覆盖层、严格替换、诊断)行为不变。

## [2.1.10] — 2026-08-23

优化计划**第二批 item 4·「刷新本页」拆三档**。默认动作变轻(省 API),破坏性动作
显式化(带确认)。

### Changed

- **圆环刷新 = 修复本页(不再默默换引擎+整页重付费)**。此前有服务商池时,点一次
  圆环会**轮换服务商 + 绕过段落库整页重译**——用户以为「补一下这页」,实则整页
  重新付费。现在圆环(及覆盖层重试)只做**修复本页**:补缺失/无效/排版失败的段,
  复用段落库里合格的译文,不换服务商、不清合格缓存。
- **顶部「刷新全部」降级进「重译…」下拉,拆成三档**:①修复本页(同圆环);
  ②换引擎重译本页(有池时轮换服务商、绕过段落库真正换家重译——原圆环行为,
  现为显式动作);③清缓存重译全文(破坏性,清本文档全部页面+段落缓存从头
  重译,**执行前弹确认**)。

## [2.1.9] — 2026-08-23

优化计划**第二批 item 2·自适应预取**。减少用户从不浏览的页产生的请求,尤其
快速跳页场景。不改译文正确性。

### Performance

- **预取窗口大幅收窄**。此前 auto 一进页就抢译多达 `min(2N,10)` 页、high 固定 12
  页——快速跳页时几乎全是无效请求。现在:省流(stable)只当前+下一页;auto 顺读
  渐扩(单引擎 1、随服务商池到 3);高速(high)≤5(不再固定 12)。backward 一律 1。
  (`providerPool.prefetchWindowFor`)
- **邻页预取去抖(默认 500ms)**。每次翻页先 `cancelExcept` 撤掉过期预取,再重置
  去抖定时器——用户还在连续翻页就不发邻页请求,停下约半秒才对**最终停留页**发
  预取;快速掠过的页不再产生请求。**当前页本身仍立即翻译**(不在去抖内)。
  (`translationManager` schedulePrefetch,可注入 `prefetchDebounceMs`)

## [2.1.8] — 2026-08-23

优化计划**第二批·架构核心**的第一项(全批四项之一):**请求级并发闸**。降低
429/卡顿/浪费,不改任何译文正确性。(item 2 预取、item 4 刷新拆分、item 3
表格语义各随后单独发版。)

### Performance

- **每服务商「真实在途 HTTP 请求」闸(两层调度补齐)**。此前 `RequestScheduler`
  只在**页面级**限每服务商并发页数,但一个页面任务内部还会并发多个真实请求
  (`CHUNK_CONCURRENCY=2`,补救阶段更高)——「N 个并行页」被放大成 2N~4N 个
  请求打向同一服务商,是 429/卡顿/浪费的直接来源。新增 `RequestGate`:所有真实
  请求(主翻译/重试/补救/残留/兜底经 `countedTranslate`,以及压缩重排、单段
  重译三处收口)出手前先取该服务商的一个在途名额,结束即释放;每服务商在途
  上限 ≈ 其页面并发上限(下限 2 保留单页两 chunk 并行),**当前页请求插到后台
  等待者之前**优先出手。纯逻辑、注入 fn 可单测(+3 单测)。

## [2.1.7] — 2026-08-23

优化计划**第一批·止血速赢**(全低风险):降并发峰值、诊断探针下热路径、
高频画布免 GPU 回读、修缓存失效粒度。缓存 schema 不变;`maxOutputTokens`
一项因移出缓存身份,会让**该项曾不同的旧缓存**不再命中(等价一次性重译),
其余照旧。

### Changed / Performance

- **默认全局并行页 12 → 8(止血)**。当前限制的是「并行页面任务」而非真实
  HTTP 请求数——每页内部还能并发普通批次、补救阶段更高,「12 个并行页」普通
  阶段可达约 24 个请求,是卡顿/429/浪费的主因。两层「请求级并发调度」落地前
  先把默认峰值降到 8(上限 24 不变,可自设)。`prefs.js`、`providerPool.ts`
  GLOBAL_MAX_DEFAULT。
- **诊断探针 `pmProbe` 下排版热路径**。此前每页 FINAL 无条件对每块每行做
  getImageData 采样底图/遮罩(仅供诊断导出),普通用户白付数百次逐像素读取、
  拖长译文显现。现在仅在开启「调试日志」时对该页采样;默认零成本。
- **三处高频采样画布声明 `willReadFrequently`**。strict 底图 ctx、遮罩 maskCtx
  与 renderPageBitmap 的 ctx 被 localPaper/samplePaper/稳定性轮询/pmProbe 高频
  getImageData;声明后走 CPU 后端,免去每次 GPU→CPU 回读与管线冲刷。

### Fixed（API 浪费）

- **`maxOutputTokens` 移出缓存身份哈希**。它只是输出安全上限、不决定已完整
  译文的内容,而截断译文本就被拦下不入缓存。此前把它计入 → 用户为解决个别
  截断把上限 4k 调到 8k,会作废整篇页面+段落缓存全量重译。移出后调高上限只让
  个别曾截断的段重译。`readerSession.ts` settingsIdentityHash。

### Verified（无需改动）

- 请求级重试策略复核**已合规**:仅网络/5xx/408/429-ratelimit/超时可重试且超时
  至多一次、429 遵守 Retry-After;密钥错(401/403)、模型不存在(404)、额度耗尽
  (402/429-quota/456)均 `retryable:false` 立即不重试(`errors.ts` +
  `translationManager.ts:1359-1391`)。

## [2.1.6] — 2026-08-23

移除「完整 PDF 服务模式」(BabelDOC 本地桥接)。它没有界面、用者寥寥,却带来
一整套本地 HTTP 服务的攻击面。默认的**零配置内置导出**保留不变。

### Removed

- **删除完整 PDF 的「服务模式」及其本地桥接。** `pdfExportMode = 'service'` 会把
  整篇 PDF POST 到本机的 `babeldoc_server.py`(11017 端口)做版面级重排。此模式
  无界面(只能靠隐藏首选项启用)、依赖用户自建 Python 服务,且引入了本地 HTTP
  端口、令牌文件、HMAC 握手这一整套需持续维护的安全面(2.0.6 起审了多轮:路径
  遍历、CSRF/DNS-rebinding、端口抢占窃密等)。现移除:`src/translation/pdfService.ts`、
  `tools/babeldoc_server.py`、首选项 `pdfServiceURL`/`pdfExportMode`,以及相关握手/
  令牌客户端代码。**导出译文 PDF 恒走零配置的内置生成**(`Zotero.PaperMirror.
  exportTranslatedPdf()`,无需任何外部程序);对照阅读主功能不受影响。插件从此
  不再开启或连接任何本地网络服务。

## [2.1.5] — 2026-08-23

完整 PDF 服务模式:旧版桥接不再给出误导性的「请确认服务正在运行」,而是明确的
可执行提示。**不改变任何安全机制**——仍在发送密钥前经 HMAC 握手验证服务端身份,
旧桥接/冒名者/未启动一律中止、密钥绝不出手,只是把失败原因说清楚。

### Changed

- **旧版 `babeldoc_server.py` 现在给出可执行的升级提示,而非「请确认已启动」。**
  2.1.0 起插件在发密钥前要求 `/handshake` 握手;升级前的桥接既不写令牌文件、也
  没有 `/handshake`,于是旧流程先撞上「未找到令牌 → 请确认服务正在运行」——可服务
  其实**正在**运行,只是旧,提示因此误导。现在插件在令牌缺失/握手缺失时探测端口:
  端口有服务在应答就判定「在跑但旧」,直接提示升级 `tools/babeldoc_server.py` 到
  2.1.0+ 并重启;真的没在监听才保留原「请启动」文案。分类逻辑抽为纯函数
  `classifyBridgeReachability`(down / legacy / live)并加单测。普通在线翻译/内置
  导出用户不涉及本条。

## [2.1.4] — 2026-08-23

2.1.3 的段落拆分补一个关键容差:引擎常把段落分隔从 `\n\n` 归一成单个 `\n`,
2.1.3 只认空行(`\n{2,}`)于是静默回退、摘要照旧塌顶。现在按**任意换行**
(`\n+`)拆分——组内文本本就无换行,故仍精确落在段落边界。缓存不变,无需重译。

## [2.1.3] — 2026-08-23

修复封面「摘要塌顶、分节留空、大标题空洞」。纯排版层,缓存不变(v5),无需重译。

### Fixed

- **结构化区域(如四节摘要)按段落拆回各自的盒子**。合并 pass(`coalesceRegions`)
  为翻译质量把 Purpose/Methods/Results/Conclusion 四节正文并成一个区域——正确;
  但排版时把整段译文塞进这个跨四节的**高联合盒**,更紧凑的中文只填满顶部、下方
  被遮罩成空白,分节标签也随之对空,大标题所在的一带也被这个巨盒波及。诊断探针
  证实第 1 页 `region-13` 是一个 532×1109 的committed巨块(2250 字=四节全并)。
  现在:合并区域仍整体送翻译(句子连贯、术语一致),但在排版层按 `\n\n` 段落边界
  把译文**拆回每个源段落组自己的盒子**(`regionParagraphs` 记录各段几何,
  `splitRegionForPlacement` 执行拆分)。**严格兜底**:仅当区域有 ≥2 段且译文正好
  拆成同样段数时才拆,引擎丢了段落分隔就整体回退旧的联合盒行为——只会改善、
  绝不劣化。已译文本若已含段落分隔即时生效,无需重译。

## [2.1.2] — 2026-08-23

诊断专项:封面「大标题整块空白」定位仪。不改翻译/排版行为,缓存不变(v5)。

### Diagnostics

- **封面标题空洞定位探针**。某些封面(如 Radiology 期刊首页)译后大标题整块
  既无中文也无英文、只剩空白。排查确认 strictPageReplacement 的「放弃即保留
  原文」契约本身正确(pmRevert 与几何复核两条 un-commit 路径都会 clearMask 并
  隐藏译文节点,底图是含原文的整页位图 —— 同页旋转的「MEDICAL PHYSICS」正常
  透出即为证),故空白不是放弃逻辑所致。新增 `probeStrictPlacement`:对每个可
  替换块采样**底图**与**遮罩**,报告 `baseInk`(底图此处是否有墨)与
  `maskOpaque`(遮罩是否不透明),写入诊断导出的 `placementProbe` 字段。据此可
  一举分辨两种病因 —— `baseInk=false` 说明独立的 `page.render()` 根本没画出这些
  字形(底图渲染/字体/时序缺口,放弃兜底无原文可露);`baseInk=true` 且
  `maskOpaque` 而该块并未 committed,则是遮罩误盖真原文。纯几何+布尔,不含文本。

### Build / CI（仅工具链,不影响已发布插件包）

- **[P3-E] 打包门禁加固**。`verify-xpi` 从「黑名单几类垃圾文件」改为**白名单**:
  任何不在允许清单上的条目都拒绝发布 —— 防止源码映射(`.map`)、未编译源码
  (`.ts`)、`.env`、密钥文件或 `node_modules` 残留悄悄混入已发布 XPI。同时修正
  默认版本选取: 原 `.sort().at(-1)` 按**字典序**会把 `2.0.9` 排在 `2.0.10` 之上、
  去校验陈旧构建,现改为按 semver 数值比较,并优先选与 `package.json` 版本相符
  的包。纯判定逻辑抽到 `scripts/xpi-checks.mjs`,+5 单测覆盖。

## [2.1.1] — 2026-08-22

安全专项审核第二批(密钥卫生与纵深防御): 缓存文件权限、会话内密钥缓存的失效
与清理、卸载残留清理。缓存格式不变(v5),不触发重译。无需升级 `babeldoc_server.py`。

### Security

- **[P2-A] 本地缓存文件对同机其他账户可读**。缓存文件里是**整篇论文的译文**
  明文,此前按默认 umask 落盘(文件 0644、目录 0755)—— 共享机器上其他本地
  账户可直接读走用户在读什么、译文全文。现在写文件后收紧到 0600、建目录后
  0700。纯尽力而为: Windows 上 POSIX 权限位不适用(由 NTFS ACL 接管),
  收紧失败绝不影响缓存写入。
- **[P2] 凭据库会话缓存的 TOCTOU 陈旧回填**。`getApiKey` 是「查登录管理器
  (最长等 4 秒)→ 写会话缓存」两步且其间不持锁。用户在这 4 秒里对同一服务商
  存了新密钥时,那次 `setApiKey` 会先删缓存,随后姗姗返回的 `getApiKey` 用**旧**
  值回填缓存 —— 陈旧密钥驻留整场会话、后续请求全用错。现在加单调失效代次:
  `getApiKey` 进入时抓代次,`setApiKey`/`deleteApiKey`/`clearKeyCache` 递增它,
  仅当代次未变才允许落缓存。
- **[P3-B] 停用/卸载后明文密钥缓存残留**。`shutdown()` 此前只清 logSanitizer 的
  已知密钥集,凭据库另有一份会话内明文密钥缓存(`keyCache`)不被清,未重启
  Zotero 时会残留。现在导出 `clearKeyCache()` 并在 `shutdown()` 里连同
  `clearSecrets()` 一起调用。
- **[P3-C] 卸载不清隐私残留**。插件被卸载后,明文回退密钥、系统凭据库里的
  API Key、以及缓存目录里的整篇论文译文全部残留。`uninstall()` 现在在**且仅在**
  真正卸载(reason=ADDON_UNINSTALL)时,重度 try/catch 内清除本插件的整个
  首选项分支(含明文回退密钥)、移除 Login Manager 条目、删除缓存目录 ——
  停用/升级/降级/退出都不触发,判定从严(宁可漏清也不误删用户数据),且
  清理的任何失败都不中断卸载。

### Docs

- `PRIVACY.md` 记录缓存文件权限收紧与卸载清理行为。

## [2.1.0] — 2026-08-22

安全专项审核第一批(隐私与密钥): 日志脱敏器覆盖缺口、本地桥接无认证攻击面。
缓存格式不变(v5),不触发重译。**完整 PDF 服务模式(高级)的用户需同步升级
`babeldoc_server.py` —— 插件与桥接之间新增了握手鉴权,旧桥接会被拒绝。**

### Security

- **[S1] 日志脱敏器对 URL 内密钥与几类密钥形状失明**。Gemini 官方 REST 用法
  是 `?key=AIza…`,这类密钥只存在于 Base URL 字符串里、从不经过 registerSecret,
  精确替换救不了;而 AIza / Groq `gsk_` / 智谱 `hex.suffix` 形状又不在正则清单里
  —— 2.0.10 新加的「diagnose 输出整体脱敏」对它们**完全失效**,用户一贴诊断即
  泄密。现在补齐四条模式(通用 URL 凭据参数、AIza、gsk_、智谱),并在组装
  provider 设置/诊断/测试连接时解析 Base URL 与 apiPath 里的查询参数值注册进
  脱敏器(结构性兜底,即使密钥以裸形态被网关回显也能命中)。
- **[S5] 脱敏器重叠密钥部分泄露**。先注册短密钥再注册长密钥(用户先粘错/截断
  再存完整)时,按插入序会先命中短的,把长密钥替换成 `[REDACTED]+残余后缀`
  —— 后半段裸露。现在按长度降序替换,先吞最长的。
- **[S6] 非 Bearer 的 Authorization scheme 值不被脱敏**。`Authorization:
  DeepL-Auth-Key <key>` / `Api-Key: <key>` 此前只吞掉 scheme 名,密钥本体存活。
  模式改为消费任意 scheme 词后的整个凭据。
- **[S2/S3/S4] 本地桥接 `babeldoc_server.py` 无认证攻击面加固**。完整 PDF 服务
  模式下,插件把 API 密钥 + 整篇 PDF POST 到本机 11017 端口的桥接。此前桥接:
  (a) 用请求体的 `filename` 字段直接拼路径 —— `../` 或绝对路径可**任意文件
  写入**(以桥接进程身份覆盖 `~/.zshrc` → 本地代码执行);(b) 不校验 Host/Origin
  —— 恶意网页可发简单请求远程触发上面那个写(CSRF/DNS-rebinding);(c) 无认证
  —— 同机其他用户抢占端口即可窃取密钥。现在: filename 强制取 basename;每个
  请求校验 Host 头必须为 loopback;桥接启动生成一次性会话令牌写入 0600 令牌
  文件,所有请求必须携带匹配令牌(网页与其他用户读不到该文件);插件在**发送
  密钥之前**先经 `/handshake` 用 HMAC 验证服务端确实是本尊(抢占端口的冒名者
  不知令牌、产不出正确 HMAC → 插件中止,密钥永不出手);请求体加 300MB 上限
  防 DoS。桥接退出时清除令牌文件。
- **[附带] registerSecret 下限 4→8**: 短串(如 "test")不再被当密钥,避免把
  所有含该子串的日志打成碎片(诊断自毁)。

### Tests

- 729 项(+6): URL 内密钥/AIza/gsk_/智谱脱敏、registerUrlCredentials 裸形态命中、
  重叠密钥长度降序、非 Bearer scheme、下限 8。桥接侧以独立脚本实测: safe_basename
  路径遍历中和、Host/令牌 403 门、handshake HMAC 正确且冒名者无法伪造、体积
  413、令牌文件 0600;插件 WebCrypto HMAC 与 Python hmac 逐位一致。

## [2.0.10] — 2026-08-22

2.0.6 审核第四批(P3 收尾): 几何审计过度回退、保留原文覆盖模式兜底、
生命周期清理口径、池空窗与 quiesce 封口、传输层四项、占位符边界、
桥接与诊断隐私、诊断口径。**至此 2.0.6 审核报告全部清零。**
缓存格式不变(v5),不触发重译。

### Fixed

- **[P3] 几何审计不再按陈旧快照过度回退**。整轮处置基于处置前的违例
  清单 —— 同轮内先处置的 offender 收缩后,归责给后处置者的重叠可能已经
  消失,但它仍被处置: 只有靠扩展才放得下的块被**永久**回退成英文。现在
  处置每个 offender 前用当前几何重算该条违例(纯函数 violationStillPresent,
  单测覆盖 overlap/occludes-preserved/out-of-page 三型),已低于容差则跳过。
- **[P3] 「查看保留原文」在覆盖模式下真正可见**。flashKeptIndicator 去掉
  `?? parentElement` 兜底 —— display:none 面板里 offsetParent 为 null 时
  它曾画出 0 几何的隐形标记并返回非 null,把回退路径屏蔽(P2-19 修复的
  残余症状)。现在定位不了如实返回 null;覆盖模式下回退前先切到左右对照,
  面板高亮才看得见(构造处注释承诺的行为首次真正实现)。
- **[P3] 生命周期清理口径统一**。disposeWindow 的窗口归属判定不再经
  Zotero.getMainWindow() 兜底 —— 缺 _window 的 reader 不会再被归到「碰巧
  的窗口」而误杀/漏杀,「归属不明保守保留」名副其实;tab-close notifier
  与 dispose() 补齐 lastTranslatedMode/busy 清理(三条销毁路径一致);
  胶囊样式表 pm-capsule-style 随窗口关闭/插件关停一并移除。
- **[P3] 池空窗与 quiesce 封口**。startTranslating/重启现在等 rebuildPool
  完成(内含密钥查询,可达秒级)再排任务 —— 多服务商池的首屏页缓存键与
  lane 不再落在错误引擎白重译;quiesce 等待期间轮询/滚动的 setCurrentPage
  一律 no-op,配置切换的静默窗口不再有侧门(P1-9 的最后一块)。
- **[P3] 止损记忆近似 LRU**。500 上限裁剪按插入序删前半,而 Map 更新不
  刷新序位 —— 正被 count≥2 保护的热点段(页眉/重复题注)若首次失败早会
  先被淘汰。更新计数时 delete 再 set,最近失败的排到队尾。
- **[P3] 传输层四项**。mapHTTPError 补 408→TIMEOUT(可重试,此前落进
  不可重试 UNKNOWN 过早放弃)与 DeepL 456→QUOTA_EXCEEDED(此前报
  「Unexpected API response」);requestText/requestTextWithURL 与 requestJSON
  一致地携带 Retry-After、XHR 回退报告 responseURL;**无 scheme 的自定义
  Bing Base URL fail-closed**(此前解析失败被静默当「未覆盖」照旧出网微软
  —— 2.0.1 要堵的路径换了个入口,现在按自定义覆盖处理、报错让用户看见);
  免费引擎长段分片重组对 CJK 目标语言空串拼接,切点不再留句中空格。
- **[P3] 裸占位符边界不吞 "PM2.5" 类真实文本**。verify/restore 的裸形态
  边界从 `(?!\d)` 收紧为 `(?![\d.])` —— 环境/医学论文正文的 "PM2.5" 不再
  满足 ⟦PM2⟧ 的裸形态: 真丢失时校验如实报告(重试不再被抑制),restore
  的裸回退不再把 "PM2" 前缀替换成公式原文撕坏正文(经回退验证)。
- **[P3] 桥接与诊断隐私**。babeldoc 桥接的 API 密钥改经环境变量传递
  (命令行参数对同机用户经 ps 可见,与 pdf2zh 分支同一纪律);引擎失败
  只回传退出码+末行(完整 stdout 尾部可能含论文文本);任务临时目录在
  终态 30 分钟后自动清理(原文/译文 PDF 不再长期落盘);pdfService 的
  HTTP 错误片段与桥接 message 先脱敏再截断;diagnose() 输出整体过脱敏
  (用户会整段贴进 issue,Base URL 里可能被放了 ?key= 形式凭据);
  preferences 面板唯一直通的日志出口也改走脱敏。
- **[P3] 诊断口径补齐**。placement 统计全类目(imageExcluded/tooSmall/
  tableIntentional 等)进入诊断导出 —— P2-15 阈值收紧后误拒面变大能被
  发现;页面缓存条目附记实际产出引擎 producedBy(仅诊断,不入键不参与
  校验)—— 轮换页的 key.provider 是规范引擎,不再误导人工检查,v5 的
  已知残余(改实际引擎参数不失效这些条目)在代码注释中如实记录。

### Tests

- 723 项(+4 新增,另 3 项随语义更新): 陈旧违例重验三型、display:none
  兜底、408/456 分类、fail-closed 判定、CJK 拼接、PM2.5 占位符往返。
  占位符边界经「回退修复 → 测试变红 → 恢复 → 变绿」验证有效。

## [2.0.9] — 2026-08-22

2.0.6 审核第三批(状态正确性 + 传输): 设置窗口静默重启、compress 丢失唤醒、
预取页记账、Gemini 禁思考自愈、代理对劈切、文章流滚动抖动。缓存格式不变
(v5),不触发重译。

### Fixed

- **[P2-4] 设置窗口改配置不再绕过静默重启**。缓存身份(温度/端点/推理
  强度/术语表/不译词/提示词/引擎/语言)在落盘时现读 pref —— 此前只有
  菜单栏换语言/换引擎走「先静默再改」,Preferences 窗口改这些配置时没有
  任何会话通知: 翻译中改温度,旧配置译文会按**新** settingsHash 落盘,
  v5「改配置旧译文失效」被反向绕过。现在会话对每个参与身份的 pref 注册
  观察者,去抖(400ms,关设置窗口常一次落多个 pref)后走与菜单相同的
  quiesce+restart,并把面板头部同步到新配置;菜单路径 apply() 期间置
  重入闸避免双重启。
- **[P2-10] compress 在飞期间的重渲染不再永远无人处理**。压缩请求可飞
  120 秒,期间页面因缩放/更新重渲染 —— 新渲染的适配流程被 compressPending
  入口闸挡回后既不缩字、不回退、不清点,也无人接力: 未适配块永远保持
  英文且不计 kept,胶囊停在「正在适配排版」直到手动刷新。现在被挡回的
  渲染记为待办(只保留最新一次),在飞压缩的 finally 里续跑,旧渲染的
  过期 token 由既有的 live() 检查自然拦截。
- **[P2-11] 预取页的记账不再被「仅当前页」早退跳过**。预取页(±1~2 页)
  settle 时通常不是当前页,旧实现把 translatedPages 与 lastPartial 记账
  连同胶囊推送一起跳过: 用户滚到该页后槽位已渲染不再触发 settle,常驻
  圆环显示「点击翻译本页」而该页实际已翻完;「查看保留原文」直达定位
  也失效。记账现在先于早退执行,胶囊推送仍只对当前页。
- **[P2-13] Gemini「禁用思考」撞上不许关思考的模型时自愈**。
  gemini-2.5-pro 不允许 thinkingBudget:0 —— 此前用户选「禁用思考」则每个
  请求都 400 且无自愈,通道永久失败;「测试连接」的探针更是无条件带
  budget 0,该模型恒失败且报文毫无指向。现在 400 且响应提及
  thinking/budget 时剥掉 thinkingConfig 重试一次(与温度自愈同构),
  并按 endpoint|model 记住,后续请求直接不带;探针同样兜底。
- **[P2-14] 免费引擎长文硬切不再劈开代理对**。数学正文常含星面字符
  (𝑥、𝛽 等占两个 UTF-16 单元);超长无句读段按固定步长硬切会把代理对
  劈成孤立半个 —— encodeURIComponent 对它抛 URIError(非
  PaperMirrorError),整页被包成不可重试的 UNKNOWN 永久失败。切点遇
  高位代理自动退一位(经回退验证)。
- **[P2-15] 文章流滚动不再对 PDF 连发 navigate**。article 分支的每个
  scroll 事件都无条件回调 onScrolledToPage —— 同步滚动链路把 PDF 反复
  钉回该页起始位置,滚动面板时左侧持续抖动回跳。现在与 page 分支同
  规则,仅页号变化时回调。

### Tests

- 716 项(+2): Gemini 思考拒绝判定五态、星面字符多步长切分无孤立代理。
  P2-14 经「回退修复 → 测试变红 → 恢复 → 变绿」验证有效;P2-4/10/11/15
  为 UI/生命周期接线,依赖类型检查与全量回归(716 项无一破坏)。

## [2.0.8] — 2026-08-22

2.0.6 审核第二批(修不彻底收尾): 参考文献墨迹默认失效、刷新换引擎实质失效、
胶囊跨标签串扰、bingFree 错误分类被洗。缓存格式不变(v5),不触发重译。

### Fixed

- **[P2-5] 参考文献墨迹遮挡在默认配置下完全失效**。2.0.4 的 P2-14 修复
  (inkObstacles)从 input.blocks 里挑 isReference 块作遮挡物 —— 但「不译
  参考文献」(默认)时,条目在**提取层**就被丢弃,根本不在 blocks 里,
  遮挡物集合拿不到任何几何: 译文照样长进参考文献叠印、几何审计照样失明,
  原始缺陷在最常见配置下原样存在(2.0.4 的单测直接喂 isReference 块,
  测不出这个集成层缺口)。现在提取层把参考文献条目保留为
  translationMode:'preserve' 的纯几何块 —— 不进翻译、不计进度、不参与
  替换、文章流视图跳过渲染,只作为墨迹几何供扩展避让与审计看见。
- **[P2-6] 多引擎池下「刷新本页」的引擎轮换实质失效**。2.0.5 的 P2-16 让
  段落缓存 context 使用规范引擎(不含轮换偏移)—— 读写键恒等是对的,但
  它摧毁了轮换刷新的前提: 'normal' 刷新把旧引擎的全部合格段落原样从
  段落库读回,零请求发向新引擎,页面逐字节不变,「对这家译得差,换一家
  重来」变成空操作,日志却打印「刷新 → provider B」。新增 'rotate' 刷新
  模式: 发生轮换时绕过段落库让新引擎真正重译,但不像 'force' 那样清空
  全局止损记忆;单引擎刷新仍是 'normal'(复用合格段落只补缺)。两个修复
  目标(缓存恒等 × 轮换重译)从此显式共存(经回退验证)。
- **[P2-9] 状态胶囊按类名在整个主窗口 document 查找 → 同窗口多阅读标签
  跨会话串扰**。对照模式下两个标签页的面板共享同一主窗口 document:
  会话 B 的每次渲染都命中会话 A 先建的胶囊,把 B 的页码/进度画进 A 的
  视图,B 自己没有胶囊,重试/取消按钮作用错文档。胶囊现在由实例自持
  元素引用(宿主切换时按 parentElement 判断重挂),render/hide/remove
  只动自己的元素,绝不按类名横扫 document(经回退验证)。
- **[P2-12] bingFree 把 429/超时/网络错误统一洗成 BAD_RESPONSE**。
  双通道(抓取 + Edge 兜底)组合错误此前丢弃底层分类: RATE_LIMITED 的
  retryAfterMs 丢失 → 429 走 400ms 盲重试而非 Retry-After 路径,真实限流
  下继续锤打;laneFeedback 收不到 'rate'/'timeout',bing-free 车道的自适应
  限流失明;TIMEOUT「只试一次」的止损被绕过;Edge 403 的不可重试属性被
  洗成可重试。组合错误现在继承主通道的 code/httpStatus/retryAfterMs/
  retryable,双通道摘要只进 message(经回退验证)。

### Tests

- 714 项(+5): 参考文献 preserve 块与 inkObstacles 集成链(提取 → 遮挡物
  选择)、'rotate' 绕段落库且不清全局止损、双胶囊互不接管、组合错误分类
  继承三态。P2-6/9/12 经「回退修复 → 测试变红 → 恢复 → 变绿」验证有效。

## [2.0.7] — 2026-08-22

2.0.6 审核第一批(数据完整性 + P1): 会话打开/销毁竞态、三处「已获得的译文
被丢失或复活」的落盘时序缺陷、密钥删除残留。缓存格式不变(仍为 v5),
本版**不**触发重译。

### Fixed

- **[P1-1] open() 与 destroy() 竞态: 销毁后继续注册轮询与事件监听,永不
  解除**。open() 里的文件哈希解析、提取器预热、密钥查询三个 await(合计
  可达数秒)期间关标签/关窗口/禁用插件都会 destroy() —— 此前 open() 继续
  执行,在销毁**之后**注册 PDF 事件监听与 350ms 轮询(tick 首行的
  destroyed 检查只让它空转,不停止),每次「打开翻译后迅速关闭」泄漏
  一条;隐私未接受分支还会对已置 null 的对象调用抛 TypeError。现在每个
  await 之后都有 destroyed 闸。
- **[P2-1] 段落库合并写在基底读取瞬时失败时,以空库覆盖整个 context 的
  段落缓存**。合并写是「读旧库 → 并入 → 原子覆盖」,此前任何读失败都被
  当作「库不存在」—— 文件被占用/权限抖动/网盘同步这类瞬时失败(正是
  P2-13 在读路径认定「内容很可能完好」的那类)会让本次写以 `{}` 为基底,
  把只含几条新增的文件覆盖上去,积累的全部段落译文静默丢失。现在与读
  路径同一条规则: 只有内容确认损坏 (SyntaxError) 或 context 不符才允许
  重建;瞬时失败直接放弃本次合并 —— 少写几条远好过丢整库。
- **[P2-2] 「刷新本页」后,在飞的 compress 请求会用旧译文覆写新缓存**。
  压缩重译请求最长可飞 120 秒,闭包捕获的是**旧** state;刷新只取消了
  主页面任务,compress 键漏网 —— 旧请求返回后把旧译文集写回页面缓存与
  段落库(写队列串行、后写者胜),表现为「反复刷新反复回退」。现在
  刷新一并取消 compress 任务,且 compress 写盘前校验状态身份,孤儿
  结果不落盘(双保险,经回退验证)。
- **[P2-3] 「刷新全部」先清盘后等取消: 被丢弃的译文复活并被复用**。
  retranslateAll 此前用同步的 resetAll()(只发 abort 不等待)后立即清
  缓存目录;被 abort 任务的 persistPartial 是 fire-and-forget,落盘发生
  在清理**之后** —— 用户明确要求丢弃的译文复活,紧接着的 normal 运行把
  它当段落命中复用,「强制全量」不成立。现在 (1) retranslateAll 改为
  await resetAllAndWait() 后再清盘;(2) persistPartial 在任务解绕前
  await 落盘完成,取消等待从此真正等得到它(经回退验证)。
- **[P2-7] 删除密钥不清明文回退副本: 被删密钥继续生效并留在 prefs.js**。
  密钥曾因凭据库不可用明文落进 apiKeyFallback;凭据库恢复后用户清空
  密钥,删除走安全分支直接返回 —— 明文留在配置文件里随备份/同步外流,
  且读取回退继续把它交给请求使用,UI 却显示删除成功。现在删除分支一并
  清除回退副本(经回退验证)。

### Tests

- 709 项(+5): 段落库瞬时失败保全/损坏重建、孤儿 compress 不落盘、
  resetAllAndWait 等到取消路径的落盘、密钥删除清回退(两阶段:
  明文落库 → 凭据库恢复 → 删除)。P2-1/2/3/7 均经
  「回退修复 → 测试变红 → 恢复 → 变绿」验证有效。

## [2.0.6] — 2026-08-21

审核第五批(P3 收尾): 免费引擎取消串扰、缓存卫生三项、止损与状态四项、
几何审计整轮处置、内存滞留、临时文件加固。至此审核报告 P0/P1/P2/P3 全部
处理完毕(仅一项暂态自愈问题明确暂缓,见下)。

**注意: 缓存 schema 升级到 v5(端点/高级参数进入缓存身份),每个文档首次
打开会做一次全量重译,旧缓存文件自动清理。**

### Fixed

- **[P3] 微软免费引擎: 一个标签页取消会打断其他标签页的共享认证**。
  Edge 匿名认证与翻译会话的共享 in-flight promise 绑定了**第一个到达者**的
  AbortSignal —— 第一个页面取消(关页/翻页)时,同刻等待的所有其他页面
  一起收到 CANCELLED。共享请求现在只受超时约束,每个调用者用自己的
  signal 与共享 promise 赛跑: 自己取消只影响自己。
- **[P3] 缓存键仍漏端点与高级参数 → schema v5**。apiBaseURL / apiPath /
  reasoning / maxOutputTokens / temperature / useContext 都会改变译文
  (不同端点/代理后面可能是不同模型),却都不在缓存身份里 —— 改了配置仍
  命中改前的译文。六项折叠为 settingsHash 进入页面键与段落 context。
- **[P3] 止损记忆把并发页面算成多次运行**。同一段文字(页眉/重复题注)
  跨页并发失败,一轮并发就打满阈值 2,从此永远保留原文 —— 但那些运行
  开始于第一次失败被记下之前,不是「明知失败仍再试」。以单调运行序号
  判轮次: 同轮并发只计一次,失败之后新发起的运行才算下一轮。
- **[P3] 刷新全部/换配置不清止损记忆**。resetAll 比 retranslatePage('force')
  意图更强,却反而不清 —— 被止损跳过的段落在「全部重来」之后仍被跳过。
  现在 resetAll/resetAllAndWait 一并清止损与提取僵尸表。
- **[P3] 孤儿状态的迟到错误会推错误胶囊**。页面被重译接管后,旧任务迟到
  的失败仍把 error 写到已被替换的状态并通知 UI —— 正常重译中的页面闪出
  过期错误。错误分支现在与 CANCELLED 分支一样先做状态身份校验。
- **[P3] 「全部由段落缓存服务」分支写入不完整页面缓存**。keepOrigin 非空
  (有段落被止损跳过)时也写页缓存 —— 下次打开当作完整命中,被跳过的段
  永远无法重试。与主路径同规则: 有未译段的页不落页面缓存。
- **[P3] 几何审计每轮只处置一个违例,上限 4 轮 → 系统性低报**。超过 4 个
  违例的页面,其余违例既不修也不进诊断。现在每轮处置全部违例(同轮同
  offender 去重),4 轮上限保留为病态防线;处置只收缩/回退,收敛性不变。
- **[P3] 内存滞留两处**。lastPartial 长期持有已卸载页的 strict 元素(含
  整页位图,约 26 MB)—— 轮询发现元素已断开即释放(「查看保留原文」对
  断开元素本就走回退路径,无功能损失);pdf.js RenderTask 在失败/超时
  放弃后从不 cancel —— 现在显式取消。
- **[P3] 缓存写队列表与 .tmp 残留**。writeQueues Map 只增不减 —— 队列排空
  后自删表项;原子写崩溃遗留的 `.json.tmp`(≥5 分钟未动)由启动清扫回收,
  新鲜 tmp(可能是进行中的写)保留。
- **[P3] 译文 PDF 临时文件可预测**。经 /tmp 中转的文件名固定、权限 0644,
  共享主机上可被预占/符号链接/读取。文件名混入密码学随机成分,写入后即
  收紧权限到 0600(尽力而为),导入后照常删除。

### Deferred

- 覆盖模式流式翻译期间,下扩可能短暂叠印尚未遮罩的相邻原文(审核标注
  **暂态自愈**: 页面排版完成即消失)。修复需要在流式阶段重画邻块遮罩,
  复杂度与收益不成比例,明确暂缓;如实记录在此。

### Tests

- 704 项(+8): raceSignal 取消隔离、schema v5 键失效、清扫 tmp 时效、
  止损并发轮次/重置/孤儿错误/不完整页缓存。止损并发计数经
  「回退修复 → 测试变红 → 恢复 → 变绿」验证有效。

## [2.0.5] — 2026-08-21

审核第四批(P2-16/18/19/20): 熔断后的引擎身份错位、明文密钥回退静默、
「查看保留原文」高亮不可见、多窗口活动会话解析错窗。缓存格式不变(仍为 v4),
本版**不**触发重译。

### Fixed

- **[P2-16] 熔断换引擎后,新引擎的 429 去砍旧引擎的限流上限;同页缓存
  读写键不一致**。两个错位同根: 引擎身份在不同时刻解析。(1) lane 反馈曾在
  页面开始时快照一次 —— 熔断把本页剩余请求切到引擎 B 后,B 的 429/timeout
  仍反馈到 A 的 lane,无辜引擎被降速、真正超载的引擎学不到任何东西;现在
  每次请求即时解析 lane,与实际发请求的引擎同刻同源。(2) 缓存键曾包含
  会话内瞬态的轮换偏移 —— 熔断切换后同一页 readCache 用 A 键、writeCache
  用 B 键,写进去的译文下次打开(偏移归零)永远读不回来;缓存身份现在只用
  **规范引擎**(不含瞬态偏移),同页读写键恒等、跨会话稳定。
- **[P2-18] 系统凭据库写入失败时,密钥明文落进配置文件且完全静默**。
  唯一痕迹是一条调试日志,设置界面照常显示「密钥保存在系统凭据库」——
  而明文密钥会随 profile 备份/同步外流。setApiKey 现在向调用方报告落库
  结果(secure / plaintext / failed / removed),设置界面在明文回退与保存
  失败时显式警告,并说明如何转回安全存储。
- **[P2-19] 「查看保留原文」高亮的是 visibility:hidden 的节点 → 什么也
  看不见**。保留原文块是被隐藏的译文 div,outline 和背景随节点一起隐藏;
  函数还凭「找到了节点」返回 true,把回退路径整个屏蔽。现在按节点几何画
  **独立的可见标记层**(隐藏不影响布局,矩形仍是原文位置),限时自动移除;
  一个标记都画不出时如实走回退路径。
- **[P2-20] 多主窗下 clearCurrentCache 等入口可能作用到另一个窗口的文档**。
  activeReader/activeSession 曾硬绑 Zotero.getMainWindow() —— 它不保证是
  用户正在操作的窗口。现在优先解析真正持有焦点的主窗口;没有主窗口持焦
  (如从设置窗口调用)时退回旧行为。

### Tests

- 696 项(+11): 熔断后 lane 反馈归属、密钥落库四种结果(驱动真实
  credentialStore,仅 mock 平台层)、可见标记层几何/限时移除/回退语义、
  多窗口焦点解析与容错。P2-16/20 经「回退修复 → 测试变红 → 恢复 → 变绿」
  验证有效。

## [2.0.4] — 2026-08-21

审核第三批(P2-12/13/14/15/17): 缓存哈希退化、误删缓存、排版对参考文献/表格
墨迹失明、图像容差带叠印、多窗口会话泄漏。

**注意: 本版缓存 schema 升级到 v4(哈希算法修正使所有旧哈希失效),每个文档
首次打开会做一次全量重译,旧缓存文件自动清理。**

### Fixed

- **[P2-12] 缓存哈希第二条 lane 退化,64 位哈希实际只有 32 位强度**。
  `fnv1a64` 的第二条 lane 只混入 `(c>>8) ^ (i&0xff)` —— ASCII 文本下
  `c>>8 === 0`,h2 完全由字符串**长度**决定:任意两段等长英文文本共享后
  8 位十六进制,实测 40 万条等长字符串中已出现完整 64 位碰撞。碰撞的后果
  是**串译文**: 两段不同原文命中同一段落缓存,一段的译文顶到另一段头上。
  新公式把字符值与位置一起混入第二条 lane(乘法常数也与第一条错开),
  40 万等长字符串扫描零碰撞。哈希全变 → schema v4 强制迁移。
- **[P2-13] 缓存清扫会误删「只是读取失败」的文件**。`sweepStaleCacheFiles`
  的 catch 分支把任何读取异常都当作「文件坏了」直接删除 —— 但文件被占用、
  权限抖动、网盘同步这类**瞬时 I/O 失败**里,文件内容往往是完好的当前版本
  缓存。启动时一次磁盘抖动就能把整个缓存清空,所有文档全量重译。现在只有
  内容确认无效(schemaVersion 不符,或 JSON 解析 SyntaxError)才删;瞬时
  失败记日志、留文件。`readPage` 的同型 catch 一并修正。
- **[P2-14] 边界扩展与几何审计对参考文献/表格墨迹失明 → 译文叠印原文**。
  `isReference` / `type==='table'` 的块被排除出替换流水线(它们永不翻译),
  但它们的原文墨迹仍在位图上 —— 而扩展算法的遮挡物列表和几何审计的
  preserved 列表都只看参与替换的块,于是译文盒子可以径直长进参考文献里
  叠印,审计也看不见这次违例。新增 `inkObstacles` 集合(纯几何成员),
  两处共用: 扩展在墨迹近边前截断,审计看得见并回退压住墨迹的块。
- **[P2-15] 图像重叠 15% 容差带内「英文透出 + 中文叠印」**。遮罩对图像是
  硬裁剪(`clearRect` 保证 mask∩image === 0),但文本盒的**准入**曾容忍
  ≤15% 的重叠 —— 容差带内的块照样替换: 遮罩盖不到图像里的那部分原文
  (英文透出),译文 div 却覆盖整个盒(中文压在图上)。准入阈值收紧到 2%
  (几何噪声级,与审计容差同数量级),准入与遮罩从此服从同一条规则。
- **[P2-17] 多窗口: 关闭副窗口整窗泄漏翻译会话**。Zotero 的「在新窗口中
  打开」场景里,关窗口不触发 tab close notifier,也不走插件 shutdown ——
  `onMainWindowUnload` 此前只从已知窗口集合里删掉窗口对象,属于该窗口的
  ReaderSession(定时器、in-flight 请求、对已死窗口的 DOM 引用)全部泄漏。
  现在按窗口维度销毁会话并清理注入的样式表;归属判定失败的会话保守保留。

### Tests

- 685 项(+12): 碰撞对回归与哈希稳定向量、清扫误删场景(瞬时失败 vs 损坏
  JSON)、墨迹遮挡物选择/扩展截断/审计可见性、图像准入阈值、按窗口销毁。
  P2-12/13/15 均经「回退修复 → 测试变红 → 恢复 → 变绿」验证有效。

## [2.0.3] — 2026-08-19

修复「微软翻译里出现了 Gemini 的 API Key」—— 一个显示错乱 + 一个真正的凭据错存。

### Fixed

- **[P1] 换服务商时,上一家的密钥会被写进这一家的输入框**。`loadApiKey()` 先
  发起查询、`await` 之后直接写回输入框,中间没有任何代次或归属校验。而
  `getApiKey` 查询系统登录管理器最长可等 **4 秒**
  (`LOGIN_LOOKUP_TIMEOUT_MS`)—— 用户在这 4 秒内切到别的引擎时,先前那次查询
  会**后**返回,把上一家的密钥写进现在这一家的框里。这就是「微软翻译里出现了
  Gemini 的 API Key」。现在加了载入代次 + 服务商归属双判,陈旧结果直接丢弃。
- **[P1] 承上:随后的保存会把密钥真的存到错误的引擎名下**。`commitApiKey` 在
  change/blur 时无条件 `setApiKey(当前服务商, 框里的值)` —— 于是 Gemini 的密钥
  被持久化到 `bing-free` 名下,从显示错乱升级为**凭据错存**。现在只有「框里的值
  确实属于当前服务商」时才保存;归属不符或不适用一律不写(宁可不保存)。
- **[P2] 免费引擎与 Ollama 仍然显示可编辑的密钥输入框**。模型行与高级参数区都
  按引擎显隐,唯独密钥区没有任何显隐逻辑(说明文字虽写着「免费引擎与 Ollama
  无需填写」,框却照常可输入,正是上述错存的入口)。现在整节按
  `requiresApiKey` 隐藏,并在切换到免费引擎时清空输入框、解除归属。

### 测试

- 668 → **673**。含注册表事实校验(bing-free / google-free / ollama 确为
  `requiresApiKey: false`,而 gemini / anthropic / openai / deepl 不被误伤)与
  三条源码级不变量闸(显隐、陈旧写入闸在写回之前、归属校验早于 setApiKey)。


## [2.0.2] — 2026-08-19

内部审核第二批(稳定性与缓存一致性)。缓存 schema 升至 v3,首次打开会整体重翻一次。

### Fixed

- **[P1] 压缩重试期间换页/滚动 → 该页排版状态机永久死锁**。`compressPending`
  的清理只写在 `.then` 的成功分支里,而 `.catch` 分支是清理的 —— 这个不对称
  本身就是遗漏。压缩请求可飞行 120 秒,期间只要该页被重渲染或滚出可视区,
  `live()` 变假走早退,标记就永久留下。此后该页任何渲染都被入口闸挡回,
  `reportPlacement` 永不执行:几何复核不跑、计数不产出、胶囊停在「正在适配
  排版」、未适配块永远显示英文且不计入。改为 `.finally()` 兜底清理
  (`.then` 里递归前那次提前清除必须保留,否则下一轮会被自己挡回)。
- **[P1] 后台标签页里 splitView 的定时器链无限增殖**。`applyRatio` 的
  「版面未就绪」重试是裸 `setTimeout`:没有句柄、没有计数、不看 `destroyed`。
  Zotero 隐藏非选中标签的容器,隐藏元素宽度恒为 0,链永不终止;而会话每
  350ms 调 `refreshLayout` → 又开一条 20Hz 链,互不取消。后台几分钟即每秒上万
  次强制重排;每 350ms 一条 warn 约 13 秒冲光 40 条日志环形缓冲,把真正的错误
  全挤掉;`destroy()` 后残链还会把已恢复的阅读器重新钉死在 50% 宽。现在:单链
  (清除上一条)、40 次上限、`destroyed` 守卫、`destroy()` 清理,且漂移看门狗
  在容器不可见时直接返回(宽度 0 不是漂移,是没在布局)。
- **[P1] 「刷新本页」被"已中止但仍在 active"的旧任务挡回**。`cancel()` 对运行中
  的任务只发 abort,它要等 `run()` reject 后才在 `execute` 的 finally 里离开
  `active`。旧代码同步 `cancel` + 立即 `ensurePage`,于是 `isScheduled` 仍为真
  被 early-return —— 而页面状态已删、新任务没入队,该页彻底不翻译(点了圆环
  什么都没发生,必须再点一次)。新增 `cancelAndWait` / `cancelAllAndWait`:
  等任务真正解绕完再返回。**不采用「abort 后立刻从 active 删除」**——`active`
  同时承担槽位与并发记账,提前删会造成超额,且同 key 新任务入 active 后会被
  旧任务的 finally 误删。
- **[P1] 配置切换瞬间旧引擎译文写进新引擎的缓存键**。缓存身份不是在产出译文时
  快照,而是在**落盘时**现读 pref;而取消一个翻译中的页面会走 `persistPartial`
  落盘。旧顺序是「先 setPref 再取消」,于是引擎 A 的译文写进引擎 B 的段落库,
  重启后依旧(切到 B 却一直读到 A 的译文)。改为**静默 → 改配置 → 重启**:
  落盘一定发生在旧身份下,各归各的键,切回去还能命中。
- **[P1] 改术语表 / 不译词列表不会让页面缓存失效**。段落 context 早就带
  `glossaryHash`,但页面缓存是**先命中的一层**且键里没有它 —— 于是「改了术语表
  要让旧译文失效」的意图对所有已翻译页面完全落空(用户会以为术语表坏了)。
  不译词更彻底:它改变占位符掩蔽从而改变译文,却两层都不在。现在两者都进入
  页面键与段落 context,并集中到一处 `configIdentity()` 构造,防止两边漂移。
- **[P2] 含表格的页面上「重译此段」永远写不进页面缓存**。完整性判据用了
  `state.blocks.every(...)`,而 preserve 块(表格数据单元格等)永远不会进
  `translations` → 条件恒假。表现是重译当场生效、重开文档又回到旧译文,
  且反复重译反复丢失。加上 preserve 豁免,与 compress 路径判据一致。

### Changed

- `CACHE_SCHEMA_VERSION` 2 → 3。旧条目读到即删,残留文件由启动时的惰性清扫
  处理。**升级后每篇文档首次打开会整体重翻一次,属预期。**

### 测试

- 654 → **668**。其中 4 条经过「回退修复 → 用例失败 → 还原 → 通过」的有效性
  验证:splitView 的单链与上限、刷新本页的取消竞态、以及 2.0.1 的看门狗 abort。
  splitView 用最小 DOM shim 驱动真实实现,断言单链、有上限、destroy 后不再改 DOM。


## [2.0.1] — 2026-08-19

安全与可靠性专项(内部审核第一批 P0/P1)。

### Fixed

- **[P0] 所有传输层失败被误判为「不可重试的 HTTP 0」**。`successCodes: false`
  让 Zotero.HTTP 对任何状态码都 resolve —— 包括 XHR 的 status 0(断网、连接被拒、
  TLS/代理失败、离线、`xhr.abort()`)。于是那个把网络错误映射成
  `NETWORK{retryable:true}`、把 abort 映射成 `CANCELLED` 的分支**永远不执行**,
  这些失败一路落到兜底 `UNKNOWN{retryable:false}`。后果有两层:网络错误从不重试、
  自适应限流收不到任何反馈;更糟的是**空闲看门狗被打废** —— 它 abort 后拿到
  UNKNOWN 而非 CANCELLED,那句「Page N 停滞 150 秒,请点重新翻译」的 TIMEOUT
  转译从来没有触发过。现在 `send()` 在返回前还原 status 0 的真实语义
  (aborted → CANCELLED,否则 → NETWORK 可重试),`mapHTTPError` 对
  `status <= 0` 加了二道防线。同文件的 XHR 回退路径本来就是这么做的,两条路径
  现在一致了。
- **[P0] 「诊断」按钮把整页原文复制到剪贴板**。1.1.7 把「语料」并进了「诊断」,
  于是一个叫「诊断」的动作会把当前页全部原文 span 与文献标题放进剪贴板 ——
  用户在「提交诊断」的心智下不会预期这一点,粘进 issue 就等于公开了未发表稿件。
  现在拆回两个按钮:**「诊断」只含脱敏指标**(不含原文、译文与密钥);
  **「语料」**独立,名字、tooltip、载荷里的 `note` 字段与复制后的提示都明说
  含本页原文。两者仍共用调试日志开关显隐。新增源码级回归闸,防止再次并回。
- **[P1] 错误消息把响应体片段原样带进 UI**。`mapHTTPError` 会把上游响应体前
  200 字节拼进 `error.message`,而 `sanitize()` **只在 logger 里**,UI 的错误行是
  直接 `textContent` 显示 message 的。自建中转网关(one-api 之类)令牌过期时常
  返回 HTTP 400 且响应体回显密钥 → 密钥显示在面板上、进用户截图。现在响应体
  **先脱敏再截断**(密钥被 200 字切成两半时同样命中)。
- **[P1] Ollama 无条件放行明文 HTTP**。`allowInsecureHTTP: () => true` 忽略入参,
  而注释写的前提「默认 URL 是 localhost」并不成立 —— Base URL 是用户可编辑字段。
  于是 `checkEndpointURL` 的回环判断与「允许 HTTP 端点」首选项对 ollama 完全失效:
  填一个公网地址就明文跨网传输整篇论文,若该端点还配了密钥,
  `Authorization: Bearer` 也会一并明文发出。现在与 `custom` 一致 —— 回环地址
  仍无条件放行(默认用法不受影响),非回环需显式开启该首选项。
- **[P1] 默认引擎 bing-free 的 Edge 兜底绕过用户 Base URL**。`translateViaScrape`
  尊重 `apiBaseURL`,但兜底的 `translateViaEdge` 端点是硬编码常量
  (`edge.microsoft.com` / `api-edge.cognitive.microsofttranslator.com`),完全不读它。
  机构用户把 Base URL 指向内网代理正是为了让论文不出网,代理返回一次 502 就会让
  同一段原文被 POST 到微软端点,UI 上毫无提示。现在配置了自定义 Base URL 时
  **禁用 Edge 兜底并明确报错**(宁可让用户看见,也不静默出网)。判据与
  `resolveBingApiBase` 共用同一套规则,并有测试钉住两处不得漂移。

### 测试

- 648 → **654**:status 0 的两种成因(断网 / 看门狗 abort)走真实 `send()` 路径、
  响应体脱敏 4 例、Ollama 明文门槛(含「被拒时零字节出网」断言)、
  bing-free 判据一致性、诊断隐私不变量(行为 + 源码级回归闸)。


## [2.0.0] — 2026-08-15

支持 Zotero 10(同时保持 Zotero 9 兼容,一个 xpi 覆盖两个大版本)。

### Changed

- **manifest 兼容边界放宽到 Zotero 10**: `strict_max_version` 9.0.* → 10.0.*,
  `strict_min_version` 保持 9.0。依据官方《Zotero 10 for Developers》:
  Zotero 10 与 9 使用同一 Firefox 140 ESR 平台,插件破坏性改动集中在
  ZoteroPane 集合选择(getSelectedCollection→getSelectedCollections 等)、
  ItemTree(collectionTreeRow 移除)、Search(fulltextWord 移除 /
  addCondition legacy 参数)与 Zotero.CookieSandbox 移除 —— 本插件对以上
  API 零使用(逐一 grep 核对)。官方文档明确 Zotero.Reader、PDF 阅读器、
  PDFWorker、PreferencePanes、Notifier、HTTP、Prefs、Fluent 本地化均无变化。
- updates.json 随 manifest 自动携带新边界;版本号跳到 2.0.0 标记大版本支持。

### 注意

- 阅读器适配层使用的 `_internalReader._primaryView._iframeWindow` 等属
  Zotero 未公开内部结构,官方未声明变化,但**建议在 Zotero 10 正式环境跑一次
  冒烟**:打开 PDF → 开启对照 → 翻页 → 表格页 → 深度解析 → 设置面板。
  若内部结构有变,插件会以 READER_API_CHANGED 明确报错而非静默失败。


## [1.3.1] — 2026-08-15

单点修复:temperature 被拒的报错误导性极强,且不可操作。

### Fixed

- **参数被拒的 400 被误标成「模型名被拒」**。temperature/reasoning_effort 的
  拒绝语里常带 "with this model" 字样,mapHTTPError 的 /model/ 分支把它包装成
  "The API rejected the model name" —— 与真实原因完全无关。新增参数被拒分支
  (先于模型名判定),消息保留原始片段(自愈匹配依赖它)。
- **显式温度被拒时给出可操作提示**。1.3.0 起显式设置的温度被模型拒绝会按
  配置错误浮出(设计如此),但用户看到的是一段原始 JSON。现在明确提示:
  「当前模型不接受你显式设置的温度,请在 设置 → 高级 清空温度后重试」。


## [1.3.0] — 2026-08-15

缓存一致性专项(外部审核第一批 P0/P1 全部处理 + 第二批多项)。缓存 schema
升至 v2,旧缓存启动时自动清理 —— 本版首次打开文档会整体重新翻译一次,属预期。

### Fixed

- **[P0] 提示词版本从未真正升级**。代码里 `PROMPT_VERSION = 2`,但缓存键读的
  是 prefs.js 固定为 1 的 promptVersion 首选项 —— v1 旧缓存持续命中,v2 的
  字符预算/紧凑要求从未让缓存失效,修复后仍会复现旧的长译文。缓存键改为直接
  使用代码常量;删除该首选项。
- **[P0] 自定义提示词不在缓存身份里**。用户改了自定义提示词仍读出改前译文。
  新增 customPromptHash,页面键与段落 context 都包含它。
- **[P1] 页面缓存命中不验证完整性**。缓存文件合法即整页标 done —— 只含部分
  译文的条目(压缩重排路径曾写入部分结果,已一并堵住写入口)让页面"显示完成
  但翻译不全"。现在:过期 ID 丢弃、每条译文重新过 looksTranslated 验收、
  所有当前可翻译块必须全有译文才算命中;不完整时仅作预填,缺失块照常翻译,
  绝不允许"不完整缓存 → done"。
- **[P1] 段落缓存并发覆盖竞态**。写入是"读旧文件→合并→原子覆盖",原子写保证
  不了合并基于最新内容:页面 A、B 同时读到 {x},A 写 {x,a},B 写 {x,b},
  a 丢失 —— 表现为翻译成功但下次打开重新翻译、段落命中率异常低。每个缓存
  路径一条写队列,同一文件的 读→合并→写 串行执行;页面缓存写同样入队。
  新增并发测试(人为读延迟复现旧竞态窗口,2 路与 8 路并发全部存活)。
- **[P2] 参数自愈记忆按端点隔离**。记忆键从 (provider, model) 扩为
  (provider, 归一化请求 URL, model) —— 端点 A 不支持某参数不再波及同名模型
  的端点 B。temperature 400 判定收紧为明确的"不支持参数/只允许默认值"形态,
  类型/越界等配置错误不再被吞;且只有插件自动加的默认温度会静默剥离重试,
  用户显式设置的温度被拒时作为配置错误浮出。
- **[P3] cacheSchema.ts 中的真实 NUL 字节**(与 1.2.2 修过的 advancedParams
  同病)。改为 `'\u0000'` 转义,文件恢复纯文本,运行结果不变。

### Changed

- `CACHE_SCHEMA_VERSION` 1 → 2;启动时惰性清扫 schema 不符的旧缓存文件
  (尽力而为,绝不阻塞启动)。

### Deferred(审核第二批余项,已知悉待做)

- 字体超时后迟到字体的只读复核;页面缓存键加入布局/块结构摘要(块源文已在
  键内,结构摘要待 DocumentIR 强化时一并做);排版结果的四态状态机。

## [1.2.6] — 2026-08-15

单点修复:推理模型拒收 temperature 导致「深度解析」/翻译请求 HTTP 400 中断。

### Fixed

- **temperature 的自愈,与 1.1.11 的 reasoning_effort 完全同构**。为翻译求
  确定性,DEFAULT_TEMP_PROVIDERS 默认发 temperature: 0;但推理模型
  (gpt-5.x / o 系列,以及各家「OpenAI 兼容」端点背后路由到的推理模型)只接受
  默认温度,直接回 HTTP 400「Unsupported value: 'temperature' does not
  support 0 with this model. Only the default (1) value is supported.」——
  「深度解析」当场中断,翻译走同一条 openaiChatExtras 同样会中招。现在识别该
  400 后剥掉 temperature 重试一次,并按 (provider, model) 记住,后续请求直接
  不发。postChat 的自愈泛化为逐参数:temperature 与 reasoning_effort 各最多
  重试一次,可在同一请求序列中先后触发;其余错误照旧抛出。新增 3 个单测
  (剥参重试 / 记忆生效 / 双雷区先后自愈)。

## [1.2.5] — 2026-08-15

封面页(前置件密集的首页)两处分类错误,合起来曾让整栏正文保留英文
(Chen 2023 radiomics 首页: 25 段中 18 段保留原文)。

### Fixed

- **正文行被批量误判为 heading**。页面正文字号取的是行字号的中位数;封面页
  的 7pt 作者单位(17 行)+ 8.5pt 摘要(18 行)在行数上压过 10pt 正文
  (28 行),中位数落在 8.5 —— 每行 10pt 正文 ratio=1.176 ≥ 1.1 全部进了
  heading 分支,逐行按 heading 翻译后大面积 unrecovered,译文页整栏英文。
  改为行字号的众数(0.5pt 桶、平票取大),该页正确解出 10。另给 heading 的
  纯 ratio 分支加防线:以小写开头(句中续写)或以连字符/逗号结尾(断词/
  断句)的行是散文续写,不判 heading —— title 分支不受此限
  (真标题的折行片段恰恰会以连字符结尾 "… Photon-"、以小写开头 "counting CT")。
- **首字下沉字符加冕整行,造出栏中假标题**。焊入首字下沉的行只有两个 span
  ("A" 25pt + 正文 10pt),按 span 计票平票、平票取大(本为防下标设计)把
  25pt 选成整行字号 → 该行 ratio 2.5 变成栏中假 title(Chen "Acauses…"、
  hakime "Chas been assessed…"、horst "Ttial and contrast…" 同病)。行字号改按
  字符数加权计票:1 个 25pt 字符 vs 50 个 10pt 字符不再是平票。假 title 消失
  后这些行还能与后续行正常并段。
  快照影响(均为修复而非回归,已审阅重生成): chen2023-p1(新增基线)、
  hakime2007-p2 / horst2024-p1(假 title 消失)、nejm-defuse3-p7(一个句中
  "heading" 转正文并使续写正常并段;表格单元完全不变)。其余 8 个语料
  字节不变。

## [1.2.4] — 2026-08-15

单点修复:密排双栏正文的栏底几行在译文侧显示为英文原文。

### Fixed

- **抵达页面底部页边的正文行被误判为页脚而丢弃**,于是严格排版没有遮罩它们,
  译文页在每栏底部露出整段英文原文(Booz 2019 首页两栏底部的
  "of lumbar disk herniation. More recently…" / "constructed from
  third-generation…" 等)。页眉页脚判定用的是页面上下 8% 的边带 + 「≤2 行且
  ≤140 字」的形状判据;设计上假设伸进边带的正文段落是多行的,可靠 `lineCount`
  排除——但当某页的栏未被合并成段落时,每一行都是独立的单行块,于是逐行落入
  边带被丢。改为:以小写字母开头(句中续写,如 "of…"/连字符断词 "agnostic…")
  的行按正文续写处理、不再丢弃;同时排除仍以小写排版的真页脚
  ("n engl j med 378;8 nejm.org February 22, 2018" —— 带域名 / 卷;期号),
  避免误留。新增 isRunningHeadOrFoot 单测,10 个既有真实语料快照零变化,
  新增 Booz 首页语料基线相应更新。

## [1.2.3] — 2026-08-15

单点修复:多行标题在译文侧"消失"。

### Fixed

- **换行到 3 行以上的论文标题被当成正文**,译文侧标题几乎看不见。
  分类器把 `title` 限定在 `lineCount <= 2`,而像 *"Virtual Noncalcium
  Dual-Energy CT: Detection of Lumbar Disk Herniation in Comparison with
  Standard Gray-Scale CT"* 这类标题会折到 3 行 —— 于是落回 `paragraph`。
  严格排版对段落用的是全页正文基准字号(≈正文大小)且不加粗,20pt 的大标题
  就被渲染成顶部一行很小的、不加粗的正文,肉眼看像"标题没了"。放宽标题的
  换行容忍到 `lineCount <= 4`,同时保留 `ratio ≥ 1.35`(明显大于正文)与
  `length < 250`(短)两道强约束 —— 多行的**正文**块远在 4 行之前就超过 250 字,
  因此不会被误判为标题。新增回归用例 + Booz 2019 首页语料基线快照。

## [1.2.2] — 2026-08-15

发布前整体审核(外部审阅)定位的问题,全部处理或明确缓交。本版无新功能,
全部是让"绿灯可信"的修复。

### Fixed

- **源码中的真实 NUL 字节**(1.1.11 引入)。`advancedParams.ts` 的
  `(providerId, model)` 记忆键用了字面 `\0` 作分隔符 —— 编辑器里不可见,但
  Git 从此把整个文件判为二进制:diff 不能逐行审阅、格式化与安全扫描工具跳过
  该文件。改为 `JSON.stringify([providerId, model])`,文件恢复为纯文本。
- **快照测试的假绿灯**。布局回归基线缺失时,测试曾"当场生成并通过"——报绿
  却没有任何被人审阅过的基线(长文件名 Aquino 语料的两份快照就是这样每次被
  临时生成的)。现在**缺失即失败**,只有显式 `UPDATE_SNAPSHOTS=1 npm test`
  才允许生成;两份长文件名 Aquino 快照补为正式基线随本版提交。本版起测试
  跑完后工作区必须零变化(fixtures 前后校验和一致)。
- **`document.fonts.ready` 永不 resolve 时整页译文不显示**。final 测量只挂在
  fonts.ready 上,字体源挂起时 final 永远不来,整页停在 provisional、一个块
  都不 reveal ——"翻译完成但页面还是英文"。`settleStrictPage` 加超时保险
  (默认 5s)与单次 final 闸:final 无论来自 ready、第二波、超时还是异常兜底,
  至多执行一次;元素断开后不再测量。三条新测试覆盖(永不 resolve → 超时
  final;超时先到 ready 迟到 → 闸住第二次;断开 → 无泄漏测量)。
- **压缩请求异常退出后页面永远停在「排版中」**。compress 的 `.catch` 只释放
  `compressPending`,从不触发 `reportPlacement` —— 最终清点、几何复核与胶囊
  完成态全部缺席。失败路径现在同样走一次 `reportPlacement`(存活检查后)。
- **表格行绕过参考文献过滤**(1.2.0 引入)。表格行块跳过所有散文过滤器,
  包括参考文献排除 —— 引用年份/编号可以以「表格单元格」身份溜回翻译流。
  现在参考文献排除对表格行同样生效;边界情形(同页 References 标题之上的
  真表)偏保守跳过、保留原文,已注释说明。
- **表格簇合并规则重构**(审核风险项,兼修一处潜伏计数 bug)。原
  `sharedRowCount ≥ 3` 按**成员多重**计数——同一行的 4 个单元格数成 4,
  一个单行碎片就能凑满 ≥3,NEJM Table 2 的表头与表体正是靠这样一座「事故桥」
  连成一张表;而任意宽度的对齐数值簇也能借它误并。重构为三条结构规则:
  (1) **去重行计数**——共享行按不同行带数,单行碎片再也凑不满;
  (2) **列合并受间距约束**——空槽间距必须 ≤ em×8;但槽里有其他表格单元格
  填充时豁免(同一张表的非相邻列对就是这样:合并顺序任意,NEJM 的计数列与
  P 值列间距 203px,中间隔着两列真实内容);图片障碍立于槽中时永不合并;
  (3) **行组纵向合并**——横向强重叠 (≥70%)、双方各有 ≥2 个列带、间距
  ≤ em×4 的上下两簇是同一张表的行组(表头行与数据行不共享任何行,靠列结构
  对齐归位,取代事故桥)。NEJM Table 2 输出与已提交快照**逐字节一致**;
  Hakime/Aquino/Horst/双栏样张全部零变化;新增三条合成测试(空槽超限不并、
  槽中有图不并、表内合法宽距合并保留)。

### Repository

- **设备仓库默认暂存区修复**。历次发版用临时索引提交,默认 `.git/index` 停在
  旧状态:8 个文件呈 `D` + 同路径 `??`,直接 `git commit` 会提交出删除约
  1905 行(含测试与语料)的提交。已把默认索引重建到当前 HEAD —— 所有历史
  提交本身完整无损,此前任何 HEAD 都没有丢过文件。

### Deferred(审核项中明确缓交)

- IR 校验分级(warning 可继续 / fatal 禁写缓存并回退)与更完整的 Document IR
  —— 架构方向正确,非本版补丁体量。
- 合并后网格密度校验 —— 现有三条结构规则已覆盖审核列举的误并形态,密度
  校验待更多真实表格语料再定阈值。
- 空槽、行对齐、间距 ≤ em×8 的两个独立并排小表理论上仍可能误并 —— 需要
  真实载体语料再收紧,现有六个快照锁住已知形态。

## [1.2.1] — 2026-08-15

### Fixed

- **1.2.0 回归:三栏页脚的作者名单/统计括号被误判成整页宽的假表格**
  (Hakime 2007, Radiology 243:712 第 2 页 —— 左「Advances in Knowledge」框、
  中正文、右「Author contributions」框)。`looksTabular` 的「符号密集短片段」
  判据只看逗号/句点/数字的密度,于是作者缩写行(`A.H., H.P., C.J.W., J.K.,
  S.L., S.N.G.`)这种句点/逗号极密、却不含数字的散文被当成表格单元格;再叠上
  1.2.0 新增的「远隔列合并(共享 ≥3 行)」,三栏页脚各自的密集尾行在同高度对齐,
  被错并成一张横跨整页的表格 —— 那些块随即结构错乱、排不上版,表现为「第 2 页
  8 段保留原文 / 排版 4/12」和左侧方框空白。修法很小很稳:符号密集判据**必须含
  至少一个数字**才算单元格 —— 作者缩写行(无数字)被排除,真正的统计单元格
  (`(SD, 0.22)`、`3.4 ± 1.2 (n=52)`,都含数字)照常识别。NEJM Table 2 的网格
  重建**不受影响**(仍完整成表);Aquino p1/p2、双栏样张、Horst p1/p5/p11、
  NEJM p7 六个既有快照**逐字节零变化**。修好后 Hakime 第 2 页探不到表格,整页
  回到正常散文路径,「Advances in Knowledge」方框恢复可译。

### Added

- 回归语料收编 Hakime 2007 第 2 页(三栏 + 双侧栏框,**无表格**的载体),
  基线快照锁定为「7 个散文块、0 个表格单元格」,防止此类页面再被误判成表格。
- 测试 609 → 610:`looksTabular` 新增作者缩写名单判负(含 `A.H., H.P., …` /
  `S.N.G.`)、统计括号判正(含数字)的覆盖。

## [1.2.0] — 2026-08-15

### Fixed

- **多列数值表的散文标签列不再坍成一块**(NEJM DEFUSE-3 第 714 页 Table 2,以及
  任何「散文标签列 + 若干横向拉开的数字列」的宽表)。这是 1.1.9 锁进回归语料的
  已知问题,现已修复。根因是两层的:
  - `groupIntoParagraphs` 是给散文设计的,它把表格的标签列**跨行黏成一堵墙**
    (13 行行标签坍成一个 443 字符块,y488–652 横跨整表高度)、把数字列纵向并块,
    等 `structureTableCells` 拿到时网格早已被毁。
  - `tableGuard.detectTableRegions` 的聚类只合并**横向重叠**的簇,而宽表的各列
    横向拉得很开(标签列 + 4 个数字列铺满 500pt),于是整表碎成一列一块的条带,
    每条 <4 单元格、够不上成表。
  修法分三步,互相配套:(1) 在「行」这一层先探出表格区域,把落在表格里的行整条
  **摘出散文分组**,各自成一行块 —— 非表格页探不到区域,摘出集为空,行为逐字节
  不变;(2) `detectTableRegions` 新增**列合并**:共享 ≥3 条对齐行中线的簇是同一表
  的相邻列,无论中间空多宽都并起来(≥3 的阈值挡住两团无关数字误并);(3) 新增
  **左槽标签归位**:落在数字列左侧、行中线对齐某条真实数字行的短块,是该行的标签,
  无论标签列离数字列多远都收进表格作 `tableCol=0`。**几何审计与 looksTabular 一个字
  没放松。** 实测:Table 2 从「1 个 443 字符标签墙 + 碎成两行的数字小表」变成完整
  网格(每行标签独立成 `tableCol=0` 单元格、与本行数字对齐);脚注仍是独立正文块
  (未被吞入表格),下方双栏正文不受影响。Aquino p1/p2、双栏样张、Horst p1/p5/p11
  五个既有快照**逐字节零变化**。

### Added

- NEJM DEFUSE-3 第 714 页回归快照从「锁定坏输出」翻新为**修复后输出**(73 块,
  含完整表格网格),继续作为宽表的回归基准。
- 测试 604 → 609:`detectTableRegions` 的宽表三例(远隔数字列共享行→并成一表、
  远左标签列按行归位、不共享表行的游离数字不被吞);`buildBlocksFromSpans` 两例
  (表格行不再被黏成标签墙、每行标签与数字各自成块;纯散文页不受重排影响)。

### Notes

- 两行的行标签会占两个网格行(数字对齐首行、标签第二行落在其下),这与原文版面
  一致,是正确渲染,不是坍缩。表头单元格(Endovascular Therapy / Medical Therapy
  等)目前仍作独立块处理,不影响标签列与数字的对齐;后续可并入表头行。

## [1.1.11] — 2026-08-15

### Fixed

- **非推理模型对 `reasoning_effort` 回 HTTP 400 导致「深度解析」失败**(OpenAI /
  OpenRouter 及自建 OpenAI 兼容端点)。当用户在设置里选了推理档位,插件会给
  openai/openrouter 请求加 `reasoning_effort`;但同一供应商下的非推理模型
  (gpt-4o / gpt-4.1) 和大量「OpenAI 兼容」端点并不认这个参数,直接回
  `400 Unrecognized request argument supplied: reasoning_effort` —— 用户在
  「深度解析」上撞见的正是它(翻译走同一条请求路径,同样会中招)。现在遇到这个
  400 时**剥掉 `reasoning_effort` 自动重试一次**,并把「这个模型不支持」按
  (供应商, 模型) 记下来,后续请求直接不发该参数,不再每次先 400 再重试。判据
  很窄:仅当响应是 400 且报错文本点名 `reasoning_effort` 才触发 —— 模型名错等
  其它 400 照旧原样抛出,不吞不重试;从未设过推理档位的配置根本不会带这个参数,
  该路径对它完全静默、零行为变化。翻译与「深度解析」两条路共用同一修复。

### Added

- 测试 597 → 604:`isReasoningEffortRejection` 判据(认 400+点名、拒模型名错 400/
  429/非错误)、按 (供应商, 模型) 记忆的注册表、以及 OpenAI 兼容适配器的
  端到端自愈五例(complete 剥参重试成功、记忆命中后首发即不带、translate 同样
  自愈且译文正常解析、无推理设置时静默单发、非 reasoning_effort 的 400 照旧抛出)。

## [1.1.10] — 2026-08-15

### Changed

- **「诊断」按钮默认隐藏,仅在开启调试日志时显示**。诊断是排障/反馈用的开发者
  入口(导出全文档脱敏指标 + 当前页布局语料),原来常驻工具栏对普通用户是噪声。
  现在它与 logger 的调试开关共用同一偏好
  (`extensions.zotero.bilingualReader.debugLogging`,设置里的「开启调试日志」):
  默认隐藏,勾选后出现,并随该偏好**实时显隐**(无需重开阅读器)。需要提交诊断
  时到「高级设置」勾一下即可;该开关明确「永不记录 API Key 或完整论文文本」,打开
  是安全的。「术语」按钮不受影响(它是面向用户的词表导出,照常显示)。
  显隐逻辑抽成 `observeBoolPref`(utils/prefs)的通用偏好观察器,面板销毁时反注册。

### Added

- 测试 593 → 597:`observeBoolPref` 的四条覆盖 —— 初始按当前偏好、翻转时实时回调、
  清理后反注册不再回调、观察者 API 缺失时降级为一次性读取且不抛错。

## [1.1.9] — 2026-08-15

### Fixed

- **整列正文被当页边栏静默丢弃**(Horst 2024 第 5、11 页左栏,以及任何在整版图
  旁边挤成窄栏的正文)。`metaFilter.isMarginSidebar` 用「窄 (<24% 页宽) + 高
  (≥30pt) + 靠外侧」三条几何判据识别期刊页边的引用/编辑/基金栏。但正文在
  整版图边上会挤成一条 96pt 的窄栏(第 11 页左栏 x48–144,右边缘 144pt),
  尺寸与真页边栏完全重合 —— 于是整列正文被判成 metadata、静默丢弃,只有被
  词距意外撕成单行的碎片(高度 <30pt)侥幸活下来,就是用户看到的那一小撮
  孤立中文(“患者已显示Bl64核可…”)。真页边栏一定比正文排得小(期刊页边
  7pt vs 正文 10pt),所以给判据加一条:块的字号 ≥ 本页正文字号 × 0.9 时,
  直接排除在页边栏判定之外。**不放松任何一条文本规则**(作者名单、版权、
  DOI、收稿日期等照旧过滤);无字号信息的老调用点回退到纯几何判定,行为与
  1.1.8 完全一致。实测:第 11 页 1759→2414 字符(左栏还原成 657 字符 31 行
  正文段落),第 5 页 3055→3795 字符;Aquino p1/p2 与双栏样张三个既有快照
  零变化。

### Added

- 回归语料收编 Horst 2024 第 11 页(整版图旁窄栏正文的载体)与 NEJM DEFUSE-3
  第 714 页(Table 2,表格标签列塌陷的载体),各附基线快照,IR 契约全过。
- 测试 589 → 593:新增页边栏修复的四条覆盖 —— 正文尺寸窄栏不再误判、真页边栏
  (7pt)仍被过滤、无字号信息的向后兼容、0.9 倍阈值的边界。

### Known issues

NEJM 第 714 页的 Table 2 语料同时锁进了一处**本版未修**的结构性缺陷,快照
记录的是当前(有缺陷的)输出,不代表它正确:

- **多列数值表的散文标签列塌陷成一块**。`tableGuard.looksTabular` 只接受数字
  密集的单元格,整列散文行标签(“Median infarct volume at 24 hr (IQR) — ml”
  等)一律落选,于是 `detectTableRegions` 只用数字列聚出表格区域,标签列被
  排除在表格模型之外;这些孤立标签随后在 `groupIntoParagraphs` 与
  `coalesceRegions` 里被并成一个 443 字符的大块(13 行标签叠在一起),数字列
  则碎成几个两行小表 —— 就是截图里图注下方那片互相压印的标签。诊断证据:该
  页 `page-6-region-6` 是一个跨整表高度(y488–652)的 443 字符段落。根因比
  「把标签列并回表格」更深:标签列在表格检测**之前**就已被段落分组黏死,而
  数字网格本身因行距(23pt)超过聚类阈值(22pt)碎成两行片段。正确修法要
  **重排流水线**——先从数字单元格定出行带,再拿行带去重新切分标签列——属于
  独立特性而非补丁,留待下一版。表格误译(“24 hr”→“24岁”“trial”→“审判”)
  是 BYOK 翻译模型的输出质量,不在本插件版式逻辑范围内;标签列结构修好后
  “24 hr” 重获行上下文,“24岁” 可能随之消失,但 “审判 vs 试验” 是纯模型
  用词。

## [1.1.8] — 2026-08-15

Horst 2024 (Radiology, 儿科光子计数 CT) 的诊断样本定位出的两处字段修复。
第 1、5 页语料进回归集,两处 bug 都由真实语料复现、修完由真实语料验收
(第 4 页语料另作交叉验证:图 4/图 5 的题注是同一 bug 的第二种几何)。

### Fixed

- **图注行碎片互相压印**(第 5 页图 6、第 4 页图 4/图 5)。`groupIntoParagraphs`
  把「相对本栏左边距的缩进」当硬断段信号,而期刊图注整体内缩排版(图 6 从
  x=162 起排、图 4 从 x=81 起排,页面左边距是 x=48),于是题注每一行都被判成
  新段落首行,首行之后全部断开;断开后的碎片不以 "Figure N" 开头,归类成
  paragraph,而 `regionCoalescer.isBodyBlock` 又规定 caption 永不参与合并——
  一条题注就此变成 1 个 caption + 若干 paragraph,各自送翻译、各自排版、
  各自撑开,在图下方叠成一片。几何审计看不见它:拆出来的行盒本来就互相
  重叠,「只报新增侵入」的规则因此豁免了整片区域(诊断里 geometryAudits 全 0
  正是这个原因)。修在聚合阶段:新增 `canMergeCaption`,把同一条图注的行碎片
  并回一块。**几何审计未放宽一个字节。** 实测:图 6 由 4 块(125/258/382/126
  字符)并成 1 块 892 字符、7 行盒,y 305–373;图 4 由 145+559 并成 705;
  图 5 由 675 并成 721。
- **图注碎片污染正文**(同一根因的第二个后果)。图注的尾行又短又常以小写开头
  (第 4 页 "was 0.59 mGy and effective dose was 0.46 mSv."、第 5 页 "counting
  detector scans (Flash + ultrahigh resolution…"),`isShard` 判成碎片、
  `canAbsorb`(不看字号)再把它塞进图下方的正文段落里——题注文字混进正文,
  正文块的盒子还被拉到题注行上。第 5 页左栏正文因此从 554 字符被撑到 813。
  图注归位跑在 shard 吸收之前,这一遍就再也看不到它们。
- **超短块被验收反复误拒**(第 1 页 region-2 / region-4)。`looksTranslated` 的
  中文占比阈值(0.45)是一个比例估计,分母越小噪声越大:一条 56–58 字符的
  标题或署名行里保留 4–5 个专名与缩写是忠实翻译的常态,却足以把比率压到
  阈值以下,而同样的噪声在同页 839 / 1783 / 1761 字符的正文段落里根本不存在
  (那三段全部一次通过)。散文 ≤80 字符的块因此换一把小样本安全的尺子:
  源文里没有可译普通词的整块(人名 / 机构 / 缩写 / 学位后缀,如
  "Marilyn J. Siegel, MD • Juan Carlos Ramirez-Giraldo, PhD •")直接放行——
  这是 `looksLikeAuthorNameList` 的推广,那条规则要求 ≥3 个人名分段,署名被
  拆到第二行只剩 2 个名字时就失效;其余短块仍看中文占比,但分母只数「还没
  翻译的普通词」(小写开头),不再把 Siemens / PCD / CT 算作未翻译的拉丁词。
  **正文段落的验收阈值一个字都没动**;短块这条路的分子不变、分母只会变小,
  所以它只可能把「拒」变成「收」,不存在新的误收路径,纯回声依然被拒
  (回声里的普通词原封不动地留着,中文字数为 0)。
- **纯文本兜底被请求预算饿死**。`pageRequestCap = 2×chunks + 2`,单 chunk 的
  页面只有 4 次:批次(1) + 缺 id 重试(1) + 两次单块打捞(2) 正好用尽——第 1 页
  的 `requests: 4 / salvage: 2` 记的就是这个。于是修复链里唯一不受 id 漂移与
  JSON 损坏影响的一环(纯文本兜底)永远轮不到,三个块直接落进 unrecovered。
  给它单独留一份不与打捞共享、上限 `FINAL_RECOVERY_MAX` 的预算;上限是硬的,
  整页最多多花 4 次请求,该路径自身的 attempts 计数闸原样保留。
- **诊断的拒绝原因码不再说谎**。引擎带着 id 回空串与「回了英文被验收拒掉」
  是两种完全不同的故障,原来都记成 `validator`,把人引向阈值——而阈值那条路
  对一个 11 字符的块根本不成立(散文词数不足 6,压根走不到比率判定)。
  空串现在记 `empty`。另外修掉打捞与纯文本兜底里两处「条件完全相同的第二个
  `else if`」死分支(永不可达),「请求回了空数组」这一支现在记
  `salvage-empty` / `plain-empty`,不再让块的 lastReject 停在批次阶段的旧值上。
- **`package-lock.json` 的依赖版本被历次发版改坏**。`@pdf-lib/fontkit`、
  `@pdf-lib/standard-fonts`、`@pdf-lib/upng` 三个依赖的 `version` 字段都被写成
  了插件自己的版本号(1.1.7),与各自 `resolved` 里的 tarball 版本
  (1.1.1 / 1.0.0 / 1.0.1)对不上——历次版本号替换是全文件替换。本次按
  `resolved` 修回,并加了一致性核对。

### Added

- 回归语料收编 Horst 2024 (Radiology) 第 1、5 页,附基线快照
  (p1: 7 块 / p5: 7 块),IR 契约全过。第 5 页是图 6 题注压印的真实载体;
  第 1 页是三个短块连拒的真实载体。
- 测试 575 → 589。新增覆盖:图注归位的正例(碎片相邻 / 隔着正文块不相邻)与
  四条反例(字号跳变、横向跨出图注范围、下一条 "Figure N"、行距超过一行的
  量级);短块验收的误拒场景(专名密集标题、被拆行的署名)与「真未翻译仍被
  拒」场景(纯回声、半译、空输出),外加一条「正文段落验收强度未被放松」的
  守门测试。请求预算的上界测试改成在两个相差 3 倍的块数上断言同一性质
  (请求数跟批次数走,不跟块数走),比原来的单点常数更强。

### Known issues

第 1 页语料同时暴露了两处**本次未修**的结构性问题,已随快照锁进回归集,
下一版处理——注意 `horst2024-p1.snapshot.json` 记录的是当前(有缺陷的)输出,
不代表它是正确的:

- 标题在连字符换行处被切成两块:`Multi-institutional Protocol Guidance for
  Pediatric Photon-`(59 字符)与 `counting CT`(11 字符)。后者单独送翻译毫无
  意义,也是诊断里那个 11 字符 `unrecovered` 块的来源(它散文词数不足 6,
  验收根本不会拒它——它是引擎回了空串)。
- 首字下沉("T")被并进了段落的**第二行**而不是第一行,产出
  `Ttial and contrast resolution, has made CT the preferred`(56 字符,归类成
  title),正文段落则从 `he operational efficiency…` 开头,并且 `spa-` 与
  `modality` 拼成了 `spamodality`。

## [1.1.7] — 2026-08-15

### Changed

- 「语料」并入「诊断」:一个按钮输出一份 JSON——全文档脱敏指标 +
  `currentPageCorpus`(当前页布局语料,dump-spans 同格式)。反馈问题从
  两次点击变一次;仍不含译文与密钥。

## [1.1.6] — 2026-08-15

### Added

- 面板菜单新增「语料」:一键把当前页文本层 span 语料(dump-spans 同格式
  JSON,含本页原文与坐标)复制到剪贴板——回归语料收编与问题反馈不再需要
  终端。与「诊断」分工明确:诊断脱敏无正文,语料含本页原文(导出动作即
  授权),两者都不含译文与密钥。

## [1.1.5] — 2026-08-15

### Added

- 回归语料收编 Aquino 2023 (Radiology) 第 1、2 页——本轮全部字段修复
  (圆括号引用掩蔽、首字下沉扩张截断、三栏混排)的真实载体,附基线快照
  (p1: 9 块 / p2: 14 块),IR 契约全过。

## [1.1.4] — 2026-08-15

首个真实诊断样本(Radiology 论文第 1 页)定位出的三处修复。

### Fixed

- 圆括号数字引用 (1) (2–4) (1,5–7) 纳入占位符掩蔽——Radiology 式引言段
  此前因译文保留全部引用被"疑似未翻译"验收反复拒绝 → 整段 unrecovered。
  仅纯数字+分隔符,(Smith et al., 2020) 不受影响。
- 排版扩展的遮挡判据从"起点在我边缘之外"改为"延伸超过我的边缘":与本块
  已有轻微重叠的邻居(首字下沉行矩形常态)此前不算遮挡物,扩张会确定性
  穿过它压住下方段落(诊断 detail: overlap:region-7→region-8, 46266px²,
  由 1.1.0 几何复核兜住);现在已重叠邻居把扩张截为 0。
- 拒绝原因埋点补齐打捞与 plain 兜底路径(salvage-validator /
  plain-validator)——1.1.3 只覆盖了批量路径,unrecovered 块的
  lastReject 曾缺失。

## [1.1.3] — 2026-08-15

字段定位版:针对首个真实诊断样本(第 1 页 2 段 unrecovered + 1 段几何撤销)
补两处原因埋点,不改行为。

### Added

- 诊断导出:keep-origin 块新增 `lastReject`(validator = 译文验收 /
  placeholder = 清单校验;仅原因码,无文本)——unrecovered 从此能区分
  "为什么每次都被拒"。
- `geometryAudits` 新增 `detail`:每轮处置的违例
  `kind:块id[→对方id](新增面积px²)`——几何撤销从此能看到判的是哪类
  违例、谁压了谁。

## [1.1.2] — 2026-08-15

四项合批:回归语料 + 诊断闭环 + 术语表出口 + P2 清理。574 测试全绿。

### Added — 布局回归语料(无 BabelDOC 的回归测试集方案)

- `scripts/dump-spans.mjs`:pdfjs-dist(devDependency)把真实 PDF 页转成
  与阅读器文本层路径同形的 span 语料(仅文本与几何,无图片);
- `tests/integration/layoutSnapshot.test.ts`:对每个语料页跑完整纯函数流水
  线,分段/栏/阅读序/表格结构摘要与快照对比,首跑自动生成;每页同时回归
  IR 契约。附带一页合成双栏样例;真实难例语料按 docs/REAL_PDF_QA.md 补充。

### Added — 诊断闭环

- PageDiagnostics 增 `placeholderTokens`/`placeholderRejected`(注册表状态
  汇总,仅计数无文本);诊断导出增 `geometryAudits`(每页几何复核的
  违例/调整/保留计数)。
- 面板菜单新增「术语」:docMemory 边翻边学的术语对 → TSV 进剪贴板
  (automatic_term_extractor 思想的增量出口,参照 BabelDOC)。

### Removed / Changed — P2 清理

- 删除 `translatedPageView.ts` 死代码族 buildTranslatedPage /
  settleTranslatedPage / buildFallbackPage 及输入/结果接口(1033 → 277 行,
  最终审核 P2;strict 分屏是唯一渲染通道后它们再无调用方);存活导出不变。
- `stripProtectable` 记忆化(FIFO 500):同段在校验/打捞/残留链里反复被剥,
  正则代价可观。
- 仍留 P2:translationManager 结构拆分、错误页退避、getPref 渲染期缓存。

## [1.1.1] — 2026-08-15

1.1.x 批次:段内样式保留 + 扫描件检测(参照 BabelDOC,AGPL-3.0,
见 THIRD-PARTY-NOTICES)。

### Added — 段内粗/斜体保留(参照 BabelDOC `il_translator.py` RichTextPlaceholder)

- `src/reader/styleRuns.ts`:char 层按字体名找出与主导样式不同的粗/斜体
  跨度(≥2 字母、≤60 字符、每段 ≤6 个、整段同样式不算);翻译请求侧以
  成对 ⟦b⟧…⟦/b⟧ / ⟦i⟧…⟦/i⟧ 夹住,译后按对恢复为 `<b>/<i>` 子元素
  (createTextNode/createElement + textContent 安全路径,不经 innerHTML)。
- **降级安全是硬约束**:样式是装饰不是内容——配对校验(栈式,拒绝交错/
  未闭合/空对)在占位符 restore 之后跑,任何破对一律剥掉全部标记回到
  纯文本,绝不因样式拒绝译文、绝不烧重试。
- 全链路接线:blockBuilder 检出 → coalescer 合并累积 → 注册表包标记 →
  prompt 成对指令 → strict 渲染 `<b>/<i>`;文本消费路径统一剥标记
  (对照面板、覆盖模式、PDF 导出、笔记、术语记忆),质量判定经
  `stripProtectable` 统一免疫。

### Added — 扫描件/坏字体页检测(参照 BabelDOC `detect_scanned_file.py` 思想)

- char 流 ≥60% 为未解码 `(cid:N)` 时按"无可用文本"处理,落到文本层路径
  ——不再把乱码块送去翻译烧请求。
- 提取干净得到 0 块的页(纯图/扫描页)在当前页提示一次"未检测到可翻译
  文本,已保留原样"——这不是失败,不该让用户对着圆环等。

### Tests

- 新增 5 个(共 573):样式跨度检测、插入/配对/解析、破对降级四态、
  注册表端到端(公式掩蔽不破坏配对)、质量判定免疫。

## [1.1.0] — 2026-08-15

IR 里程碑(目标架构第 1、3、5 步 + 第 6 步的接口部分;方案审核第四节)。
绞杀式渐进:类型形式化与单一边界收敛,无架构迁移;568 测试全绿。

### Added — Document IR 显式化(第 1 步)

- `src/ir/documentIR.ts`:`PageParser` 解析器接口(fast = TextExtractor,
  为将来 deep 路径预留插槽——安全基线:仅 127.0.0.1、opt-in、超时回落)、
  `PageIR` 显式页形状、`validatePageIR` 不变量契约(id 唯一、pageIndex 一致、
  readingIndex 稠密、tableRow/tableCol 成对、preserve 无占位符、memberIds
  非空、盒尺寸为正——每条都对应一次真实修过的回归)。
- TextExtractor `implements PageParser`;两条提取路径出口挂 log-only IR
  审计:违例不改行为,只让"隐式不变量被搬丢"类回归在日志立即可见。
- 契约测试:真实提取流水线(buildBlocks→order→表格→聚合→order)输出
  满足全部不变量。

### Added — 几何安全验证阶段(第 5 步,流水线末端唯一新增阶段)

- `src/ui/layoutSafety.ts`(纯函数):排版后整页复核——翻译块互相压盖、
  压图形、压 preserve 区域、越页。关键设计:**只报"新增"侵入**(现在的
  侵入面积 − 原始盒侵入面积 > 容差),原始 PDF 自带的紧贴/交叠不误报;
  违例按面积降序。
- `buildStrictPage` 新增 `pmGeometryAudit` 钩子:处置顺序与 pmShrinkFit
  相反——回退扩展 → 原盒缩字梯 → 仍不适配才保留原文(un-commit 与
  pmRevert 同动作,mask 清除、原文完整重现);幂等收敛,上限 4 轮。
- `reportPlacement` 在 FINAL 状态先审计后取数;shrink-only 路径此前静默
  结束、从不清点的缺口顺手补上(诚实计数 #6)。

### Changed — 占位符注册表(第 3 步)

- `src/translation/placeholderRegistry.ts`:protect/verify/restore 收敛为
  请求级单一边界对象,占位符清单从签发到还原绑定同一实例——"串块拿错
  清单"这类错误从此在类型上不可表达;translationManager 的 4 个 protect
  点与 7 个校验/还原点全部收敛,不再直接调 formulaGuard 的散装函数。
- 注册表记录校验通过/拒绝计数与最近报告(仅 token 名,不含文本——日志
  卫生基线),供后续诊断汇总。

### Tests

- 新增 14 个(共 568):IR 不变量逐条触发 + 流水线契约、几何审计六场景
  (新增侵入规则、归责扩展较多方、噪声容差、排序)、注册表往返/状态/
  变体委托。

## [1.0.6] — 2026-08-15

首个按"目标架构八步"重排序执行的批次(方案审核第四节的 1.0.6 项)。

### Added(参照 BabelDOC `il_translator.py`,AGPL-3.0,见 THIRD-PARTY-NOTICES)

- 占位符碰撞规避:原文自带 ⟦PMn⟧ 字样时,签发序号自动跳到其最大号之后,
  且该字样本身作为易碎字面量掩蔽保护——杜绝原文与签发 token 串号导致的
  无声公式错换与清单校验误判(`protectFormulas`)。
- 幻觉占位符变体归一:译文中的 【PM1】、[PM1]、[[PM1]]、⟦ PM1 ⟧、大小写
  混写与全角数字变体,在清单校验与还原前归一为规范 token。规则保守:仅限
  已签发号、正规形缺席时、每号一次;其余留给残留规则
  (`normalizePlaceholderVariants`,自动生效于 `verifyPlaceholders` 与
  `restoreFormulas` 全部 7 个调用路径)。

### Added(IR 字段显式化,目标架构第 2 步收尾)

- 表格单元格导出 `tableRow`(0 = 表头行)——此前行号只存在于单元格 id 的
  `r<row>` 里,无法编程消费;与 `tableCol` 配对。
- `orderBlocksForReading` 在所有返回路径写入显式 `readingIndex`(含无栏
  几何的退化路径,此时流顺序即阅读顺序)。

### Tests

- 新增 5 个测试(共 554):碰撞规避往返、变体归一(含全角/大小写/空格)、
  归一保守性(未签发号不动、重复变体不复制公式)、tableRow 与 id 一致性、
  readingIndex 全路径打点。

## [1.0.5] — 2026-08-15

### Added(参照 BabelDOC / MinerU 批次2)

- **排版"先扩边界、再缩字号"**(移植自 BabelDOC Typesetting「算法3」,
  funstory-ai/BabelDOC,AGPL-3.0):strict 适配器在缩字梯子之前,先测量块
  右侧/下方的实际空白(以版心 90%/页高 95% 为界,被最近的几何块或图形盒截断,
  3px 边距;右扩 ≤0.6×原宽、下扩 ≤max(两行, 0.5×原高)),按 右扩 → 下扩 →
  双向 的阶梯尝试适配;全部失败才进入缩字,最后再以"最小字号+最大扩展"组合
  兜底。"图 1 → Figure 1"式越译越长的短标签、标题溢出从此优先靠空白救回,
  而不是缩小或保留原文。空白测量为纯函数(computeExpansionAllowance),
  有单测锁定。
- **跨页续接几何收紧**(参照 MinerU `para_split.py::__merge_2_text_blocks`,
  Apache-2.0):在既有"未完句/续接开头"文本条件之上增加两条几何否决——
  上页末段最后一行必须顶到块右边界(差 < 行高;没顶满说明段落已结束),
  且两块宽度差 < min(两块宽)。减少"上页末段其实已完"时的错误上下文注入。
- 新增 3 组回归测试(扩展被邻块截断与上限钳制、90% 版心界限与非负、
  短末行否决跨页注入)。全套 549 项通过。

## [1.0.4] — 2026-08-15

### Added(参照 MinerU / PDFMathTranslate / BabelDOC 批次1)

- **字形级公式判定**(移植自 PDFMathTranslate `converter.py::vflag` 与 BabelDOC
  `formular_helper.py`,均 AGPL-3.0,来源标注于 `src/reader/glyphFormula.ts`
  与 THIRD-PARTY-NOTICES.md):公式保护从"拼好文本后猜正则"升级为字符层证据——
  数学字体名(CM/TeX/XY/MT/Sym/Math/Mono/Ital 族,子集前缀截断)、公式码位
  (希腊区、数学运算符、箭头、组合附标、上下标、=<>+±×÷ 等、(cid:) 未解码字形、
  私有区)、同段字号 <0.79× 判角标(0.76 角标与 0.799 大写取中,首字下沉豁免)、
  公式括号配平、RUN 间单空格桥接。后处理按 BabelDOC:纯数字"公式"降级回数据、
  无强证据的短 RUN 不掩蔽。char 提取路径逐段生成 `formulaRuns` 字面量,掩蔽
  优先于文本正则;合并/表格建模全程保留;span 路径保持正则兜底。
- **列表块检测**(参照 MinerU `para_split.py::__is_list_or_index_block`,
  Apache-2.0):≥3 行且 ≥80% 行以列表终止符(.。;;)结尾 → 判为列表块,
  参考文献式列表不再被并进正文段。
- **短行分段与目录行**(参照 BabelDOC ParagraphFinding 设计):行宽 < 页面
  中位行宽 ×0.7 且下一行非续接 → 段落在此结束;连续点号引导行
  ("1. Introduction …… 5")自成一段,目录页不再拼坏。两条提取路径同步生效。
- THIRD-PARTY-NOTICES 新增三个项目的来源致谢与协议说明(pdf2zh/BabelDOC 与
  本项目同为 AGPL-3.0;MinerU 启发式为 Apache-2.0)。
- 新增 9 组回归测试(字体名族、码位、整段 RUN、角标、括号配平、纯数字降级、
  散文零误报、字面量掩蔽往返)。全套 546 项通过。

## [1.0.3] — 2026-08-15

### Fixed(用户实测两症状:表格页排版卡死 / 到页仍要点圆环)

- **表格页"翻译 N/N·排版 0/N"永久卡死.** 1.0.2 的提取期表格建模让 preserve
  单元格进入了 strict 渲染器的几何集合,但像素框映射(pxOf)仍只按可译块构建
  ——第一张表格页就在 detectTableRegions/containedFraction 里抛异常,
  buildStrictPage 中断,排版任务停在 0/N,页面永远保持英文。pxOf 改为覆盖
  全部几何块;另给渲染调用加兜底:构建器抛任何异常都降级为保留原文 + 清除
  卡住的排版任务 + 提示,而不是冻结。
- **刚滚到的页面被标成"完成"导致必须手点圆环.** 僵尸提取的文字层恢复路径在
  用户**刚到达**页面时读到的常是还没渲染出来的空文字层,旧逻辑把空结果标为
  done——此后该页永远不再自动翻译。现在文字层读取带 6×500ms 重试(到页后
  文字层通常一秒内出现);仍为空则给出**可重试错误**而非假完成,僵尸路径的
  二次访问同样如此。
- 新增 2 组回归测试(文字层延迟出现后自动完成、永久为空必须报错不许假完成)。
  全套 538 项通过。

## [1.0.2] — 2026-08-15

### Fixed(两轮审核发现一并修复;含 Codex 改动合入)

Codex 改动(经专项审核后合入):

- 当前页切换会取消过期的提取等待者;已经离开预取窗口的旧页面即使稍后提取完成,
  也不会再进入翻译队列或覆盖当前页面状态。
- PDF worker 提取超时后,当前可见页面直接从已渲染文字层恢复;仍在运行的同页底层
  请求会被记录且不再重复启动,避免"闪一下又消失"和切到新页卡住。
- 全局并行数最小值统一为 2,始终给当前页保留处理能力。
- 表格在正文合并前建立稳定的行/列/单元格模型:文字单元格逐格翻译,纯数字与统计值
  原样保留,已经归入单元格的碎片不再作为正文重复翻译或叠加。
- 发布包过滤 `.DS_Store`/AppleDouble 和构建时间戳,包含 LICENSE 与第三方声明,
  打包后自动校验文件清单和版本号(verify-xpi 闸门)。

审核修复:

- **表格单元格不再把表格列号写进页面栏号**(审核 P1):`column` 改为继承成员碎片
  的页面栏(不一致时 -1 通栏),表格内列号移入新字段 `tableCol`——单栏页放三列
  表格不再被误判成三栏页、阅读序不再被打散。
- **裸占位符前缀碰撞**(审核 P1):PM1 是 PM10–PM19 的前缀,旧的子串匹配会把
  丢失的 PM1 误判为存在、还原时把 PM10 撕成"公式1+0"。校验与还原统一改用
  `(?!\d)` 数字边界正则,公式偶发乱码的根源修复。
- **keep-origin 段不再被页缓存冻死**(审核 P1):带 keep-origin 标记的页面不进
  页缓存;**普通刷新**现在会清除本页段落的止损记忆,给被跳过的段落真实的重试
  机会(其他页面共享同一文本的止损记忆保留)。
- **字号功能移植到实际渲染路径**(审核自查):0.9.28 的页面基准字号(role_min)
  与字号/行距倍率写在了无消费者的 renderTranslatedPage 上,左右对照从未生效;
  现已移植进实际使用的 buildStrictPage——严格测量闸门仍然把关,放大超框的块
  照旧保留原文而不是溢出。
- 提取信号量 `if` 改 `while`,关闭唤醒间隙的 3 路并发竞态;文字层恢复路径同样
  套提取超时;可见页 worker 挂起且文字层为空时给出可重试错误提示而非静默回到
  等待点击;强制重译清除本页僵尸提取记录,允许一次全质量 worker 重试;单段重译
  任务在滚动时按前缀保活,不再被静默取消,失败提示改为中性文案;flashNotice
  加销毁保护;两处缩进修正。

### Tests

- 新增/重写 7 组回归(跨页止损语义、普通刷新重试、keep-origin 不入缓存、
  PM1/PM10 边界、表格页面栏继承、文字层为空的可重试错误、codex 的 4 组保留)。
  全套 536 项通过。

## [1.0.1] — 2026-08-15

### Fixed(每翻一页都要手动点圆环)

- **提取悬挂不再卡死自动翻译.** 0.9.22 把页面提取移出服务商并发槽时,提取阶段
  同时脱离了空闲看门狗:预取一个**尚未渲染**的页面时,PDF.js 文字提取可能永久
  挂起——该页状态永远停在"读取中"(之后每次访问都被提前返回拦下,什么也不做),
  两个悬挂更会占满 2 槽提取信号量,后面所有页面无限排队;切页时又清掉过期任务,
  胶囊落回"点击圆环翻译本页"。表现即为:**每到新页面必须手点圆环**(点击等于
  删状态重来,而此时页面已渲染、提取秒过)。
- 修复:提取阶段加 20s 超时——超时立即释放信号量槽位并**遗忘该页状态**,下次
  滚动到该页自动重跑完整流程,无需任何点击;僵尸提取在后台自然结束、结果按
  "已被取代"丢弃。另加保险:状态卡在"读取中"超过 2×超时的页面,访问时直接
  替换重跑,不再被提前返回拦截。
- 新增 2 组回归测试(悬挂超时后自动重试、双悬挂不饿死后续页面)。
  全套 529 项通过。

## [1.0.0] — 2026-08-15

### 正式版

0.9.x 系列三十余个版本的收敛点。相对 0.9.2 时代,1.0.0 的翻译内核、调度器、
版面识别与诊断体系均已重写:占位符保护与清单校验、copy-dominance/截断/混合
残留判定、三分道调度与公式风险路由、纯文本兜底与 keep-origin 止损、文档术语
记忆、跨页续接、页面基准字号、段级诊断与单段重译。全套 527 项单元测试。

### 致谢 (Acknowledgements)

本项目多项核心算法参照并移植自开源项目
**[RetainPDF](https://github.com/wxyhgk/retain-pdf)**(作者 **wxyhgk** 及
RetainPDF contributors,MIT 协议):译文质量判定规则(copy-dominance、截断、
混合残留、数据密集与署名豁免、协议壳检测)、公式密集风险评分,以及占位符保护、
分道调度、修复链路、术语记忆、跨页续接、页面基准字号等设计思想。诚挚感谢原
项目和作者的开创性工作。按 MIT 协议要求,完整版权与许可声明收录于新增的
`THIRD-PARTY-NOTICES.md`;移植文件(`src/translation/residueRules.ts`、
`src/reader/formulaGuard.ts` 相关部分)已内嵌来源与协议标注。README 许可
章节同步致谢。

## [0.9.31] — 2026-08-15

### Changed(retain-pdf 源码级核心算法移植)

深读 retain-pdf 源码(`llm/validation/english_residue.py`、`quality.py`、
`segment_risk.py`、`protocol_shell.py`),把其在真实失败样本上磨出来的判定规则
逐条移植进对应模块:

- **copy-dominance 判据.** "未翻译/混合残留"的硬判据改为表面相似度:归一化
  英文表面(小写、去标点)与原文相似度 ≥0.82 才算"抄的"——标点大小写变化藏
  不住回声,而真翻译永远不会相似。新增 `residueRules.ts` 纯模块。
- **混合残留硬拒.** 长段落整体中文占比达标、但结尾仍拖着一句 ≥12 词且与原文
  copy-similar 的英文——比例检查看不见它,现在拒收进补救链。
- **截断硬拒.** 原文 ≥200 字符而译文不足其 15%(EN→ZH 正常 0.3–0.5)必是
  半截/尾巴输出,原先比例检查会把一小段全中文的残缺译文当"已翻译"存下。
- **两项误拒豁免.** 数据密集片段(数字 ≥ max(6, 字母×0.35),NMR 谱线/数值串)
  永不算残留;作者署名行(≥3 段、每段 2–5 个人名词)合法保留拉丁文。
- **残留检测跨度规则.** 除既有的 6 连小写词规则外,新增 ≥30 字符、≥10 词、
  像散文且非数据密集的拉丁跨度判定——Title-Case 残留句(原规则刻意跳过大写词)
  现在也能触发补译。
- **公式密集评分路由.** 慢道判据从"占位符 ≥5"升级为 retain-pdf 的风险评分:
  定义句触发短语(defined as / denoted as / where …)+3、占位符 ≥4/≥8 各 +2、
  散文 ≥180 字符 +1、密度阈值 +1/+1、占位符前置/中置 +1/+1;占位符 ≥4 且
  总分 ≥6 即入慢道——比单纯计数更早识别高风险块。
- **协议壳检测.** 纯文本兜底若返回 JSON 信封则从中恢复译文,若返回"请提供
  原文"类提示语则按失败处理——两者都不再可能被当成译文存储。
- 新增 12 组移植对照测试。全套 527 项通过。

## [0.9.30] — 2026-08-14

### Added(参照 retain-pdf 批次7:跨页续接 + context_bleed + 不译词列表)

- **跨页段落续接.** 上一页末段以逗号/未完句结尾、或本页开头像续接(小写/标点开头)
  时,上一页尾部原文(≤600 字符)注入为本页第一批的理解上下文——只注入上下文,
  不跨页合并块,逐页覆盖与整页一次提交的契约不变。译文衔接不再生硬。
- **context_bleed 校验.** 注入了上下文的批次,其**首块**译文若长得离谱(CJK 字数
  超过原文单词数×2.4,含下限),判定为"上下文被翻进当前块",拒收并经无上下文的
  补救链重新获取。
- **不译词列表.** 设置页新增多行输入:产品名、算法名等必须逐字保留的词,发送前
  经占位符掩蔽、译后原样还原——比 prompt 恳求可靠。每行一个,# 开头为注释。
- 新增 3 组回归测试(bleed 判定、跨页尾巴注入、不译词掩蔽往返)。全套 515 项通过。

## [0.9.29] — 2026-08-14

### Added(参照 retain-pdf 批次6:段级诊断 + 单段重译 + 诊断导出)

- **右键"重译此段".** 左右对照页的任意译文段右键即单独重译:一个前台单块请求,
  清除该段的 keep-origin 标记与止损记忆,成功后就地替换并写入缓存;进行中段落
  呼吸闪烁,完成后胶囊提示。不再需要为一段错译强制重译整页。
- **页级诊断采集.** 每页记录真实请求数、补救数、429 限流数、超时数、段缓存命中、
  耗时(`state.diagnostics`);段级状态(已译 / unrecovered / repeated-failure)
  可查。
- **一键脱敏诊断导出.** 面板菜单栏新增"诊断"按钮:整文档逐页诊断 JSON 复制到
  剪贴板——只含状态、计数与原因,**不含任何正文、译文、密钥、URL**。以后排查问题
  贴 JSON 即可,不用截图倒推。
- 新增 2 组回归测试(429 计数与导出脱敏、单段重译清除 keep-origin)。
  全套 512 项通过。

## [0.9.28] — 2026-08-14

### Added(参照 retain-pdf 批次5:页面基准字号 + 用户倍率)

- **同页正文统一到页面基准字号(role_min).** 左右对照的重排页里,正文段不再各用
  各的提取字号——按页取"中位数带内的稳健最小值"为锚点,所有正文段统一到锚点,
  相邻段落大小不再发花;标题、图注保持自己的字号。零星的 6pt 上标碎片或 14pt
  首行样式拖不动锚点。
- **译文字号/行距用户倍率.** 设置页"阅读界面"新增两个倍率下拉(0.8×–1.3×):译文
  偏大/偏挤时整体微调。左右对照页完全生效;覆盖模式受原文框约束,倍率只在缩小方向
  生效(放大由适配器按框裁定)。行距经 CSS 变量作用于正文/标题/图注三档。
- 新增 2 组回归测试(稳健最小值锚点、倍率解析与钳制)。全套 510 项通过。

## [0.9.27] — 2026-08-14

### Added(参照 retain-pdf 批次4:文档术语记忆)

- **同一术语全文一致.** 新增文档级术语记忆:译文中出现标准首现形式「中文术语(ABBR)」
  且缩写确实存在于原文时(防幻觉交叉校验),该词对被记住;后续页面凡命中此缩写的请求
  自动注入为**参考性**术语规则——第 12 页的 CEUS 从此和第 2 页译得一样。
- 首现优先(论文自己的首次展开是权威),统计缩写(CI/OR/HR/SD 等)与单大写字母
  排除,上限 200 条防膨胀;换语言/换服务商时清空。**用户词汇表始终优先**于记忆
  (matched 合并注入,不整表塞 prompt)。
- 新增 6 组回归测试(抽取、防幻觉、停用词、首现优先、容量上限、跨页注入)。
  全套 508 项通过。

## [0.9.26] — 2026-08-14

### Added(参照 retain-pdf 批次3:拒收段修复链路)

- **纯文本兜底重译.** 被 JSON 链反复拒收(回声、半翻、丢 id)的段落,在整页收尾时
  获得最后一次**纯文本模式**请求:系统提示只要求"只输出译文本身",响应整体即译文,
  JSON 包裹和 id 漂移不可能再让它失败。每页预算 4 段,计入页请求上限。原则同
  retain-pdf:「该翻的段不允许以空译文结束」。
- **keep-origin 止损.** 连续两轮整页运行仍失败的段落记入止损记忆,后续访问直接标记
  keep-origin 跳过——不再每次翻页都为注定失败的段落重复烧请求。**强制重译**清空
  止损记忆,给所有段落重新机会。段级状态记入 `state.keepOrigin`(原因:
  unrecovered / repeated-failure),为批次6的诊断面板铺路。
- 纯文本模式下不再强制 JSON 输出(response_format / responseMimeType 按模式切换),
  三家 LLM 适配器 + Gemini 原生接口统一接入。
- 新增 5 组回归测试(纯文本解析、纯文本 prompt、兜底拯救、止损跳过、空响应抛错)。
  全套 502 项通过。

## [0.9.25] — 2026-08-14

### Changed(参照 retain-pdf 批次2:三分道调度)

- **高风险块隔离进单块慢道.** 表格、超长段(>2400 字符)、公式密集段(≥5 个保护
  token)不再混入 24 块的大批次:一个难块的 id 漂移或截断曾让整批重试,现在它单独
  成请求、排在快批之后——快批先回来把页面的大部分先画出来,难块慢慢来。
- **慢道不参与位置上下文.** 慢道块脱离阅读序,既不接收也不提供前后文——孤立难块
  不需要邻居的尾巴,错误的邻居比没有更糟。
- 新增 3 组回归测试(风险块隔离、无 riskOf 行为不变、公式密集块单独成请求)。
  全套 497 项通过。

## [0.9.24] — 2026-08-14

### Added(参照 retain-pdf 批次1:占位符保护体系)

- **引用标记与统计量进入占位符保护.** 原先只有公式(LaTeX 定界 + 符号密集判定)被
  替换为 ⟦PMn⟧ token;现在方括号数字引用 `[12,15-18]`、p 值 `p = 0.003`、置信区间
  `95% CI: 0.71–0.94`、均值±偏差 `34.2 ± 5.1`、样本量 `n = 342` 也在请求前掩蔽、
  译后逐字回填——模型再也无法改写或漏掉它们。
- **占位符清单校验.** 译文丢失 token(⟦PMn⟧ 与裸 PMn 均认)或凭空多出 token 的响应
  按无效处理,进入既有的重试/补救链,而不是无声还原成缺公式的段落。刻意**不校验顺序**
  ——目标语言重排引用位置是合法的翻译行为。
- **"疑似未翻译"校验改为只看散文.** 统计密集行(数值、引用、单位在完美译文里本来就
  与原文逐字相同)以前把拉丁字符比例拉低而被误拒留在英文——斑马纹的一半成因。现在
  比例计算前先剥掉可保护片段,只对散文部分打分;短统计行掩蔽后直接过闸不再误判。
- `protectFormulas` 支持外部字面量列表(为批次7的"不译词列表"铺路,掩蔽比 prompt
  恳求可靠)。
- 新增 6 组回归测试(掩蔽往返、清单校验、散文剥离、统计密集行通过校验、回声仍被拒、
  不译词掩蔽)。全套 494 项通过。

## [0.9.23] — 2026-08-14

### Fixed(双栏识别第五批:三栏首页左中两栏逐行中英夹杂)

- **一根悬垂矩形不能再把两栏"焊"成一条带.** 三栏排版的栏距比双栏窄(约 12–15pt),
  只要有一行连字符悬垂几 pt 伸进栏距(或栏距本身窄于链式阈值 11pt),贪心链式列检测
  就把左、中两栏焊成一条带:列标注塌掉 → 规范阅读序失效 → 两栏文字按基线交错成
  逐行小块 → 逐行翻译后一半被"疑似未翻译"校验拒收留在英文——正是 Radiology 首页
  左中两栏中英夹杂、而第三栏完好的原因。现在每条带内做**覆盖投票**(与栏距投票同一
  思路):≥90% 成员避开的、宽 ≥6pt 的内部空白通道判定为栏距,带在此处拆开;悬垂
  矩形归属自己一侧,其突出边缘不再把带拉伸过栏距。参差右缘、缩进首行、零星矩形均
  无法触发拆分,普通单栏/双栏页行为不变。
- **列标注改由行级矩形推导.** 原先最终标注用"段落并集矩形"重新检测列——一页只有
  几个段落,数量不足以投票,一个悬垂段落并集又会在标注环节把两栏重新焊死;两条提取
  路径均改用行级矩形(数量足、粒度细)。
- **投票栏距上的连字符悬垂不再把左右两行拼成一行.** 行分组允许前一行超过栏距中心
  ≤0.6em 的悬垂仍按跨栏处理。
- 新增 3 组以该页真实形态为原型的回归测试(反焊接、焊接矩形下三栏各自成段、
  普通单双栏不被误拆)。全套 488 项测试通过。

## [0.9.22] — 2026-08-14

### Fixed(审核报告第 2+3 步:调度第二批 + 进度真实化)

- **页面提取不再占用服务商并发槽.** 提取(PDF.js getTextContent 等)从翻译任务里拆出,
  改在**独立的提取信号量**(并发 2,当前阅读页优先)中先行完成,拿到块后才进入服务商
  队列。原先一次慢提取会白白占住一个 API 并发槽,后面所有页面跟着排队——三症状里
  "速度太慢"的调度层主因之一。
- **429 限流按服务器指示退避.** HTTP 客户端现在读取 `Retry-After` 响应头(秒数或
  HTTP 日期均可,上限 120s)并透传到错误对象;重试等待改为
  `max(Retry-After, 2.5s)`,不再固定盲等。同时新增调度器公开接口
  `laneFeedback`:每次成功/限流/超时都实时驱动自适应并发的升降,而不是只在整页任务
  结束时才反馈一次。
- **取消或超时的页面保留已译段落(增量持久化).** 整页任务被取消、超时或中途失败时,
  已完成分块的译文现在**立即写入存储**;重新打开该页时这些段落直接命中缓存,只补译
  剩余部分。原先一个分块超时会让同页已成功的几千字全部作废重来——"大文本卡住后
  又从头再来"的直接原因。
- **状态胶囊显示真实阶段.** 提取阶段显示"正在读取 第 x / y 页"(副行"正在读取页面
  文字"),拿到分段后才显示"正在翻译";排版阶段在尚未落下任何段落时用不确定动画,
  避免"0/0 段"的假进度。切页时清除上一页遗留的过期"翻译中"任务提示。
- **几何收尾两处.** 栏间距检测的覆盖标记改为精确取整(消除 1px 级误差把窄间距抹掉的
  问题),最短间距游程收紧为 6;区域合并器 canMerge/canAbsorb 现在**尊重列标注**——
  两块分属不同列时无论几何测试怎么说都拒绝合并(审核项:一个坏 union 矩形曾污染整页
  后续几何判断)。
- 新增回归测试:提取信号量并发上限、取消页增量持久化;更新胶囊阶段文案断言。
  全套 485 项测试通过。

## [0.9.21] — 2026-08-10

### Fixed(双栏识别第四批:中英逐行"斑马纹"页)

- **流序逐行交错的双栏页现在能正确拼段.** 不少 PDF 按"行"输出双栏(左一行、右一行交替),
  而区域合并器只合并**相邻**块——交错后同栏邻居永不相邻,一行一个块永远拼不回去,逐行翻译
  后统计密集的行又被"疑似未翻译"校验拒收留在英文,形成中英逐行斑马纹。新增**规范阅读序**
  (`readingOrder`):全宽块作为水平分隔把页面切成若干"段带",每带内按 左栏自上而下 → 右栏
  自上而下 输出,再进入合并器。顺带修复了审核项"页中全宽块(跨栏表题)被排到整个右栏之后"。
- **逐行乱打的段落断行标志不再被信任.** 某些 PDF 给几乎每一行都打 `paragraphBreakAfter`
  (行数≥8 且 >80% 带标志时判为无信息量),此时忽略标志、完全由几何决定分段——原先它被
  当作权威信号,一页被切成逐行块。
- 新增 4 组以"斑马纹"真实形态为原型的回归测试(交错重排、重排后并段、页中全宽块定位、
  假断行标志)。

## [0.9.20] — 2026-08-10

### Fixed(双栏识别第三批:按内容剔除出版商样板文字)

- **首页居中的 reprint 声明不再毁掉整页.** "This copy is for personal use only. To order
  copies, contact reprints@rsna.org" 在第 1 页常出现在**页面中部**(摘要附近)——不在上下 6%
  家具区内,0.9.18 的位置过滤看不到它,它照旧横跨栏间距把双栏桥接成单栏。位置不可靠,改用
  **内容识别**:reprint/personal use/Downloaded from/© 年份/all rights reserved 这类出版商
  样板行,**无论出现在页面何处**,都在进入栏检测之前整行剔除(两条提取路径一致),它们也
  不再被翻译。行长上限 180 字符——正文中"引用"这类句子的长段落不受影响。
- 新增 2 组以第 1 页真实形态为原型的回归测试(页中部居中声明不桥接双栏、样板行不成块)。

## [0.9.19] — 2026-08-10

### Fixed(双栏识别第二批:边框硬屏障)

- **图框成为提取阶段的一等公民.** 操作符列表得到的真实图片矩形此前只用于渲染,提取器
  看不见它们——图内文字("X-rays"、"Low keV" 等图注标签)混入正文流,相邻两图的题注隔着
  图框互相融合,一个坏合并的联合矩形随后吞掉半页(用户实拍 Radiology 三栏图文页确认)。
  现在图框在提取全程生效,三条硬规则:
  1. **框内文字不进翻译流**(≥60% 落在图内的行判为图表标签,保留原样);
  2. **隔框的行永不合并成段**(段落构建与修复合并均被图框强制断开);
  3. **区域合并器见框即停**(canMerge/canAbsorb 对被图框分隔的区域直接拒绝)。
- 图框获取为 best-effort(2.5s 超时、按页缓存),拿不到时行为与旧版完全一致。
- 新增 figureBarriers 纯函数模块与 4 组回归测试(以该真实页面几何为原型)。

## [0.9.18] — 2026-08-10

### Fixed(双栏识别第一批,由真实 RSNA 页面验证驱动)

- **居中的页脚/页码不再把双栏"桥接"成单栏(P0,用户实拍页面确认).** 页底居中的
  "This copy is for personal use only…" 这类边界文字宽度不足 62% 全宽阈值、又横跨栏间距,
  被贪心链连带后左右栏合成一个带 → 整页判单栏 → 右栏被"缩进"规则切成逐行碎片(截图中
  一行一句的断裂形态)。现在栏检测**排除页面上下 6% 区域内的行**和 **<5% 页宽的小矩形**
  (页码、公式号、首字下沉),它们不再参与投票。
- **栏左缘改用成员行左缘的中位数(P0).** 原取最小值,一个悬挂缩进/项目符号就把左缘拉低
  ~1em,之后全栏每行都判"缩进"而逐行断段。
- **文字层回退路径补上 column 标注(P0)**,语义模块不再在该路径下跨栏;两条提取路径对
  同一页的输出趋于一致。
- **文字层栏距回退阈值 2.5em → 1.6em.** 10pt 正文下旧阈值 25pt 大于常见 18–24pt 栏距,
  左右栏同基线的行被直接拼接(跨栏乱序的经典来源);两端对齐的词间距不超过 ~1.3em,
  1.6em 仍不会把句子从中间切开。
- **只剩碎条带时报告"无栏"**(整页单栏流)而不是把页码带当成栏(旧行为使每行恒判
  wrapped 或 indented)。
- 新增 5 个以该真实页面几何为原型的回归测试。

## [0.9.17] — 2026-08-10

### Fixed(全代码审核后的大文本卡住/提速修复)

- **看门狗改为「空闲心跳 + 真正中止」.** 旧看门狗量的是页面总时长(120s),大页面所有请求都
  成功也会被杀;且它只拒绝 Promise 不中止任务——超时后 translatePage 变成僵尸继续发请求、
  取消无效、还会把已判错的页面改回 done,并误罚服务商并发。现在:每个完成的请求(含提取)
  都会重置 150s 空闲时钟,只要页面在推进就永不误杀;真挂起时通过本地 AbortController
  **真正中止**全部在飞请求,并以 TIMEOUT(而非 CANCELLED)上报。
- **请求超时随文本量缩放.** 固定 60s 对 8000 字符批次在慢/思考模型上结构性不足(真实生成
  需 70–200s)。现在按 `20s + 12ms/字符` 缩放、封顶 120s(< 空闲看门狗 150s);单块补救仍
  是小超时。TIMEOUT 类错误只重试一次——烧满整个超时的请求再重试通常还是超时,这正是
  旧版"卡住几分钟"的来源。
- **页内批次 2 路并行.** 审核确认批次请求只依赖原文(上下文预生成),不依赖上一批响应,
  因此当前页的多个批次并发发送(上限 2)——多批大页面的墙钟时间约减半,也不再因"串行总和
  超过看门狗"而全体失败。
- **英文残留补译纳入请求预算**(逐块检查,预算耗尽即停,不再在封顶之外追加最多 8 个串行请求)。
- **Anthropic 输出上限 8192 → 16384**,且思考预算改为**加在输出之外**(原先从 max_tokens 里
  扣走一半,大批次 JSON 截断 → 不可重试错误 → 整批丢失)。

### Notes

- 本版为三症状审核(大文本卡住/速度慢/双栏识别)中"引擎层安全可修"的部分。其余已定位、
  待真实 PDF 验证的项:整页对照的渐进显示(目前 done 前隐藏全部译文)、提取移出服务商并发槽、
  429 的 Retry-After 透传、增量持久化、以及双栏几何缺陷族(居中页码桥接栏检测、band.left
  取最小值致逐行断段、文字层路径缺 column、三套同栏判定不一致等)。

## [0.9.16] — 2026-08-10

### Changed

- **模型下拉只显示纯模型 ID(Bob 插件风格).** 移除所有「需付费档」「预览」等文字标注 —
  Gemini 与通义千问的残留标注全部清掉,下拉里就是干净的模型名。账户档位不足时由
  连接测试的错误提示说明("当前账户不可用,如免费档限制"),不再写进列表。
- 「自定义模型」输入框的提示语随服务商变化(如 Gemini 下显示 `e.g. gemini-2.5-flash`)。

## [0.9.15] — 2026-08-10

### Changed

- **Gemini 改走 Google 原生 generateContent API(参照 Bob Gemini 插件设计).**
  Base URL 默认 `https://generativelanguage.googleapis.com`(便于代理转发,同
  bob-plugin-gemini);请求打到 `/v1beta/models/{model}:generateContent`,Key 走
  `x-goog-api-key` 头(绝不进 URL/日志)。带来的实质改进:
  - 「深度思考」用**一等公民** `thinkingConfig`:禁用思考 → `thinkingBudget: 0`,
    自动思考 → `thinkingBudget: -1`(此前经 OpenAI 兼容层二手模拟);
  - 严格 JSON 输出用官方 `responseMimeType: "application/json"`;
  - 温度 / 最大输出 token 进 `generationConfig`(温度默认 0);
  - 连接测试关思考 + 小输出上限,更快更省。
  自定义 API Path 仍可整体覆盖路径(网关场景)。

### Removed

- **仓库清理**:删除 5 个历史版本发布说明(`发布说明-0.9.x.md`)——内容已完整存在于
  CHANGELOG.md 与 GitHub Releases;`.gitignore` 新增会话传输包(`pm-*.tgz`)与
  版本发布说明的忽略规则,仓库根目录不再越积越杂。

## [0.9.14] — 2026-08-10

### Changed

- **Gemini 改用独立的「深度思考」开关(与 GPT 的推理档位不同).** Gemini 的思考控制是
  开关语义而非强度阶梯,现在按 Bob 的设计单独显示:默认设置 / **禁用思考** / **自动思考**。
  映射到 Google 官方接口:禁用思考 → `reasoning_effort: "none"`;自动思考 → 动态思考预算
  (`extra_body.google.thinking_config.thinking_budget: -1`);默认设置不下发。翻译建议
  「禁用思考」(更快更省)。GPT 系(OpenAI/OpenRouter)保持 minimal…xhigh 档位不变;
  旧存的 minimal 在 Gemini 下自动显示为「禁用思考」。

## [0.9.13] — 2026-08-10

### Fixed

- **Gemini 只有 2.5-flash / 2.5-flash-lite 可用.** 根因是 Google 2026-04 收紧了
  API 免费档:Pro 与 3.x 系模型转为付费档专属,免费 Key 仅开放 2.5-flash / lite。
  默认模型改回对所有账户都可用的 **`gemini-2.5-flash`**;3.x 与 Pro 保留在列表中并
  标注「需付费档」。连接测试的"模型不存在"提示补充了"当前账户不可用(如免费档限制)"
  这一常见原因。

## [0.9.12] — 2026-08-10

### Changed

- **模型下拉取消分组标题.** The model picker is now a FLAT list (still ordered
  推荐 → 高质量 → 快速 → 预览 → 旧版) ending in 自定义模型… — no category headers.
- **推理强度改为官方英文取值.** 默认设置 / minimal / low / medium / high / **xhigh**
  (xhigh 为 OpenAI gpt-5.4+ 的官方档位)。映射:OpenAI/OpenRouter → 原样下发
  `reasoning_effort`;Gemini → minimal→`none`、xhigh→`high`;Anthropic → high/xhigh
  启用 extended thinking;其它服务商不下发。
- **最大输出 token 改为下拉选择.** 默认(不限制)/ 1024 / 2048 / 3072 / 4096 / 8192。
  批量翻译仍建议保持默认以免 JSON 截断丢段。
- **温度默认 0.** 翻译确定性最好。未显式设置时,对安全的服务商默认下发
  `temperature: 0`(deepseek/qwen/zhipu/moonshot/siliconflow/groq/gemini/ollama/
  兼容端点/Anthropic 非思考模式);OpenAI 官方端点与 OpenRouter 不注入默认值
  (gpt-5.x 仅接受默认温度),显式设置则始终照发。

## [0.9.11] — 2026-08-10

### Added

- **每个服务商的高级参数(Bob 风格,全部选填).** 在「模型/地址」下新增「高级参数」区,按各家
  官方 API 适配,**均为选填——不设置则请求体与之前完全一致(零回归)**:
  - **推理强度 / 思考**:默认 / 最低 / 低 / 中 / 高。翻译建议设「最低」——gpt-5.x、Gemini、
    Claude 等推理/思考模型会更快更省。映射:OpenAI/OpenRouter → `reasoning_effort`;
    Gemini → `reasoning_effort`(「最低」→ `none` 关闭思考);Anthropic → 仅「高」启用
    extended thinking;其它服务商暂不下发该参数(避免 400)。
  - **自定义 API Path**:单独的请求路径字段(留空用默认 `/v1/chat/completions` 等),应对特殊
    代理/网关;「实际请求地址」预览会实时反映。
  - **最大输出 token 数**:OpenAI 官方端点用 `max_completion_tokens`,其它兼容端点用
    `max_tokens`;批量翻译建议留空以免截断。
  - **温度 temperature**:留空用服务商默认(部分新模型只接受默认温度)。
- 每个参数按服务商各自保存;连接测试使用当前(未保存的)高级参数实测;非 LLM 引擎
  (微软/谷歌/DeepL)不显示高级参数。

## [0.9.10] — 2026-08-10

### Fixed

- **gpt-5.x 等新模型"测试连接"误报"模型不存在".** OpenAI 的推理模型(gpt-5.x)在
  chat/completions 上**拒绝旧的 `max_tokens` 参数**,必须用 `max_completion_tokens`。
  连接测试原本发送 `max_tokens: 32`,导致合法的 gpt-5.5 返回 400 并被归类为"模型不存在
  /路径不对"。现在对官方 OpenAI 端点(`openai`)改用 `max_completion_tokens`(对
  gpt-4o 与 gpt-5.x 都适用),其它 OpenAI 兼容端点仍用 `max_tokens`。
  (实际翻译请求本就不带 `max_tokens`,所以翻译可能一直是好的——坏的只是测试。)

## [0.9.9] — 2026-08-10

### Fixed

- **翻页后"卡住很久"的根因:取消整页级自动重试(P0).** 一个页面过去是一个可重试的
  调度任务——可重试错误会把**整个** `translatePage` 重跑(重新提取 PDF、重新分批、重新
  翻译、重新补救),最多 3 次重试 × 300 秒看门狗,最坏接近 20 分钟才真正失败。现在:
  - 页面调度任务 **`maxRetries: 0`**(最多执行一次,绝不整页重跑);
  - 瞬时错误(网络/超时/限流)改为在**单个请求层**重试(最多 2 次、短退避),不再重新提取;
  - 单个 chunk 请求彻底失败**不再拖垮整页**——其区块交给(已封顶的)补救,后续 chunk 继续;
  - 页面看门狗 **300s → 120s**;已翻译的区块始终即时保留。
  - 调度器新增 per-job `maxRetries` 覆盖(默认仍沿用全局值)。

### Notes

- 这是调度架构 P0 的第一项(消除"卡住")。其余 P0/P1——整表"完整保护"、active 页面升级为
  前台(`promoteActive`)、提取队列与 API 队列分离、整页任务拆成 chunk 级任务、真实阶段
  文案——会改动表格几何与调度粒度,需在真实 PDF 上验证,按"我改一块、你装 xpi 实测"推进。

## [0.9.8] — 2026-08-10

### Changed

- **翻译请求计划改造(第一阶段:软边界 + 高填充 + 补救封顶).** 效率优化全部发生在
  请求计划与调度层,页面结构(blocks / ID / 矩形 / 栏位 / 表格边界 / 阅读顺序)完全冻结不动。
  - **语义模块由硬边界改为软边界(上下文标签).** 以前"模块边界 = 请求边界",小标题一多
    就产生 4–8 个半空请求;现在字符预算才是真正的请求边界,普通页尽量打包成 1–2 个高填充
    请求。请求预算从 6000 提到 **8000 字符 / 24 块**,目标填充率 85%。
  - **模块被拆分时,标题作为 `moduleContext` 传给后续批次**(仅供理解,不再要求返回标题译文);
    标题只翻译一次。上下文直接来自原文,两个批次可并行翻译而不丢语义背景。
  - **补救流程封顶,杜绝请求风暴.** 单页总请求数硬性限制为 **初始 chunk 数 × 2 + 2**;达到上限
    即停止自动补救,已成功的译文全部保留,剩余留给「刷新本页」(熔断仍会把该页改投备用服务商)。
    英文残留补译也纳入同一预算,不再无限追加。

### Notes

- 这是调度层重构的第一阶段(高收益、低结构风险),不触碰任何页面几何。后续阶段(当前页双槽
  并行 + 后台预取让路、提取队列与 API 队列分离、不可变 PageLayoutSnapshot 与三层缓存)会改变
  取数/调度时序,需在真实 PDF 上验证,仍列于 Unreleased。
- 提示词只是新增了 `moduleContext` 的"仅理解"说明,输出格式不变,因此不提升 promptVersion
  (不使现有翻译缓存失效)。

## [0.9.7] — 2026-08-10

### Changed

- **模型选择器改版:分组 + 精选清单.** Each provider now offers a curated set of
  *current, paper-translation-suitable text models*, grouped in the picker as
  推荐 / 高质量 / 快速·低成本 / 预览版 / 旧版兼容, then a separator and 自定义模型….
  Image, audio/realtime, embedding, code-only, safety and retiring models are
  deliberately excluded. The list is a picker convenience, never a call
  whitelist — any model you type is still used, and preview models are labelled
  as such.
- **Refreshed default models** (low-latency / stable structured output over the
  strongest/most-expensive): OpenAI `gpt-5.6-luna`, Gemini `gemini-3.6-flash`,
  Anthropic `claude-sonnet-5`, Qwen `qwen3.7-plus`, Zhipu `glm-5`, Kimi `kimi-k3`,
  DeepSeek `deepseek-v4-flash`, Groq `llama-3.3-70b-versatile`, OpenRouter
  `openrouter/auto`, SiliconFlow `deepseek-ai/DeepSeek-V4-Flash`, Ollama `qwen3.5`.
- Retired aliases moved to 旧版兼容 or dropped: Qwen `qwen-plus/max/turbo`,
  Zhipu bare `glm-4-*` (dated ids kept under 旧版), OpenAI `gpt-4.1*`/`gpt-5-mini`.
- OpenAI-compatible / Custom endpoints show only 自定义模型… (unknown backend);
  Microsoft / Google / DeepL show no model field (fixed server-side model).

### Notes

- No account isolation and no online model-list fetching, by design — the
  built-in list plus a custom entry covers it. Model ids are compiled from each
  provider's official docs (2026-08-10); aggregator ids (OpenRouter/SiliconFlow)
  follow their consoles, so the custom entry is always available.

## [0.9.6] — 2026-08-10

### Fixed

- **首次打开慢:当前页被提取两次.** Opening the reader ran `getPageData()` for the
  current page twice back to back — once in `prime()` to measure the body font
  size, then again in `extractPage()` for the real work — up to two 8 s
  round-trips on a slow PDF. `prime()` now keeps that page's char stream and the
  first `extractPage()` of the page reuses it (one-shot; a re-translate still
  re-reads fresh), removing the duplicate round-trip.

### Notes

- This is the first, zero-risk step of the extraction-pipeline review. The larger
  items (table modeling moved ahead of translation with per-cell ids;
  structure-aware coalescing; a separate extraction queue) change table geometry
  and layout and must be validated against real PDFs, so they are staged in
  Unreleased rather than shipped blind.

## [0.9.5] — 2026-08-10

### Fixed

- **内置模型清单里部分模型不可用.** Re-verified every provider's model IDs against
  official docs and replaced retired ones:
  - **OpenAI**: `gpt-5-mini` / `gpt-4o-mini` → the current **GPT-5.6** family
    (`gpt-5.6-luna` default, plus `gpt-5.6-terra` / `gpt-5.6-sol`).
  - **Kimi (Moonshot)**: `moonshot-v1-*` is being sunset and `kimi-k2` / `kimi-latest`
    were discontinued → **`kimi-k3`** (plus `kimi-k2.6` / `kimi-k2.7-code` / `kimi-k2.5`).
    The default Base URL also moved to **`https://api.moonshot.ai`**.
  - **智谱 GLM**: the bare `glm-4-flash` / `glm-4-air` / `glm-4-flashx` aliases are
    retired → the dated IDs **`glm-4-flash-250414`** (free), `glm-4-air-250414`,
    `glm-4-flashx-250414` (plus `glm-4-plus` / `glm-4-airx`).
  - **DeepSeek**: removed the discontinued `deepseek-chat`; keeps `deepseek-v4-flash`
    (default) / `deepseek-v4-pro`.
  - **OpenRouter**: refreshed to current slugs (`deepseek/deepseek-v4-flash-latest`
    default, `deepseek/deepseek-v4-pro`, `google/gemini-2.5-flash`).
  - **Gemini**: added `gemini-3.5-flash` / `gemini-3.6-flash` (2.5-flash still default).
  - **Anthropic**: added `claude-fable-5`. **Qwen**: added `qwen3.7-plus/flash`.
    **Ollama**: suggestions bumped to `qwen3` / `llama3.3` / `gemma3`.
- The catalog remains a *fallback*, never a whitelist — any model you have typed
  yourself is still shown and used; only the built-in suggestions changed.

## [0.9.4] — 2026-08-10

### Added

- **语义模块锚点 (semantic layout modules).** Blocks are now grouped into modules
  anchored on structure — a heading and the paragraphs beneath it, a figure, a
  table, the references section, or a virtual 「column-continuation」anchor for the
  top of a column that continues a section with no heading of its own. A module
  is used only to give the model the section's CONTEXT: every heading, paragraph,
  caption and reference still carries its own id, rectangle and translation and
  is replaced in place independently — modules are never rendered as one merged
  block. New pure module `reader/layoutModules.ts` (`buildLayoutModules`).
- **模块级翻译分批.** Requests are now chunked by module (`chunkByModules`), so a
  heading reaches the model together with its paragraphs instead of being split
  across requests by a blind character budget. Small modules still share a
  request; a module larger than the budget is split but keeps reading order.

### Fixed

- **粗体小标题被当成正文.** Heading detection was size-only, so a bold subheading
  typeset at the body size (very common in medical/《…》journals) was classified as
  a paragraph and merged into the text around it. `classifyBlock` now also uses
  the embedded font weight (`isBoldFontName`): a bold, short, non-sentence line at
  body size is recognised as a subheading. Guarded hard (≤2 lines, <90 chars, not
  ending like a sentence) so bold emphasis inside a paragraph is not promoted.
- **模块不再跨栏.** Each block now records its column; a module never spans the
  gutter, so a right-column heading can no longer be associated with left-column
  body. Figures, tables and the references heading are hard anchors that break the
  running body.

### Notes

- Per-block in-place replacement, rectangles and the strict-fit renderer are
  unchanged — this release changes how blocks are GROUPED for context, not how
  they are replaced. `SourceBlock` gains optional `column` and `moduleId` fields.

## [0.9.3] — 2026-08-10

### Fixed

- **Cross-provider配置串味 (root cause).** Base URL and model were single GLOBAL
  prefs shared by every provider, so a model typed for one provider (e.g.
  `gpt-4o`) stayed in the field after switching to another (e.g. Gemini) and was
  sent there → `INVALID_MODEL` / HTTP 404. Each provider now keeps its OWN Base
  URL / model / custom model in a per-provider profile (`providerProfiles`), and
  BOTH the primary and every parallel provider read from their own profile — no
  more bleed. Old global values migrate ONCE into the currently-selected
  provider only.

### Added

- **Per-provider 模型选择器.** LLM providers show a dropdown of current models
  (recommended first, marked 推荐) plus 自定义模型…; the custom model is saved per
  provider and restored when you switch back. A saved model that is not in the
  built-in list is never dropped — it is shown as the current custom model. The
  built-in model catalog records the date it was checked and cites each
  provider's official docs; it is a fallback, never a whitelist. Fixed MT engines
  (Microsoft/Google/DeepL) hide the field with 「该服务商无需选择模型」.
- **Per-provider Base URL 编辑.** Each provider shows its 默认地址, a
  「当前使用自定义地址」marker when overridden, a 「恢复默认地址」button, and a separate
  「实际请求地址」line computed from the SAME URL builder the transport uses (so the
  preview can never drift). URL normalization avoids `/v1/v1`,
  `/chat/completions/chat/completions`, and the doubled native paths for
  Anthropic (`/v1/messages`) and DeepL (`/v2/translate`).
- **差异化的连接测试.** 「测试」now runs against the LIVE, unsaved Base URL / model in
  the pane and reports a specific reason — API Key 无效 / 模型不存在或路径不对 /
  限流 / 额度不足 / 超时 / 网络错误 etc. The API Key is never printed in any result.

### Changed

- Refreshed default models to current, verified IDs (2026-08-10, official docs):
  OpenAI `gpt-4o-mini` → `gpt-5-mini`, DeepSeek `deepseek-chat` (discontinued
  2026-07-24) → `deepseek-v4-flash`, Anthropic default → `claude-sonnet-4-6`.
  Any model you have saved yourself is untouched.

### Security

- API keys continue to live only in the OS credential store (Mozilla Login
  Manager) — never in `providerProfiles`, never in logs, never in the test result.

## [0.9.2] — 2026-08-10

### Added

- **自定义性能模式 (custom performance mode).** A fourth mode alongside 稳定/自动/
  高速 that sets the max parallel pages PER enabled provider: cloud LLM 1–6
  (default 3), traditional paid MT 1–4 (default 3), Ollama 1–2 (default 1); free
  engines are shown as 1 and locked. Each provider's value is saved separately
  (`providerConcurrency` pref), kept when switching to another mode and restored
  on return; a newly-enabled LLM defaults to 3; safe throttling (429/timeout
  back-off) still applies. 「恢复默认」resets only the concurrency values, not keys
  or provider selection.
- **性能与并行 settings redesign.** Horizontal segmented mode selector (selection
  shown by background + border + the native mark, not colour alone) with a
  description that changes per mode; a 「当前配置」summary card showing 预计并行 N
  页 · 当前页优先 (· 自动调节中 in auto), per-provider chips (wrapping, free chips
  dimmed), and an explicit line for why the real parallelism can be below the
  global ceiling (全局上限 vs 服务商能力合计). Custom mode expands per-provider
  page-limit rows. The summary updates immediately on any change.

### Notes

- The global 最大并行页面数 (1–24, default 12) and the actual-parallelism rule
  `min(global, Σ lane caps, schedulable pages)` are unchanged from 0.9.0; the
  wording avoids implying throughput always multiplies by provider count.

## [0.9.1] — 2026-08-10

### Fixed

断句与残留 (in-page segmentation):

- **行尾断词不再残留 (line-break de-hyphenation).** Line-broken words rejoin across
  all four hyphens — `-` `­` `‐` `‑` — when a Latin letter sits on both sides, so
  `sen-`/`sory` → `sensory` and no more `ional` / `est` / `sory` fragments. The
  span path joins a paragraph's lines with `joinLines()` (de-hyphenate + CJK
  without spaces) instead of a naive space-join; the char-stream path was widened
  to the same hyphen set and both letter cases.
- **编号列表不再误判为大标题 (numbered list vs section heading).** A single-level
  `1.` / `2)` / `10.` at body font size is a list item; a larger numbered line
  (`3. Model Architecture`) or a multi-level `1.1` / `4.6.1` is a heading. A
  short numbered list at a page foot is no longer read as a title.
- **段内英文残留局部补译 (local English-residue re-translation).** A block that
  passed validation but still carries a run of 6+ consecutive untranslated
  English words is re-translated ON ITS OWN (single-block request, capped 8/page)
  and patched in place — the block is replaced only if the retry clears the
  residue, and the page is never re-extracted or wholesale re-rendered.

对照同步 (side-by-side sync):

- **换页不再跳到页首 (no snap on page change).** A page change no longer forces the
  pane to the new page's top; the continuous updateviewarea → anchor sync keeps
  the two sides aligned to the reader's exact position, so page transitions are
  smooth. Page-change now only updates the label and translation priority.
- **锚点不再被截断 (untruncated scroll anchor).** The page scroll ratio is no longer
  clamped to 0–1, so a partly-visible page maps to the real position instead of
  snapping to an edge (drift-free across many pages because each page anchors on
  its own offset).
- **右页尺寸取自左页实际像素 (right page matches the left page's actual size).** The
  pane's display scale is derived from the current PDF page's real rendered
  `clientWidth` rather than re-deriving from PDF-points × viewport scale, so
  corresponding pages match within ~1px.

### Deferred

- Absolute-positioned page labels (step 6) — the per-page anchored sync already
  prevents cumulative drift, so this was unnecessary for correctness and would
  have required an `offsetParent` refactor; left as-is.
- True cross-page sentence merge (step 8) — kept out per the review's own
  recommendation; the next visit re-runs; a cross-page 断词 is de-hyphenated
  within a page but not merged across the page boundary yet.

## [0.9.0] — 2026-08-10

### Added

- **性能模式:稳定 ｜ 自动(推荐) ｜ 高速 (performance modes).** 「性能与并行」now
  has three modes that control how EACH provider runs — its per-lane
  concurrency band, prefetch reach and dynamic throttling:
  - **稳定** — free 1 / cloud LLM 2 / local 1 / paid-MT 2; prefetch +2/−1. Fewest
    requests, highest success rate, least likely to hit limits.
  - **自动(默认)** — free 1 / cloud LLM 3→6 / local 1→2 / paid-MT 3→4; prefetch
    +min(2N,10)/−1. Each lane grows on sustained success and backs off on
    429/timeout, per provider.
  - **高速** — free 1 / cloud LLM 6 / local 2 / paid-MT 4; prefetch +12/−2. Fills
    each provider's capacity and prefetches aggressively; still auto-throttles on
    429/timeout and never exceeds a provider's own limit or the global ceiling.

### Changed

- **「最大并行页面数」改为纯全局上限 (global ceiling, no more 0=自动).** Now a plain
  number 1–24 (default 12) capping the pages ALL providers run at once. The
  actual parallelism is `min(global ceiling, Σ current lane caps, schedulable
  pages)`, so raising it never forces a provider past its own lane. Legacy
  values migrate: 0/absent → 12, >24 → 24, 1–6 kept.
- **调度带上下限 (adaptive bands with min/initial/max).** Lane caps are now bands:
  auto mode grows a lane from its initial toward its max on a run of clean
  successes and backs off toward its min on 429/timeout; stable and high are
  fixed. Only the erroring provider is throttled.
- Settings pane: mode radios + a live 「当前配置」preview (预计并行 N 页 · 当前页
  优先, with each provider's current lane), and the global-ceiling field
  relabelled and re-ranged 1–24.

## [0.8.10] — 2026-08-09

### Fixed

- **缩放时译文闪现后消失 (zoom no longer drops overlay translations).** The
  on-page overlay's redraw lifecycle is fixed — the translations were never
  lost (they stayed in memory); only the DOM layer vanished. Four root causes:
  - **重绘请求累积 (redraw requests accumulate).** `scheduleRedraw()` kept only
    the LAST event's page (it reset the timer and captured one index), so during
    a zoom storm every page but the last was dropped and never repainted. It now
    merges a dirty-page set (and a redraw-all flag for scalechanging /
    rotationchanging) into one pass.
  - **原子替换 (atomic layer swap).** The new layer is now built in full and only
    swapped in once it has real content — the old layer stays visible right up to
    that frame. Previously the old layer was removed up front, so any mid-zoom
    early-return left the page showing only the original.
  - **签名只在挂载成功后保存 (signature saved after mount).** The geometry
    signature that suppresses redundant redraws is now recorded ONLY after a real
    layer mounts; an aborted or empty draw leaves it unset so the next event
    retries, instead of marking a blank page "done".
  - **失效节点保护 (stale-node guard).** If PDF.js swapped the page div during the
    debounce, the draw reschedules onto the fresh node instead of painting a
    layer that is about to be discarded.

## [0.8.9] — 2026-08-09

### Changed

并发调度重构 — 每服务商独立限流 + 真正的多服务商并行(引擎层,无 UI 模式选择器):

- **每服务商独立限流 (per-provider lanes).** The scheduler now caps concurrency
  PER PROVIDER (a "lane") instead of by one global number keyed off the main
  provider. Each provider runs its own pages in parallel up to its own cap;
  another provider's busy lane never blocks a page on a different lane. Lane
  caps come from a per-type profile: free MT (Bing/Google) 1, DeepL-style paid
  MT 3, cloud LLM 3, local (Ollama) 1.
- **全局并发自动计算 (auto global cap).** The global page-task cap is the SUM of
  the enabled providers' lane caps, clamped to [2, 24] — e.g. Google-free +
  OpenAI + Gemini = 1 + 3 + 3 = 7. The setting is renamed 并发请求数 →
  **最大并行页面数**, defaulting to **0 = 自动**; a value >0 is an optional manual
  ceiling. Actual HTTP requests are managed internally, not exposed.
- **多服务商真正并行 (pool actually multiplies throughput).** Neighbour prefetch
  is now enqueued immediately (not only after the current page finishes), so
  other providers' lanes fill while the current page translates. The current
  page's provider lane always keeps one reserved foreground slot, so the visible
  page never waits for a prefetch.
- **预取窗口随服务商数扩展 (pool-sized prefetch window).** Forward = min(2 × pool
  size, 12), backward = 1 (was a fixed ±1). Nearer pages get higher priority.
- **动态降速 (per-lane adaptive throttling).** A `429` halves the offending
  lane's cap; a timeout drops it by one; `Retry-After` is honoured when present;
  a run of clean successes slowly restores one slot. Only the erroring provider
  is throttled — never the whole pool. Session-scoped, not persisted.

## [0.8.8] — 2026-08-09

### Changed

- **通知彻底统一到胶囊 (single capsule is now the ONLY notification surface).**
  The bottom-center success toast module (`.pm-toast`) is fully removed —
  fields, DOM, `toast()` method, timer and CSS. Transient successes (译文已复制 /
  已保存为子笔记 / 缓存已清除) now flash as a new capsule `notice` state: a ✓ over
  a green ring with the message, auto-hiding after ~1.9s, priced just under a
  failure so it shows over active work then reverts to whatever was underneath.
- **刷新全部 / 清除缓存不再弹底部提示.** Both immediately re-translate the current
  page, so the capsule's own progress IS the confirmation — no separate toast.

## [0.8.7] — 2026-08-09

### Changed

翻译效率专项(按审核清单逐项实施):

- **1. 段落分组指标 (grouping metrics).** Extraction now logs
  `fragments → units (ratio)` per page and warns when a fragment-heavy page
  barely merges (ratio > 0.85 at ≥40 fragments) — the signal that the semantic
  coalescer is not working for that layout. Page completion logs
  units / segment-cache hits / chunks / requests / salvage / untranslated / ms.
- **2. 首次请求携带排版字符预算 (initial char budget).** Prose blocks with real
  geometry now send `charBudget ≈ Σ(line width / font) × 0.9` in the FIRST
  request (zh targets), so most paragraphs fit on the first pass instead of the
  translate → measure-fail → compress → re-measure round trip. The prompt's
  budget rules (dense phrasing, never dropped facts/numbers/units/citations)
  apply from the start.
- **3. 段落级缓存与增量刷新 (segment-level cache + tiered refresh).** Beneath the
  page cache there is now a per-segment store keyed by content+language hash and
  scoped by provider/model/promptVersion/glossary. 圆环「刷新本页」is now 普通刷新:
  it bypasses the page cache but reuses qualified segments — only untranslated /
  previously-invalid segments cost new requests (with a provider pool it still
  rotates engines, whose separate store forces a genuine re-translation). 菜单
  「刷新全部」remains 强制: it clears everything. Compress-accepted shorter
  translations overwrite their segment entry, so a refresh cannot resurrect a
  translation that already failed placement.
- **4. 缺失 ID 分级补救与服务商熔断 (tiered salvage + circuit breaker).** Missing
  ids now recover in tiers: full-batch retry → SMALL batches of 4 → single-block
  requests only for the leftovers, instead of one request per dropped block.
  When >25% of a chunk is still missing after batch+retry, `onProviderUnstable`
  fires once per page and the session deals the page's remaining requests to the
  next engine in the pool (logged; single-provider setups log and continue).

## [0.8.6] — 2026-08-09

### Changed

- **圆环颜色改为状态语义 (semantic ring colours).** The ring colour now means a
  STATE, never a provider or a per-page hue: 翻译中蓝 `#6C9BFF`, 排版紫
  `#8B7CF6`, 完成绿 `#37C871`, 部分完成橙 `#F5A623`, 失败红 `#FF6B6B`, 已停止灰
  `#8B93A1`, 空闲深灰蓝 `#748096`, 底部轨道 `rgba(255,255,255,.16)`. All routed
  through CSS variables. The centre percentage is ALWAYS white (never tinted by
  the ring), and the stop button is red only on hover — not a permanent alarm.
- **永远优先翻译当前页 (the visible page always wins scheduling).** Two scheduler
  gaps closed:
  - **已排队页可被提升 (queued pages can be promoted).** Navigating to a page that
    was already enqueued as a low-priority prefetch used to hit an early return
    and keep its prefetch priority. `RequestScheduler.promote()` now raises it to
    the foreground current-page priority (and `setCurrentPage` calls it), so it
    stops waiting behind neighbours.
  - **当前页专属并发槽 (a reserved foreground slot).** The scheduler reserves one
    slot for foreground work (`reservedForeground`), so background prefetch can
    never occupy every slot and make the visible page wait for a neighbour to
    finish. Even on the free engines' 2 slots, prefetch is capped at 1.
- Prefetch window narrowed to `[current, next, previous]` (dropped current+2);
  explicit priority tiers (retranslate 1000 > current 900 > compress 850 > next
  100 > previous 80 > second-next 20); neighbour prefetch is never enqueued
  until the current page is done; pages leaving the window are cancelled.

## [0.8.5] — 2026-08-09

### Changed

- **缩小圆环在两种翻译模式中常驻右下角 (the shrunk ring is now persistent and
  shared).** Two problems fixed together:
  - **折叠状态共享 (shared collapsed state).** The collapsed/expanded state is no
    longer stored inside each of the two `StatusCapsule` instances (overlay vs
    pane) — the `ReaderSession` owns one `capsuleCollapsed` flag and mirrors it
    onto both surfaces (`onCollapsedChange` callback + `setCollapsed`). Collapse
    in 覆盖原文, switch to 对照翻译 → still collapsed, and vice-versa.
  - **常驻 idle 状态 (persistent idle state).** When the task queue empties the
    capsule no longer calls `setProgress(null)` and disappears. It renders a new
    `idle` phase: a bottom-right resting ring showing ✓ 本页翻译已完成 if the
    current page is translated, or ↻ 点击圆环翻译本页 if it never was. The center
    still re-translates the page.
- **完成后自动缩小而非隐藏 (done auto-collapses, not auto-hides).** A finished
  page shows its full “已完成” message for ~2.2s, then auto-collapses into the
  persistent idle ring. Failed and partial states never auto-collapse — they
  stay expanded until the user acts.
- The idle ring retargets the current page on scroll (✓ vs ↻), and 原文 mode
  shows no capsule (there is nothing to refresh there).

## [0.8.4] — 2026-08-09

### Fixed

- **百分比稳定居中 (percentage sits at the ring's true centre).** The center
  button is now a fully-reset native `<button>` (appearance / margin / min-width
  / text-align / text-indent / box-sizing all cleared) sized to the full 34×34
  ring, and the `%` moved into an independent `.pm-ring-label` span
  (`inset: 0; display: grid; place-items: center; pointer-events: none`). Host
  platform button defaults can no longer nudge the number, and “100%” no longer
  overflows a too-narrow (24px) button.
- **折叠后不再变高 (collapsed capsule keeps the same 56px height).** Collapsed
  state used `padding: 6px` around a 56px shell (= 68px tall) while expanded was
  56px, so the widget jumped taller when collapsed. Collapsed is now a fixed
  56×56 box (`padding: 0; gap: 0`); the SVG stays 34×34, so only the clickable
  background fills the square — the ring itself does not grow.

## [0.8.3] — 2026-08-09

### Changed

- **胶囊圆环改用真实 DOM 点击分层 (real hit layers, no distance guessing).** The
  ring no longer decides center-vs-edge by measuring click distance
  (`dist > rect.width * 0.42`), which left the capsule stuck-collapsed. It is now
  three real, stacked elements:
  - **圆环中心按钮** (`.pm-ring-refresh`, over the %) → 重新翻译本页; it stops
    propagation so refreshing never doubles as expand/collapse. Works in both
    expanded and collapsed states.
  - **圆环外圈** (`.pm-ring-progress` SVG) → progress display only,
    `pointer-events: none`, never a hit zone.
  - **文字区域** (`.pm-body`) → 收起.
  - **收起后的方形背景** (`.pm-ring-shell`, enlarged to 56×56px) → 展开, via the
    capsule's own click handler that fires only while collapsed.
- **停止按钮改用 ■ (stop, not pause).** The right-hand action during
  translation/layout is now `■ 停止任务` — the backend cancels and restarts, so a
  pause/resume glyph would misrepresent it.
- Hover tooltips: center reads “重新翻译本页”, the collapsed background reads
  “展开任务详情”.

## [0.8.2] — 2026-08-09

### Fixed

- **通知按钮不再执行错误动作 (retry no longer mis-fires).** A non-translation
  failure — save note, copy, clear cache, open — now shows a `×` that dismisses
  the notice, instead of a 重试 that wrongly re-translated the page. Only real
  translation failures offer 重试.
- **「查看保留原文」真正定位 (view-kept-original actually locates).** The capsule's
  查看 action now scrolls to the page that kept segments in the source language
  and briefly highlights those exact segments (the strict `[data-pm-unfit]`
  boxes, or the pending blocks in the pane) — previously a no-op.
- **表格单元格不再重复计数 (no double-count of table cells).** Placed table text
  cells are counted once (they are ordinary committed items); the final tally is
  `placed = committed`, so pages no longer over-report placement.
- **保留原文与失败区分开 (kept-by-design vs real failure).** Table cells kept on
  purpose (numeric/data/spanning) no longer count as failures, so a fully-placed
  page reads 已完成; cells that truly failed to translate or place count as kept
  and correctly show 保留原文.
- **导出与翻译不再互相覆盖 (export and translation stop overwriting each other).**
  The single capsule is now backed by a task queue: when a page translates while
  a PDF exports, the capsule shows the highest-priority task (failed > active
  export > partial > active translation > terminal) instead of whichever updated
  last.

### Removed

- Unused `StatusCapsule.setStatus()`, the dead internal `autoHide` field (the
  session now owns auto-hide via the task queue), and a stale `.pm-status-row`
  comment. The whole-page dim during a page refresh is gone — only the refresh
  button spins now, so translated text no longer appears to vanish mid-refresh.

## [0.8.1] — 2026-08-09

### Changed

- **单一通知组件 (StatusCapsule is the only task/error notifier).** The legacy
  floating status note (`statusNote` + `pane.setStatus`) is deleted from the
  pane and its CSS; `PdfOverlay.setStatus` is gone too. All task and error
  feedback — translation, layout, partial, failed, cancelled, AND PDF export —
  now flows through the one capsule via a progress model, so two components can
  never show conflicting status.
- **PDF 导出走胶囊 (export uses the capsule).** Export progress/done/failure
  render in the capsule (`task: 'export'`); the duplicate completion Toast is
  removed. Toast is now reserved for transient successes only — copy, save
  note, cache cleared.
- **错误进入状态机 (errors go through the model, not raw strings).** Open
  failures and action errors post a `failed` capsule state instead of an
  ad-hoc status string; the translation-success Toast is dropped (the capsule's
  done state is the single completion signal).



### Added

- **圆环两段式交互 (two-zone ring).** Clicking the ring's OUTER edge expands or
  collapses the capsule's detail body; clicking the INNER disc (where the % is)
  re-translates the current page. The hit test splits the ring at ~42% of its
  radius, so the two gestures don't collide on the small ring.

### Changed

- **优先翻译当前页 (current page first).** The page you are on now gets a
  dominant scheduler priority AND the concurrency to itself — neighbour pages
  are not prefetched until the current page is done, so a slow free engine never
  spends its slots translating pages ahead while the visible page waits. Once
  the current page finishes, its neighbours prefetch as before.



### Fixed

- **状态提示不再双显示 / 不再回退到 0% (one state model).** The capsule and the
  old pane status pill were both driving translation feedback, so a page showed
  "✓ 第 3 页已翻译" and a "0%" ring at the same time. All translation-process
  feedback now flows through the ONE capsule; the old `pane.setStatus` is no
  longer called for translating/done/error, and the `done` branch no longer
  shows a completion tick — the FINAL done/partial state is posted only by the
  placement pass. Non-translation messages (export, open failure) still use the
  pane pill.
- **进度百分比连续、不超 100% (combined, monotonic progress).** The ring now
  shows combined progress `(translated + placed) / (2 × total)` — translation
  is the first 50%, placement the second — so a fully-translated page entering
  layout sits at 50% and climbs, instead of resetting to 0%. Placement counts
  use one consistent denominator (placed + real failures), so the ring can
  never exceed 100%.
- **统一的"保留原文"判定.** Partial vs done is decided solely by real failures
  (`abandoned + untranslated`); intentionally-original content (figures, tables,
  tiny fragments) is no longer double-counted by a second status component, so
  the two surfaces can't disagree.
- **取消/圆环交互修正.** In 对照翻译 mode the capsule's 取消 button now actually
  cancels the page (it was wired to re-translate); and 覆盖原文 mode's ring now
  receives its `onRefreshRing` handler, so clicking the ring re-translates the
  current page in both modes.

## [0.7.9] — 2026-08-09

### Changed

- **对照翻译模式也用同一个状态胶囊 (same capsule in the side pane).** The status
  capsule is now a shared component (`statusCapsule.ts`) used by BOTH 覆盖原文 and
  对照翻译 modes, so the pane's bottom-right shows the same real progress ring,
  page position, and honest 翻译 a/b · 排版 c/b counts as the overlay. Progress
  is routed to whichever surface is visible; the other's capsule is dismissed.

### Added

- **点按圆环 = 刷新本页 (click the ring to re-translate the current page).** The
  progress ring is now a button: clicking it re-translates the page you are on
  (rotating to the next pool engine, as before).
- **菜单栏刷新按钮改为「刷新全部」(top button = re-translate everything).** The
  pane menu-bar refresh button now clears the whole document's cached +
  in-memory translations and re-translates; because translation is lazy, the
  current page re-runs now and the rest re-translate as they are viewed. Cancel
  in the capsule now works in both modes.

### Changed

- **覆盖原文模式合并为一个状态胶囊 (one status capsule).** The separate floating
  refresh button (0.7.6) and the status pill are gone; 覆盖原文 mode now has a
  single bottom-right capsule (~260px, 16px radius, offset 22px) with a REAL
  progress ring (no fake infinite spin — the arc reflects an actual fraction),
  a main line `正在处理 第 3 / 12 页` (current page / total — translation is
  lazy, so this is position, not a document-wide bar), and a sub line with
  honest per-page counts `翻译 a/b 段 · 排版 c/b 段` so you can tell "translating
  slowly" from "translated but layout failed".
- **Distinct, honest end states.** 翻译中 / 排版中 (ring) → 已完成 (✓, auto-hides
  after 2s) / 部分保留原文 (!, persists, 查看 opens the pane for per-block detail)
  / 翻译失败 (!, persists, 重试) / 已停止翻译 (—). "Done" is never shown while
  segments are still unplaced — a partial page says so with its kept count.
- **Cancel + collapse.** The right-hand button cancels the current page's
  translation while it runs; clicking the capsule body collapses it to just the
  ring (and back). Placement counts come from the strict renderer's real tally
  (committed paragraphs + translated table cells vs kept-original).

## [0.7.7] — 2026-08-09

### Fixed

- **半段翻译/中英混杂不再被当成功 (target-language RATIO check).** The
  completeness check accepted any response containing a single CJK character,
  so a half-translated or English/Chinese-mixed response passed and the page
  stored it as done. A Chinese target now requires a prose source (≥6 Latin
  words — a real sentence, not a label or acronym/numeric cell) to come back
  PREDOMINANTLY Chinese: CJK characters over (CJK + Latin words) must be ≥0.45.
  A few embedded acronyms (PCCT, MRI) still pass; a mostly-English response is
  rejected and re-translated whole. Short cells/labels and non-CJK targets are
  unaffected.

## [0.7.6] — 2026-08-09

### Added

- **覆盖原文模式的浮动刷新按钮 (floating 刷新 in overlay mode).** In 覆盖原文
  (on-page overlay) mode the translation pane is hidden, so its 刷新 button
  was out of reach. A round refresh button now floats in the bottom-right
  corner whenever the overlay is on — just above where the status pill appears,
  so the two never overlap. It re-translates the current page (rotating to the
  next pool engine, like the pane's 刷新), spins while the request is in flight,
  and is removed when the overlay is turned off.

## [0.7.5] — 2026-08-09

**语义段落组 (semantic paragraph groups).** The coalesced region IS the
paragraph group: one id, one translation request, one strict node, one union
rectangle, per-line masks, and an all-or-nothing commit — there is no
member-by-member commit inside a group by construction. The mixed-language
paragraphs came from fragments that FAILED to join a group; this release makes
grouping authoritative.

### Fixed

- **Length is no longer a rejection criterion for continuations.** A
  lowercase-opening fragment ("least as robust, if not better, than…") is the
  middle of someone else's sentence no matter how long it is — the old ≤60-char
  shard cap left exactly these long fragments as independent blocks, stranded
  in English inside a Chinese paragraph. Absorption's total-length gate is also
  relaxed (bounded at 1.5× the region cap) so the tail of a cap-split paragraph
  can rejoin instead of being stranded.
- **Colon no longer ends a sentence in the region separator** (the same
  contradiction fixed in paragraphHeuristics in 0.7.4, in regionCoalescer's own
  copy): a colon-ending fragment joins with a space, never a paragraph break.
- **Groups keep provenance.** A merged region records `memberIds` — the ids of
  every extraction fragment it absorbed, in reading order — so the source
  relationship survives the merge instead of being lost.

## [0.7.4] — 2026-08-09

### Fixed

- **冒号/分号行不再断段 (colon/semicolon merge contradiction).** `endsSentence`
  counted ":" and ";" as sentence-final, while `danglingEnd` counted the same
  marks as mid-sentence — so a line ending in a colon or semicolon satisfied
  both, and the merge condition (`!endsSentence && (startsContinuation ||
  danglingEnd)`) always rejected it. Such a line was stranded as its own
  one-line block and left in English. Colon and semicolon are no longer treated
  as sentence-final, so these lines rejoin their paragraph.
- **译文完整性/语言校验 (target-language check).** A response was accepted as a
  translation as long as it was non-empty — so a provider that echoed the
  English back, or returned it untranslated, was stored as "done" and the page
  showed English that was indistinguishable from a layout failure. Responses are
  now checked: for a Chinese target, a translation of prose (≥3 English words in
  the source) must contain CJK characters, or it is treated as missing and goes
  through the retry/salvage path; a page that still can't be fully translated is
  left uncached so a revisit retries. Acronym/numeric cells (legitimately
  CJK-free) and non-CJK targets are unaffected.

Still ahead (from the review, not yet done): atomic per-semantic-paragraph
commit so a residual multi-block paragraph can't show half Chinese/half
English; absorbing long (>60-char) continuation fragments the paragraph merge
still misses; retrying a whole failed segment rather than many line fragments;
and an end-to-end two-column page test asserting no non-term English remains.

## [0.7.3] — 2026-08-09

### Fixed

- **残留的段中英文行 (residual mid-paragraph English lines).** After 0.7.2 fixed
  the wholesale line-fragmentation, a few lines mid-paragraph (often the
  citation-bearing ones) still stayed in English. The repair pass `planMerges`
  was rejoining split fragments but still gated on the same unstable *column
  index* — a line whose index had flipped never rejoined, so it stood alone as
  a one-line block and its Chinese couldn't fit a one-line box. The same-column
  test is now geometry-first: when both fragments have rectangles (the real
  pipeline) they belong together iff their x-ranges overlap; the column index is
  only a fallback for rect-less inputs. Different columns never share an
  x-range, so columns still never interleave.

## [0.7.2] — 2026-08-09

### Fixed

- **正文间隔不翻译 (alternating untranslated lines).** On narrow two-column
  pages a paragraph was being cut into one-line blocks, each translated alone
  and dropped into a one-line box — a line whose Chinese needs two lines could
  not fit and stayed English, so translated and original lines alternated down
  the column. The cause: a paragraph break was forced whenever two stacked
  lines got different *column indices*, and the column detector flips that index
  row-to-row on a narrow layout. Column continuity is now decided by geometry
  alone (two stacked lines that share an x-range are one column; lines in
  different columns never share an x-range), so paragraphs stay whole and
  translate as units. Applied to both the char-stream and text-layer builders.
- **表格整表未翻译 (whole text table left in English).** A table cell was flagged
  "data → keep original" if it merely clipped a second column band (a 0.35
  overlap test); a legitimately wide column like "Key results" trips that, so a
  5-column text table had most cells flagged and stayed English. A cell now
  counts as spanning only when it actually COVERS ≥2 bands (>0.5 of each) —
  which only a true full-width stitched fragment does — so wide prose columns
  translate while genuine cross-column fragments still stay original.

## [0.7.1] — 2026-08-09

**表格单元格模型 (table cell model).** From this release on, every fix bumps the
patch version by 0.0.1.

### Added

- **Table cell model — text cells now translate in place (`tableStructure.ts`).**
  A detected table region is no longer kept wholesale in English. Its Row/Cell
  grid is inferred geometrically (columns from the members' x-extents seeded
  narrowest-first so a spanning cell can't fuse two columns; rows from their
  y-extents), and each cell is handled on its own: prose cells (labels,
  recommendations, prose headers like "2025 Recommendation") are translated and
  replaced inside their own rectangle through the same measure-before-commit
  pipeline, while data cells (numbers, value±sd, ranges, symbols), whole
  numeric columns, and any fragment stitched ACROSS columns stay original — so
  a data table's figures and alignment are never disturbed and nothing is ever
  stamped across the grid. Each cell carries a stable id
  (`page-<p>-table-<t>-r<row>-c<col>`). The placement tally gains a
  `tableTranslated` count beside `tableExcluded`.

Still ahead: ruling-line detection from the operator list for tables drawn with
rules rather than whitespace; splitting a multi-block cell's translation across
its members; and paragraph-granular placement so one overflowing line can't hold
back a long paragraph.

## [0.7.0] — 2026-08-09

**修复表格和图片排版问题 (table & figure layout fixes).** 整页对照重写为严格
原位替换:译文永远写在原文的矩形里,表格整体保留原样,图片零像素变化,页面
尺寸与原版完全一致。This release folds five review rounds of layout work into
one architecture: strict in-place replacement.

### Added

- **刷新 = 只重译当前页,并轮换引擎.** The refresh button re-translates ONLY
  the page you are on (bypassing its cache), and when 多服务商并行 is active
  it also deals that page to the NEXT engine in the pool — a page that came
  out poorly on one service gets a genuinely different translator, with the
  cache entry keyed to the newly chosen engine. Rotation resets when the
  provider/language configuration changes.
- GitHub Releases now carry this changelog section as their release notes
  (the auto-generated commit list follows below).

### Fixed

- **长文本不稳定 — 译文显示后又消失 (measure before commit).** The disappearing
  translation was deterministic, not random loss: the renderer showed every
  block's translation immediately, then — after fonts loaded and it re-measured
  — hid the ones that overflowed, so a long paragraph flashed in Chinese and
  reverted to English. The renderer no longer shows a block it might take back.
  Each block starts hidden with its ORIGINAL text visible (no mask painted);
  only a block measured to fit is revealed — mask and text committed together
  in one step. A block that cannot fit is never shown translated in the first
  place, so there is nothing to retract. Transitions are only ever English →
  Chinese (when a fix lands), never Chinese → English.
- **Compress rounds are counted per block, only once, on the final measure.**
  The measure pass runs several times per render (font-readiness insurance);
  only the final, fonts-settled pass now reveals blocks or spends a round, and
  an in-flight guard stops two compress requests racing on one page. Round
  counters are keyed per block (not per page), so two long paragraphs at the
  top of a page can no longer exhaust the budget for every long paragraph below.
- **Compressed retries applied in place; only shorter results accepted.** A
  budgeted retry patches just its own blocks into the live page — already-fit
  blocks never flicker on a re-render — and the manager rejects any retry that
  is not actually shorter than the translation it would replace, so a service
  echoing back the same (or longer) text can't waste a round or clobber a good
  result. Budgets are the tighter of the geometric estimate and the block's own
  measured need (`textLen × boxHeight/scrollHeight × 0.92`).
- **Free MT engines skip straight to shrink.** Character budgets only help
  prompt-driven engines, so provider capability is now an explicit
  `supportsCharBudget` flag (LLM/OpenAI-compatible = yes; Bing/Google/DeepL =
  no) rather than being inferred from the explain feature — non-budget engines
  no longer waste compress rounds.
- **Last-resort font shrink before abandoning a block.** A block still too long
  after its budgeted retries tries 94% then 88% of its fixed size (floor 8.5px,
  plus a new tightest 1.14/−0.02em ladder step) before giving up — a
  deliberate, bounded exception to the fixed-type-size rule. Only a block that
  fails even this keeps the original.
- **Stale renders can't overwrite the live page.** Each page render claims a
  generation token; an older render still finishing its async tail (bitmap,
  image rects, compress) bows out instead of flashing an outdated page in over
  a newer one.
- Scrolling no longer cancels the in-flight compress task of a page still near
  the viewport — the scheduler keeps `page-N-compress` alive for wanted pages.
- **Fit to the block's own leading, not a fixed floor (完整率).** The fit ladder
  now bottoms out at each block's ORIGINAL line spacing (median gap between its
  source line tops), never a blanket 1.14. A one-line heading whose rectangle
  is barely taller than its glyphs gets a ~1.0 step it can actually pass —
  short titles no longer fail placement outright — while a body paragraph is
  never crushed below its own leading. This markedly raises how many blocks fit
  in place instead of keeping English.
- **Every missing block is salvaged, not just the first eight.** The one-by-one
  salvage pass (single-block requests that can't suffer id drift) now covers
  ALL ids a provider dropped from a batch, with a log warning when an engine is
  systematically dropping many. Leaving a block untranslated to save a request
  was exactly the mixed-language page this was meant to prevent.
- **Honest placement accounting.** A strict page now reports a full tally —
  shown, won't-fit, untranslated, in-table, on-image, too-small — logged every
  render, and when any block is kept in English a non-blocking pane note says
  so ("本页 N 段过长，已保留英文"). "Translation complete" and "every block
  placed" are surfaced as distinct states: with rectangle-fixed, no-shrink-past-
  floor, no-continuation constraints, an arbitrarily long translation cannot be
  guaranteed to fit, so the rare true failure is now stated rather than left
  silently English.

- **Long-page translation no longer stalls on salvage.** Salvaging every
  dropped id one-by-one, strictly sequentially, made a page where the provider
  dropped many ids crawl. Salvage now runs in bounded-parallel waves (4 at a
  time) — still one block per request (no id drift), but a long page finishes
  in a fraction of the wall-clock instead of appearing to hang.
- **Tables stay cleanly original, never half-translated or bled over.** Any
  block overlapping a detected table region is now kept in the original — not
  only the cells the detector flagged, but also long recommendation cells that
  look like paragraphs and the stitched-across-cells paragraphs the extractor
  sometimes emits. This removes the mixed English/Chinese cells and the Chinese
  text that was overlapping table rows. (A table is all-original until the real
  cell model lands.)

### Changed (architecture)

- **整页对照 is now STRICT in-place replacement** (`strictPageReplacement.ts`).
  The page the reader sees is the original page — same size, figures, table
  lines, background and positions — with translations written into exactly
  the rectangles the source text occupied. No block moves, no page growth, no
  continuation sheet, no reflow. pageFlow (flow/packing/sweep) is no longer
  used by this mode; it remains available to the 文章流 mode where reflow is
  the point.
- **Fixed geometry, fixed type size.** Fit uses only the leading/tracking
  ladder (1.42 → 1.18, up to −0.02em); the font size never shrinks. Body
  blocks are typeset at their own body-cluster MINIMUM size
  (`replacementFontSize`: sizes filtered to [0.75×, 1.25×] of the median —
  drop caps and superscript citations excluded — then the smallest survivor),
  so a decorated first letter never inflates a paragraph and a 6pt citation
  never shrinks one.
- **Budgeted compress-and-retry.** A translation that cannot fit its
  rectangle is re-requested with a character budget
  (`estimateCjkCapacity` of the box; `charBudget` on the request; prompt
  rules demand denser academic phrasing, never dropped facts/numbers/units).
  Up to two rounds; a block that still cannot fit REVERTS to the original
  text (its masks are wiped) — never clipped, never overlapped, never moved.
  `PROMPT_VERSION` → 2, invalidating every cache entry produced under the
  old long-form prompts.
- **Masks hug the strokes and can never touch a figure.** Per-line masks use
  font-relative padding (0.08em, clamped 1–3px) instead of a fixed 2px, and
  the real image rectangles are wiped out of the mask canvas afterwards —
  `intersection(mask, image) === 0` holds by construction. A "paragraph" box
  overlapping an image by >15% is treated as an extraction error and left
  entirely alone. Table regions keep their whole original rendering (the
  cell-level model with per-cell translation IDs is the next stage).

### Added

- **Table protection.** Detected table regions (clusters of numeric/symbol
  cells, transitively merged across column strips, anchored by `Table N`
  captions, sweeping in row labels beside them) keep their ENTIRE original
  rendering: no cell is translated, nothing may be parked on the region.
  Real Table→Row→Cell re-layout stays future work; this stops translated
  fragments being stamped across data tables today.
- **Real image boundaries.** The operator list (walked with the matrix stack,
  via the poll-the-flags pattern — content promises are never awaited) yields
  every painted image's true rectangle; those join the flow as obstacles with
  exact horizontal extents and the sweep as no-park boxes. The luminance grid
  remains the fallback when the operator list is unavailable.
- **Header/footer guard bands.** Extraction deletes running heads/feet, so
  the layout never knew the furniture was there. Adaptive bands (never
  swallowing real source content) now make pushed blocks hop past the footer
  onto grown paper instead of flowing through it.
- **Final visual safety check with a safe fallback.** After settling, every
  page is checked for block-block overlaps, block-on-figure/table/band
  violations, and sideways clipping. A failing page is not shown wrong — it
  degrades to the untouched original page with the full translation flowed
  cleanly underneath.
- **Representative font size.** Block sizes now come from the MODE of the
  member lines (`dominantFontSize`) at build and at merge, not the first
  line — drop caps, superscripts and heading-styled lead-ins no longer skew
  a whole paragraph's translated size. Region merges follow the longer
  fragment's size.
- planFlow only hops obstacles a block actually intersects horizontally.
- **Shard absorption.** Bare citation markers ("(5,6)."), superscript runs
  and torn-off lowercase continuations ("ated light is isolated…") are no
  longer independent blocks: a looser second coalescing pass folds them into
  the adjacent body region, so they translate with their sentence instead of
  surviving as English crumbs below the replacement threshold.
- **Figure groups.** A bare "Figure N:"/"图 N" label re-unites with the
  caption text PDF.js tore it from (the union classifies as one caption and
  is translated whole), captions get a laxer replacement size gate, and the
  strip between an image and its caption is a no-park zone — the caption can
  grow downward but can never be separated from its figure.
- **Real pagination instead of unbounded growth.** Blocks pushed to or past
  the footer zone leave the absolute layout entirely and re-flow, in
  column-major reading order, on a tidy continuation sheet appended after
  the page ("本页译文续") — no more footer stranded mid-article with
  fragments and single-word slivers scattered after it.
- **Body text packs upward.** planFlow gains anchor semantics: headings,
  titles and captions still hold their source position, but ordinary body
  paragraphs now pack from the column cursor, reclaiming the whitespace a
  shorter Chinese paragraph leaves. Packing knows every hard box (images,
  tables, kept-original text, caption gaps) as obstacles, so it can never
  climb onto anything. Legacy callers without the flag keep the old rule.

### Fixed

- **The final overlap sweep can no longer park a block on a figure.** planFlow
  hops figure/table obstacles, but `resolveOverlaps` — the last global sweep —
  knew nothing about them: a block pushed down to clear another block could
  land squarely on a figure. The obstacles now join the sweep as immovable
  boxes (`obstaclesToBoxes`), with a regression test reproducing the exact
  push-onto-figure case.
- **The column-tightening pass no longer undoes the containment ladder.** When
  a column overflowed the page, the second pass blanket-reset every block in
  it to line-height 1.34 — making blocks that had settled at 1.24 *taller*
  and re-breaking their own boxes. It now only ever tightens
  (`min(current, 1.34)`).
- **Borderline boxes no longer seep past their bottom edge.** The fit
  tolerance shrinks from ±2px to ±0.5px, and the containment ladder gains a
  final 1.18 leading step (matching the overlay's ladder) before the type
  starts shrinking.
- **Measure-once insurance.** `pmSettle` is now idempotent — every block AND
  the page height reset to their start state first, so a re-settle can shrink
  a previously grown page instead of leaving a band of stale blank paper —
  and the settle re-runs via an unconditional `document.fonts.ready` hook
  (catching loads our own text insertion triggers, with one second-wave
  re-check, never an unbounded chain). Slot height re-syncs after every
  settle. With the system CJK stack this is normally a cheap no-op.
- **Small obstacles no longer wall off their whole column.** `inkToObstacles`
  now records each obstacle's actual horizontal ink extent, and the final
  sweep uses that tight box — a small inline figure only repels blocks that
  genuinely overlap it, instead of bouncing every pushed block in the column
  below it (the column band remains the fallback when no extent is known).

### Notes

- The overlay's behaviour is unchanged and deliberate: boxes clip with an "…"
  badge at the 8.5px readability floor (expand caps at 2.2× the original
  height) and a click expands the paragraph.

## [0.6.1] — 2026-08-08

### Changed

- Adopted the Chinese name **文镜** ("a mirror for text"). It now appears
  alongside PaperMirror in the add-on's name (Zotero's add-ons list), the
  settings pane label, and the README home page. No functional changes.

## [0.6.0] — 2026-08-08

First stable release. Same reader, now presented as a finished project.

### Added

- Bilingual (中文 / English) GitHub home page with a step‑by‑step usage guide,
  an FAQ, and a language switcher — covering the three reading modes, install
  and auto‑update, translation engines and BYOK, the glossary, and privacy.

### Notes

- No functional changes to the reader from 0.5.3; 0.6.0 marks the point where
  the feature set, the auto‑update pipeline (release‑asset `updates.json` via
  the latest‑release alias) and the VS Code‑only publish flow are all settled.

## [0.5.3] — 2026-08-08

### Changed

- **Releases now trigger on a push to `main`, not on a tag** — so the whole
  flow works from VS Code's Sync button with no terminal and no manual tags.
  Bump the version in `manifest.json` + `package.json`, commit, Sync; the
  workflow reads the version, creates the `v<version>` tag itself, and
  publishes the XPI + `updates.json` release assets. Versions already released
  are skipped, and pushes that don't change `manifest.json` are ignored.
- Combined with 0.5.2's release-asset auto-update (nothing is ever written to
  `main`), `main` no longer diverges, so VS Code Sync stays a clean
  fast-forward. Anyone who installs a 0.5.2+ `.xpi` from Releases auto-updates
  from the latest release — no configuration on their end.

## [0.5.2] — 2026-08-08

### Changed

- **Auto-update no longer depends on committing to main.** `updates.json` is
  now published as a *release asset* on every tag, and the manifest's
  `update_url` points at GitHub's latest-release alias
  (`releases/latest/download/updates.json`). Previously the release workflow
  tried to commit the manifest back to `main`; that push kept being rejected
  (branch protection / concurrent tag builds), so `updates.json` stayed stuck
  at 0.4.2 and Zotero reported "No updates found" even though newer releases
  existed. Nothing is written to `main` anymore, so the stall cannot recur.
- Dropped `update_hash` from the manifest: a hash that had to match a build
  byte-for-byte only ever produced mismatches between locally-built and
  CI-built XPIs. The XPI is fetched over HTTPS from the project's own release.

## [0.5.1] — 2026-08-08

### Fixed

- **Auto-update was stuck.** `updates.json` on main stopped advancing past
  0.4.2, so Zotero reported "No updates found" even though newer releases had
  been published. Two root causes in the release workflow: it regenerated the
  file *after* `git checkout main` (which swapped `package.json`'s version out
  from under `gen-updates`), and when tags were pushed close together the
  later build regenerated against a stale main and its push was rejected. The
  workflow now resets to the true `origin/main` before regenerating and takes
  the version from the release tag explicitly (`gen-updates.mjs <version>
  [xpi]`), so the manifest always lands on the version just released.

## [0.5.0] — 2026-08-08

Housekeeping release: a full audit of the tree, removing everything the
current version no longer reaches. No behaviour changes.

### Removed

- Dead modules and helpers: `src/utils/throttle.ts` (no importers), the
  adapter's `getPdfViewerWindow` (only user was the deleted floating chip),
  and the session's `copyCurrent` (its only caller was an unwired callback).
- Dead pane surface: the never-mounted 显示原文对照 / PDF叠加 switches, the
  unused `onToggleShowOriginal` / `onToggleOverlay` / `onCopy` /
  `onExportPdf` callbacks and their session wiring, and `setOverlayEnabled`.
  (The compare state itself, `Zotero.PaperMirror.exportTranslatedPdf()` and
  the overlay mode are untouched — only the orphaned plumbing is gone.)
- The 141-line hand-drawn brand-badge set, obsolete since the real official
  marks landed in 0.4.3. The fallback is now the neutral letter tile; the
  generic-endpoint globe stays.
- 25 orphaned locale strings per language (old settings-pane labels, mode
  tooltips, 复制译文 / 生成译文PDF button labels), the dead `paneRatio`
  pref, the 生成译文PDF button CSS, and the empty `addon/` and
  `docs/design/` scaffolding directories.

## [0.4.3] — 2026-08-08

### Changed

- **The 翻译服务 picker now shows the real, official brand marks.** The
  hand-drawn approximations are replaced by the services' actual vector
  logos (vendored from the MIT-licensed lobe-icons set): Microsoft's four
  squares, Google's G, OpenAI's knot, Claude's coral starburst, Gemini's
  gradient star, DeepSeek's whale, DeepL, Kimi's K + blue dot, 通义千问,
  智谱, SiliconFlow, Groq, Ollama and OpenRouter. Monochrome marks follow
  the pane's text colour so they stay visible in dark mode; the drawn
  glyphs survive only as a parse-failure fallback. OpenAI-compatible and
  Custom HTTP keep the neutral globe — generic endpoints have no brand.

## [0.4.2] — 2026-08-08

### Fixed

- CI/release builds failed on Node 20: the test runner passed a
  `build/tests/**/*.test.mjs` glob to `node --test`, but Node only expands
  test globs itself from v21 — on 20 the literal pattern "could not be
  found" and the release workflow died before publishing. The runner now
  lists the compiled test files explicitly, which works on every Node
  version. (v0.4.1 never got a published release because of this; 0.4.2 is
  the first tag the workflow publishes.)

## [0.4.1] — 2026-08-08

### Changed

- **解析 moved into the 译文面板 menu bar.** The floating selection chip is
  gone — it fought the reader's selection events and never behaved reliably.
  In its place, a fixed 「✦ 解析」 button sits in the pane's top bar next to
  保存到笔记: select text in the PDF (or click a 译文 paragraph) and press it.
  With nothing selected it shows the "select text first" hint. The 划词解析按钮
  setting was removed along with the chip.

## [0.4.0] — 2026-08-08

The 0.4 line is the first prepared for public GitHub distribution.

### Added

- **Automatic updates from GitHub.** The manifest now points Zotero at
  `updates.json` on the repository's main branch, and the release workflow
  regenerates that file — with the release's sha256 — on every tag. Once a
  user has any v0.4.0+ build installed, Zotero downloads and installs each new
  release on its own; no manual `.xpi` re-install. `scripts/gen-updates.mjs`
  produces the manifest, and the CI tag build publishes the release and commits
  the pointer back to main.

### Fixed

- **划词解析 chip never disappeared.** Its `#id` style rule out-specified the
  UA `[hidden]` rule, so hiding it had no visual effect — it now hides through
  a dedicated attribute and reliably auto-dismisses.
- **Clicking the 解析 chip did nothing.** A stray document-level mousedown
  cleared the captured text before the click ran; the click now also falls
  back to the live selection, and an over-chip guard stops the mousedown from
  dismissing the chip mid-interaction. The idle auto-hide now runs on the
  plugin's own timer rather than through the content window.

## [0.3.11] — 2026-08-08

### Fixed

- The 划词解析 chip no longer lingers on the page. It now auto-hides after a
  few idle seconds (the countdown pauses while the pointer is over it and
  restarts, shorter, when the pointer leaves), on top of the existing
  hide-on-click-elsewhere / scroll / empty-selection paths — so it is never
  left showing over the document.

### Changed

- The mode caret beside the 翻译 toolbar icon is now a crisp stroked chevron
  icon instead of the text "▾" (which rendered as a small off-centre glyph).
  It shares the icon button's sizing, tracks an open/pressed state, and the
  menu's single close path keeps the caret state in sync.

## [0.3.10] — 2026-08-08

### Fixed

- The 划词解析 chip now hides the instant the selection collapses. A
  `selectionchange` listener is the authoritative "nothing selected → hidden"
  signal (mouse-up only ever shows it), so the button is present only while
  text is actually selected and never lingers on the page.

### Changed

- Removed the explanatory sub-line under the 划词解析按钮 setting; the
  checkbox label alone is enough.

## [0.3.9] — 2026-08-08

### Changed

- **划词解析 no longer lives in Zotero's shared selection popup.** The button
  that appeared among the highlight swatches (where every translation/note
  plugin competes for space and order) has been removed. Selecting text in
  the PDF now floats our own standalone 「解析」 chip just under the selection —
  it belongs to no shared surface, so nothing else can push it around. It
  hides on the next click, scroll, or empty selection.
- 讲解 renamed to **解析** throughout the UI.

### Added

- 阅读界面 settings gain a **划词解析按钮** toggle (default on). Turn it off
  and the selection chip disappears entirely; 解析 is still available by
  double-clicking a paragraph in the 译文 pane. The toggle applies to every
  open reader immediately.

## [0.3.8] — 2026-08-08

### Changed

- The Anthropic entry in the 翻译服务 picker now shows the company's radial
  burst mark instead of a plain "A" tile, so every provider row carries its
  real brand symbol. (The OpenAI-compatible and Custom HTTP entries keep a
  neutral globe on purpose — they are generic endpoints with no brand.)

## [0.3.7] — 2026-08-08

### Fixed

- **Clicking translated text destroyed the layout.** A single click on a
  paragraph in the 译文 pane ran 深度讲解, jumped the pane to the top of the
  document AND navigated the PDF — and could leave the split collapsed with
  the reader unreachable until the tab was closed. A single click now only
  moves the focus highlight; 深度讲解 is a deliberate **double-click**.
- **Split-view watchdog.** Zotero occasionally rewrites the reader browser's
  inline styles (navigation, theme changes), erasing the split's pixel
  pinning — the reader then collapsed to its minimum and the pane swallowed
  the whole tab. The layout poll now detects the drift and re-pins within
  ~350 ms, and the pane additionally carries a hard `max-width` so it can
  never take the reader's half even if the pinning is lost.

### Changed

- 深度讲解 card no longer scrolls the pane to the document top. It floats
  over the pane's lower edge, the document behind it never moves, and Esc
  (or ×) dismisses it.

## [0.3.6] — 2026-08-08

### Fixed

- **Mixed-language pages (中英混排).** LLM providers sometimes drop block ids
  from a batched response; the manager retried the missing ids once as a
  batch and then silently gave up, leaving those regions untranslated —
  English paragraphs interleaved with Chinese ones in a single column (the
  JACC report). Ids still missing after the batch retry are now salvaged one
  request per block (a single-block answer cannot misalign, and the
  translation is accepted even when the model rewrites the id), capped at 8
  per chunk.
- A page that still has untranslated blocks after salvage is no longer
  written to the cache: previously the partial page was cached and every
  revisit re-served the mixed rendering forever. Left uncached, the next
  visit — or 重新翻译 — runs the whole pipeline again and completes it.

## [0.3.5] — 2026-08-08

### Fixed

- Gemini answered 404/INVALID_MODEL: the preset's default model
  `gemini-2.0-flash` was retired upstream. The preset now defaults to
  `gemini-2.5-flash`, and a stored auto-filled `gemini-2.0-flash` is cleared
  once at startup so the new default applies.
- The provider-pool list rendered once at pane load, so a key saved a minute
  later — or a provider switch — left every LLM row stuck on 「未配置密钥」
  and the just-configured provider still listed. The list re-renders when
  the primary provider changes and after a key is saved.

## [0.3.4] — 2026-08-07

### Added

- **多服务商并行 (provider pool).** Settings gains a 性能与并行 section: check
  any additional configured services and the document's pages are dealt
  round-robin between the primary and every checked provider, each using its
  own key. Throughput multiplies by the number of independent services
  without touching any single provider's rate limits. Sharding is by page
  and deterministic, so each page's cache entry stays with its provider.
  Providers without a stored key are skipped (and shown disabled). The
  section states plainly that text is sent to every checked service.
- 并发请求数 returns to settings (1–6). Key-based providers may run up to 6
  page requests in flight; the free engines stay clamped at 2 internally.

### Notes

- Multi-key rotation on a SINGLE provider is deliberately not offered:
  extra keys of one account share that account's limits, and using multiple
  accounts to evade limits violates provider terms.

## [0.3.3] — 2026-08-07

### Fixed

- The free Microsoft engine was slow for a structural reason: every
  paragraph part was a separate HTTP round trip, awaited strictly one after
  another — ~20 sequential round trips per page after region coalescing.
  Requests now run through a small parallel pool (3 in flight, order
  preserved, first failure aborts), collapsing a page into a few waves.
  The Google engine's independent batches go through the same pool.

## [0.3.2] — 2026-08-07

### Fixed

- Microsoft engine: the real reason the www/cn fix never took effect. The
  settings pane auto-fills the provider's default Base URL
  (`https://www.bing.com`) into the preference, and "use apiBaseURL when
  set" silently overrode the session origin learned from the redirect — so
  cn-issued tokens were posted to www on every install, by construction.
  `resolveBingApiBase` now treats ANY bing.com host in the Base URL as
  "no override" and follows the session-issuing host; only a genuine
  non-bing mirror wins. Covered by regression tests.
- The Bing web channel now goes FIRST; Edge anonymous auth (observed
  returning HTTP 404) is the fallback behind a 5-minute breaker.
- An Edge auth 404 is reported as 「Edge 匿名认证端点不可用」 instead of the
  generic — and here actively misleading — "Endpoint or model not found".

## [0.3.1] — 2026-08-07

### Fixed

- Microsoft engine, continued. The browser-identity fix moved the failure
  from an HTML challenge page to a silent-empty HTTP 200 — Bing's
  rate-limit/flag response for this host. Three counters:
  - The request IID now carries the per-session counter suffix Bing's own
    client sends.
  - A silent-empty 200 rotates to the sibling host (www ↔ cn bing.com),
    refreshes the session there and retries once.
  - The Edge channel's last failure is reported even while the channel is in
    its 5-minute breaker (`Edge通道: 熔断中, 上次: …`), so every
    test-connection line carries both channels' truth.

## [0.3.0] — 2026-08-07

Start of the 0.3 line.

### Fixed

- Microsoft translation, round three — the user's screenshot finally carried
  the decisive clue: **HTTP 200 with a non-JSON body**, which is Microsoft's
  bot check answering an unfamiliar client with an HTML challenge page.
  - Every request to a Microsoft host now introduces itself with a browser
    User-Agent (privileged XHR may set one); the translate POST also carries
    the Referer the endpoint expects.
  - Non-JSON responses are now diagnosed precisely: an HTML page, an empty
    body and other garbage each get their own message.
  - When both paths fail, the error carries BOTH: `Edge通道: … ｜ Bing通道:
    …` — one screenshot of the test-connection line now tells the whole
    story.
  - The scrape path's internal session-refresh retry no longer restarts from
    the Edge path.

## [0.2.10] — 2026-08-07

### Removed

- Settings the reader never needed to see: request timeout and concurrency,
  send-adjacent-context, auto-prefetch, sync-scroll (lives in the header
  bar), 显示原文对照, the article font-size slider, the 生成译文PDF section
  (the capability stays behind `Zotero.PaperMirror.exportTranslatedPdf()`),
  and 仅本地服务模式. Every removed knob keeps working at its default.

### Fixed

- Six checkboxes rendered as bare text with no box (Fluent value-style
  labels): all checkboxes now carry explicit labels.
- The footer version was hardcoded "0.1.0"; it now reads the installed
  version from the plugin.

## [0.2.9] — 2026-08-07

### Changed

- 悬停看原文 and 原文淡化 are ON by default (one-time migration for existing
  installs): in overlay mode the masks are translucent so the original stays
  faintly visible, and hovering a paragraph reveals its source — comparison
  reading needs no setup.

### Added

- The settings pane's 阅读 section now carries the same choices as the
  toolbar menu: a 默认阅读模式 picker (左右对照 / 覆盖翻译 — the mode the
  toolbar button opens), plus checkboxes for 悬停看原文 and 原文淡化.

## [0.2.8] — 2026-08-07

### Added

- **Region-based translation.** A coalescing pass between extraction and
  translation rebuilds semantic regions from whatever fragments extraction
  produced: consecutive body blocks in the same column, at the same type
  size, with only line-spacing between them merge into ONE region — extracted
  in reading order, translated as one semantic block, masked line by line,
  and typeset into the region's own union bounding box. The shredded
  one-line-per-block abstracts are gone.
- Paragraph roles (Background, Methods and Results, Conclusions, Key Words)
  survive the round trip: genuine paragraph boundaries join as blank lines,
  the free Microsoft/Google engines translate paragraph-by-paragraph so the
  structure is never flattened, and the typesetter renders it back as
  separate paragraphs (`white-space: pre-line`).
- Containment-first typesetting: before the flow may move anything, each
  region walks a typographic ladder — leading 1.5 → 1.34 → 1.24, then type
  down to 88% of source (floor 8.5px) — inside its own box. Only what still
  does not fit spills into the push-down/grow machinery, so translated text
  stays inside its region and never overlaps adjacent content.

## [0.2.7] — 2026-08-07

### Fixed

- Translated text is no longer larger than the original. The right pane
  always filled its own width, so whenever the reader displayed the original
  smaller than the pane (fit-page zoom, wide windows) the rebuilt page — and
  every glyph on it — rendered bigger than the page beside it. Pages now
  render at the READER's own zoom (CSS px per PDF point), capped by the pane
  width, and follow zoom changes live.
- The garbled overlap band at the bottom of dense pages is gone. Overflowing
  blocks used to be clamped back inside the page height, piling every long
  translation onto the same bottom strip over unmasked original text. A block
  that runs long now keeps flowing downward and the page GROWS below the
  artwork (plain paper extension); the slot carries the real footprint so the
  next page never overlaps the tail.

## [0.2.6] — 2026-08-07

### Fixed

- The split now reads 缩略图 | 原文 | 译文 with the original and the
  translation dividing the space AFTER the thumbnails equally. Previously the
  raw reader browser was split 50/50, so the left half was sidebar+original
  while the right half was all translation — the translated page ran wider
  than the original and its typography was set for the wrong measure. The
  sidebar's width is measured (browser minus the PDF iframe, so it survives
  Zotero renames) and granted to the reader's side on top of its half; the
  divider drag and the periodic sidebar open/close check both honour it.

## [0.2.5] — 2026-08-07

### Fixed

- Microsoft translation, round two — two independent breakages found:
  - In mainland networks www.bing.com 302s to cn.bing.com; the session was
    scraped from the redirected page but the API call went back to
    www.bing.com, where the token is invalid by construction. The engine now
    tracks where the redirect landed and keeps every call same-origin.
  - Bing renamed the credentials variable from `params_RichTranslateHelper`
    to `params_AbusePreventionHelper`; the parser accepts both.
  - Engine self-test errors now carry the underlying message, not just a code.

### Changed

- Provider badges upgraded to faithful vector reproductions of the real
  marks: Microsoft's four squares, Google's four-colour G (canonical path),
  the OpenAI hexagonal knot, Anthropic's dark A on cream, the Gemini gradient
  star, the DeepSeek whale, the DeepL dart, Kimi's black K tile, the 通义
  hexagram, the SiliconFlow pinwheel, the Ollama llama face, Groq's G ring
  and the OpenRouter fork.

## [0.2.4] — 2026-08-07

### Added

- Brand badges for every translation service — Microsoft's four squares,
  Google's four-colour G, coloured monograms for the LLM providers — drawn in
  code and shared between the header chip and the switcher menu.
- The header chips are now switchers: clicking the language pair opens a
  source/target menu, clicking the engine opens the full provider roster with
  badges. Switching restarts translation in place; no trip through the
  settings pane. (Settings stays one entry away at the bottom of the menu.)

### Fixed

- Microsoft translation works again. The bing.com page-scrape session flow had
  broken; the engine now uses the Edge browser's translator auth (a keyless
  JWT from edge.microsoft.com, the flow current immersive-translate uses) as
  the primary path, with the page scrape kept as a fallback.

### Changed

- Free engines are named plainly in the header — "Microsoft 微软翻译" and
  "Google 谷歌翻译" — without the "(free, no key)" clutter.

## [0.2.3] — 2026-08-07

### Fixed

- The pane no longer freezes on a page spinner. Core page rendering awaited a
  promise from the PDF.js content compartment, which can simply never settle
  for a sandbox awaiter — the same cross-compartment trap getPageData fell
  into long ago — and the render pump hung on it forever. Completion is now
  detected by polling plain flags plus a pixel-stability check on the canvas,
  under a hard deadline; a failed page falls back to copying the left viewer's
  canvas (scaled to fit), and failing slots retry with backoff instead of
  spinning. A 20-second race in the pump is the final backstop.

## [0.2.2] — 2026-08-07

### Fixed

- The header bar now spreads edge to edge. A legacy compact-header rule made
  the header a row flex container, so the bar was sized to its content and the
  flexible gap had no room to grow — every control clustered on the left. The
  bar claims the full line; the left group sits at the left edge and the
  layout/settings/close group is pinned to the right.

## [0.2.1] — 2026-08-07

### Changed

- 整页对照 now shows the **whole document**, not just the page the reader is
  on: one slot per page, laid out from the page boxes before anything renders,
  so the scrollbar and page positions are correct from the first frame. A page
  shows the **original** until its translation completes, then swaps to the
  rebuilt translated page.
- Pages render through pdf.js core directly (`adapter.renderPageBitmap`), so
  the right pane no longer depends on which pages the left viewer happens to
  keep rendered — and no longer needs to rebuild on left-side re-renders or
  zooms at all.

### Fixed

- 同步滚动 offset (原文第 2 页对着译文第 1 页): the pane now follows the
  reader's document position continuously — page **and** fraction within the
  page — instead of snapping per page. Scrolling the pane drives the reader
  the same way, with echo suppression in both directions.
- Memory bounded: only pages near the viewport hold canvases (rendered one at
  a time, nearest first, re-prioritised between renders); far pages release
  back to sized placeholders. The per-canvas supersampling budget was halved
  to match.

## [0.2.0] — 2026-08-07

### Changed

- The application icon is now **vector**. `assets/icons/icon.svg` is the single
  source of truth; the PNGs the manifest needs are renders of it, and the
  regeneration command is documented beside them. The preferences pane loads the
  SVG directly, so it stays sharp at any display scale.
- The mark itself was redrawn as the product it describes — two sheets side by
  side, the original in paper and dark type, the translation in the plugin's
  purple with light type and a live dot. No backdrop plate: the card is the whole
  icon.
- The in-app miniatures (reader toolbar button, pane brand) are drawn on the same
  16px grid the SVG uses ×8, so the large mark and the small one cannot drift
  apart.

## [0.1.9] — 2026-08-07

### Changed

- Redesigned the translation pane's header bar into three zones — what is being
  translated, what to do with it, what to do with the window — separated by
  hairlines. Chips are borderless and quiet; colour is reserved for the primary
  action and the active sync switch.
- The source and target languages share one chip (`English → 简体中文`) instead
  of two chips that both truncated to `Eng… → 简体…`.
- Narrow panes now drop control labels rather than truncating every element.

## [0.1.8] — 2026-08-07

### Changed

- Translation status moved out of the header bar into a floating note in the
  bottom-right corner: it appears on a new action, stays while work is running,
  and leaves on its own. Repeating the same message no longer re-triggers it.
- The rebuilt page now fills the pane in both directions (previously it would
  only ever scale down, leaving empty space beside it).

### Fixed

- Corrected the sign of the transform footprint compensation, which left a band
  of dead space below and to the right of a scaled page.

## [0.1.7] — 2026-08-07

### Added

- The rebuilt page scales with the pane: dragging the divider resizes the
  translation smoothly via a CSS transform, with no re-render and no way for the
  text layer and the artwork to drift apart.
- The layout-swap button draws the current arrangement — two panels with the
  translation's half filled — so it reads as state rather than as a generic
  arrow.

### Changed

- Header bar rebuilt in a fixed order: icon, languages, engine, refresh, status,
  sync scroll, save to note, layout, settings, close.

### Removed

- The 生成译文PDF button. The capability remains available as
  `Zotero.PaperMirror.exportTranslatedPdf()`.

## [0.1.6] — 2026-08-07

### Fixed

- A final overlap-resolution pass guarantees no two blocks occupy the same
  pixels, whatever the column analysis concluded. Blocks left in the original
  (author lists, affiliations) participate as immovable obstacles — previously
  translations could be printed straight over them.
- The rebuilt page no longer sits offset and clipped inside its host.

## [0.1.5] — 2026-08-07

### Fixed

- The rebuilt page is now built at the reader's own pixel geometry (1:1) instead
  of being scaled to the pane's width. Every earlier formula failed the same
  way: an ancestor would clamp the page's width, the bitmap scaled down with the
  container while the text layer kept its pixel coordinates, and the result was
  oversized type spilling past the edge with masks no longer covering the words
  they were cut for. Zoom now propagates to both halves for free.

### Removed

- The width-cap, clamp-detection and resize-redraw machinery that existed only
  to fight the clamping described above.

## [0.1.4] — 2026-08-07

### Changed

- 左右对照 is the default reading mode again: the original PDF on the left, the
  re-flowed translated page on the right. 覆盖模式 remains one click away in the
  toolbar menu.

## [0.1.3] — 2026-08-07

### Added

- Column-aware flow layout for the rebuilt page (`src/ui/pageFlow.ts`). Blocks
  are set at one consistent size and take the height the Chinese needs; three
  strict rules keep the page sane: a block never moves up, never leaves its
  column, and never crosses an obstacle.
- Obstacle detection reads the rendered bitmap directly — the page is
  downsampled to a coarse grid, cells that contrast with the paper are marked,
  the blocks being replaced are erased, and what remains (figures, plots, logos,
  coloured bands) is what the flow hops over.

### Fixed

- A full-width title no longer merges the two text columns into one. Column
  membership is measured against the wider span, not the narrower.

## [0.1.2] — 2026-08-07

### Added

- 悬停看原文: hovering a translated paragraph in overlay mode lifts that
  paragraph's masks and fades its text, revealing the source underneath.
- Toolbar menu entries for 悬停看原文 and 原文淡化.

### Fixed

- Line rects handed back bottom-to-top no longer shatter a paragraph into one
  fragment per source line. The reading direction is detected and normalised.

## [0.1.1] — 2026-08-07

### Fixed

- The translation pane no longer flashes open and then vanishes: the pane's
  visibility is settled when the split view is created rather than after text
  extraction finishes, seconds later.
- Overlay mode shows a progress chip on the page, so a click on 翻译 is
  acknowledged immediately even though the side pane is hidden.
- A failure while opening keeps the pane on screen carrying the error instead of
  silently tearing the session down.

### Added

- An in-memory ring buffer of recent warnings and errors, readable at any time
  via `Zotero.PaperMirror.lastErrors()` — no need to have enabled debug logging
  beforehand.

## [0.1.0] — 2026-08-07

Initial working plugin for Zotero 9.0.x.

### Added

- Side-by-side bilingual reading in the built-in PDF reader, with synchronised
  scrolling and a reader-toolbar toggle.
- On-page overlay mode: the translation is painted onto the rendered page, one
  mask per source line, with the page's own sampled paper colour.
- Structured extraction: paragraph merging, de-hyphenation, two-column reading
  order, heading and caption classification, formula protection, and a
  metadata filter for author rosters, affiliations, copyright, DOI lines,
  running heads and page feet.
- Providers: Bing and Google free engines (no key), plus OpenAI-compatible,
  Anthropic, DeepL and custom endpoints. BYOK only — no developer keys are
  bundled, and keys are stored in the system credential store.
- Deep explanation of a selected passage, a glossary, a persistent local cache
  keyed by file hash and settings, and save-to-note.
- In-plugin translated-PDF generation with pdf-lib and a build-time GB2312
  subset of Noto Sans SC, plus an optional local BabelDOC bridge for full
  layout re-flow.

[Unreleased]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.8.1...HEAD
[0.8.1]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.8.0...v0.8.1
[0.8.0]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.7.10...v0.8.0
[0.7.10]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.7.9...v0.7.10
[0.7.9]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.7.8...v0.7.9
[0.7.8]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.7.7...v0.7.8
[0.7.7]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.7.6...v0.7.7
[0.7.6]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.7.5...v0.7.6
[0.7.5]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.7.4...v0.7.5
[0.7.4]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.7.3...v0.7.4
[0.7.3]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.7.2...v0.7.3
[0.7.2]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.7.1...v0.7.2
[0.7.1]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.7.0...v0.7.1
[0.7.0]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.6.1...v0.7.0
[0.6.1]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.6.0...v0.6.1
[0.6.0]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.5.3...v0.6.0
[0.5.3]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.5.2...v0.5.3
[0.5.2]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.5.1...v0.5.2
[0.5.1]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.5.0...v0.5.1
[0.5.0]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.4.3...v0.5.0
[0.4.3]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.4.2...v0.4.3
[0.4.2]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.4.1...v0.4.2
[0.4.1]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.4.0...v0.4.1
[0.4.0]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.3.11...v0.4.0
[0.3.11]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.3.10...v0.3.11
[0.3.10]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.3.9...v0.3.10
[0.3.9]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.3.8...v0.3.9
[0.3.8]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.3.7...v0.3.8
[0.3.7]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.3.6...v0.3.7
[0.3.6]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.3.5...v0.3.6
[0.3.5]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.3.4...v0.3.5
[0.3.4]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.3.3...v0.3.4
[0.3.3]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.3.2...v0.3.3
[0.3.2]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.3.1...v0.3.2
[0.3.1]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.3.0...v0.3.1
[0.3.0]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.2.10...v0.3.0
[0.2.10]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.2.9...v0.2.10
[0.2.9]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.2.8...v0.2.9
[0.2.8]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.2.7...v0.2.8
[0.2.7]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.2.6...v0.2.7
[0.2.6]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.2.5...v0.2.6
[0.2.5]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.2.4...v0.2.5
[0.2.4]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.2.3...v0.2.4
[0.2.3]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.2.2...v0.2.3
[0.2.2]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.2.1...v0.2.2
[0.2.1]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.1.9...v0.2.0
[0.1.9]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.1.8...v0.1.9
[0.1.8]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.1.7...v0.1.8
[0.1.7]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.1.6...v0.1.7
[0.1.6]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.1.5...v0.1.6
[0.1.5]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.1.4...v0.1.5
[0.1.4]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.1.3...v0.1.4
[0.1.3]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.1.2...v0.1.3
[0.1.2]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.1.1...v0.1.2
[0.1.1]: https://github.com/wandonwe/PaperMirror-for-Zotero/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/wandonwe/PaperMirror-for-Zotero/releases/tag/v0.1.0
